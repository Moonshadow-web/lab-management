# 2026-07-08 工作记录

## 补充待补充项目性能.xlsx 中的8个项目到 data.js
- 来源文件：C:/Users/81526/Desktop/待补充项目性能.xlsx（Sheet1，8行12列）
- 新增记录 id 180~187，performanceData 由 116 → 124 条
- 项目清单（均为乳胶增强免疫比浊法、生化类）：
  - 180 KAP κ轻链（血清） AU58-1 景源 g/L 2~4.4
  - 181 LAM λ轻链（血清） AU58-1 景源 g/L 1.1~2.4
  - 182 uKAP 尿κ轻链 AU58-1 景源 mg/L <7.9
  - 183 uLAM 尿λ轻链 AU58-1 景源 mg/L <3.97
  - 184 UIgG 尿IgG HT7600 柏荣 mg/L <8
  - 185 Ua1MG 尿α1微球蛋白 HT7600 柏荣 mg/L <12
  - 186 UTF 尿转铁蛋白 HT7600 柏荣 mg/L <2
  - 187 IgG4 免疫球蛋白G4 AU58-1 柏荣 g/L 0~2
- 映射规则：机台AU58-1→instrument=AU生化仪；HT7600→instrument=HT7600（新增机台）；参考/线性/可报告范围补单位、连字符转"~"；校准品空→"—"
- 已 git commit（9929293），git push 因网络连不上 GitHub 失败（多次重试均 Connection reset/443不可达），待网络恢复后再推送
- 最终推送方式：沙箱环境 HTTPS(443) 不可达，但 **SSH(22) 可连通 GitHub**。在沙箱生成 ed25519 密钥（~/.ssh/id_ed25519，comment=815268425@qq.com），公钥已加到用户 GitHub 账号；remote 改为 SSH：`git@github.com:Moonshadow-web/lab-management.git`。git push 成功（46f1dca..9929293）。
- 结论：本仓库以后从沙箱推送一律走 SSH，不要再试 HTTPS（443 被墙）。

## 环境说明
- 本机 python 托管版(3.13.12)已建 venv：c:/Users/81526/.workbuddy/binaries/python/envs/default，装有 pandas+openpyxl，读取 xlsx 用此环境
- Node 校验：c:/Users/81526/.workbuddy/binaries/node/versions/22.22.2/node.exe --check data.js

## 新建「生免组实验室管理系统」(8模块 ISO15189 系统) 前端
- 后端：FastAPI 已在 localhost:8000 运行；前端独立在 frontend/（Vue3+Vite+Element Plus+Pinia）
- 已实现 Phase 0+1 前端文件：
  - api/：auth, testItems, documents, instruments, notifications（axios 封装；baseURL '/'，路径带 /api/v1）
  - components/：CrudTable(列表+搜索+分页，props: columns/fetch/extraParams，暴露 refresh)、EditDialog(配置驱动表单)、FileUpload(el-upload 封装，emit change 出 File)、AppCard
  - views/：Login, Dashboard(统计卡片+提醒列表), test_items/TestItemList, documents/DocumentList, instruments/InstrumentList, Placeholder
  - 路由 8 模块（qc/reagents/training/verification/iso15189 为建设中占位）
- 关键约定：
  - 文件预览/下载必须带鉴权头，故走 axios 取 blob（fetchDocumentBlob → previewBlob/downloadBlob），不能直接 window.open(会被 401 拦截)
  - 文档分类 DOC_CATEGORIES=["通用SOP","项目SOP","仪器SOP","记录表格","项目说明书"]；状态 DOC_STATUS=["草稿","生效","作废"]
  - 仪器状态 INSTRUMENT_STATUS=["在用","备用","维修","停用"]；校准到期由 notification_service 自动生成提醒
  - Vite proxy：/api 与 /uploads → localhost:8000
- 待办：npm install 完成后 npm run dev，端到端验证（登录/搜索/上传/新版本/校准提醒），然后 git commit

## 端到端验证结果（2026-07-08 晚）
- npm install 成功（83 包）；`npm run build` 编译通过（仅 @vueuse PURE 注记/大 chunk 无害告警）
- dev server 运行于 http://localhost:5173 ；Vite proxy /api、/uploads → localhost:8000 正常
- 登录 admin/admin123 拿到 JWT(119字符)；经 5173 代理查询 test_items(124)/documents(129)/notifications(校准提醒1条) 全部 200
- 文件下载/预览：后端直接返回完整 PDF（doc129=395458字节 %PDF-1.7），走 axios blob（前端预览/下载）正确；curl 经 Git-Bash /tmp 显示 0 字节属工具假象，已用 Python 验证为真实 395KB
- 已 git commit（6671fc6），新增 .gitignore 排除 node_modules/venv/data/__pycache__/.workbuddy 等；59 个源文件入库。未推送（计划为可选，待用户确认后再 push SSH）
- 阶段0+1（登录/工作台/项目查询/文件管理/仪器档案）完成；qc/reagents/training/verification/iso15189 为占位"建设中"

## 2026-07-10 补全五大业务模块 + 登录修复复现
- 新增 5 个模块（后端模型/schema/make_router + 前端 axios/列表页）：质控 QCRecord、试剂 Reagent、继教 TrainingRecord、性能验证 VerificationRecord、15189不符合项 Nonconformity。
- 均复用 make_router 通用 CRUD 工厂 + 前端 CrudTable/EditDialog 配置驱动模式；状态字段用 el-tag 着色。
- 路由 frontend/src/router/index.js 接入真实页面并去除 wip；AppLayout 菜单去掉"建设中"；Dashboard 新增"业务台账概览"行（5 模块计数可点击跳转）。
- 后端启动 Base.metadata.create_all 自动建新表，无需迁移。
- 验证：后端直连 + 前端代理(5173) 两轮，5 模块 POST/PUT/GET/DELETE 全链路 CRUD 全部通过；npm run build 编译干净。
- 重要复现：登录时序 bug（auth.js 先 /me 后 setItem token 导致 401 顶回）此前的修复未持久化，本次重新修复并已提交。教训：Edit 后要 grep 复核落盘。
- 提交 58ce5cb（本地，未推送；后续上云 CloudBase）。后端仍在 8000 运行，前端 5173 运行。
- 各模块目前各留 1 条测试样例行（张三/贝克曼等假数据），可在界面直接删除。
