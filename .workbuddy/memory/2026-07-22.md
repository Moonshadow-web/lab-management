# 2026-07-22 工作日志

## 专利可行性咨询 + 公开网络查新
用户连续追问：管理系统能否申请专利 / 现有功能转化价值 / 室间比对·仪器间比对·累靶·EQA 四功能价值 / 查新怎么查 / 论文数据-结果-结论 / 实际试查新。

### 核实的代码事实（backend/app）
- eqa.py:969-1239 单位换算闭环（因子表+按项目名/别名匹配+上报单位自动覆盖）；eqa.py:567 卫健委 EQA 报告正文专用解析。
- qc_target.py 即刻法(Grubbs衍生)+SI界值表(n=3..20)+常规法±2s/±3s，n=20确立靶值。
- interlab_report.py 按 WS/T 415 偏倚+4/5通过、定性符合率、双比较系统、阴阳归一化。
- comparison_report.py WS/T 403-2024 字典(60项)+按水平 te_by_level/mode_by_level+校准修正(IgA8/IgM10/HBDH10等)。

### 转化价值结论
- 软著：100% 可办（建议模块拆分登记）。
- 专利：仅 EQA 全流程「报告解析→单位校正→多源比对→总结生成」组合有中等授权概率；仪器间/室间比对、累靶均为执行公开标准/公式，单点难授权。
- 论文（应用类）性价比最高：EQA信息化 > 比对管理 > 累靶；数据直接从库提取。

### 实际查新（Google Patents / CNKI类 / PubMed / 厂商官网）
产出文件：查新检索记录_2026-07-22.md
- 最接近专利 CN107545361A（室间比对+人工录入，非质评自动处理）。
- 商业平台 EQASOnline/PTP(iLab+Bio-Rad QCEC 2025论文)覆盖部分场景；通用报告解析工具(Affinda/LabExtract/parselabs)为OCR/LLM通用。
- 单位换算是行业公开手工查表常识。
- **关键发现：WS/T 403—2024 主要起草单位含「民航总医院」**（起草人王治国、尚红、陈文祥等）。→ 论文权威加成；但仪器间比对=执行本单位起草的公开标准，专利创造性更受挑战。

### 建议
1) 办软著；2) 冲专利只保 EQA 组合方法，且先出正式查新报告(省医学情报所/高校查新站)；3) 论文走应用类，数据从库取。

## e601 HBsAg 月结质量目标为空 — 根因与修复（commit 20e3739 已部署）
- **现象**：e601 室内质控月结中「表面抗原」的质量目标列仍为空。
- **两层根因**：
  1. 月结 `qc_monthly_summaries.quality_goal` 在 LIS 上传时**冻结写入**（非实时查表）；修复查找逻辑只影响新上传，历史空行需回填。
  2. 真实 bug：LIS 名称 `表面抗原` → `find_test_item_by_name("表面抗原","免疫分析仪")` 命中 DxI800 定量项 id 83（别名 `qHBsAg`）；旧桥接按精确括号代码 `HBsAg` 拒绝匹配 → 查不到 QR `乙肝病毒表面抗原(HBsAg)`。
- **修复**：`qc_service._lookup_qr_goal` 桥接由精确改为**包含匹配**（rc⊂w 或 w⊂rc 任一成立即命中）；新增回填端点 `POST /api/v1/qc-summaries/_backfill_goals`（admin/qc_manager），对空 quality_goal 行按 `find_test_item_by_name`+`lookup_quality_goal` 补填。
- **验证**：本地 py_compile + 单测通过；线上两次调用回填（`updated` 首次>0、二次=0 幂等）。e601 表面抗原(ids 56/47/46)/丙肝抗体/梅毒特异抗体 quality_goal 均为 `10%`。

## 定性仪器间比对 — 试剂批号按项目录入（commit b8d8bc8 已部署）
- 将 `qual_meta` 由「按仪器」升级为「按项目→按仪器」：结构 `{项目名: {instrument_id(字符串): {method, reagent_manufacturer, reagent_lot}}}`；保留旧结构兼容（`_normalize_qual_meta` 自动展开）。
- 后端：comparison_report.py 定性分支 build_html/build_docx 改为「每个项目单独的检测系统信息 + 比对结果」小节；修正 `_set_cell_font(r1)` 误用于 Run → 改 `_run_font`。
- 前端 ResultEntry.vue 定性分支每个项目渲染独立试剂批号表（el-scrollbar 内），加载/保存按 allItemNames 映射。
- 验证：py_compile + 单测（legacy→新结构、新结构直过、`LOT-HBSAG-69` 存在于报告）通过；vite build 仅良性警告。

## 修正（23:45）：北京市卫生副高——软著不计代表作
- 用户指出软著对北京市申副高没用。检索北京市2023《深化卫生专业技术人员职称制度改革实施办法》+2026年度评审通知确认：
  - 实行「成果代表作制度」，代表作清单含：论文论著、项目/研究/技术报告、**专利**、专著、新技术推广报告、临床病案、卫生标准、技术规范、科普作品等——**未列软著**。
  - 破格申报硬条件明确为「**国家发明专利授权**」（排名前2，单项年产值≥50万；多项≥100万）。
- 修正前结论「软著对评职称有用」在卫生技术系列不成立。软著仅对单位成果奖 / ISO15189认可 / 防抄维权 / 横向推广 / 产业化有用，**非副高筹码**。
- 对申北京市副高（医学技术/检验类）有效代表作：论文论著 > 发明专利 > 技术报告(系统设计与应用)/新技术推广报告 > 卫生标准规范(如WS/T403) > 奖项/课题。
- 转化优先级修正：论文(应用类,数据现成) > 冲发明专利(EQA组合流程,现为副高硬通货) > 技术报告/标准规范 > 软著(非副高用，但仍可办)。
- 注：纯软件难申请实用新型，软件专利路径只有「发明专利」；故EQA组合冲发明是唯一专利路径，且恰好是副高最看重项。

## 原始结果附件预览/下载修复（commit 681bb84 已部署，build mark attachment-blob-mysql-2026-07-23）
- **根因澄清**：之前误判「附件文件在容器本地磁盘丢失」。实测两模块附件记录数均为 0，旧 404 是拿 `aid=35` 瞎测的「ID 不存在」；真正问题是附件字节存在容器本地磁盘，每次 redeploy 容器重建即丢失。
- **修复（用户提示「现在用的MySQL」→ 字节落库）**：
  - `ComparisonAttachment`/`InterlabAttachment` 新增 `data` 列（`LargeBinary(16MB)` → MySQL `LONGBLOB`）；上传字节直接入库，不再写本地磁盘；预览/下载从 DB 读字节返回 `Response`；删除仅删记录。
  - 启动 `_ensure_missing_columns()` 自动 `ALTER TABLE ADD COLUMN data ...` 补列（已验证生效）。
  - 上一轮（e522927, build mark attachment-auth-token-2026-07-22）已修鉴权：后端 `get_current_user` 支持 URL `?token=`/cookie 兜底，`<img>`/`<iframe>` 能带 token 预览；前端 `AttachmentPreview.vue` 支持 图片/PDF/Word/Excel 内嵌预览。
- **端到端验证**：上传 1x1 PNG → 拉取 200、`Content-Type: image/png`、`inline`、字节完全一致（ROUNDTRIP_OK=True）。测试记录已清理。
- **部署坑（重要）**：`tcb cloudrun deploy` 有两个交互提示——①「Enable gray deployment?」用回车选默认 `No`（自动切换流量）；②若已有部署任务在跑，会问「Confirm to continue (Y/n)」。管道单 `Y` 只喂了提示①且误选了 Yes(灰度)，导致流量不切换、标记不更新。正确喂法：`(echo ""; sleep 3; echo "Y") | tcb cloudrun deploy ...`（先回车选No，等3秒再Y确认取消旧任务）。

## 提醒能否走短信 + 腾讯云开发怎么做（咨询，未写代码）
- 用户问「提醒设置能否用短信模式，腾讯云开发怎么做」。
- **澄清关键误解**：CloudBase 内置短信**只用于登录验证码**，业务通知（质评/校准提醒）必须单独开通**腾讯云短信 SMS 服务**（与 CloudBase 同主账号）。
- **现状核实**：`NotifyRecipient` 已预留 `phone`+`channels`(email/sms)，但 `reminder_engine` 从未发短信；触发点 `_reminder_scheduler` 已就绪；模型无需改表（无迁移）。
- **给出用户侧 4 步操作指引**：同主账号确认→企业实名+开通SMS→申请签名(医院需《医疗机构执业许可证》，如"民航总医院")→申请通知类模板(含{1}{2}变量)→拿到5个凭据填环境变量。代码侧规划（config加6变量→新建sms_service.py→reminder_engine加get_sms_recipients并并行发短信→前端渠道开关）已就绪但**用户仅要"怎么做"的指引，未让落地实现**。
- **备选**：企微/公众号模板消息免费免签名，可作内部提醒替代。
- 详见 MEMORY.md「提醒短信(SMS)（规划中）」段。

## WxPusher 微信按人推送 — 全功能已落地，卡在环境变量注入（本时段）
- **代码/部署已完成**（commit 29ee1a9，build mark `wxpusher-channel-2026-07-23`）：config 加 WXPUSHER_ENABLED/APP_TOKEN；NotifyRecipient 加 wx_uid 列；新建 wxpusher_service.py（send_wxpusher/resolve_uid_by_extra/create_follow_qrcode，urllib 直连）；reminder_engine 加微信分支与 _record_send 去重；reminders.py 加 /recipients/{rid}/wx-qrcode + /wx-sync 两个 admin 端点；前端 ReminderSettings.vue 渠道 UI。直连 WxPusher API 发测试消息已实际送达用户微信（code 1000）。
- **卡点（已用 tcb api 证实根因）**：部署后 `GET .../recipients/1/wx-qrcode` 返回 502「WxPusher 未配置」。经 `tcb cloudrun deploy --help`、`tcb --help`、`tcb api tcb DescribeCloudBaseRunServers/DescribeCloudBaseRunServerVersion` 核实：
  - `cloudbaserc.json` 的 `envs` 只在**服务创建时**注入；后期新增的 WXPUSHER_* key，即使 redeploy（已试两次）也**不会注入运行中 pod**。
  - `tcb cloudrun deploy` 无 `--env` 子命令；`tcb` 无 `cloudrun env set`；`tcb` 命名空间下 Run 仅暴露版本级动作（`CreateCloudBaseRunServerVersion` 含 `EnvParams`），无独立 set-env 动作；独立的 `UpdateCloudRunServerConfig` 属 `tcbr`（独立云托管产品 647），**非本服务**。
- **解决路径（待用户拍板）**：①控制台手动给 lab-management 加 WXPUSHER_ENABLED=true / WXPUSHER_APP_TOKEN=AT_c4z7ZO7rBn1wup5HYboK0bSQwEJ96Mfp（最稳，1分钟，持久）；②用户从控制台给当前版本名，我走 `CreateCloudBaseRunServerVersion` API 建带新环境变量的新版本并发布；③改 DB 存储（新增 system_config 表，wxpusher_service 读 env→DB 兜底，我全程执行，但 token 明文入 DB）。
- **注意**：控制台加完 env 后若提示需重启实例，确认重启即生效；后续 cloudbaserc redeploy 不触碰 env，故不会冲掉。

## R-4s 同天两水平判定改为「两个都失控」（待部署）
- **背景**：用户确认——同一天两个水平触发 R-4s 时，**两个水平都判失控(ooc)**，而非之前的「后点失控/前点警告」。
- **改动**（`backend/app/services/qc_service.py`）：
  - `evaluate_r4s_project`：相邻对触发 R-4s 时，若 `prev.date == curr.date`（同天）→ `(prev)` 与 `(curr)` **都加 ooc_add**；跨天相邻 → 后点 ooc、前点 warn（保持原行为）。前点已因同天对失控则不被跨天对降级。
  - 同步更新模块文档 + 函数文档 + `aggregate_project` 文档。
- **测试**（`backend/scripts/test_westgard_r4s.py`）：用例4 改为期望同天两水平都 ooc；新增用例9 验证「同天已失控点不被跨天相邻对降级」。9 用例全过（本地 `scripts/test_westgard_r4s.py`）。
- **已完成（部署+回填）**：commit `80f9802` → push origin/main → `tcb cloudrun deploy`（正确喂参，提交成功）→ `/_diag/build` 返回 200（新 pod 就绪）→ `POST /qc-summaries/_recalc`（admin token）返回 `updated:65, total:94`、HTTP 200，无报错；updated 较上次 86 降为 65，系同天两水平改判均失控后 ooc 计数变化所致，证明新逻辑已生效。
- **前端**：`is_warning` 黄色「警告」标签此前已实现并部署；本次纯后端判定改动，前端无需改。
