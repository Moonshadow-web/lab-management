# 2026-07-16 工作记录

## 修复：室间质评"预览回报"404 + 员工角色只读收敛
- **404 根因**：`backend/app/api/v1/eqa.py` 的 `EQA_REPORT_DIR` 硬编码 `parents[3]/"data"/"eqa_reports"`，本地落在 `backend/data/eqa_reports`；线上 CFS 后挂载，最初上传的报告 PDF 在临时层被遮蔽，库里有 `report_file` 引用但磁盘无文件 → 预览 `GET /api/v1/eqa-plans/report/{id}` 返回 404 "报告文件缺失"。本地 49 个 PDF 实际在 `backend/data/eqa_reports`，所以本地正常、线上 404。
- **修复**：
  1. `EQA_REPORT_DIR = DATA_DIR / "eqa_reports"`（与 documents/instruments 的 `UPLOAD_ROOT` 一致，本地/线上均走持久卷）。
  2. 把 `backend/data/eqa_reports` 的 49 个 PDF 迁到 `data/eqa_reports`；DB 49 条引用全部命中，本地 49 个计划预览均 200。
  3. `.dockerignore` 加 `!data/eqa_reports/*.pdf` 放行报告 PDF；Dockerfile 把 `data/eqa_reports` COPY 到 `/app/backup/eqa_reports`，entrypoint 在 CFS 的 `eqa_reports` 为空/缺失时 `cp -rn` 恢复到 `/app/data/eqa_reports`（修复线上并防复发）。
- **员工权限守卫**（用户要求：文件管理不能上传新版本、仪器档案只能查看不能改、室间质评只看预览回报）：
  - `DocumentList.vue`："新版本"按钮加 `v-if="auth.canWrite('documents')"`。
  - `InstrumentList.vue`：校准记录对话框"上传报告/删除记录/新增校准记录"、档案抽屉"替换/删除/导入档案"加 `v-if="auth.canWrite('instruments')"`；保留"校准记录/档案"打开与"预览/下载"只读。
  - EQA 标签页"预览回报"本就对所有角色可见，无需改。
- 后端实测：49 个有报告的计划预览全部 200；前端 `npm run build` 通过。
- 待办：线上需**重新部署** CloudBase Run 才能让上述修复（尤其报告恢复到 CFS）生效；若 CFS 的 `eqa_reports` 已非空则不会覆盖，个别缺失的需在线重传。

## 新增：室间质评·仪器间比对模块（comparison）
- **需求**：同一项目在 2 台及以上仪器检测 → 按仪器分组建组（如 3 台 AU 生化 / 6 台 DXI800 / 3 台罗氏等），显示比对项目、频率半年一次、结果录入、报告生成/预览/上传/下载，保留表格编号。
- **对照表单**（已研读并种子）：021 定性报告、022 定量报告模板、024 DXI800、025 生化、026 凝血、027 早孕、071 血气。
- **后端**：
  - `models/comparison.py`：ComparisonGroup（分组：instrument_ids/参照仪器/levels/items[{name,te,mode}]/form_code/form_title）、ComparisonPlan（年+半年+比对日期/操作者/审核者/结论/报告路径）、ComparisonResult（定量 项目×水平 参照值+各仪器值）、ComparisonQualResult（定性 各仪器5样本P/N）。
  - `services/comparison_report.py`：`compute_data`（定量偏倚=(val-ref)/ref×100 或绝对；定性以参照仪器算符合率）、`build_docx`（按表单版式生成，严格保留表格编号 BG-SM-CZ-0xx）、`build_html`（预览）、`ensure_comparison_defaults`（幂等种子 6 分组，仪器按名称互不重复匹配；允许TE/偏倚方式取自表单，血气 PH/PCO2 为绝对、其余相对）。
  - `api/v1/comparison.py`：分组CRUD、计划CRUD、结果批量读写、报告 preview/generate/download/upload/delete；router 注册 `/api/v1/comparison`。
- **前端**：`api/comparison.js` + `views/comparison/`（Comparison 主页面、GroupDialog、PlanDialog、ResultEntry、ReportPanel）；路由 `/comparison` 与菜单"仪器间比对"（icon Coordinate，位于质控管理下）。
- **验证**：本地后端跑通 定量+定性 全流程（分组种子6个、建计划、录结果、生成/预览/下载 docx 均 200，预览 HTML 含表格编号）；前端 `npm run build` 通过。
- **部署**：deploy_run11（含上一轮提醒角色路由 + 本模块）已发起 CloudBase Run；SMTP 已在 cloudbaserc.json，`.gitignore` 已忽略 cloudbaserc.json。

## 北京市 EQA 上报流程修正（用户 20:13 纠正，重要）
- **S/CO 必填**：肝炎标志物(21)/感染性疾病系列B(86)/感染性疾病系列C(87) 上报时**不仅要填 P/N，还必须填 S/CO 比值**（S/CO 行是普通文本输入框，直接 fill 数值；这两类属 ECLIA，有 S/CO）。**胶体金（感染快检 88）例外**：仅填 P/N、无 S/CO。
- **先保存后发送**：发送前必须**先点「保存」再点「发送」**（两独立按钮），只点发送会丢数据。
- 已同步更新 `nccl-eqa-export` 技能 C 节：坑12(S/CO 必填+胶体金例外)、坑14(保存再发送)、步骤9(先保存后发送)；并修正项目 MEMORY.md 北京市临检中心条目（删掉错误"23:00后关闭"、已删 automation-1784129880885 引用）。
- **注意/风险**：此前（2026-07-15）我用 Playwright 自动上报这 4 项时，对 89/87/91 只填了 P/N、未填 S/CO、且直接点了"发送"未先"保存"。若那次自动提交仍是现行数据，则 89/87/91 可能缺 S/CO。用户表示本次已自行手工上报（含 S/CO + 保存），最终状态应以用户手工上报为准。下次自动化上报脚本必须按新规则补 S/CO 并加"保存"步骤。
- **20:25 用户确认结论**：因我自动上报 89/87/91 不完整（缺 S/CO+漏保存），用户已**手工重新上报**这 3 个 ECLIA 专业，最终以用户手工上报为准。**教训：定性 ECLIA 自动上报脚本不可轻信为最终成功——上报后必须让用户核对门户实际内容，勿自认完成。**

## 重新部署 CloudBase Run（已完成）
- 命令踩坑：`tcb run deploy` 已废弃强制报错，改用 `tcb cloudrun deploy -e cloud1-0gjhamv53ff2298d -s lab-management --force`；`--force` 不跳过灰度发布交互提示，需 `printf '\n' | ...` 管道喂回车选默认 No（自动切流量）。
- `.dockerignore` 追加 `!data/uploads/**` 放行整个上传目录（原 `*.pdf`/`*.xlsx` 会误伤文档 PDF/xlsx，导致镜像备份不全）；`data/eqa_reports/*.pdf` 此前已放行。
- 结果：`DEPLOY_EXIT=0`，`cloudrun list` 显示 lab-management 状态 normal、更新时间 2026-07-16 12:47:59、公网 Allowed；前端域名 `https://lab-management-282724-9-1408547492.sh.run.tcloudbase.com` 返回 200+HTML（容器就绪）。
- 验证限制：线上 jinzizheng 密码已非 123456（用户改过），无法用 API 直接确认预览回报 200；逻辑上 entrypoint 已在容器启动时把 49 份报告恢复到 CFS，请用户登录自测。若个别仍 404，说明 CFS 的 `eqa_reports` 非空（之前在线传过零散报告）未触发恢复，需 Webshell 进 `/app/data/eqa_reports` 核对或用控制台确认。
- 临时日志 `deploy_run.log` 已清理。

## 用户告知线上密码 + 补验预览回报（已闭环）
- 用户告知：**线上 jinzizheng 密码已改为 `Jzz6827556`**（2026-07-16）。已写入 MEMORY.md 的部署命令段（区分线上=Jzz6827556、本地种子库仍=123456 仅调试）。
- 用新密码登录线上成功（200），并实测 `GET /api/v1/eqa-plans/report/{1,4,28}` 全部返回 **200 + PDF 字节**，确认上次部署的 404 修复**已在线上生效**、报告已从镜像恢复到 CFS。问题闭环。

## 北京市临检中心报告抓取：新增"统计结果表"并合并（已完成）
- 需求：用户要在现有"汇总页"基础上，再抓"统计结果表"，两份合成一个 PDF 放进报告（report_file）。
- 探查结论：官网成绩列表 `pdfptscoretitle.asp` 每项目其实有**两类** PDF 链接——
  - 「统计汇总表」(路径 `/汇总/`) = 原已抓的"汇总页"（本室成绩汇总）；
  - 「统计结果表」(路径 `/定量/` 或 `/定性/`，链接文字如"2026年肿瘤标志物第1次室间质评统计结果表(美国)") = 用户要新加的（全部实验室原始结果/统计）。
- 改造 `outputs/download_bj_eqa.py`：① REPORT_DIR 从误写的 `backend/data/eqa_reports` 修正为 `data/eqa_reports`（与线上 `EQA_REPORT_DIR` 一致）；② 对每个 plan 同时下载「统计汇总表」+「统计结果表」，用 `pypdf` 合并成一份 PDF 写 report_file；③ 加 `--force` 选项重合并覆盖已有。venv 已装 `pypdf` 6.14.2。
- 执行：`--force --download` 重新合并 11 个北京市第1次 plan（常规化学A/内分泌/肿瘤标志物A/凝血/糖化白蛋白/PCT/细胞因子/感染快检/感B/感C/中孕期），全部成功（EXIT=0），本地 `data/eqa_reports` 对应文件已更新为合并版（抽查页数：常规化学A 10页、内分泌 5页、肿瘤标志物A 3页，均含两份报告）。库 report_file 已更新。
- 前置：cookie 过期(500)，用 `bj_login.js` 重新登录拿 `bj_auth_state.json`，从中提取 cookie 串写入 `bj_cookies.txt` 后列表可正常抓取（126 条链接 / 29 项目）。
- 线上同步已做（用户问"你让我挨个传吗"→ 明确不要手动传）：改 Dockerfile entrypoint 的 eqa_reports 恢复从 `cp -rn`(不覆盖/仅空目录) 改为 `cp -r`(每次部署强制用镜像里的合并版覆盖 CFS 的 eqa_reports，本地 repo 为权威源)。重新部署（cloudrun deploy --force，喂回车跳灰度，DEPLOY_EXIT=0）。线上验证：用新密码登录 + 对北京市有 report_file 的 8 个 plan 做**字节级 md5 比对，8/8 与本地合并版完全一致**、预览均 200 → 合并版已上线，无需用户手动传。
- 用户追问：① skill 是否记录这次拉取方式 → 已更新 `nccl-eqa-export` SKILL.md（B节链接结构写清两类报告+合并、参考脚本加 --force、通用后端知识改正确路径、验证小节路径改 `data/eqa_reports`）；② 成绩合格判定是否受影响 → 不影响（合并只写 report_file，不碰 result/score/qualified，判定与抓取解耦）。已记入 MEMORY.md 成绩下载段。
- 范围限制：当前仅 `round_no=第1次`（官网未发第2次）；第2次出分后脚本需放宽轮次匹配。

## 室间质评模块 UI 增强（2026-07-16 晚）
- 需求 1：总结文字框给长一些 → QCList.vue 总结区 `el-input` 由 `:rows="4"` 改为 `:autosize="{ minRows: 8, maxRows: 24 }"`（可随内容增长，最高 24 行）。
- 需求 2：总结区下方新增「结果报告合并打印」功能，分别取生化+凝血、免疫 2 类报告合并打印。
- 后端新增 `GET /api/v1/eqa-plans/reports-merge?year&half&category&department`：复用 summary 的分类（`_category_of`→生化+凝血/免疫）+ 部门（`_normalize_department`→org）+ 半年（`due_date` 月份 `_H1/_H2`）筛选口径，对筛选后已导入 `report_file` 且磁盘存在的计划，用 pypdf 合并为一份 PDF 返回 `inline`（浏览器直接打印）。无报告返回 404「该范围下没有可合并的已导入报告」。`requirements.txt` 加 `pypdf>=4.0.0`。
- **pypdf 版本坑**：环境装的是 pypdf 6.14.2，`PdfMerger` 已被删除 → 合并改用 `PdfReader`+`PdfWriter.add_page`（不可用 PdfMerger，否则 ImportError 500）。
- 前端：`eqa.js` 加 `mergeEqaReports({year,half,category,department})` 返回 blob；QCList.vue 加「结果报告合并打印」区（生化+凝血 / 免疫 两按钮 + 提示），沿用当前 `eqaYear/summaryHalf/summaryDept`，`mergePrint` 取 blob 新标签打开；错误时 404 显示友好警告。已加 `.merge-print` 样式。
- 本地验证（jinzizheng/123456）：4 组合并全部 200 且页数正确（北京市 生化+凝血 19页、免疫 17页；卫健委 生化+凝血 112页、免疫 52页）；2020/免疫 卫健委 返回 404 符合预期。前端 `npm run build` 通过。
- **未重新部署**：本次只改本地代码并实测，未上线；如需线上生效需再次 `tcb cloudrun deploy`（命令与之前一致）。

## 合并打印 PDF 无法加载：根因 + 修复 + 重新部署
- 用户反馈线上「结果报告合并打印」PDF 无法加载（接口本身正常：实测 200 + 真实 PDF 字节，最大卫健委生化+凝血 2.5MB）。
- **根因**：`request` 响应拦截器是 `(response) => response.data`，即把 `response.data`（Blob 本身）直接作为返回值。原 `mergePrint` 误取 `resp.data`（因 resp 已是 Blob，`.data` 为 undefined），再 `new Blob([undefined])` 生成损坏 Blob → 新标签打开空白/无法加载。
- **修复** `QCList.vue` mergePrint：① 直接用 `resp`（已是 Blob），不再取 `.data`；② `revokeObjectURL` 从 60s 延到 5min（避免大 PDF 未加载完被撤销导致空白）；③ 弹窗被拦截时降级为 `<a download>` 触发下载。
- 重新部署（yCuzgi / `deploy_run2.log`）显示 submission completed，但 **20:11 抓线上 JS 验证前端修复未生效**（bundle `index-N-cYslOP.js` 不含 `结果报告合并_` 下载降级代码）→ 灰度流量可能未切到新版本或前端构建缓存。已再触发部署（XzMQpW / `deploy_run3.log`），待抓 JS 复核是否含修复。

## 运维：本地后端启动慢（用户问“上次怎么说”）
- 上次约定的应对：① 后端 `uvicorn` **避免 `--reload`**（会递归监控 backend/ 全目录含 data/ 大库与 uploads，启动+热重载都慢且易重复 import 卡死），改普通启动、改完手动重启；② 重启前按 PID 精确 `taskkill` 旧 python 进程、确认 8123 空闲再起，否则旧进程占端口新进程起不来；③ managed Python 3.13.12 默认无 `openpyxl`，启动前需 `pip install openpyxl`（在 venv），否则 import 失败看似“卡住/起不来”。

## 纠正：合并打印前端其实已上线（验证查错文件导致虚惊）
- 20:24 复核：线上 `QCList-Xj6IUzut.js`（合并打印所在的按需加载路由分块）**含** `结果报告合并_` 下载降级逻辑与 `300000`(5min revoke)，且不含坏逻辑 `resp.data instanceof Blob`；与本地 `frontend/dist/assets/QCList-Xj6IUzut.js` 一致。
- 之前“前端未生效”的判断是**验证方法错误**：只查了首页主 chunk `index-N-cYslOP.js`，而合并打印代码在 Vite 拆出的 `QCList` 分块里，主 chunk 本就不含该功能。本地 build 主 chunk hash 也是 `N-cYslOP` 与线上一致 → 云端构建正常、修复早已上线。
- **结论**：无需改 Dockerfile、无需重部署。合并打印链路已闭环（前端含正确逻辑 + 接口返回真实 PDF 479KB）。用户“无法加载”应是在 deploy_run2/3 滚动部署未生效时测的；建议强刷浏览器(Ctrl+F5)再试。

## 冷启动 vs 常驻（用户确认）
- 用户确认慢的根因是 CloudBase Run 冷启动：按需计费下无流量实例缩容到0，下次访问需拉镜像/起容器/连CFS，首屏数十秒。解法=最小实例数1常驻（“一直跑1个”）。
- 现状：`cloudbaserc.json` 已是 `minNum:1, maxNum:1`（常驻已生效）。20:24 实测连续访问首页 0.12~0.26s，实例常驻、无冷启动 → 配置有效。
- 用户之前觉得慢的时段正值多次重新部署滚动更新实例，部署期间瞬时慢属正常；部署稳定后即恢复。
- 提醒：minNum=1 会持续计费（按实例时长，即使无人访问）；maxNum=1 单实例无水平扩容，实验室内部够用。日后若需更高并发可上调 maxNum 但保留 minNum=1。

## 修复：北京市肝炎标志物成绩回报漏抓（用户 20:34 反馈）
- **现象**：用户说"北京市肝炎标志物的成绩回报，一个也没抓到，官网上可是有的"。核查 DB：plan 88(第1次)/89(第2次) 的 `report_file` 均为空、`returned=1` 但成绩字段全空。
- **根因**：`outputs/download_bj_eqa.py` 的 `PROGRAM_TO_CODE` 映射**缺 `"肝炎标志物":"21"` 条目**；旧注释还误称"截至 2026-07-15 官网尚未发布，故跳过"——实为脚本漏配映射。但链接快照 `eqa_bj_links.json` 里**早就含 code=21**（两条：`21_2026_1` 汇总 + 定性/统计结果表，CDN 直链 `pdf.clinet.cn/.../2026年肝炎标志物室间质量评价/`），说明官网一直有，是脚本漏配。
- **修复**：① 脚本加 `"肝炎标志物":"21"`，并更正注释（编码21、统计结果表路径为/定性/、第2次未出分仍仅匹配第1次）；② 另写 `outputs/capture_hep_bj.py` 直接按快照 CDN 直链下载 code=21 的「汇总」+「统计结果表(定性)」合并 → `data/eqa_reports/肝炎标志物_第1次_88.pdf`（3页，含本室01110025、HBsAg…HCVAb、总成绩30/30 100%满意），并写库 plan 88 的 report_file + result="成绩100%"/score="100"/qualified=1。
- **关键经验（避免重蹈覆辙）**：download_bj_eqa.py 主流程依赖重新拉官网列表，需要**有效会话 cookie（~20-30min 过期）**；cookie 过期时重跑主脚本会失败。应优先用已解析的 `eqa_bj_links.json` 快照里的**永久 CDN 直链**直接下载（pdf.clinet.cn 直链不依赖会话），不要等重登。重新部署（deploy_run5.log / STIZfl）把新 PDF 推到 CFS。
- **第2次(plan 89)**：官网成绩页仅第1次出分，快照全为 `_2026_1`，第2次暂无法抓，待发布后需放宽 round_no 匹配（且需有效 cookie 重拉列表或用新快照）。

## 移动端适配（手机打开只有左侧菜单、右侧看不全）
- 根因：`AppLayout.vue` 的 `el-aside width="220px"` 固定宽度，手机屏(≈375px)下吃满大半，主内容区被挤成155px宽、表格/卡片溢出。Element Plus 容器默认不响应式。
- 修复：≤768px 时侧边栏改抽屉式（默认隐藏，顶部汉堡按钮☰唤出、点遮罩/选菜单自动收起，主区占满全宽；主区加 overflow-x:auto 横向滚）。桌面(>768px)完全不变。改动仅 `frontend/src/layout/AppLayout.vue`（模板加 overlay/hamburger/close、el-menu @select、脚本 isMobile+drawerVisible+resize 监听、scoped 样式 .aside-mobile/.overlay/.hamburger/@media）。
- 本地 `vite build` 通过；部署 wygVC7 已 submission completed。
- 线上验证(20:49)：主chunk=`index-B7QMn9vF.js` 含 `aside-mobile`/`hamburger`（True）；gzip 生效(首页+JS Content-Encoding:gzip)；JS Cache-Control `public,max-age=31536000,immutable`。移动端修复+gzip优化均上线。

## 用户反馈：部署后"主动汇报"体验差
- 用户指出我多次说"部署完成后验证并告诉你"，但不发新消息就无动静，体验差。属执行方式问题：把后台任务完成通知的"继续"落在"等用户再发消息"语境，未在通知到达时独立验证主动回。
- 后续约定：**部署/长任务一律用后台任务完成通知自动触发验证汇报，回复中不留悬空的"等确认"，绝不依赖用户再发消息催促**；若以"等部署完成"结尾，即代表我会在通知到时自动继续。

## 工作流纠偏：上报和报告抓取必须直接读写线上（用户 20:43 明确要求）
- **用户原话**："现在上报和报告抓取，需要直接从线上版本取值和导入"。
- **根因（已证实）**：线上库是 CFS 持久化的独立 SQLite(`/app/data/app.db`)，**部署只 `cp -r` 报告 PDF 文件、并不覆盖 `app.db`**。所以本地改 `data/app.db` 永远传不上线——此前肝炎 plan 88 在本地设了 report_file、部署多次，线上仍 404 即此因。
- **改动**：
  1. `download_bj_eqa.py` 加 `--online` 模式 + `push_online()`：合并好 PDF 后直接 `POST /api/v1/eqa-plans/report/{id}` 写入线上 CFS+库（端点自动解析成绩回填，`result/score/qualified` 可不传）。实测 push_online(88,...) 成功，线上 plan88 现 report_file 已填、`score=100`/`qualified=True`。
  2. 实测线上列表接口 `GET /api/v1/eqa-plans`（**无结尾斜杠**）→ 200/31条；对比线上 vs 本地北京市2026 计划，确认**仅 plan 88 曾漏传**，其余第1次计划线上 report_file 均已 True，与用户"其他都补上了"一致。修复后已对齐，无需批量同步。
  3. 顺带修 `main.py` bug：`spa_fallback` 里 `raise HTTPException` 但**未 import `HTTPException`** → 任何掉进兜底路由的请求（如带结尾斜杠的 API 路径）都 500。补 `from fastapi import FastAPI, HTTPException`。注：前端用无斜杠路径，列表本就正常，此前我测试多加斜杠造成 500 假警报。
- **新工作流（固定）**：报告抓取=北京官网下载合并→`POST /report/{id}` 直接导入线上（勿本地改库+部署）；上报=线上 `GET /{id}` 取值→提交北京→`PUT /{id}`(需qc_manager/admin)回写 returned/meta。导入后 `GET /report/{id}` 返回 200 即线上生效，无需部署。已同步写入 MEMORY.md「北京市临检中心」条目与 `nccl-eqa-export` 技能 C 节(坑15)。部署 deploy_run6(CQyIsN) 让 main.py 修复生效。

## 提醒功能（邮箱自动提醒）新增 2026-07-16
- 用户需求：增删发送人 + 可配置提醒类型(生化+凝血质评/免疫质评/设备校准) + 触发自动发信 + 未消除按7天/14天再发；测试用 815268425@qq.com/13752760970，质评14天、校准30天。
- 用户拍板：① 发信用 815268425 自收自发(需其 SMTP 授权码)；② 每日08:00 自动 + 手动「立即检查并发送」；③ 再发按剩余天数里程碑(质评剩7天、校准剩14/7天再发)；④ 短信暂不做、手机号仅存档。
- 实现：新表 notify_recipients/reminder_rules/reminder_send_logs；引擎 services/reminder_engine.py(run_reminders，里程碑去重+逾期每7天再发+邮件聚合)；API api/v1/reminders.py(增删改查/run(dry_run,as_of)/send-log)；main.py lifespan 种子默认3规则+初始运行+每日08:00 asyncio 定时；前端 ReminderSettings.vue(admin)+路由+菜单 Bell 图标。
- 本地验证(端口8137，降级日志模式)：规则/接收人种子正确；dry_run 预览17条 will_send=0(同里程碑幂等)；真实运行#1/#2 items_sent=0(幂等不重发)；as_of=2026-08-10 升级逻辑 27条中12条 will_send=True(逾期净触发再发)✅；send-log 校准项 sent_milestones='7,14,30' cnt=1 去重正确 ✅。
- **重要修正**：本地 jinzizheng 密码实测已是 Jzz6827556（bcrypt 校验 123456=False/ Jzz6827556=True），旧记忆「本地仍为123456」过时，已更正 MEMORY.md。登录本地服务用 Jzz6827556。
- 真实发信已验证（2026-07-16 21:47 起，用用户提供的 QQ SMTP 授权码）：`POST /notifications/test-email?to=815268425@qq.com` 返回 sent=True；后端启动 lifespan 初始运行在真实 SMTP 下**真实发出**待办汇总邮件（send-log 记录 34 条、sent_milestones 正确，证明 send_email 返回 sent=True）。SMTP 配置：`SMTP_HOST=smtp.qq.com SMTP_PORT=465 SMTP_TLS=false MAIL_FROM=815268425@qq.com`（授权码仅运行时 env，不入代码/记忆）。
- **幂等竞态修复**：发现 initial run(lifespan) 与本地手动 run 之间，SQLite 连接池复用读到陈旧事务快照，导致手动 run 偶发重复发信（items_sent=17）。修复：`run_reminders` 开头加 `db.rollback()` 强制干净事务。修复后降级+真实两次验证 manual run 均 items_sent=0、send-log 不变 ✅。
- **遗留 bug 已修（引擎发送语义）**：原代码在发送前就写 `sent_milestones`，降级模式（SMTP 未配）也会标"已发"→ 配好 SMTP 后每日任务因幂等永不补发。改为**仅 send_email 返回 sent=True 后才写 sent_milestones/last_sent_at/send_count**。
- 待办：部署 CloudBase 让每日08:00 自动发信生效；部署需给 CloudBase Run 服务配置 SMTP envs(smtp.qq.com/465/815268425@qq.com/授权码)，否则线上 initial run 降级不真发。线上库为 CFS 独立库，新表经 create_all 自动建、种子由 lifespan ensure_reminder_defaults 注入。

## 移动端精细适配（用户反馈“字显示反人类”）
- 21:30 用 Playwright 截线上 iPhone 视口真实渲染，定位问题：① 表格列数多被压成“科”“验”等竖条；② 顶部工具栏多按钮/开关挤在一行；③ 项目查询搜索框 placeholder 被截成“搜”；④ 卡片头部按钮与标题抢一行。
- 修复点：
  - `frontend/src/styles/mobile.css`：crud-toolbar 纵向排列且所有子元素 100% 宽；表格容器 `.crud-table/.page>.el-table` 加 `overflow-x:auto` + `min-width:760px` 保护列不被压；分页器居中换行；AppCard 头部纵向排列。
  - `frontend/src/components/CrudTable.vue`：表格外包 `.table-wrap`，移动端 `overflow-x:auto` + `min-width:760px`；操作列 link 按钮改为竖排。
  - `frontend/src/views/test_items/TestItemList.vue`：搜索区 `.search-area` 移动端 `flex-direction:column`；工具栏按钮/dropdown 100% 宽。
  - `frontend/src/components/AppCard.vue`：header 移动端纵向排列。
- 21:31 本地 build 因 Vite 缓存随机报 `Could not resolve ...`（reminders/notifications 文件实际存在），清 `node_modules/.vite` 后 build 通过。
- 21:32 部署到 CloudBase Run（任务 OOy9UW），等待完成后 Playwright 截图验证。

## 录入结果打印/导出：左右双栏 → 上下两行（已改）
- 用户说"项目多时改成上下2列表"，初版误做成左右双栏；纠正为**上下两张表叠放**（每张整页宽、项目减半、列宽翻倍、字更大）。
- `QCList.vue`：`buildPrintHtml` 项目>10 时切成上下两张 `<table>`（各带（一）/（二）小标题，`.tbl-block` 包裹），字号屏幕13px/打印11px；`exportExcel` 项目>10 时拆 `groups` 上下两张表，**两组共用同一列区域**（sheetCols=前半项目数+1），使 `fitToWidth=1` 每张表都占满 A4 横向页宽（若按 totalCols 合并列宽，第二张只占半页）。单表时保留 `printTitlesRow='3:3'`。`npm run build` EXIT=0。

## 备注框分离：系统说明 vs 人工备注（已改）
- 用户要求：录入结果弹窗的"备注"只能人工填、且要打印出来；系统自动返回的内容不要填进这个框。
- 链路：后端自动解析成绩时把"β2-微球蛋白不适用"等系统说明存进**计划级 `eqa_plans.note`**；前端录入结果弹窗此前未把它和人工备注（`result_data.meta.note`）分开，易混淆。
- 改动 `QCList.vue`：
  1. 弹窗备注框上方加只读灰条"系统自动说明：{resultPlan.note}"（`.sys-note`），不参与人工备注、不打印进备注栏。
  2. 打印模板 `.meta` 后加 `.sys-note-print` 单独一行"系统自动说明：…（本行为系统生成，非人工备注）"；人工"备注"行保持 `meta.note`（留空供手写）。
  3. `exportExcel` 签字区前加"系统自动说明"单独行（斜体灰字），不与人工备注混。
  4. 打开弹窗 `openResultDialog` 加历史防护：若已存 `meta.note` 与系统说明雷同，视为系统内容，**不回填人工备注框**（留空给人填）。
  5. 备注框 placeholder 明确"人工填写，打印用"。
- 构建中暴露预存在 bug：`ReminderSettings.vue` 两处 `import ... from '../../api/...'` 路径多一层 `../`（从 src/views 应为 `../api/...`），导致 `Could not resolve "../../api/reminders"` 构建失败。已改为 `../api/reminders`、`../api/notifications`，构建恢复 EXIT=0。

## 打印时屏蔽系统自动说明（已改）
- 用户追加要求：系统自动说明**打印时不要出现**。
- `buildPrintHtml` 删除 `.sys-note-print` 那行（原1439行），打印纸质版不再输出系统说明；弹窗屏幕上的只读灰条（`.sys-note`，546-547行）保留作参考提示。
- 注：Excel 导出版（`exportExcel` 1552行）仍保留"系统自动说明"单独行，因用户只说"打印时"。若也要 Excel 屏蔽，再提。
- `npm run build` EXIT=0。

## 移动端 Tab 适配（质控管理 Tab 拥挤）
- 22:00 截图复核发现：QC 管理顶部 el-tabs（室内质控月结/室间质评/仪器间比对）在 390px 下折叠成箭头、标签被挤压。
- 修复：`frontend/src/styles/mobile.css` 新增 el-tabs 移动端适配——nav 改 flex-nowrap + overflow-x:auto（隐藏滚动条与 prev/next 箭头），item flex-shrink:0 + 字号13px，标签完整可横滑。
- 22:02 本地 build 通过（清 .vite 缓存）；部署任务 6d2Yce submission completed。
- 22:04 线上验证：CSS index-7Aw5GHUU.css 含 el-tabs 适配规则(True)；主 chunk=index-BksoGu1w.js。QC Tab 修复上线。
- **本轮移动端精细适配累计修复**：① 弹窗全宽可滚 ② 表单 label 顶部 ③ 表格横向滚动不压扁 ④ 工具栏纵向排列 ⑤ 分页器居中 ⑥ 卡片头纵向 ⑦ Dashboard 2列 ⑧ 项目查询搜索区纵向 ⑨ el-tabs 横滑。覆盖登录/工作台/项目查询/文件/仪器/质控/试剂/继教/性能验证/15189/编辑弹窗共11个视图。
- 验证方式：Playwright iPhone视口(390x844)截线上真实渲染，截图存 mobile_shots/（共11张）供用户目检确认。

## 提醒功能增强：按接收人路由不同提醒类型（2026-07-16 22:0x）
- 用户新需求：提醒要"按角色给不同的角色发不同的信息"，例：A发校准、B发生化质评、C发免疫质评。即每个接收人订阅特定提醒分类。
- 实现：
  - `models/reminder.py` NotifyRecipient 新增 `rule_categories`(VARCHAR(200)，CSV 的 rule.category；空=不接收)。
  - `services/reminder_engine.py` `run_reminders`：原先给"所有启用邮箱接收人"发全部事项，改为按 rule.category 过滤——仅发给 `rule_categories` 含该分类的接收人；`get_email_recipients` 只取一次；planned 加 `to` 字段(匹配邮箱列表)便于预览核对。`DEFAULT_RECIPIENT` 加 `rule_categories="eqa_biochem_coag,eqa_immuno,calibration"`(全订阅，保默认行为)。
  - `api/v1/reminders.py` RecipientIn/RecipientUpdate 加 `rule_categories`。
  - `main.py`：`_migrate_schema` alters 加 `notify_recipients.rule_categories`；lifespan 在 `ensure_reminder_defaults` 后**回填**已有接收人(rule_categories 空)→全部现有规则分类(避免升级后收不到)。
  - 前端 `ReminderSettings.vue`：发送人表格加"接收提醒类型"列(标签展示)；编辑弹窗加"接收提醒"勾选组(categoryOptions 由 rules 列表生成)；新增接收人默认全选；提交时数组→CSV、编辑时 CSV→数组。`import` 加 `computed`。
- 本地验证(8123，jinzizheng/Jzz6827556)：建 A(校准)/B(生化质评)/C(免疫质评) 三接收人，dry_run 17 条计划项 `to` 正确——生化质评项只含 b@example.com、免疫质评项只含 c@example.com、校准项只含 a@example.com（另含默认全订阅的 815268425@qq.com），路由生效 ✅。前端 `npm run build` 通过(ReminderSettings 分块 14KB)。
- 部署：之前一次 `tcb cloudrun deploy` 卡在第二个交互提示"Platform currently has deployment tasks running. Confirm (Y/n)"（首个灰度提示已用 `\n` 答 No，第二个需 Y 才能取消进行中任务并部署最新代码）。本次用 `printf '\nY\n' | tcb cloudrun deploy -e cloud1-0gjhamv53ff2298d -s lab-management --force`(deploy_run10.log)。SMTP envs 已在 cloudbaserc.json(envs) 配置好（smtp.qq.com/465/815268425@qq.com/授权码），前次部署未成功因此本次为首次真正上线提醒功能+路由增强。
- 注：cloudbaserc.json 已加进 .gitignore(含 SECRET_KEY 与 SMTP 授权码，勿入 git)。

## 质评 item 字段 ↔ 项目库(test_items) 一致性统一（仅分析，未改库）
- 用户要求："统一主数据库，以项目查询里的为基准，对照关联的室间质评所有项目做到一致性。"先参考既有只读关联模块 `backend/app/api/v1/eqa_associations.py`（项目库×质评关联页 `EqaAssociationManage.vue` 同款算法）。
- 分析脚本 `outputs/eqa_unify_analysis.py` / `outputs/eqa_unify_report.py`（复刻 eqa_associations 纯函数，零依赖 sqlite3）。
- 结论：质评 item 拆 token 共 591（排除"正确度"类）；**可统一改写 331 处**（安全可改 137 / 需复核 194）、已一致 249、无法匹配 3。
- **两风险（已向用户说明，故选"先出对照表暂不改动"）**：
  1. 破坏已录入成绩：plan 的 `result_data.cells/items/units` 的 **key 直接=item token 原样**（验证 plan#6），改 item 会导致旧成绩 key 对不上→丢失；42 条已录入 plan 受影响。
  2. 自动匹配有 bug：子串匹配把 `INR` 误配成「可溶性转铁蛋白受体」（plan 19/20/50/51）；`总铁结合力`→`不饱和铁结合力/总铁结合力`（含"/"会二次拆分，3 处）；`血管紧张素II`(73)、`京津冀鲁盲样`(54/55) 无法匹配。
- 交付：`outputs/eqa_unify_report.csv`（331 行+风险标记列，Excel 可开）、`outputs/eqa_unify_report.md`（按"需复核/安全可改"分组）。用户待逐条审核后决定统一策略（选项：全批量/只改未录入/全改+迁移key/反向补项目库别名）。
- 待办：若用户确认"缩写→中文"中有错配（如 INR），需先**修复 eqa_associations.py 的匹配算法**（INR 应经 curated 命中"凝血酶原时间国际标准化比值"，子串分支误中，需加命中优先级/黑名单）再统一。

## 质评↔项目库 统一：采用 D反向（库别名）+ 梅毒正向改名（2026-07-16 22:4x）
- 用户逐条拍板映射后选 **D反向**：把质评缩写/异名补进 `test_items.aliases`，库本身成唯一真相源；不改质评 item 字段（不破坏已录入成绩）。梅毒两项**正向改名**（用户明确）。
- **库内改动**（事务，备份 `data/app.db.bak_eqaunify_20260716_225320`）：
  - id82 梅毒螺旋体抗体→**梅毒特异性抗体**（code TP-Ab 保留）+ 别名加 `TPAb`；id208 梅毒特异性抗体（仅孕妇）→**梅毒特异性抗体**（code TPPA 保留）+ 别名加 `TPAb`。两行同名、编号/别名/品牌/方法各异。
  - 别名补齐：id175 甲型肝炎病毒IgM抗体+`HAV-IgM`；id77 总Ⅰ型胶原氨基端延长肽+`TP1NP`；id40 肌酸激酶同工酶+`肌酸激酶-MB`；id125 凝血酶原时间+`INR`；id132 血浆蛋白C活性+`蛋白C`；id188 血管紧张素Ⅱ+`血管紧张素II`；id60 甲状腺球蛋白(已有Tg)；id111/112 免疫球蛋白G/M(已有IgG/IgM)。
  - 京津冀鲁盲样：不配（不动）。
- **算法修正** `backend/app/api/v1/eqa_associations.py` _CURATED_EQA_ALIASES：
  - `inr`→凝血酶原时间（原错指"凝血酶原时间国际标准化比值"库无此名）；`ck-mb/ckmb/肌酸激酶-mb`→肌酸激酶同工酶（原错指"肌酸激酶-mb"库无此名）；`tpab/tppa`→梅毒特异性抗体（因改名）。
  - 消歧：加 `tg`→甲状腺球蛋白（避免被甘油三酯TG抢，归一化同为tg且id更小）、`igg`→免疫球蛋白G、`igm`→免疫球蛋白M（避免被抗-X-IgG/IgM抢）。
- **覆盖率验证**（复刻 auto+manual 逻辑）：EQA 细项 token 575（排除正确度），auto 命中 573 = **99.7%**；唯一未匹配=京津冀鲁盲样（用户指定不配）→ 实质 100% 可匹配项已统一。
- 说明：尿液定量生化/脑脊液生化的同名 token 仍会先 auto 命中血清版，但 `_MANUAL_ASSOCIATIONS` 已按 id 把这些尿液/脑脊液条目强制标记为"有质评"（id139-150/151-153/205/210 等），关联页展示正确；该碰撞无法仅靠库别名消歧，靠 program 标本类型 override 兜底。
- **改名副作用提示**：`qc_monthly_summaries.test_item` 存的是名字字符串，旧"梅毒螺旋体抗体/梅毒特异性抗体（仅孕妇）"历史行不会自动更新（属历史 QC 汇总，可接受）；关联页按 id 分组不受影响。
- 注：改了 backend 文件，本地若后端在跑需重启 8123 才生效；前端无需改。

## 仪器间比对模块：5点整改（并入质控Tab / 500→400 / 档案关联 / 型号显示 / 遮蔽）
- 用户5点整改要求：①仪器间比对应在**质控管理模块内**做子Tab（非顶级菜单）；②新建分组保存 500 报错须修；③用仪器档案里"仪器与项目关联"(test_items.instrument_group)判定哪个仪器测哪个项目；④仪器名称要显示**可识别型号**(如"贝克曼AU5821 A")非通用名("全自动生化分析仪")；⑤预置仪器组模板(如3台AU生化)，只纳入≥2台共有项目，涉及3台/2台不一 → 录入与报告需**遮蔽**不适用仪器。
- **后端** `api/v1/comparison.py`：
  - `create_group`/`update_group` 包 `try/except IntegrityError`→`HTTPException(400,"分组名称已存在，请更换名称")`+`db.rollback()`（修 #2 的 500=重名唯一约束未捕获）；另修 `update_group` 预存 bug：`g.items=json.dumps(data["items"])`（data 经 body.model_dump() 已是 dict，原 `[i.model_dump() for i in ...]` 触发 AttributeError 500）。
  - 新增 `GET /comparison/instruments/options`→`[{id,name(disp_name),raw_name,model,status}]`（#4）；`POST /comparison/groups/resolve-items`(ResolveItemsPayload: instrument_ids/category/min_count)→调 `resolve_common_items`（#3+#5）。
- **后端** `services/comparison_report.py`：
  - `disp_name(inst)`=model(≤40字)否则 name（#4）；`_instruments_of` 返回含 model/去重/补参照。
  - `compute_data` 定量+定性两分支都加 `_applicable`/`_qual_applicable`+`masked` 标记，被遮蔽单元格 `{value:"",masked:True}`，汇总只统计适用仪器（#5）。修预存**定性 HTML/DOCX bug**：原读 `ins["results"]/["agreement"]`(错 dict)→改读 `row["insts"][ins["id"]]`+遮蔽。
  - 新增 `_build_te_lookup/_split_tokens/resolve_common_items(db,instrument_ids,category,min_count)`：经 test_items.instrument_group token→小家族→仪器 id，回 `{items:[{name,label,te,mode,instrument_ids}],instruments:[...]}`（#3）。
  - `ensure_comparison_defaults` **自愈**：加 `_stale()` 检测(重复id/空/含非在用仪器) 修复旧脏种子(曾为[2,2,2])。正确在用 id：生化=[67,68,5]、DXI800=[69,70,71,72]、凝血=[9,10,11]、早孕=[74,73]、血气=[13,21]、定性=[14,15,16]。
- **schemas** `comparison.py`：GroupItem 加 `label/instrument_ids:list[int]=[]`；新增 `ResolveItemsPayload`。
- **前端**：`api/comparison.js` 加 `instrumentOptions()`/`resolveItems()`；`QCList.vue` 占位Tab 换成 `<el-tab-pane label="仪器间比对" name="cmp" lazy><Comparison/></el-tab-pane>`+import(#1)；`router/index.js`+`AppLayout.vue` 删独立 comparison 路由与顶级菜单(#1)；`Comparison.vue` loadInstruments 改用 instrumentOptions；`GroupDialog.vue` 重写(仪器/参照显示型号+状态标签、"按仪器档案生成共有项目"按钮 onResolve、item 表加 中文名+适用仪器(遮蔽)多选列)；`ResultEntry.vue` 加 `isApplicable`+遮蔽渲染(`/`)+保存过滤被遮蔽值(#5)。
- **验证**(本地 8123 jinzizheng/Jzz6827556，脚本 scripts/test_cmp_api.py + test_cmp_flow.py)：instruments/options 200 count=77 型号可识别；6 种子组 id 正确；dup-name→400；resolve-items 生化 49 项 appl_ids=[5,67,68]、instruments 显示"贝克曼AU5821 A/B"；全流程 105 个遮蔽单元格渲染+docx 生成通过。前端 `npm run build` EXIT=0，`QCList-Cx2lFqBB.js`(1MB) 含 groups/resolve-items、instruments/options、按档案生成共有项目、适用仪器（Comparison 已并入 QC 分块）。
- **部署**：`printf '\n' | tcb cloudrun deploy -e cloud1-0gjhamv53ff2298d -s lab-management --force`(deploy_cmp.log)。Dockerfile 多阶段容器内 `npm run build`，源改即上线，无需手动 copy dist。

## 批号累积靶值板块（质控管理模块新增）
- 用户需求：删「留样再测」tab；新增「批号累积靶值」板块。两种累计方法：常规法(≥10暂定/≥20确立) 与 即刻法(≥3次起按SI界值表判在控/警告/失控，累计到20次)。质控品：生化多项质控品(仅存档上传PDF可预览)、伯乐免疫多项、昆涞免疫多项(可录入结果)；用户确认粒度=一盒质控品一条批号(含多项目，逐条结果标所属项目，靶值按项目分别累计)；即刻法失控值保留标记不自动舍去(人工决定)。
- 实现：
  - `backend/app/models/qc_target.py`：QCTargetBatch(主记录:qc_material/lot_no/instrument/method/mode/established/targets_json/archive_*/note) + QCTargetResult(明细:analyte/value/qc_date/seq/si_upper/si_lower/status/is_out/operator)。
  - `backend/app/services/qc_target.py`：SI界值表(n=3..20, n2s/n3s)、mean/sd、classify_immediate(在控/警告/失控)、classify_conventional(±2s/±3s)、compute_analyte(per-analyte统计+确立阈值)。
  - `backend/app/api/v1/qc_target.py`：make_router做batches CRUD + 录入/查明细/删结果/切换失控标记/手动确立/上传存档/预览；write_roles=admin,qc_manager。
  - 前端 `frontend/src/views/qc/TargetValueBoard.vue`(列表+新建/编辑弹窗+详情录入弹窗[archive显PDF预览;entry显per-analyte累计卡+逐次录入+历史表+即刻法实时判读]) + `api/qc_target.js`；QCList.vue删留样再测tab、批号累计靶值tab接组件。
- **坑**：make_router(prefix=\"/qc-target-batches\") 后手动 append 进同 prefix 的 router，再经 router.py include_router 会双重前缀→405。修正：make_router prefix=\"\"，重写路由path为 /qc-target-batches* 再 append。已 unit test 即刻法(文献n=3→在控)与常规法(n=10可暂定/n=20确立)通过。
- 部署 task HJe1Oe(submission待确认)。部署后跑 verify_qc_target.py(建批/录入/即刻法判定/失控保留/确立/清理) + Playwright 截桌面+移动端新板块验证。
