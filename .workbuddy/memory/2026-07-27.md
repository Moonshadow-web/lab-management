# 2026-07-27 工作日志

## 到货接收模块改造 + 试剂配送角色（commit b603efd，已推送部署）

### 需求（用户原话要点）
1. 新增收货里搜索试剂要**模糊搜索**
2. 填好的收货单可以**打印**
3. 有**确认接收**按钮，接收人**默认为系统登录人员**
4. **只有确认接收后**才进入实时库存
5. 权限增加角色**试剂配送**：仅能看到货接收界面；只能新建/修改自己建的；不能删除、不能修改他人记录；工作台也不能看

### 后端改动
- `models/reagent_management.py` Receiving 新增 4 字段：`created_by` / `is_confirmed`(默认False) / `confirmed_at` / `confirmed_by`。
- `api/v1/reagent_management.py`：
  - `create_receiving`：**不再建时入库存**，改写 `created_by=当前用户`、`is_confirmed=False`；权限 `require_roles("admin","reagent_manager","reagent_delivery")`（修复原 orphan 的 `lab_technician`）。
  - 新增 `POST /receivings/{id}/confirm`：写入实时库存(累加 ReagentStock)、置 is_confirmed/confirmed_at/confirmed_by；重复确认 400 防重复加库存；接收人空时取当前登录人。
  - 新增 `PUT /receivings/{id}`：编辑收货单；**已确认则 400 不可改**；试剂配送角色仅能改 `created_by==当前用户` 的记录(403)，管理员/试剂管理员可改任意未确认单。
  - `list_receivings` 增加 `confirmed` 筛选；试剂配送角色仅看自己(`created_by==当前用户`)的记录。
- `main.py` 迁移回填：`UPDATE reagent_receivings SET is_confirmed=1 WHERE is_confirmed IS NULL`（旧记录改版前已入库，视为已确认，避免确认时重复加库存）；新列由 `_ensure_missing_columns` 自动补。
- 权限常量：`module_permission.py` 加角色 `reagent_delivery`、模块 `reagent-receivings`(默认 admin/reagent_manager/reagent_delivery)、`ALL_MODULES`；`security.py ROLE_LABELS`、`users.py ROLE_OPTIONS` 均加「试剂配送」。`DEFAULT_MODULE_PERMISSIONS` 里 `reagents` 仍仅 admin/reagent_manager（试剂配送拿不到试剂管理其余页）。

### 前端改动
- `ReagentReceivings.vue` 重写：远程模糊搜索添加试剂(`listReagentItems q`，el-select remote)；状态列(待确认/已确认+确认人时间)；**确认接收**按钮；**打印**对话框(`#print-area`+`@media print`)；仅本人未确认可**编辑**；无删除按钮；`canWrite('reagent-receivings')`。
- `api/reagent.js` 加 `updateReagentReceiving` / `confirmReagentReceiving`。
- `store/auth.js`：FALLBACK 加 `reagent-receivings`；`canAccessMenu` 对 `reagent_delivery` 也严格按授权收口（同 technical_support）。
- `router/index.js`：receivings `moduleKey` 改 `reagent-receivings`；试剂配送访问 `/dashboard` 重定向到 `/reagent/receivings`。
- `layout/AppLayout.vue`：新增独立「到货接收」菜单(moduleKey `reagent-receivings`)，从试剂管理子项移除避免重复；**试剂配送隐藏工作台菜单**。

### 构建标记
`_BUILD_MARK=reagent-receiving-confirm-role-2026-07-27`。已 `tcb cloudrun deploy --force` 提交（自动切流量，无灰度）。

### 待核对
- 部署后请在 Cloud 控制台看构建状态/流量；`curl /api/v1/_diag/build` 返回新标记即生效。
- 验证建议：用 admin 新建收货单→确认接收→查实时库存是否已加；建一个「试剂配送」角色用户(用户管理里角色下拉已含)验证其只见得到货接收、只能改自己建的、无工作台。
- 注：未自动建「试剂配送」种子账号（users 表非空 seed 不生效）；由管理员在用户管理手动建。

## 排班模块：换班微信推送 + 计划简化 + 早班/连班提醒时机（commit acb69ad，已部署）

### 需求
1. 发起换班申请时，通过**微信(ServerChan)推送**给对方，提醒登录系统确认。
2. 「排班计划」冗余 → 弱化：所有排班自动归属默认计划「主班表」，月视图自动取，前端**移除计划下拉/计划管理 tab**。
3. 换班入口：工作台换班按钮直达 `?tab=month`（默认当月，可选下月）；之前已去掉点排班计划步骤。
4. 提醒时机变更：**早班=前1天11:30**；**连班=前1天11:30 + 当天11:30**。

### 后端改动
- `main.py`：`ensure_scheduling_defaults` 末尾 upsert 默认计划「主班表」(start 2000-01-01 / end 2099-12-31)；`_reminder_scheduler` 拆成 2 个定时点：08:00 跑非班次类(exclude shift_early/shift_continuous)，11:30 只跑班次类(only_categories)。
- `reminder_engine.py`：`_milestones` 早班=`["1_early"]`、连班=`["1_cont","0_cont"]`（字符串里程碑区分前1天/当天）；`_fetch_items` 早班仅 days_left==1、连班 days_left in (1,0)；`_record_send`/`run_reminders` 支持字符串里程碑去重；`run_reminders` 加 `only_categories`/`exclude_categories` 参数。
- `scheduling.py`：新增 `GET /scheduling/default-plan`（返回主班表 id/起止/发热固定人）；`request_swap` 建 Notification 后按 `NotifyRecipient.name==对方` 匹配启用且 wx_uid 非空的接收人，调 `send_serverchan` 推送（try/except 仅记录，不阻断建单）；引用 `FRONTEND_ORIGIN` 拼链接（`/scheduling?tab=myswap`）。
- `diag.py`：`_BUILD_MARK` → `scheduling-swap-wechat-plan-simplify-2026-07-27`。

### 前端改动
- `api/scheduling.js` 加 `getDefaultPlan()`。
- `Dashboard.vue` 换班按钮 `?tab=swap`→`?tab=month`。
- `SchedulingList.vue`：移除「排班计划」tab-pane 及 2 个 plan `<el-select>`；`genDays` 默认 14→30；`onMounted` 先 `await loadDefaultPlan()` 再初始化月份与首次查网格；删 `loadPlanOptions/onPlanChange/planOptions` 及整块 plan CRUD 死代码；`loadGrid/onGenerate/loadMatrix/saveMatrix/submitBatch` 守卫文案改「默认排班计划未就绪」；`selPlan` 由 `getDefaultPlan()` 赋值。

### 部署验证
- 已 `git add -f frontend/dist` + 提交推送 + `tcb cloudrun deploy --force`（选 No=自动切流量）。
- 轮询确认：`/api/v1/_diag/build` 返回新标记；`/assets/SchedulingList-B1wq83Wu.js` 返回 200。
- **注意**：部署提交后约需 3-4 分钟才滚出新容器，期间内网仍返回旧标记+旧资产 404，属正常构建延迟，非灰度卡死。

### 待核对
- 在 Cloud 控制台确认构建/流量状态。
- 验证：用 A 给 B 发换班 → B 是否收到 ServerChan 微信；连班当天 11:30 是否补推；月视图默认取当月且可切下月、无需选计划。
- 微信推送依赖对方在「接收人」里登记了启用且填了 SendKey(wx_uid) 的 ServerChan 通道；未登记则静默跳过（仅记日志）。

## DB 复合索引补建确认
- `ix_qc_monthly_summaries_ym_inst (year, month, instrument_id)` — 在外网 CloudBase MySQL 控制台 SHOW INDEX 确认**已存在**（Cardinality: year=1, month=1, instrument_id=17），无需手动 ALTER TABLE。之前沙箱无法连内网 DB 以为未建，实际某次部署已落表。

## LIS 别名映射 7 条 + OxLDL/CER 质量目标（commit a2f4b1d，已部署）
### 别名映射（_LIS_ITEM_ALIASES）
| LIS 名（规范化） | → 规范名 |
|---|---|
| c-反应蛋白 | C反应蛋白 |
| apo-b | 载脂蛋白B |
| apo-a | 载脂蛋白A |
| 碱磷酶 | 碱性磷酸酶 |
| 抗链-o | 抗链球菌素O |
| λ轻链测定 | λ轻链（血清） |
| κ轻链测定 | κ轻链（血清） |

### 质量目标新增
- **OxLDL → 6%**：QC_GOAL_EXACT_OVERRIDES + seed WST403_ITEMS
- **CER → 8%**：QC_GOAL_EXACT_OVERRIDES + seed WST403_ITEMS

### BUILD_MARK
`lis-aliases-oxldl-cer-goals-2026-07-27`（等待容器滚动生效）
## 早/连班提醒时机可配置 + 早连班排除名单（commit 待部署，BUILD_MARK=scheduling-shift-config-exclude-2026-07-27）

### 需求
1. 提醒设置页：早/连班「当天 11:30」做成可配置（连班前一天 + 当天两档开关；早班默认仅前一天）。
2. 排班的人里，不参与早班和连班：杨静、王春馨（仍正常排白班）。

### 后端改动
- `models/reminder.py` `ReminderRule` 加 `shift_send_prev_day`/`shift_send_same_day`（bool 默认 True）；`models/scheduling.py` `SchedulingConfig` 加 `early_continuous_excluded`（JSON 默认 list）。
- `schemas`：`RuleIn/RuleUpdate` 加两开关；`SchedulingConfigBase` 加 `early_continuous_excluded`。
- `reminder_engine.py`：`_shift_milestone`/`_milestones`/`_fetch_items`（shift 分支）按两开关门控里程碑；shift `_fetch_items` 读 `SchedulingConfig.early_continuous_excluded` 排除对应人（这些人不上早/连班也不收提醒）；补 `from ..models.scheduling import SchedulingConfig`。
- `scheduling.py` `put_config` 持久化 `early_continuous_excluded`；`generate_assignments` 自动生成早/连班时从 shift_pool 剔除该名单（仍排白班岗）。
- `main.py` 种子 `SchedulingConfig` 默认 `early_continuous_excluded=["杨静","王春馨"]`。
- `DEFAULT_RULES` 早班 `shift_send_prev_day=True,same_day=False`；连班两者均 True。

### 前端改动
- `ReminderSettings.vue` 规则表对 `ref_kind=='shift'` 显示「前一天11:30 / 当天11:30」两个 tag；编辑框对 shift 规则显示两个开关（早班仅前一天，连班两档）。
- `SchedulingList.vue` 设置页摘要加「不参与早/连班」展示；配置弹窗加「不参与早班/连班人员」文本框(复用 parsePeople)；`onMounted` loadConfigSummary 同步加载该字段。

### 部署验证
- 已 `git add -f frontend/dist` + 提交推送 + `tcb cloudrun deploy --force`（选 No=自动切流量）。
- 轮询 `/api/v1/_diag/build` 返回 `scheduling-shift-config-exclude-2026-07-27` 即生效；新列由 `_ensure_missing_columns` 启动自愈补齐（MySQL 路径），无需手跑迁移。

### 待核对
- Cloud 控制台确认构建/流量；提醒设置页切早/连班规则看两个开关；排班设置页看杨静/王春馨已在排除名单（默认种子）。
- 验证：关掉连班「当天 11:30」后 run dry-run（as_of=连班当天）不该再发 0_cont；生成白班后杨静/王春馨不应出现在早/连班行。

## _norm 剥离(自动)/(自动质控)前缀 + 免疫项目别名（commit 364ca7e，已部署）

### 需求
- 抗链-O 规范名修正为"抗链球菌溶血素 O"（补全 溶血素）
- 解析时去掉 LIS 前缀「（自动质控）」或「（自动）」
- 补充免疫/血脂别名：sdLDL-C/TPAb/HCV/HBsAg/HIV/CT/IL-6

### 改动
- `_norm()`：新增前缀剥离逻辑 `(自动质控)` / `(自动)` → 去掉后继续匹配
- `_LIS_ITEM_ALIASES`：
  - 修正 `抗链-o` → `抗链球菌溶血素 O`（原 抗链球菌素O 缺 溶血素）
  - 新增 8 条：sdldl-c/小而密低密度脂蛋白胆固醇、tpab/梅毒特异性抗体、hcv/丙型肝炎病毒抗体、hbsag/乙型肝炎病毒表面抗原、hiv/人类免疫缺陷病毒抗原及抗体、ct/降钙素、il-6/白介素-6、sdldl-c/小而密低密度脂蛋白胆固醇

### BUILD_MARK
`qc-strip-prefix-immuno-aliases-2026-07-28`

## 订购管理模块：单号默认日期+顺序 + 试剂配送可访问 + created_by收口/确认锁定/无删除（commit f12017d + 406a7fe，已提交部署）

### 需求
- 新建订购单号默认=日期+顺序(如 2026072701)；级别下拉加「试剂配送」；试剂配送只能新建+改自己建的(改他人403)；已确认单不可改；无删除入口。
- 隐含：试剂配送须能进订购页 → 新增 `reagent-orders` 模块权限 + 独立「订购管理」菜单。

### 改动（f12017d）
- `models/reagent_management.py` ReagentOrder 加 `created_by/is_confirmed/confirmed_at/confirmed_by/created_at`。
- `reagent_management.py`：新增 `_gen_order_no(db)`（YYYYMMDD+当日2位序号+碰撞顺延）；`GET /orders/_next-no`；`create_order` 写 created_by + 自动/手填单号；`update_order` 已确认403 + 试剂配送改他人403；新增 `POST /orders/{id}/confirm`；**删除 delete_order**；list 试剂配送仅看自己。
- `module_permission.py` 加模块 `reagent-orders`(admin/reagent_manager/reagent_delivery) + ALL_MODULES；`auth.js` FALLBACK 加；`router` orders moduleKey 改 `reagent-orders`；`AppLayout.vue` 独立「订购管理」菜单。
- `ReagentOrders.vue` 加创建人列、确认按钮、编辑按 canEditRow 显隐、移除删除、onNewOrder 预填单号。
- `UserList.vue` 级别下拉加「试剂配送」option + ROLE_LABELS。

### 构建标记事故（406a7fe 修复）
- f12017d 提交时 `diag.py _BUILD_MARK` 实际仍是父提交 `qc-strip-prefix-immuno-aliases-2026-07-28`（并发 AI 覆盖/此前 edit 未落库），但 commit message 误写 reagent-orders 标记。
- 经 git 比对确认：live 标记 == f12017d 实际内容 → **证明首轮部署生效、流量已切、功能已上线**（非灰度卡死）。
- 已commit 406a7fe 把 `_BUILD_MARK` 更正为 `reagent-orders-selfgated-2026-07-27-2306` 并二次部署。
- **注意**：本工作区存在其他并发 AI 会话的未提交改动（QCList.vue、backend/scripts/*.py、材料采购明细.xlsx），改 diag.py 时仅 `--only` 提交了该文件，未触碰他人改动。

### 当前状态（待用户 Cloud 核对）
- 两次部署均「Submission completed」，选 No=自动切流量。
- 二次部署后内网 `/_diag/build` 曾返回旧 `qc-strip-...` 标记，疑容器滚动构建延迟或并发覆盖。**后续 curl 已确认**线上标记变为 `reagent-orders-selfgated-2026-07-27-2306`（=406a7fe），即二次部署生效、功能完全上线。结论：非灰度卡死，仅为构建延迟。

## 材料采购明细_2025年1-10月.xlsx 结构核对（用户要求仅核对、暂不处理）

### 文件位置
用户在 Desktop 引用；仓库内亦被其他会话以同名 tracked 文件放入（git status 见 ` M 材料采购明细_2025年1-10月.xlsx`）。

### 结构/质量（只读分析，643 数据行）
- 唯一材料编码 285 个；**170 个编码跨月重复**（必须按 material_code 聚合）。
- 专业组取值：免疫 367 / 生化 201 / 凝血 75（注意有「凝血」，系统 library 是 免疫/生化凝血 → 生化+凝血 需确认都归「生化凝血」）。
- 月份：1月–10月（2025）；供应商去重 18 家。
- **金额 = 数量×单价 全部一致（0 错）**，数据干净。
- 空值：规格型号 5、单位 74、备注 549（备注空正常）。

### 列→系统字段映射（核心结论）
| Excel | 系统字段 | 落点 |
|---|---|---|
| 材料编码 | ReagentItem.material_code | 目录(主匹配键,unique) |
| 材料名称 | ReagentItem.name | 目录 |
| 规格型号 | ReagentItem.spec | 目录(5空) |
| 单位 | ReagentItem.unit | 目录(74空,需补默认) |
| 单价 | ReagentItem.unit_price | 目录参考价 |
| 供应商 | ReagentItem.supplier | 目录(18家) |
| 专业组 | ReagentItem.library | 目录(免疫/生化凝血映射待确认) |
| 备注 | ReagentItem.remark | 目录 |
| 数量 | → ReagentOrderItem.ordered_quantity 或 聚合为 ReagentItem.annual_usage | 取决于用途 |
| 金额 | 派生值(系统不单独存) | 校验用 |
| 月份 | ReagentOrder.order_date(月) 或 仅信息 | 取决于用途 |

### 关键发现
- `ReagentItem.annual_usage` 字段注释明确写「年用量参考（采购明细），用于目录/盘库/订购默认排序」→ **本表恰好是 annual_usage 的数据源**：按 material_code 汇总各月数量即可填充。
- 三种落点假设（待用户选）：① 导入试剂目录(reagent_items，按编码聚合，数量→annual_usage)；② 生成订购单草稿(orders+items，每行=细项，跨月重复需合并规则)；③ 历史采购台账(系统暂无独立表，需新建模型或仅做报表)。
- 用户已选「仅核对结构，暂不处理」；何时定用途再动手。

## 排班页：移除冗余「今日我的岗位」+ 点人名支持组长改岗（commit 43682c4 + 5147474，已部署）
- **诉求**：①排班管理页的「今日我的岗位」卡片删掉（工作台已有，重复）；②月视图已有班表后，点人名时除换班外，还要允许**组长/管理员直接修改具体岗位/人员**。
- **改动（SchedulingList.vue）**：
  - 删掉顶部「今日我的岗位」el-card 整块（template + script myToday/loadMyToday/todayStr + getMyToday 导入 + 相关 CSS）；onMounted 去掉 loadMyToday。
  - 点人名新增 `onPersonClick(row,d,person,assignId)`：非管理员(仅 `canWrite('scheduling')` 之外的成员)→ 直接 `openSwap`（维持原换班行为）；**管理员/组长**→ 弹 `actionDialog` 选择「发起换班」或「修改岗位/人员」。
  - 选择「修改岗位/人员」→ `openCellEditor(row,d)`（已内置 isManager 守卫），即在原单元格编辑/补录弹窗里改该人岗位或人员，落 `setSchedulingCell`/`deleteSchedulingAssignment`。`assignId` 透传确保状态行（休息等）换班时也指向正确对方班次。
  - 新增弹窗 `.action-target` + 两个按钮（发起换班/修改岗位人员），按钮在自己=接收人时禁用发起换班。
- **前端构建**：`C:/.../node.exe node_modules/vite/bin/vite.js build` → 新 chunk `SchedulingList-C5Mamq3F.js`；`git add -f frontend/dist`（dist 在 .gitignore，必须强提交）；`Dockerfile` 直接 COPY dist，云端不重构建前端。
- **部署**：`(echo ""; sleep 3; echo "Y") | tcb cloudrun deploy --force`；BUILD_MARK=scheduling-remove-mytoday-editpost-2026-07-27-2334（**注意**：本次会话 diag._BUILD_MARK 被另一并发 AI 改回很旧的 `reagent-orders-selfgated-2026-07-27-2306`，我重新置为本轮标记）。
- **验证**：内网 `/_diag/build` 标记更新 + 内网静态资源 `/assets/SchedulingList-C5Mamq3F.js` 返回 200（旧 hash 404）→ 前后端均上线。
- **注意（并发 AI 铁律再现）**：推送/部署时 origin/main 被别的 AI 顶过；本轮确保我的提交 43682c4/5147474 是 tip 祖先后才部署。

## P2PSA 7.5% + 心肌/免疫项目别名（commit 0c8968b，已部署）

### 需求
- 别名：肌钙蛋白I→超敏肌钙蛋白Ⅰ、P2SA→P2PSA(7.5%)、TPO抗体→抗甲状腺过氧化物酶抗体、甲球抗体→抗甲状腺球蛋白抗体、内因子抗体测定→抗内因子抗体、CK-MB→肌酸激酶-MB 同工酶质量(10%)
- P2PSA 质量目标 7.5%（CK-MB 已有 WST403 10%）

### 改动
- `QC_GOAL_EXACT_OVERRIDES`: +P2PSA 7.5%
- `_LIS_ITEM_ALIASES`: +肌钙蛋白i/ⅰ(双写法)→超敏肌钙蛋白Ⅰ、p2sa→P2PSA、tpo抗体→抗甲状腺过氧化物酶抗体、甲球抗体→抗甲状腺球蛋白抗体、内因子抗体测定→抗内因子抗体、ck-mb→肌酸激酶-MB 同工酶质量
- `quality_requirements_seed.py`: +P2PSA cv 7.5%

### BUILD_MARK
`qc-cardiac-immuno-aliases-2026-07-28`

## 权限收口：试剂配送关订购+质控、技术支持关工作台（commit 49719b1 + 9e747b5，已部署）

### 需求（用户最新指令）
- 「试剂配送关闭质控管理和订购管理的权限，技术支持关闭工作台权限。」
- ⚠️ **反转点**：49719b1 之前的上轮刚给试剂配送**开了**订购管理权限(f12017d/406a7fe)，本轮按新指令又收回。已在回复中向用户标注此反转。

### 改动（49719b1：权限三管齐下）
- `models/module_permission.py` `DEFAULT_MODULE_PERMISSIONS`：`reagent-orders` 由 `["admin","reagent_manager","reagent_delivery"]` → **`["admin","reagent_manager"]`**（试剂配送移出订购，保留到货接收 `reagent-receivings` 仍含 reagent_delivery）。质控模块(qc-monthly/qc-target/eqa/comparison/interlab) DEFAULT 本就不含 reagent_delivery → 无操作。
- `frontend/src/store/auth.js` `FALLBACK_MODULE_WRITE_ROLES`：`reagent-orders` fallback 同步改为 `['admin','reagent_manager']`。
- `backend/app/api/v1/reagent_management.py`：orders 的 `create_order`/`update_order`/`confirm_order` 三处 `require_roles` 去掉 `"reagent_delivery"`（改 `("admin","reagent_manager")`）。update_order 内「改他人403/已确认403」防御逻辑保留（无害）。
- `frontend/src/layout/AppLayout.vue`：工作台菜单隐藏条件扩展为「试剂配送 OR 技术支持」（均 && !admin）。
- `frontend/src/router/index.js`：`/dashboard` 守卫 → 试剂配送重定向 `/reagent/receivings`，技术支持重定向 `/qc`（comparison/interlab 对 technical_support 可见，避免死循环），admin 放行。

### 修复（9e747b5：module_permissions PUT 端点 500 bug）
- 根因：`set_module_roles` 把字符串 `module_key` 传给 `write_audit` 的 `record_id`（AuditLog.record_id 是 Integer）→ 审计写入类型错误 500。权限的删/插在 write_audit 之前已 commit，故权限实际已生效，仅端点返回 500（导致「权限配置」页保存必败）。
- 修复：`record_id=0`、module_key 移入 detail、去掉 `request=None` 形参。
- GET/PUT 复用并集机制：`get_structure` 返回 DB 行与 DEFAULT 的**并集**（DEFAULT 是地板，只放宽不收窄）→ 故 DB 残留 `reagent-orders↔reagent_delivery` 行需通过服务端 PUT 清理（沙箱连不到私有库，无法直接 DELETE）。

### 部署与标记核对（并发覆盖典型）
- 49719b1 BUILD_MARK=`perm-reagent-delivery-close-orders-qc-techsupport-nodash-2026-07-28`（nodash 因 memory 文件名约束）。
- 9e747b5 BUILD_MARK=`perm-fix-module-put-audit-2026-07-28`，已 push origin/main=9e747b5。
- **并发覆盖复现**：首轮部署 ff4IRF 后，内网 `/_diag/build` 仍返回 49719b1 的 `nodash` 标记（被并发 AI 顶回），但实测 PUT 已返回 200 + 权限结构正确 → 证明 9e747b5 功能实际已上线，仅标记字符串被覆盖（功能不受影响）。
- 按用户「被覆盖就重设标记+强提交+再 deploy」流程：再跑一次 `(echo ""; sleep 3; echo "Y") | tcb cloudrun deploy --force`（Y 取消并发任务），内网标记已同步为 `perm-fix-module-put-audit-2026-07-28`，与 origin/main tip 一致。
- **最终实测温故**：
  - `reagent-orders` → `['admin','reagent_manager']` ✓
  - `reagent-receivings` → `['admin','reagent_manager','reagent_delivery']` ✓
  - `qc-monthly`/`comparison` 等质控模块均无 reagent_delivery ✓
  - `PUT /module-permissions/reagent-orders` → HTTP 200（"权限配置"页保存可用）✓
- 结论：三项收口全部上线且验证通过；标记已干净同步。

## 测试数据清理：收货单 RCV-2026-07-327（commit 75eae80，已部署 078ecf4 滚出后通过 API 删除）

### 用户需求
删除测试用收货单 RCV-2026-07-327（id=1，试剂 AA/item_id=175，数量 1，已确认），同时清掉确认时入库的库存记录。

### 执行过程
- 收货单详情：id=1, receipt_no=RCV-2026-07-327, item_id=175(AA), quantity=1, 已确认(jinzizheng 14:54)。
- 库存查询：`reagent_stocks` 中**无** item_id=175 的记录（可能该试剂目录条目较新、或库存已被盘库清零）。
- 系统原无删除收货单端点（设计上「无删除入口」），新增 `DELETE /receivings/{id}`（仅 admin）：
  - 已确认单 → 先回退库存（按 item_id+batch_no 匹配 reagent_stocks 减数量，≤0 则删行）
  - 再删 reagent_receiving_items → 删 reagent_receivings 主记录
- 同时在 `main._ensure_missing_columns()` 末尾加一次性启动清理（幂等 SQL：WHERE id=1 AND receipt_no='RCV-2026-07-327'），作为部署延迟时的兜底。
- **部署波折**：连续 3 次 deploy 均提示「有并发部署任务在运行」，标记始终停在旧值（`perm-fix-module-put-audit-2026-07-28`），DELETE 端点返回 405。第 4 次 deploy 无并发提示，等 90s 后标记变为 `admin-delete-receiving-2026-07-28`（078ecf4 滚出）。
- 通过 API 调用 `DELETE /receivings/1` → HTTP 200，返回「收货单 RCV-2026-07-327 已删除，库存已回退」。
- 验证：收货单列表查 RCV-2026-07-327 → **空** ✓；库存 item_id=175 → **无残留** ✓。

### 改动文件
- `backend/app/api/v1/reagent_management.py`：+`delete_receiving` 端点（admin only）
- `backend/app/main.py`：`_ensure_missing_columns` 末尾 +一次性清理 SQL（75eae80，待下次容器滚动时自动执行 no-op）
- `backend/app/api/v1/diag.py`：BUILD_MARK=`cleanup-test-receiving-rcv327-2026-07-28`
