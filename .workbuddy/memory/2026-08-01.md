# 2026-08-01 工作日志

## 人员继教：新员工培训默认勾选与打印格式修复
- 用户反馈：① 培训方式/考核方式默认勾选应与 BG-SM-PX-005 纸质表格一致（有的√一个，有的多个）；② 打印出来只有第一页头部，后面空白，格式不对。
- 逐行核对：按用户提供的 5 张截图，把 36 行培训内容的默认 `method`/`exam_method` 逐行写死（不再全选 讲解/PPT/文件 + 试卷/问答/实操）。例如生物安全风险点告知=讲解/问答；溢撒标本处理=讲解/实操；急诊 AU5800...=讲解/问答+实操 等。
- 同时修正此前归类错误：了解试剂性能/深入学习 5800 归为**门诊岗**；出凝血疾病实验室诊断步骤归为**电泳岗**；免疫室化学发光法检测原理归为**免疫仪器岗**。
- 编辑表格与打印表格移除原表没有的「培训老师」「考核成绩」列，打印列严格对齐 BG-SM-PX-005：序号/培训类别/培训内容/培训方式/考核方式。
- 打印分页修复：原 `@media print` 用 `body * { visibility:hidden } + .print-root { position:absolute; top:0 }`，长表格被裁到第一页。改为 Vue `<Teleport to="body">` 挂载打印区，`@media print` 用 `body > *:not(.print-root) { display:none }` + `.print-root { position:static }`，多页正常分页；表格行加 `page-break-inside: avoid`。
- 构建部署：`vite build --emptyOutDir false` 生成新 chunk `StaffEducation-D0C6b30e.js`；mv dist.old 绕过沙箱 guard；force-add dist；commit `56f5df3`；tcb deploy；`_BUILD_MARK=new-emp-train-form-match-2026-08-01`。约 5 分钟后内网 curl `/api/v1/_diag/build` 翻到新标记，`/assets/StaffEducation-D0C6b30e.js` 200，新前端生效。

## SOP 溯源补全 + 录入项目库（续 7/31）
- SOP 表格「溯源性」列补全：德赛 6 项 + ACE(柏定) + IgG4(柏荣)。β2-MG/景源 KAP 等该溯源表无对应厂家条目，留空未造。原 Word 文件曾被 WPS 占用，最终用完整副本覆盖原路径 + python-docx 校验通过。
- 录入 test_items 项目库按需分页参数 `page/page_size`（非 limit/skip）。8 项的 traceability 由占位符 `—` 改为真实值并复核 8/8：ASO=Internal working calibrator；C3/C4/IgA/IgM=ERM-DA470k/IFCC；RF=WHO NIBSC W 1066；ACE=公司内部工作校准品；IgG4=企业标准。
- 8/1 用户追加：IgG(德赛, id=111) 溯源也按 ERM-DA470k/IFCC 录入系统（**不写 Word 文件**）。已 PUT 写入并校验通过。现 9 项（含 IgG）溯源已入库，项目查询页刷新即见。

## AU5821B SOP 空表按 AU58-2 项目填写
- 目标：`SM-SOP-2003-贝克曼AU5821B全自动生化分析仪操作作业指导书.docx` 的 table[1]（50x4，表头 项目/试剂/校准品/溯源性，49 空数据行）。
- 数据来源：后端 test_items，过滤 instrument_group/instrument 含 AU58-2 / AU5821B → 50 个项目（含 37 个 AU58-1/AU58-2 共享项）。按 code 升序，无 code 用 name（β2-微球蛋白）。
- 表格仅 49 数据行，python-docx `add_row()` 追加 1 行→51 行(表头+50)。4 列填：项目(code)/试剂(brand)/校准品(calibrator)/溯源性(traceability)，库里为空或占位符 `—` 的格留空。
- 结果：50 行全部写入；38 项有溯源值，12 项溯源空（项目库空：APOA1/APOB/CER/Cu/GA/LPa/oxLDL/sdLDL/TSGF/Zn/β-HB/β2-微球蛋白），11 项校准品空。
- **回写完成**：用户关闭 WPS 后 `cp` 覆盖原文件成功，副本已删；python-docx 校验 table[1] = 51 行(表头+50)×4，抽样 Alb/APOA1/Zn/β2-MG/SAA 均正确。
- **重要发现（德赛项归属）**：AU5821B 表里没有 ASO/IgG/IgA/IgM/RF/C3/C4 这 7 个德赛项——核对系统 test_items，这些项的 `instrument_group` 仅标 `AU58-1 /AU5800`（或 `AU58-1`），**未挂 AU58-2**。即 AU58-2 仪器档案在系统里本身就未配置这些德赛免疫项目（AU58-2 实配 50 项不含它们）。填表严格按"AU58-2 的项目"，故表中缺失属系统数据现状、非填漏。若 AU5821B 实际也开展这些项目，需先在系统项目库把这几项的 instrument_group 补上 AU58-2，再补填表格。
- 注：editor_sdk 对本文件 `doc_get_table_info` 持续 `document is null`（异步打开未完成），故改用 python-docx 直接填，可靠。

## β2-微球蛋白 溯源补录（仅系统）
- 用户要求只在系统 test_items 补 β2-微球蛋白(id=160, 日本生研, group=`AU58-1 / AU58-2/AU5800`) 溯源=`WHO NIBSC 19/302`；已 PUT 写入并复核通过。**未改 Word 文件**（AU5821A/AU5821B 两个 SOP 表格该格仍空，符合"只改系统"）。项目查询页刷新即见。

## test_items 批量补录校准品+溯源（9 项，仅系统）
- 用户给表格式 9 行（项目/品牌/校准品/溯源），要求补入系统。已 PUT 更新 `brand/calibrator/traceability`（品牌与系统原值一致，仅校准品+溯源由 `—` 实填），9/9 通过：
  - APOA1(id105,载脂蛋白A1)=德赛APO校准品 / IFCC WHO-IRP 1992-10 SP1-01
  - APOB(id106,载脂蛋白B)=德赛APO校准品 / IFCC WHO-IRP 1992-10 SP3-07
  - Cu(id98,铜)=安图Cu校准品 / GBW08615
  - CER(id99,铜蓝蛋白)=安图CER校准品 / ERM DA470K
  - LPa(id107,脂蛋白a)=德赛LPa校准品 / WHO/IFCC SRM 2B
  - sdLDL(id95,小而密LDL-C)=柏定 / 公司内部工作校准品
  - Zn(id97,锌)=柏定生化多项校准品 / GBW09152
  - β-HB(id94,β-羟丁酸)=柏定生化多项校准品 / 公司内部工作校准品
  - TSGF(id102,肿瘤特异性生长因子)=美康TSGF校准品 / 制造商选定的测量程序
- 仅改系统，未动 Word 文件。

## 15189 内审模块（新增，跨轮大模块）
- 目标：在现有 `iso15189` 模块（已建 `NonconformityList.vue` + `nonconformities` 表 + module_permission 已授权 admin/quality_manager/specialty_leader 等）扩展三子功能：①文件评审 ②自查(条款内审) ③科室内审(不符合项+逐条款整改)。参考文件：A-027文件评审记录表、CNAS-AL02-07附表3自查表、2026.7.23工作部署分工、2025.10金子铮内部审核检查表、生免组不符合项/整改zip。
- 数据模型（新建 `backend/app/models/iso15189.py`，三模块全建）：
  - 文件评审：`ReviewCampaign`(活动)+`ReviewAssignment`(分配:document_id,reviewer,reviewer_id,status,record_json[A-027内容JSON],revised_cloud_key[修订文件],revised_filename,submitted_at,admin_received_at,document_new_version,note)。
  - 自查：`AuditClause`(条款字典,种子CNAS附表3:clause_no/chapter/title/content/check_point)+`SelfInspectionCampaign`+`SelfInspectionAssignment`(clause_ids JSON,assignee,status)+`SelfInspectionRecord`(clause_id,check_content,result[符合/不符合/观察项],finding,action,filled_at)。
  - 科室内审：复用 `nonconformities` 表；新增 `CorrectiveAction`(clause,title,source,description,root_cause,corrective_action,preventive_action,responsible,due_date,verify_result,status[未整改/整改中/待验证/已关闭]) 逐条款整改。
- 复用机制：`documents` 的 `POST /{doc_id}/new-version`（bump版本）、`/download`、`/versions`；`storage`/`cos_storage` 附件；`User`(department/roles/full_name) 组员；`FileChangeLog` 记版本更新。文档分类 `DOC_CATEGORIES`=["通用SOP","项目SOP","仪器SOP","记录表格","项目说明书"]。
- 文件评审工作流：管理员建活动→选在用文档(通用SOP/仪器SOP/项目SOP,status=生效)分配给人→人下载Word修订模式改审核人名字→上传修订文件+提交A-027记录表(record_json)→管理员接收→调 documents new-version 更新版本(reviewer设分配人)+记 FileChangeLog。
- 前端：`iso15189` 目录现有 `NonconformityList.vue`，需扩为三子页+路由+菜单（sidebar）。
- 本轮(8/1)：完成数据模型设计+建表注册；文件评审API/前端、自查API、科室内审API 后续轮次实现。

## 新员工培训：加回 培训老师/考核成绩 列 + 80分自动判定
- 用户要求把上一轮移除的「培训老师」「考核成绩」两列加回；默认培训老师不变（沿用 BG-SM-PX-005 逐行老师：金子铮/杨静/各岗人员/朱春阳）；考核成绩按 80 分自动判断合格与否。
- 编辑表格：培训老师列 `el-input v-model=row.teacher`（默认即原表老师）；考核成绩列 `el-input` 录分数 + `scoreVerdict()` 实时判定：`>=80 合格`（el-tag success 绿）/` <80 不合格`（danger 红）/空不显示。
- 打印表格：同步加回 培训老师、考核成绩 两列；成绩列显示「分数（合格/不合格）」，空显示「—」。
- `normalizeRecord()` 恢复透传 `teacher`/`score`（此前只传 category/content/method/exam_method，会导致编辑/打印丢这两字段）；`defaultOutline()` 逐行补回 `teacher`。
- 部署：vite build 新 chunk `StaffEducation-FqcKIS3u.js`；commit `0842d97`；`_BUILD_MARK=new-emp-train-teacher-score-2026-08-01`；内网 build mark + chunk 均 200 生效。

## 新员工培训：列表增加 入组时间 列
- 用户反馈：新员工培训列表页应显示「入组时间」字段（截图显示原列为 员工姓名/员工类别/培训专业/考核结果/考核负责人/状态/操作）。
- 改动：`NewEmployeeTrain.vue` 的 `columns` 数组在「培训专业」与「考核结果」之间加入 `{ prop: 'group_join_date', label: '入组时间', width: 110 }`。
- 部署：vite build 新 chunk `StaffEducation-B8oGH62L.js`；commit `ee392de`；`_BUILD_MARK=new-emp-train-join-date-2026-08-01`；内网 build mark + chunk 均 200，列表已显示入组时间。

## 15189 内审三子模块：文件评审 / 自查 / 科室内审 —— 全量实现并部署上线（本轮收尾）
- 用户回复"做吧"，落地 2026-08-01.md 第41-50行的设计。后端 + 前端 + 部署 + 验证全链路完成。
- 后端新增文件：
  - `backend/app/schemas/iso15189.py` + `schemas/__init__.py` 追加导出（三模块全部 Base/Create/Update/Read schema；`record_json`/`clause_ids` 走 json_fields 由 CRUD 处理）。
  - `backend/app/api/v1/review.py`：文件评审。4 个 make_router（campaigns/assignments/json_fields=record_json）+ 自定义端点：
    - `POST /review/campaigns/{cid}/assign-batch`（批量分配在用文档）
    - `GET /review/my-assignments`（成员看自己）
    - `POST /review/assignments/{aid}/upload-revision`（COS 上传修订文件）
    - `POST /review/assignments/{aid}/submit`（提交+record_json A-027）
    - `POST /review/assignments/{aid}/receive`（管理员接收→调 documents `POST /{doc_id}/new-version` bump 版本 + 记 FileChangeLog）
    - `GET /review/campaigns/{cid}/summary`（汇总 A-027）
  - `backend/app/api/v1/self_inspection.py`：自查。4 个 make_router（audit_clauses/self_inspection_campaigns/self_inspection_assignments[json_fields=clause_ids]/self_inspection_records）+ 自定义：
    - `POST /audit-clauses/batch-import`（格式 `条款号|章节|标题|内容`）
    - `POST /self-inspection/campaigns/{cid}/assign-batch`
    - `GET /self-inspection/my-assignments`
    - `GET /self-inspection/assignments/{aid}/clauses`（返回条款+已填记录）
    - `POST /self-inspection/records/upsert`（按 assignment_id+clause_id upsert）
    - `POST .../assignments/{aid}/submit`、`GET /self-inspection/campaigns/{cid}/summary`
  - `backend/app/api/v1/corrective_action.py`：科室内审逐条款整改 `CorrectiveAction`（make_router prefix `/corrective-actions`）。
  - `backend/app/api/v1/router.py`：education 后追加 `from . import review, self_inspection, corrective_action` + 三个 include_router。
  - `backend/app/api/v1/diag.py`：`_BUILD_MARK="iso15189-internal-audit-2026-08-01"`。
- 前端新增/改：
  - `frontend/src/api/review.js` / `selfInspection.js` / `correctiveAction.js`（REST 封装）。
  - `frontend/src/views/iso15189/Iso15189Hub.vue`（`el-tabs` 三页签：文件评审 / 自查 / 科室内审）替代原 `NonconformityList.vue` 作为 iso15189 路由组件。
  - `ReviewTab.vue`（活动管理/分配/接收/成员"我的评审任务"/A-027 弹窗/汇总弹窗）、`SelfInspectionTab.vue`（条款字典CRUD+批量导入+活动+分配+成员逐条填写）、`InternalAuditTab.vue`（嵌入不符合项列表 + 逐条款整改 CrudTable）。
  - `router/index.js`：iso15189 路由组件改为 `Iso15189Hub.vue`。
- 部署与验证：vite build 新 chunk `Iso15189Hub-deuy6Cp3.js`（首 build 因旧 dist 被占用报 EPERM，rm -rf dist 后重 build 成功）；commit `87aed08` push origin main；`tcb cloudrun deploy` 提交成功（2m8s）；轮询内网 `/api/v1/_diag/build` 第10次翻到 `iso15189-internal-audit-2026-08-01`（前9次旧标记，确认生效）。
- 验证结果：登录正常；`/review/campaigns`、`/audit-clauses`、`/self-inspection/campaigns`、`/corrective-actions`、`/review/assignments` 列表均 HTTP 200；`/assets/Iso15189Hub-deuy6Cp3.js` + 同名 css HTTP 200；`/review/my-assignments` HTTP 200 —— 证明新表自动建表成功、路由无冲突。
- 后续可选项（用户未明确要求）：①导入 CNAS-AL02-07 条款字典种子数据；②实际创建评审/自查活动；③分配文件与条款；④走一轮真实流程。三子模块功能框架已可用。
