# 2026-07-24 工作日志

## 成果升级路线（中低等 → 高等）
用户已知系统各功能专利/成果为"中低等"（中=EQA组合靠已知手段组合；低=比对/累靶执行公开标准），问如何完善成"高等"。
- 核心判断：中低等 = 信息化（执行标准/公式）；高等 = 方法学创新 / AI / 标准化贡献。纯包装上限"中高"，真高等须落地实质创新。
- 升级路径（贴合用户）：
  1) **PBRTQC 患者数据实时质控做成系统模块**：方法专利 + 高 IF 论文 + 新技术代表作；用户本有该研究方向，是"管理→智能"关键一跃。
  2) 真算法创新：EQA 报告自适应解析（小样本/多模态，解决换模板即失效）、累靶升级贝叶斯动态靶值、单位换算升级检验单位知识图谱+LOINC 映射。
  3) 循证反哺标准：用系统多年真实 EQA/比对数据做偏倚阈值/允许总误差实证研究；借民航总医院 WS/T 403 起草身份提炼专家共识/团体标准/技术规范。
  4) EQA 组合冲发明保底（中高）。
- 用户两张王牌：① 参与 WS/T 403—2024 起草（规范类高等代表作）；② AI-PBRTQC 研究方向。
- 取决于用户是否愿意投入开发（路径2/3需写代码/做研究）；若只做包装，最多中高。

## 排班模块第三轮重构（修复"录休息必须选岗位" + 月视图）
用户指出：① 固定岗优先人应是严格优先级递减（已改，非轮转）；② 录休息被迫选岗位是 bug——休息/夜班/发热是与岗位平行的状态，不应绑 post_id；③ 工作流应为先批量录入非白班约束→再生成白班；④ 需独立月视图排班表（日期列×岗位/状态行，美观+移动端友好）。
- 后端：SchedulingAssignment.post_id 显式 nullable；ASSIGN_STATUS_ALL 扩为 在岗/休息/病假/开会/行政/质控/教学/采血/卫生部门上，新增 STATUS_CATEGORIES。
- /cell 支持 post_id 为空 upsert（按 person 唯一，一人一天一条无岗位记录）；新增 POST /scheduling/batch（批量录入夜班/发热/休息等）；/grid 返回岗位行+状态行（cells 按 post:<id>/status:<key> 组织，状态行按人聚合）；/generate 修复"夜班人员当天被排入白班"（夜班岗在岗记录视为全天占用）。
- main._migrate_schema 新增数据清洗：历史 post_id=0 的非在岗记录置 NULL。
- 前端 SchedulingList.vue 重写：月视图主页（桌面矩阵+移动端日卡自适应，色标，点格编辑/删除/补录）+ 批量录入 tab + 岗位/计划/设置 tab；api 加 batchSchedulingCells。
- diag.py 移除临时 /dbg-scheduling，build mark 改 scheduling-v3-2026-07-24。
- 部署：commit + push origin/main + tcb cloudrun deploy --force。

## 质控上传「空=在控」语义完善（续 2026-07-26 规则列覆盖）
用户要求"空 = 一律视为在控"。关键区分：
- **仅当本次上传识别到规则列**（rule_column_present=True，LIS 导出含 violateRule 等）→ 空单元格一律在控（清空后端 ooc/警告）；
- **上传无规则列** → 空单元格回落后端 Westgard（防老数据批量误翻成在控，Westgard 仍生效）。
- 空+含规则列的点一并冻结：加入 uploaded_present 集合，不参与跨水平 R-4s，避免被 R-4s 反标失控。
- 实现：QCMonthlySummary 增 rule_column_present 列；aggregate_project step1.5 读 lv.rule_column_present；qc_summaries 上传/_recalc 透传并落库；schema QCMonthlySummaryBase 暴露该字段。
- 测试 9→11 断言全过（用例F 空+含列→在控；用例G 空+无列→回落后端仍判 1-3S）。
- 提交 6ae817d + push + 部署进行中。

## 排班矩阵批量录入 v5/v6（日期×排班项矩阵）
用户诉求：① 批量录入失败/字段歧义 → 改成「日期×排班项」矩阵（列=一个月日期，行=各排班项，单元格多选人员可多人）；② 矩阵行太窄人名显示不全 → 休息行加高约5行人名、一人占一行、去掉标签截断；③ 末尾追加夜班/发热白班行。
- 前端 MATRIX_ROWS 配置数组：6 个 status 行（休息带 tall，病假/开会/行政/质控/教学）+ 3 个 post 行（生化夜班/发热夜班/发热白班，key=种子岗位名匹配）。
- 单元格 el-select multiple（去掉 collapse-tags 与 max-width:60px 截断）→ 姓名完整显示、自动换行。
- 休息行 CSS：`.bm-row-tall td { height:124px }`、`.bm-row-tall .bm-select{ min-height:112px }`、`.bm-table td { vertical-align:top }`、`.el-select__tags{flex-wrap:wrap}`。
- 后端 /batch 已支持 prune_post_keys（[date,str(post_id)] 岗位行裁剪）= 取消勾选即移除，与 prune_keys（状态行）并列；saveMatrix 统一构造 items+prune_keys+prune_post_keys。
- loadMatrix：status 行从 res.cells[`status:${key}`] 回填 persons；post 行从 res.cells[`post:${pid}`] 回填单人。
- 构建标记：scheduling-v6-matrixrows-tall-2026-07-26；commit 24ad995 + push + deploy 进行中。
