
## 【真 bug】CNAS 认可判定误判「尿微量白蛋白」（commit 751efd07）
用户截图反馈：「尿微量白蛋白」被标成了认可，但它不是认可项目。
- **根因**（在共用的 `verification_reports._is_cnas` 里）：`_PROJECT_MODIFIERS`（边缘包含匹配时允许的"无实义修饰词"）里
  含 **"尿" / "血" / "血清" / "血浆"** → 造成：
  | 记录项目名 | 误命中 | 路径 |
  |---|---|---|
  | 尿微量白蛋白 | **白蛋白** | `endswith("白蛋白")`，残余"尿微量"以"尿"开头 → 放行 |
  | 尿转铁蛋白 | **铁蛋白** | 残余"尿转"以"尿"开头 → 放行（两个完全不同的蛋白！）|
  | 尿免疫球蛋白G | 免疫球蛋白G | 残余"尿" → 放行 |
- **修正①**：从 `_PROJECT_MODIFIERS` **移除「尿/血/血清/血浆」**——它们是**标本类型**而不是无实义修饰，会改变项目含义。
- **修正②**：新增 `_SPECIMEN_PREFIXES` + `_strip_specimen()`，规则为「**去掉开头标本词后精确等于能力范围项目名**」才算同一项目，
  用于保留「血浆D-二聚体 = D-二聚体」这类真·同一项目。
- **效果**：验收记录认可数 **47 → 45 条**；纠正 2 条（尿微量白蛋白、尿转铁蛋白）；保留 2 条（尿免疫球蛋白G、血浆D-二聚体）。
  ⚠️ 「尿免疫球蛋白G」仍判为认可（去"尿"后 = 能力范围里的「免疫球蛋白G」），**待用户确认是否符合预期**。
- **影响面**：`_is_cnas` 是**试剂验收 + 性能验证两个模块共用**的，本次修正让两边判定同时变严格（这是修正而非回归）。
  **以后改 `verification_reports` 里的 CNAS 判定规则时，必须同时考虑试剂验收模块。**
- 本次只改后端，无需重新构建前端。
��检查它是否复用了单条接口的业务逻辑。**
# 2026-09-19 工作日志

## 修复：试剂验收「检验项目」全空 + 部分新批号效期为空（commit 0682dfe7）
用户截图反馈：列表里「检验项目」列全是 `—`，且部分行「效期（新批号）」为空。

### 问题 1：95 条记录的检验项目全空（我的 bug）
- **根因**：`_generate`（批量生成）里只有「若传入 test_item_id 则取名字」的逻辑，**漏了自动关联**；
  而单个新建 `create_verification` 后来我加了自动关联 → 两条路径行为不一致，批量生成的 95 条全空。
- **修复**：`_generate` 补上按 `test_item_reagents`（role=试剂，order_by TestItem.id 取第一个）自动关联项目。
- **回填**：`scripts/backfill_lot_item_expiry.py` 用 `_prepare` 返回的 `test_items` 回填 → **95/95 条全部补上**。

### 问题 2：14 条新批号效期为空
- 来源：安图销售表**没有有效期列**（戊肝IgM/IgG、醛固酮、ACTH、肾素、血管紧张素II、皮质醇、结核、单纯疱疹1/2型IgM、铜蓝蛋白、铜离子）；另 2 条（内因子抗体、可溶性转铁蛋白受体）来自更早那批发货记录。
- **回填 10 条**：从 `_prepare` 的 `new_batch_candidates`（收货单）与 `stock_batches`（库存）里找**同批号**的效期。
- **剩 4 条确实无源可查，需手工填**：铜蓝蛋白 `51218D11`、铜离子 `51217B11`、戊肝IgM `20260326`、戊肝IgG `20260127`（均为安图试剂，表里没效期、库存也无该批号）。

### 教训（已写入长期记忆 MEMORY.md 部署铁律）
- **`_generate` 与 `create` 两条写入路径必须共用同一套「自动填充」逻辑**——本次就是只在一条路径加了自动关联，另一条漏了，造成 95 条数据缺陷。**新增批量接口时，先检查它是否复用了单条接口的业务逻辑。**
# 2026-09-19 工作记录

## ⚠️ 重要坑：Git Bash 里禁止用裸 `python`
- 事故：一条后台命令 `python - << 'EOF' 2>/dev/null || sed -i ...` **挂起 1 小时 5 分钟**，无任何输出，用户发现了主动问起。
- **根因**：Git Bash 中的**裸 `python`** 会命中 **Windows Store 的 App Execution Alias 存根**，它不报错也不退出，直接**挂起等待**（被 playwright 式拦截）。`2>/dev/null` 还把提示吞了。
- **正确做法**：一律用**绝对路径**：
  `C:/Users/81526/.workbuddy/binaries/python/envs/default/Scripts/python.exe`
  （node 同理：`C:/Users/81526/.workbuddy/binaries/node/versions/22.22.2-2/node.exe`）
- **次要问题**：那个 `python - <<EOF ... || sed -i` 的"兜底"写法本身多余且危险 —— 直接用一条 `sed -i` 即可，不要叠加 `python -` 前置。
- **影响评估**：该任务卡在第一步，`sed`/`git add`/`commit`/`push` 全未执行 → **文件未被改动**；已用 `TaskStop` 终止；核查 `git status`/`git log` 与 origin/main 同步、无 .git 锁残留，仓库完好。
- 该任务原意（设置 build mark 为 `uncert-multi-report-fix-2026-09-18`）已无必要 —— 多会话并发下 build mark 已被他人反复覆盖（现为 `lot-verif-generate-autolink-2026-09-19`），**验证自身改动应抓线上具体 chunk 查关键字，而非依赖 build mark**。

## 修复：性能验证「参考区间」只显示"超出0个"、不显示区间数值（2026-09-19 00:0x~00:4x）
- 用户反馈：导入的性能验证报告，「参考区间」一项只显示"超出参考区间 0个"，应显示具体区间（如"参考区间 男 0-19.8/女 0-11.6 pg/mL"）。
- **根因（vrf_parser._read_summary）**：结果汇总表里**区间文本就写在 B 列**（与"参考区间"这个标签同列，如 `B22="参考区间：男 0-19.8 ,\n女 0-11.6pg/mL\n20个标本中超出参考区间不多于2"`）。解析器先用 `_content_to_key` 扫 B-E 列找 content → **B22 因为含"参考"被整体当成 content**，于是 `requirement`（B-E 列中 content 之外的文本）变成空 → 拼接区间的那段逻辑 `if content in ('参考范围','参考区间') and requirement:` 直接跳过 → result 只剩 F 列的"超出参考区间 0个"。
- **修复**（提交 `1ec1ec68`）：① 归一化前保留 `content_raw`；② 参考区间分支改为 `src = requirement or content_raw`（requirement 为空时回退到 B 列原文）；③ 用 `re.split(r'\d+\s*个?\s*标本|标本中超出|超出参考区间', seg)[0]` 截掉后面的判定要求文字，避免把"20个标本中超出参考区间不多于2"拼进来。
- **历史数据刷新**：解析器修好后老记录不会自动变（result_summary 是上传时写入的）→ 用 `POST /api/v1/report-archives/{aid}/reparse`（会删旧 verification_report 重建）刷新。共刷新 **57 条**（正是 9-18 导入那批），0 失败。
- **验证**：线上 `verification-reports` 140 条中 **93 条含"参考区间"验证项 → 93/93 全部显示具体区间**（0 条残缺）。样例：BNP `参考区间：＜100pg/mL；超出参考区间 0个`；β-HCG `参考区间：未绝经女性：＜5 ,绝经后：＜7.7mIU/mL；...`；GGT `参考区间：男：10-60U/L、女：7-45U/L；男性组超出1个，女性组超出0个`。
- **UI 自动化验证受阻（记录以免重复踩）**：`/verification` 页的 5 个页签（性能验证记录/项目验证档案/验证报告归档/…）在 Playwright 里点击后 `document.querySelector('.el-tabs__item.is-active')` 已变成目标页签，但**页面内容仍停在「验证报告归档」**（截图可见归档列"归档时间"，且无"参考区间"文本）→ 自动化点击无法可靠驱动该页签切换。**结论：数据正确性以 API 为准（`list-by-project` 的 `latest_items_summary` / `groups[].current.items_summary`）；涉及该页签的 UI 断言不要用无头点击，改由人工确认或直接断言接口。**
- 另注：`_recompute_summary` → `_auto_fill_result_summary` 里有 `if not data_field: return data`（上传解析的记录 data 为空→直接用已存 result_summary，不重算），`normalize_conclusion_summary` 对 reference 只把换行换成空格、不改写内容 → 所以**只要 result_summary 里存对了，前端就是直出显示**。

## 试剂验收：判读方式 + 行级偏倚方式（commit ba769387）
用户两点反馈：
1. **「只有选择定性的时候，这三列才显示」** → 加**记录级 `judge_mode`**：`quantitative`（定量，默认）/ `qualitative`（定性）/ `both`（定量+定性）。
   前端用 `v-if="form.judge_mode !== 'qualitative'"` 控制数值列与偏倚列、`!== 'quantitative'` 控制定性三列；打印同步按 jm 输出列。
2. **「有时候可能 5 个样本 1 个用绝对 4 个用相对」** → **偏倚方式下沉到样本行**：`samples[].bias_mode`（relative/absolute，留空继承记录级）。
   **关键补漏**：行级模式意味着**允许值也要分行取**——新增记录级 `allow_bias_abs`（允许绝对偏倚，如 CO₂ 的 5 mmHg），
   行判定时 `row_allow = allow_abs if row_mode=='absolute' else allow_pct`。同时 `allow_bias_pct` 语义明确为「允许相对偏倚%」。
   `compute_samples(samples, allow_pct, need, mode, allow_abs=0.0)`，返回值每项增加 `bias_mode`/`row_allow`。
- 前端：顶部「判读方式」单选 + 「允许偏倚 相对[__]% ／ 绝对[__]」两个输入；样本表新增「偏倚方式」列（相对%/绝对，可留空继承）；
  行级 `rowMode(r)`/`rowAllow(r)` 两个函数取代原来直接用 `form.bias_mode`。
- 打印：表头按 judge_mode 输出；偏倚列小字标注「相对%/绝对」；meta 加「判读方式」。
- 自测：CO₂ 场景（允许相对 8% + 允许绝对 5）——1 行绝对 22→25.5(=3.5≤5 合格)、4 行相对 100→104(=4%≤8% 合格) → 符合要求 ✓；绝对行 22→28(=6>5) → 不合格 ✓
- `_BUILD_MARK=lot-verif-judge-mode-row-bias-2026-09-19`

## 文件清单编号批量改前缀（2026-09-19 00:45）
- 文件：`D:\民航总医院\15189\生免组文件\BG-SM-GL-000-生化免疫组文件清单.docx`（1 个表格 322 行，第 3 列=文件编号）
- 任务：`MHZYY-SM-SOP-xxx` → **`MHZYY-JYK-SM-SOP-xxx`**（插入 `JYK-`，**序号不变**）
- 结果：改 **22 条**（206 条原已是新格式）→ 现 `MHZYY-JYK-SM-SOP` 共 **228** 条，旧格式 **0** 条。
- **不动**的编号体系（不同体系，勿误改）：`BG-SM-CZ`（66，记录表格）、`BG-SM-GL`（17，管理表格）、`BG-SM-PX`（5，培训表格）。
- 备份：`BG-SM-GL-000-生化免疫组文件清单.bak-20260919.docx`（同目录）
- 做法：python-docx 遍历 `tables[0].rows[1:]` 的 `cells[2]`，在 **run 级**替换（保留格式）；同时扫正文段落 + 页眉页脚（本次均为 0）。
- 注意：用户举例写"001→006"只是随手举例（006 当时已是新格式），**实际规则是仅插前缀、序号保持不变** —— 遇到编号改造先看文档内已有样本再定规则。

## 废止文件比对 + 填登记表 + 修清单异常（2026-09-19 00:55）
- **比对**：A=`15189\生免组文件\BG-SM-GL-000…docx`（新版 321 条） vs B=`生免组管理体系文件\生免组记录表格\1管理类表格\BG-SM-GL-000…docx`（旧版 329 条）；**B 有 A 无 = 废止，共 36 条**（SOP 20 / BG-SM-GL 7 / BG-SM-CZ 9）。
  - 比对前需**归一化编号**（`MHZYY-JYK-` ↔ `MHZYY-`），否则同一文件新旧版本匹配不上。
  - 编号用正则抽取：`(MHZYY(?:-JYK)?-SM-[\w]+-\d+|BG-SM-[A-Z]{2}-\d{2,4})`
- **填表**：`C:/Users/81526/Desktop/BG-KS-GL-020-文件回收、废止、销毁登记表.docx`（8 列：文件名称|文件编码|版本|生效日期|回收/废止/销毁|申请人|批准人|备注）
  - **清空原有 25 条（微生物组）**，按模板行(行1) deepcopy 追加 36 行 → 共 37 行
  - 填写：版本=01、生效日期=旧版生效日(2025.04.01)、处置=废止、申请人=旧版编制人(金子铮34/吕文娟2)、批准人=王学晶、备注="文件清单更新，新版清单中取消"
  - 备份：`…登记表.bak-20260919.docx`
- **修清单异常**：A 中有 **5 行编号列混入正文**（如 `MHZYY-JYK-SM-SOP-502批准日期2026-09-01审核者：赵海元`）→ 只保留编号（那些信息在其他列已有）→ 复核异常 0。备份 `…清单.bak-fix-20260919.docx`。
- **坑**：quoted heredoc (`<< 'PYEOF'`) 里**不需要转义 `$`**！我写成 `\$` 导致正则匹配字面 `$`，误报 321 行异常。**复杂脚本改写成 .py 文件再执行**更稳。
- 坑2：登记表若被 Word 打开会 PermissionError，脚本已加 fallback（另存 `_生免组废止.docx`）。

## ⚠️ 修正：废止清单应为 34 条（不是 36 条）
- 用户抓到我错误：`MHZYY-JYK-SM-SOP-184`（AU5800 轻链κ）**在新版清单 A 中确实存在**（A 行106），却被我列为废止。
- **原因**：首次运行 `fill_abolition.py` 时，A 的编号列**尚有 5 行混入正文的异常未被修复**（502-505、568），这批行在 `CODE.search()` 后虽能取出编号，但**整体比对处于中间状态**，导致差集多算了 2 条（184、185）。
- **修复顺序很重要**：**必须先修 A 的编号列异常 → 再比对**。我虽先跑了 fix_abnormal.py，但首版 fill 脚本因正则 `\$` 转义 bug 未正确识别异常行，等于带着脏数据比对。
- 重跑（`fill_abolition2.py`）结果：A 321 / B 329 → **废止 34 条**（SOP 18 + BG-SM-GL 7 + BG-SM-CZ 9），**184/185 已排除** ✅，脚本内加了 `assert not any('184' in ...)` 断言防复发。
- 输出：原文件被 Word 占用 → 落到 `C:/Users/81526/Desktop/BG-KS-GL-020-文件回收、废止、销毁登记表_生免组废止.docx`（35 行）。备份 `…登记表.bak2-20260919.docx`。
- **教训（供以后所有"比对/差集"类任务）**：
  1. 先**清洗两份源数据**（编号列去杂）再比对；
  2. 差集结果要**抽样人工核对**（尤其抽查"疑似误判"的编号是否在对方表存在）；
  3. 关键业务结论加 **assert 断言**（如已知不该出现的编号）；
  4. 复杂脚本写 .py 文件执行，**别用 heredoc**（`$` 转义已坑两次）。
- **上线验证（三级核对全中）**：chunk=`ReagentLotVerification-Dvul63BQ.js`（我的构建）、关键字「判读方式」「定量+定性」「偏倚方式」全命中、
  后端 `allow_bias_abs` / `judge_mode` 字段均存在 ✓。
- **本轮部署教训**：连续 4 次部署才翻面（前 3 次线上仍是旧 chunk）。原因：**CloudBase 并发部署时，多次提交会互相覆盖/排队丢失**；
  且 `build mark` 已被对方改成 `ref-range-req-line-fix-2026-09-19`，**再次印证标记不可信**。
  另外发现 **`git fetch` 同样需要 SSH，也不能放后台**（前台 + dangerouslyDisableSandbox 才行）。
  处置套路：`git fetch`（前台）确认远端含自己提交 → 后台只跑部署（sleep 12 喂两个 Y）→ 等 5-8 分钟 → 三级核对 → 不对就**再跑一次**。
- **上线验证（三级核对全中）**：chunk=`ReagentLotVerification-Dvul63BQ.js`（我的构建）、关键字「判读方式」「定量+定性」「偏倚方式」全命中、
  后端 `allow_bias_abs` / `judge_mode` 字段均存在 ✓。
- **本轮部署教训**：连续 4 次部署才翻面（前 3 次线上仍是旧 chunk）。原因：**CloudBase 并发部署时，多次提交会互相覆盖/排队丢失**；
  且 `build mark` 已被对方改成 `ref-range-req-line-fix-2026-09-19`，**再次印证标记不可信**。
  另外发现 **`git fetch` 同样需要 SSH，也不能放后台**（前台 + dangerouslyDisableSandbox 才行）。
  处置套路：`git fetch`（前台）确认远端含自己提交 → 后台只跑部署（sleep 12 喂两个 Y）→ 等 5-8 分钟 → 三级核对 → 不对就**再跑一次**。

## ⚠️ 再修正：比对必须以【文件名称】为主，编号会被重排！
- 用户第二次抓错：`MHZYY-JYK-SM-SOP-509`（荣盛 TRUST 梅毒非特异性抗体）在新版 A 中**标题仍在、但编号改成了 508** → 只按编号比对会误判为废止。
- **根因：文件编号会被重新编排**（如 A 中 508 由"罗氏梅毒特异性"换成"荣盛TRUST非特异性"，509 空出）。编号不是稳定主键，**标题才是**。
- **正确比对逻辑**（已写入 `outputs/fill_abolition3.py`）：
  1. 标题归一化 `tnorm()`：去空白/括号/逗号/点/冒号/分号/斜杠/连字符
  2. **标题命中 或 编号（归一化去 JYK-）命中 → 视为仍存在，不废止**
  3. 两者都不命中 → 废止
- 结果演进：36（脏数据）→ 34（修编号列后）→ **30（按标题比对后）**
  - 因"标题相同但编号已变更"而保留 4 条：537(iPTH)、538(骨钙素)、594(IgG4)、1025(爱康酶标仪)
  - 校验 184/185/509 均未被误列 ✅
- 分类：SOP 14 + BG-SM-GL 7 + BG-SM-CZ 9
- **需人工复核的可疑项**：
  - `BG-SM-GL-000 生化免疫组文件清单` —— 旧清单"列了自己"，新清单未列自己，属**结构性误判**，应剔除
  - 其余 BG-SM-GL/CZ 多为旧表格，可能是改名/合并/取消，需用户确认
- 输出落在 `…登记表_生免组废止.docx`（原文件被 Word 占用）。
- **通用教训**：做"两版清单差集"时，**主键顺序 = 标题 > 编号**；且结果必须交用户人工复核，尤其"清单自引用"和"改名合并"两类假阳性。

## 科普短视频策划（「强国复兴有我 精医提质为民」主题系列文化活动）
- 通知要点：短视频比赛团队≤3 人、30s-3min、竖屏 1080P+、MP4 25-60Mbps <1GB、字幕每屏≤14 字、无台标水印/无商业品牌、患者出镜需同意、视频内不得出现创作者信息、命名「短视频+支部+科室+作者」、附附件6、报党办高瑜键（OA 9005）。通知截止 9/17，9/19 才启动→需先电话确认可否补报。
- 产出方案：主推《一管血的旅行》（纪实+科普，90 秒分镜 6 段：采血/分色管与空腹/离心上机/质控/危急值/点题），备选 B《报告单上的箭头，别慌》（口播+图表，1 天兜底）、C《危急值的十分钟》（真实个案，隐私与虚构风险高）。
- 三人分工：A 内容主创+专业把关（脚本/数据/合规终审/报名）；B 出镜与拍摄统筹（道具/排期/授权）；C 拍摄剪辑包装（4K 竖屏/字幕/配乐/导出复核）。
- 交付：`output/shortvideo-plan-20260919/stage3/科普短视频策划方案-一管血的旅行.docx`（tencent-docx 插件全链路产出）。

## 德赛免疫 7 项：加序号 + 归档 AC临床化学 + 导入系统（2026-09-19 00:35~01:20）
- 源：`…\新流水线\德赛免疫7项`（ASO/C3/C4/IGA/IGG/IGM/RF.xlsx，共 7 个，封面已带全信息：AU5821A / MHZYY-JYK-SM-2002 / 试剂德赛诊断 / 金子铮·杨静 / 2026-08-13~19）。
- **命名沿用 AC 目录既有规范**（不是 AD 那批的写法）：`{seq}-{项目名}性能验证（{机型}）.xlsx`。AC 目录 1–26 是成对（AU5821A + 靶机AU5821B），27/28 单文件，**29–35 正空缺**，正好落位：
  `29-类风湿因子`、`30-抗链球菌溶血素O`、`31-免疫球蛋白A`、`32-免疫球蛋白G`、`33-免疫球蛋白M`、`34-补体C3`、`35-补体C4`（均 `性能验证（AU5821A）.xlsx`）。
- 结果：7 个已移动（源目录清空），AC临床化学 **54 → 61**；备份在 `…\_backup_德赛免疫7项_20260919\`（源 7 + AC 54）。
- **发现了参考区间解析的第二类错误（已修）**：这批模板在参考区间行下面**另起一行只写判定要求**（B25"男女两组各10个标本中，共计超出参考区间的不多于2个"）。该行文本含"参考"→ 被当成 content，提取出 seg="的不多于2个" 且 result（取 F24 兜底）非空 → **覆盖掉上一行好不容易拼好的真实区间**。修法：在参考区间分支加"提取不出有效区间（不含数字/含判定词）**且** requirement 为空 **且** src 含 判定词(不多于|标本|要求) → `continue` 丢弃该行"（提交 `6164b77f`，build mark `ref-range-req-line-fix-2026-09-19`）。
  - 三类模板已回归验证：德赛（区间写在 B24，另有判定要求行）、AD 免疫（区间与判定写在同一格）、AC 既有（"参考区间：137-147"）—— 全部正确，无回归。
- 导入：7 个全部 `report-archives/upload` 成功（archive 409~415，verification id 303~309），抓取内容逐项核对正确（项目名/型号/编号/**德赛诊断**/参考区间）。
- 线上全量复核：147 条记录中含参考区间项 **100 条 → 100/100 全部显示具体区间，0 条异常**。
- **部署血泪坑（重要）**：`tcb cloudrun deploy` 会提示「Platform currently has deployment tasks running…(Y/n)」；**多会话并发时互相"取消并部署最新"**，导致我的版本连续 2 次提交后长时间未生效（线上仍是他人 build mark `lot-verif-generate-autolink`）。**用户建议等 20 分钟再试 —— 实际 10 分钟后就翻面成功**。结论：并发高峰期不要连打部署，**等一个冷却窗口再部署，并用功能探针（reparse 一条 + 查 result）确认，而不是只看 build mark**。
- 补充（用户追问「为什么选题/是否扣题/能否全AI」）：方案升级为 v2（9 章 9 表）——新增「二、选题依据」按真实性/典型性/故事性/导向性/岗位特点逐项对照；「三、扣题自查与补强」逐条比对通知四项宣讲内容，命中 3 项，短板=民航特色弱+故事性弱（补法：空勤体检一句口播、危急值用真实个案隐去患者信息、片尾字幕点建党105周年/十五五）；「五、AI 使用边界」结论=不建议全 AI（纪实类 AI 画面即虚构、违反"严禁虚构"；宣讲条明确禁纯 AI 反映评审导向；人像/人声涉肖像权声音权），AI 仅限脚本润色/字幕/图表动画/配乐/调色，主体实拍与真人配音不可用，确用须显著注明。
- 再补充（用户问「纯 AI 卡通型科普视频呢」）：判定=合规可做（卡通是表现形式，非"虚构事实"；短视频条只要求显著注明+素材授权），但评奖风险中等（通知在宣讲条明确禁纯 AI；短视频无独立评分细则，评委可能沿用同一口径）。已加「附录：纯 AI 卡通型方案评估」（合规判定表/与实拍对比表/执行清单6条/建议）。**推荐混合**：关键镜头实拍 2-3 个 + 流程示意用 AI 卡通，周期压到 2-3 天。卡通方案实操坑：AI 画面内文字会错字乱码→字幕必须剪映另做；平台水印要去；党旗党徽不得卡通化变形；采血管颜色/加样顺序须专业核对。
- 混合方案执行手册（用户要"具体怎么操作"）：已加「附录二」7 节 8 表——镜头分配（实拍段：开场合采血/离心上机/质控屏/结尾；卡通段：分色管顺序、进食影响、仪器内旅程、质控拟人、危急值报警）、10 个实拍镜头清单（含避坑：避开厂家标识/患者信息/胸牌姓名，屏幕切演示界面）、卡通四步法（先定风格参考图→负面词写全→先出静帧经专业审核→剪映关键帧动态化）、配音230字/字幕≤14字/剪映曲库、导出参数（1080×1920、25-60Mbps、<1GB、关水印、片尾标"本片部分画面由AI生成"）、三天时间表。
- 用户嫌方案长，要求"精简+只写怎么操作"→ 另出精简版《〈一管血的旅行〉操作手册》（5 节 4 表 2 页：定案/第一天写稿实拍10镜头/第二天卡通5段+生成四步+配音/第三天剪映合成与导出参数/出片前三查+报送）。完整论证版仍在 `科普短视频策划方案-一管血的旅行.docx`，两份并存：操作手册发给干活的，策划方案留给报批依据。
- 用户偏好（党建/活动类材料）：方案类文档要短，只留操作步骤与参数，论证过程不进正文。
- 用户确认：补报他自行处理；口播改"不出镜画外配音"；要求把稿子写好。已更新操作手册 v2：① 删补报提示 ② 新增完整配音稿（七段 260 字，含时间点/画面/配音词对照表）③ 镜头清单换成用户给的 10 条（新增"标本接收（由师傅完成）"，5=质控图屏幕、7=复核动作去重）④ 新增"配音怎么做"表：真人画外音（推荐，无标注）vs 剪映文本朗读（须把片尾标注改为"本片部分画面及配音由 AI 生成"）。
- 用户明确："AI 没有关系，不用考虑评委" → 手册改为卡通为主、实拍点缀；配音主推剪映文本朗读（真人备选）。**但保留两条硬约束**（属规则不是评委观感）：① 片尾必须显著标注 AI 生成（画面+配音）② 内容真实，流程/步骤/数字不得编造，卡通可示意但不能与真实操作不符。
- 工具选型（2026-09 实测横评依据）：所有横评共识=**先出分镜静帧再图生视频**，一致性明显优于直接文生视频、废片率降一半。可灵3.0（一致性/首尾帧/原生音频/15s/4K Ultra）；即梦 Seedance2.5（单次30秒、50个多模态参考、1080p、与剪映生态打通）；Vidu（多主体一致、成本低至1秒4分、长视频）；海螺2.3（角色参考图稳、氛围感）；Runway Gen-4（精控强但英文界面、国内访问不便）。**卡通静帧+剪映关键帧**是零成本最稳路线；真动态才上图生视频。
- 已产出 `output/shortvideo-plan-20260919/一管血的旅行-字幕.srt`（27 条，按七段配音稿切好时间轴，每屏≤14 字，末条为 AI 标注，总时长 94 秒），剪映可直接导入。
- 用户要求"先说计划"再动手生成图 → 图像生成暂缓，等他定风格方向（扁平矢量/三维卡通/手绘）与是否上真动态。
- 用户反馈"没有亮点、像仪器流水线干完了"，要求加体现"人"价值的审核环节，并问案例是罗列几个还是突出一个（肌酐偏低→羟苯磺酸钙干扰→改苦味酸法；结果异常高→排查 RF/异嗜性抗体）。
- 决策：**突出一个**（羟苯磺酸钙干扰肌酐，25 秒完整链条），RF/异嗜性抗体一句带过（暗示"不是个例"）。理由：审核段仅 25 秒，两个案例互相稀释；一个案例才走得出"数值异常→起疑→查用药史→换方法复核→报告加注"的完整链条，符合评审"有起伏有细节"。
- 脚本重构：前半 36s 流水线（钩子/分色管空腹/接收离心上机/质控/机器读数）→ 后半 54s 人（审核案例 25s + 一句带过 11s + 点题 12s）。**结尾落点升级为"机器给出数字，人给出判断"**。配音稿 310 字/8 段，全片约 96 秒。
- 新增 5 个"人的价值"实拍镜头（历史结果趋势比对、翻用药记录、换方法重测、备注栏加注、电话联系临床），清单扩到 15 条。节奏：前半 3 秒/屏 → 审核段 4 秒/屏（慢下来）。
- 合规要点：案例必须真实且脱敏（无姓名/住院号/日期）；"苦味酸法"按科室当时实际做法表述；报告加注说明这一笔必须留（体现不掩盖、主动告知临床）。
- 交付：操作手册 docx 更新（7 表），SRT 重做（27 条，最长 14 字 ≤14 ✓）。

## 批间验证记录表打印优化（commit 611ecf0a）
用户反馈截图：表头信息区（试剂/批号变更/检验项目/结论）文字太紧凑；且要求**不显示规格**。
- 改动（`frontend/src/views/reagent/ReagentLotVerification.vue` 的 `onPrint`）：
  ① **去掉「规格」**；
  ② 表头信息区从**纯文本拼接**改为**表格排版**：标签列 96px 灰底(#f4f5f7)居中 + 值列左对齐，边框 1px #b9b9b9，
     字号从继承的偏小值**放大到 13px**、padding 7px 8px、`line-height:1.7`；结论行字号 14px 加粗；
  ③ 容器 `<div class="meta">` 显式加 `font-size:13px;line-height:1.7`（防被外层样式压小）。
- **踩坑：工具链路径变了**（重要）
  - `node` 目录：`versions/22.22.2-2/` **已被删**，现只有 `versions/22.22.2-3/` → 构建命令静默失败（`No such file or directory`），
    我一开始没看构建输出就提交了 dist，**提交的是旧产物**。教训：**构建后必须 grep 产物确认改动进去了**。
  - `tcb` 在 `node/workspace/node_modules/@cloudbase/cli/bin/tcb`（不在 versions 下）。
  - **`node.exe` 是 Windows 程序，不认 Git Bash 的 `/c/Users/...` 路径** → 会被解析成 `D:\c\Users\...` 报 `Cannot find module`。
    **构建和部署都必须用 `C:/Users/...` 形式**。
  - 已把上述全部写进长期记忆 MEMORY.md 的部署铁律。

## 试剂验收：检验项目标注「认可」+ 认可优先排序（commit 17867ea6）
用户需求：检验项目若属申请 CNAS 认可的项目则标注「认可」，其他不标注；排序时认可项目排前面。
- **复用而不是重写**：直接惰性导入 `verification_reports` 的 `_is_cnas` / `_load_cnas_names`
  （认可能力范围表 `accredited_scopes` 动态匹配 + `_CNAS_ALIAS_PAIRS` 别名 + `_CNAS_ABBR` 缩写兜底），
  避免同一套匹配规则维护两份。封装成 `_cnas_checker()` + `is_accredited_item(db, test_item_name)`。
- **排序**：SQL 层不好按"Python 算出来的布尔值"排，改为取全部 → 计算 flag → `flagged.sort(key=lambda t: (not t[1],))`
  → 手动切片分页。**Python 的 sort 是稳定排序**，所以认可组内仍保持原来的 change_date 倒序。
- **前端**：检验项目列前加绿色 `el-tag`「认可」（`v-if="row.is_cnas"`），列宽 150→188；打印 meta 的检验项目也加【认可】绿色标注。
- **实测结果**：95 条记录 → **认可 47 条 / 非认可 48 条**，列表前 16 条全部为认可项目 ✓
- **又一次并发部署**：`_BUILD_MARK` 显示的是**上一轮** `lot-verif-print-meta-table`，但 API 与 chunk 都已生效
  （API 有 `is_cnas` 字段、入口 chunk 引用了新的 `BIrH3Gw6`）——**再次证明只能靠功能/chunk 验证，不能看标记**。
  ⚠️ 另注意：`&&` 链中 `grep -c` 无匹配时返回退出码 1，会**中断后续命令**；验证脚本里要写 `|| echo 0` 或分步执行。
