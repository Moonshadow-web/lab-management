## 2026-07-10 优化项目查询页为卡片式结果页
- 将 frontend/src/views/test_items/TestItemList.vue 从表格改为参考原网页版速查工具的卡片详情展示。
- 功能保留：顶部搜索框（回车/清空触发）、分类筛选（生化/免疫/其他）、新增项目、卡片内编辑/删除。
- 卡片布局：项目名 + 编号标签；别名/仪器/仪器组/方法学标签；单位/稀释倍数/稀释液三列指标；参考范围（蓝色）、线性范围、可报告范围、校准品、溯源性信息行；溶血/黄疸/脂血抗干扰性能卡片；底部标本类型与更新时间。
- 响应式适配移动端；npm run build 编译通过。
- 本地提交 61bd86d，未推送。

## 2026-07-10 修复项目查询搜索无效
- Bug：后端 make_router 的搜索参数名是 `q`（见 crud_base.py list_items 及 CrudTable.vue 第84行），新写的卡片页 onSearch 误传 `search`，导致搜索被忽略、始终返回全部124条（搜 alt 出全部）。
- 修正：TestItemList.vue 把 `params.search` 改为 `params.q`。验证：alt/ALT（大小写不敏感）/钾/IgG 均正确命中。
- 约定备忘：本项目所有列表搜索统一用查询参数 `q`；分类等精确过滤用 filter_fields 同名参数（category/specimen/method）。
- 本地提交 ebfcd26，未推送。

## 2026-07-10 搜索相关性排序（缩写搜索被淹没）
- 现象：用户搜 "PA" 不出"前白蛋白"。根因：后端搜索按 id 排序的纯 ILIKE，前白蛋白(code=PA)虽命中，但被英文名里含 "pa" 子串的项目（Hepatitis 肝炎、Lipoprotein 脂蛋白等）淹没，新项目 id 大排后面。
- 修复：crud_base.make_router 的 list_items 在 q 非空时，对 search_fields 各列按权重排序——精确匹配=3、前缀=2、模糊包含=1（SQLAlchemy case + reduce 求和），再按 id 降序；无 q 时维持 id 降序。
- 验证：PA→前白蛋白第一、钾→钾第一、IgG→IgG类排前；其他模块（试剂/质控等）搜索不受影响。
- 通用工厂级修复，覆盖全部模块。本地提交 24d1829，未推送。

## 2026-07-10 项目查询卡片页关键词高亮
- 在 frontend/src/views/test_items/TestItemList.vue 新增 highlight()：先 html.escape 文本与关键字（防 XSS），再大小写不敏感正则包裹 `<mark class="hl">`。
- 高亮覆盖：项目名/编号/类别、别名/仪器/仪器组/方法标签、单位/稀释倍数/稀释液、参考/线性/可报告/校准品/溯源性、抗干扰溶血/黄疸/脂血、标本类型。
- 样式 .hl（黄底红字加粗）用 `.item-card :deep(mark.hl)` 穿透 scoped（v-html 内容不受 scoped 约束）。
- 验证：Node/Python 双重测试命中正确（PA/前白/2.74/Pre 大小写不敏感），XSS 关键字与数据内 `<`/`&` 均被转义，安全。
- 本地提交 d4a79bc，未推送。Vite dev(5173) HMR 已生效，刷新即见。

## 2026-07-10 系统项目 vs 参考清单对比
- 用户提供 201 项参考清单（含尿液/脑脊液/胸腹水标本拆分），与系统 124 项（生化68/免疫56）比对，产出 `项目对比报告.md`。
- 关键发现：① 系统**完全缺失凝血类 14 项**；② 系统**无标本特异性版本**（只有血清版，缺尿/脑脊液/胸腹水版，如尿钠、脑脊液氯等）；③ 缺糖化血红蛋白(HbA1c)、β2-微球蛋白、血氨、病毒IgM/IgG血清学、肾素-血管紧张素-醛固酮-皮质醇轴、肝纤维化三项等；④ 系统多余仅 oxLDL、GALB%(糖化白蛋白比)，GALB(白蛋白GA)疑似重复；⑤ 大量"缺失"实为命名写法差异（补体3↔补体C3、载脂蛋白AI↔A1、κ轻链↔免疫球蛋白轻链κ、肌钙蛋白I↔超敏肌钙蛋白Ⅰ、TPO/TG-Ab/TRAB 等），系统已含。
- 教训：自动子串匹配误判严重（"巨细胞病毒IgG抗体"因含IgG误匹配"尿IgG"；A1/AI 差异漏匹配）。最终以**人工逐项校正**为准，勿直接采信脚本初判。
- 临时脚本/清单文件已删除，仅保留校正后 Markdown 报告。用户未要求补录数据库。

## 2026-07-10 把缺失项目补录进 test_items 表（124→205）
- 用户确认"补进去，未包含的项目也保留"。编写 tmp_insert_items.py：解析参考清单(200项去重)，对照系统 124 项，真缺失项批量 INSERT，已有项(含命名差异等价)与系统多余项均不动。
- 匹配规则三处坑（均已修复）：① 空标本误判——`r_suffix in suffix` 当 r_suffix="" 永远为真，导致系统 specimen 为空的血清项目被误判为"已覆盖标本版"，漏插全部尿/脑脊液/胸腹水版；改为仅"都无标本 或 同标本"才命中。② 快速法被标准法吞——`快速抗梅毒螺旋体抗体` 因含 `梅毒螺旋体抗体` 被误判已覆盖；加 `('快速' in base)==('快速' in r.name)` 修饰一致性约束。③ 单字元素被长名吞——`铜蓝蛋白` 因含单字 `铜` 被误判；加 `len<=2` 短名保护。另加命名差异等价映射表(26项)与别名精确匹配分支。
- 结果：插入 **81 项**，test_items 总数 **124→205**（生化106/免疫85/其他14）。含凝血14、尿液12、脑脊液5、胸腹水6、糖化血红蛋白/HbA1c、β2-微球蛋白、血氨、17羟/17酮/香草扁桃酸、血气、血清/免疫固定电泳、肝纤维化四型、病毒IgM/IgG血清学、戊肝、结核T-SPOT、RAAS轴、肺癌七项、快速法4、铜蓝蛋白、ST2、EPO、P2PSA 等。系统多余项 oxLDL/GALB%/GALB 保留；查重确认无重复 name。
- 数据直接写本地 data/app.db（.gitignore 已排除 data/，**未提交 git**，符合"不发布"；上云时迁移）。后端无需重启，经 5173 代理 API 已能查到新项目（凝血酶原时间/钠（尿液）/铜蓝蛋白 均命中）。
- 临时脚本执行后已删除。新项目参考范围/单位等字段留空待用户补全。

## 2026-07-10 待补字段清单改为离线协作模式
- 用户决定不用系统「批量导入」UI，改为「我发Excel → 用户填 → 发回我处理」的离线协作方式。
- 交付文件：项目根目录 `检验项目_待补清单.xlsx`（81条，即 reference 为空的脚本补录项；表头带*为待补字段并黄色高亮，含「填写说明」sheet）。
- 后端已加的 /api/v1/test-items/import 端点与前端「批量导入」按钮保留（备用），本轮不依赖。
- 后续用户回填后，我需按项目名称 upsert 写回 data/app.db（只覆盖非空字段）。

## 2026-07-10 确认IMA中81项缺字段的数据源（生免组知识库）
- 用户确认权威来源是共享知识库「生免组检验知识库」(id 7445972045036619，821项)，而非 MHZYY-JYK。
- 重要：民航总医院检验科项目速查手册.md 实为 TAT/价格/标本手册（含送检/报告时间、价格），**不含参考范围**，不能直接取参考值。
- 结构化参考数据在 SOP 文档：BG-SM-GL-018(生化线性范围)、BG-SM-GL-016(免疫定量线性/可报告)、BG-SM-GL-017(凝血线性/可报告)、BG-SM-GL-014(生化免疫组定量认可项目线性范围与分析质量要求)。均为 markdown 表格，含 项目/线性范围/可报告范围/稀释方式/备注，可解析。
- 已验证 BG-SM-GL-017 含 PT/APTT/TT/FIB/D二聚体/FDP 的线性与可报告范围。
- 结论：线性范围/可报告范围/单位/稀释方式可从 GL 文档提取；参考范围(临床参考区间)不在这几个文档里，需从 BG-SM-GL-014 或操作规程PDF另挖。
- 使用连接器 mcp__ima-mcp 读取（OpenAPI 凭证已失效 401，改用连接器）。

## 2026-07-10 从生免组GL文档预填81项线性/可报告/稀释
- 用 mcp__ima-mcp 拉取生免组库(7445972045036619)的 BG-SM-GL-018(生化线性)、BG-SM-GL-016(免疫定量线性/可报告)、BG-SM-GL-017(凝血线性/可报告) 三个文档 markdown 表格。
- 写脚本按 id 精确匹配 81 个 reference 空项：匹配预填 47 条（凝血6/生化26/免疫15），34 条未匹配（快速法、IgM抗体、病毒血清学、肿瘤标志物、血气组合等 GL 未覆盖）。
- 生成 检验项目_待补清单_预填.xlsx：绿色=已从IMA预填(线性/可报告/稀释液)，黄色=仍需手填(方法学/单位/参考范围/校准品/溯源性/干扰)。
- 注意：这三个GL文档只含线性/可报告/稀释方式，**不含单位、不含临床参考范围**；单位与参考范围仍需用户补。稀释方式已映射到"稀释液"列。
- 用户确认走离线方式：发回Excel→我按名称 upsert 写库。

## 2026-07-10 从具体项目SOP提取参考范围（按用户优先级：SOP优先，无则说明书）
- 用户指令：参考范围优先取具体项目SOP，SOP没有的再查仪器/试剂说明书。
- 建立 resolved.json：81项(125-205) → 76项命中SOP（映射至69个唯一SOP文档），5项无SOP(170四型胶原/172层粘连蛋白/199 HSV-2 IgM/202 P2PSA/203 ST2)。
- 用 6 个并行 subagent 经 mcp__ima-mcp__fetch_media_content 抓取 69 个 SOP，逐项目提取 参考范围/可报告范围/单位/溯源性/干扰/方法学/仪器，写入 sop_out/{pid}.json（76个文件，由 subagent 写入避免主上下文膨胀）。
- 5 个"无SOP"项另起 subagent 查说明书：发现 170/172 实际在KB有SOP(SM-SOP-555/553 迈瑞CL-6000i)，参考范围 ≤98ng/mL、≤133ng/mL；199 HSV-2 IgM 仅代理判读(安图HSV-1 IgM SOP)；202 P2PSA、203 ST2 KB无手册。
- 生成 检验项目_待补清单_SOP预填.xlsx（覆盖上一版GL预填）：81行，参考范围列 78绿(已填)/3黄(待补：167定性、202、203)；GL预填的线性/可报告/稀释/校准品/溯源性/干扰一并保留。填写说明sheet列出需人工复核项：150总蛋白SOP疑似错配(实为NAG)、156腺苷脱氨酶正文空取报表值、167定性无数值、199代理判读、202/203无手册。
- 待用户审核Excel→发回→按pid upsert写 data/app.db（仅覆盖非空字段，不提交git）。

## 2026-07-10 优化：标本类型置顶（针对新系统 Vue）
- 用户纠正：所有操作针对**新开发的 Vue+FastAPI 系统**（frontend/ + 后端 app.db），不是根目录 data.js 旧应用。
- 旧应用此前误改（specimen_data.js / index.html script标签 / script.js banner / styles.css）——已全部回退删除，仓库只服务于新系统。
- 新系统实现：frontend/src/views/test_items/TestItemList.vue
  - 卡片顶部新增 `.specimen-banner` 横条（蓝底"标本类型"标签 + 值，支持搜索高亮），`v-if="row.specimen"`。
  - 底部 footer 原"标本：xxx"移除（避免重复），仅保留"更新于"。
  - 数据来自后端 test_items.specimen（经 listTestItems API），无需额外 JS 文件。
  - 新增 CSS：.specimen-banner / .sb-label / .sb-value（蓝调渐变横条）。
- 已 `vite build` 编译通过（TestItemList chunk 10.48kB）。
- git status 显示 backend/app/api/v1/test_items.py 与 frontend/src/api/testItems.js 有未提交修改——为**新系统此前已有改动**（非本轮），未触碰。
- 遗留待确认：GitHub Pages 实际服务于根目录 data.js 应用 还是 frontend/dist？新系统改动需依赖部署指向 dist 才上线（用户已知）。

## 2026-07-10 空白标本类型统一默认=血清
- 用户指令：没有标本类型的，先都改为「血清」。
- 执行：`UPDATE test_items SET specimen='血清' WHERE specimen IS NULL OR TRIM(specimen)=''`（data/app.db，未提交git，data/已被gitignore）。
- 结果：118 条空白→血清；总数 206，其中 血清165、其它41（血浆/全血/尿液/脑脊液/胸腹水等）。
- 因 TestItemList.vue 标本横条 v-if=row.specimen，现所有卡片顶部均会显示标本类型。非血清项待用户后续补数据时更正。

## 2026-07-10 标本类型改为指标区首块（与单位等并列）
- 用户细化：标本类型不要顶部横条，改放在「单位」左边，与 单位/稀释倍数/稀释液 三个呈同样块。
- 实现：移除 `.specimen-banner` 顶部横条(模板+CSS .specimen-banner/.sb-label/.sb-value 全删)；在 `.metric-grid` 首位置入 `<div class="metric">标本类型</div>`，与另外三个 `.metric` 同款。
- `.metric-grid` 由 `repeat(3,1fr)` 改 `repeat(4,1fr)`，四个块同一行等宽；移动端仍 1fr 竖排。
- vite build 通过，div 平衡，无残留样式。
