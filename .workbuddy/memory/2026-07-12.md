# 2026-07-12 工作记录

## 仪器档案预览改造：doc 自动转 docx，浏览器内预览（不下载）
- 背景：原档案预览对 `.doc` 只能强制下载，用户要求"像文件管理那样"在浏览器内直接看、并把 doc 转 docx。
- 新增 `backend/app/core/doc_convert.py`：`convert_doc_bytes_to_docx(content)` 经 subprocess 调系统 Python（含 pywin32）跑 Word COM `SaveAs(FileFormat=16)` 转 docx。后端 venv 无 pywin32，故用子进程隔离。
- `instruments.py` 增加 `_maybe_convert_doc()`，在 `upload_archive` 与 `import_archives_folder` 中把 `.doc` 自动转为 `.docx`（文件名/扩展名/original_filename 一并改）。
- 一次性脚本 `backend/scripts/convert_archives_doc_to_docx.py` 把存量 34 个 `.doc` 全转 `.docx`（成功 34/失败 0）。
- 前端 `InstrumentList.vue`：`previewArchive` 改为 `.docx`→mammoth 弹窗渲染（宽82%、v-loading），`.pdf`→新标签，去掉 .doc 强制下载分支；与文件管理预览一致。
- 操作前已备份 `data/app.db.bak_before_archive_docx_*` 与 `data/uploads/instrument_archives.bak_*`（41文件）。
- 验证：TestClient 确认 archives/status 40 个全 .docx、下载返回 PK 头 docx、上传 .doc 自动转 .docx。
- 事故与修复：验证脚本误将 SM-2010 档案覆盖到 instrument_id=1（SM-1001），已用备份原件重转恢复；删除了临时验证/修复脚本，仅保留 convert_archives_doc_to_docx.py。
- 已杀掉残留双 uvicorn 进程、干净重启后端（8123 单实例，PID 22356），健康检查正常。
- 现状：40 个档案 DB↔磁盘完全一致，全部 .docx，SM-1001 与 SM-2010 均正确。

## 校准记录功能完善（基于 BG-KS-GL-073-检定仪器一览表.docx）
- 用户需求：回填校准日期等；提前一个月提醒；校准报告上传/预览/下载。
- 该一览表实为 `.doc`（OLE2，扩展名误标 .docx），用 Word COM 转 docx 后解析 35 行（2 行表头），列：名称/制造商+型号/出厂编号/科室编号(dept_no)/周期(月)/应用范围/机构/最近检定日期/备注。
- 解析脚本 `extract_calibration_json.py`（系统 Python + win32com）→ `_calib_data.json`；回填脚本 `backfill_calibrations.py`（后端 venv）ALERT 新列并写入 31 条记录。
- 数据清洗：SM-2002 两模块合并为 1；SM-2011/SM-2012/SM-1032 无日期跳过；新列 agency/cycle_months/report_filename 已 ALTER 加入 calibration_records 表（result 默认"合格"）。
- 删除 2 条历史测试垃圾记录（result='OK'、无 agency/cycle、日期 2026-07-09），保留 31 条真实记录。
- 后端改动：
  - `models/instrument.py` CalibrationRecord 新增 agency/cycle_months/report_filename。
  - `schemas` CalibrationRecordBase 同增字段。
  - `instruments.py` 新增 `POST/GET/DELETE /{iid}/calibrations/{rid}/report`（上传/预览下载/删除，doc 自动转 docx，存 calibration_reports/）、`GET /calibrations/status`（每台在用仪器下次校准日期/剩余天数/level/has_report）。
  - `notification_service.py` 重构出 `compute_calibration_alerts(db)`（WARN_DAYS=30，逾期 danger / ≤30天 warning / 否则 info），`refresh_calibration_notifications` 复用；启动 lifespan 调用刷新。
- 前端 `InstrumentList.vue`：校准对话框表格加"检定机构/周期(月)/报告(上传·预览·下载)"列与表单字段；报告上传/预览（docx mammoth、pdf 新标签）/下载复用 archive 逻辑；仪器列表新增"下次校准"列（颜色+逾期/即将到期标记），"校准记录"按钮按 level 变色显示 逾期/临期；`fetch()` 合并 `/calibrations/status`。`api/instruments.js` 增 4 个函数。
- 验证：/calibrations/status 返回 30 台在用（danger21/warning2/info7），校准提醒通知 30 条；报告上传/下载/删除 round-trip 通过（测完清理）；前端 `vite build` 通过。
- 提醒逻辑：默认仅"在用"仪器参与；next_due_date 由 校准日期+周期 推算；提前 30 天预警。
- 备份：`data/app.db.bak_before_calib_20260712_001905`。

## 修复：仪器使用授权书预览失败（fake-docx 问题）
- 现象：文件管理里预览 `BG-KS-GL-070-仪器使用授权书.docx` 失败。根因：该文件扩展名是 .docx，但实际是旧版 `.doc`（OLE2 复合文档，魔数 `d0cf 11e0`），mammoth 只能解析真 docx（ZIP/PK 头），故预览报错。
- 全量扫描 `data/uploads/{docs,manuals,instrument_archives,calibration_reports}` 发现 **10 个"伪 docx"**（实为 OLE2）：docs 4 个（BG-KS-GL-069/070/070_1/073），instrument_archives 6 个（SM-2001/2003/2004/2006/2007/2009）---后者是上次批量转换**漏掉**的（当时只转 .doc 扩展名，没识别伪 .docx）。
- 修复：一次性脚本把 10 个全部用 Word COM 转成真 docx **就地覆盖**（同名，DB 路径仍有效），备份 `data/uploads/_fake_docx_bak_20260712_003000`。
- 加固 `instruments.py` 的 `_maybe_convert_doc()`：**改为按文件头魔数判定**（content[:4]==`d0cf11e0` 即 OLE2 就转），不再只看 `.doc` 扩展名，使今后"伪 .docx"上传/导入也能自动转真 docx。
- 验证：mammoth 对授权书解析成功（标题+正文）；重新扫描 0 个伪 docx；`GET /documents/496/download` 返回 PK 头真 docx。已杀残留 uvicorn、干净重启后端（PID 28028）。
- 附注：docs/ 下原 4 个 162 字节占位文件（`__-SM-CZ-044/GL-003/014/017`）已于本日后续处理——删除占位文件+版本记录，上传产生的新文件重复主记录也已去重（见本文档末尾"处理 4 个 162 字节占位文档文件"段）。
- 备份：`data/app.db.bak_before_fake_docx_20260712_003000`。

## 二次排查：授权书预览仍报 "Can't find end of central directory"
- 首次修完伪 docx 后用户刷新仍报 JSZip(mammoth) 错。复现：用 node 以浏览器相同路径 `GET /documents/{id}/preview`（带鉴权）拉字节喂 mammoth，4 个授权书（496/195/196/197）**全部成功解析**（PK 头、正文正常）。说明服务端文件已正确，错误源于**浏览器缓存了修复前的旧（伪 docx）响应**。
- 加固（防复发）：① 后端 `documents.py` 的 download/preview 以及 `instruments.py` 的 archive/report 下载 `FileResponse` 均加 `headers={"Cache-Control":"no-store"}`；② 前端 `fetchDocumentBlob` 的 URL 追加 `?_=${Date.now()}` 缓存破坏。重新干净重启后端（杀掉 25368/28028 双实例，仅留 26816 单实例）。
- 验证：`curl -I` 预览返回 `cache-control: no-store` + 正确 docx（22545 字节，PK）；前端 `vite build` 通过。
- 结论：用户只需浏览器 **Ctrl+F5 硬刷新**一次即可（清掉缓存的旧响应），此后 no-store+缓存破坏保证不再复发。

## 处理 4 个 162 字节占位文档文件（__-SM-*）
- 背景：上次（伪 docx 修复段）提到 docs/ 下 4 个仅 162 字节"未知魔数"文件（__-SM-CZ-044/GL-003/GL-014/GL-017）预览会失败，当时未处理。用户本次指出"后来传了新文件"。
- 排查：
  - 这 4 个 `__-SM-*.docx` 是 162 字节损坏/占位文件（魔数 `06bd…`/`053831353236…`，非 PK/ZIP），被错误登记为 `document_versions` 的"1.0 版本"（v259/v184/v185/v186），点该版本即预览失败。
  - 用户后来传的"新文件"确实在，存为同名 `_1` 文件（BG-SM-*-…_1.docx，均 PK 真 docx：19249/28677/31950/17973 字节）。
  - **根因**：文档"上传新文件"走的是**新建主记录**而非"更新版本"，导致 CZ-044/GL-014/GL-017 各出现 2 条"生效"记录（旧+_1，内容相同），GL-003 仅 1 条指向 _1 但被标"作废"。
- 处理（备份 `data/app.db.bak_before_placeholder_cleanup_20260712_004226`、`app.db.bak_before_dedup_20260712_004349`）：
  1. 删 4 个 `__-SM-*.docx` 磁盘占位；删 `document_versions` 中 `file_path GLOB 'docs/__-SM-*'` 的 4 条记录。
  2. 按用户决策去重：删旧主记录 254/201/204（及其版本 v230/v177/v180），保留 _1 记录（283/209/210）；GL-003(208) 维持"作废"。删磁盘无_1旧文件 3 个。
- 结果：4 文档各 1 条主记录，全指向 _1 真 docx，版本表干净；磁盘仅留 4 个 _1 文件。
- **待修 Bug**：文档上传接口实为"新建文档"导致重复记录——应改为追加 `document_versions` 并更新主记录 file_path；另建议落库前按魔数校验，拒绝 162 字节占位类无效文件。

## 提醒事项标题列增加仪器型号
- 需求：Dashboard"提醒事项"卡片的"标题"列只显示仪器名称（如"全自动化学发光仪 校准提醒"），同名仪器多时难区分，要求加上规格型号（Instrument.model）。
- 改动：`backend/app/services/notification_service.py` 的 `refresh_calibration_notifications`，title 由 `f"{inst.name} 校准提醒"` 改为 `f"{inst.name}（{inst.model}）校准提醒"`（model 为空则不显示括号）。
- 生效：后端启动时 lifespan 会重跑 refresh 重新生成全部校准通知（instruments.py 在校准记录变更时也会刷新）。干净重启后端（杀旧 26816/16832，venv python 单实例 PID 24704）后，`data/app.db` 的 notifications 已更新，样本如"免疫分析仪（安图A6200）校准提醒""高速低温离心机A（Fresc021）校准提醒"。
- 前端 Dashboard.vue 直接渲染 row.title，无需改动；用户刷新页面（建议 Ctrl+F5）即可看到带型号标题。
- 备注：`backend/data/app.db` 是空库（非在用），真实库为 `data/app.db`（DB_PATH=DATA_DIR/app.db）。

## 仪器列表删除"档案"列
- 需求：用户原说"档案列不需要显示已建档"，随即纠正为"去掉这一列"。
- 改动：`frontend/src/views/instruments/InstrumentList.vue` 的 `columns` 数组中删除 `prop: 'has_archive', label: '档案'` 整列（含其 formatter）。`fetch()` 仍拉 `getArchivesStatus` 的 has_archive 数据，但不渲染，不影响其他列（如"校准记录"按钮仍正常）。
- 生效：前端 dev server(5173, PID 660) 热更新已生效，刷新列表即可看到"档案"列消失。

## 校准提醒范围收窄：仅逾期/一个月内临期才提醒
- 需求：没有逾期、且距下次校准还有一个月以上的仪器，不再出现在提醒事项里（即去掉 info 档提醒）。
- 改动：`backend/app/services/notification_service.py` 的 `refresh_calibration_notifications`，在算出 `days_left` 后增加 `if days_left > WARN_DAYS: continue`，跳过非逾期且超 30 天的仪器、不生成通知。注意 `compute_calibration_alerts`（供列表"下次校准"列 status 接口）保持全量不变——列表仍显示全部下次校准日期与颜色，仅提醒中心不再显示 info。
- 生效：干净重启后端（杀旧 24704/6156，venv python 单实例 PID 5484），lifespan 重跑 refresh 重新生成通知。验证 `data/app.db` 校准通知 level 分布由 30 条(21 danger/2 warning/7 info) 变为 23 条(21 danger/2 warning/0 info)。
- 前端 Dashboard 提醒事项直接渲染 notifications 表，刷新即可看到提醒数减少；前端 dev server(5173) 无需改。

## 项目查询模块：统计分类 + 品牌标识 + 导出汇总清单
- 需求（用户第6条）：项目查询页做一个简单统计分类，导出"项目汇总清单"（区分生化/免疫/…）；并加品牌标识（贝克曼/罗氏/安图/沃芬/沃文特/柏定…），品牌参考校准品的牌子。
- 决策（AskUserQuestion）：① 品牌**不新增数据库字段**，从 `calibrator`（校准品）文本按关键词自动解析；② 导出格式 Excel(.xlsx)。
- 品牌识别：新增 `backend/app/core/brand.py`（`BRAND_KEYWORDS` 列表 + `extract_brand(calibrator)` 关键词命中即返回）；前端 `TestItemList.vue` 维护同款 `BRAND_KEYWORDS` 自行 `extractBrand`（因 list 接口走 ORM 不经 Pydantic，`computed_field` 不生效，见坑③）。
- 后端接口（`backend/app/api/v1/test_items.py`）：
  - `GET /api/v1/test-items/stats`：返回 `{total, category_counts, brand_counts}`，品牌用 `extract_brand(r.calibrator) or "未标识"`。
  - `GET /api/v1/test-items/export`：按 `q`/`category`/`brand` 筛选（brand 转 `calibrator.ilike(f"%{brand}%")`）；openpyxl 生成 xlsx——主表"项目汇总清单"（列：编号/名称/品牌/类别/标本/方法/单位/参考范围/线性范围/可报告范围/校准品/溯源性/使用仪器/仪器组），附"分类统计"sheet（按类别、按品牌计数）；用 `Response(content=buf.getvalue(), media_type="application/vnd.openxmlformats-officedocument.spreadsheetml.sheet", headers={"Content-Disposition":"attachment; filename=test_items_summary.xlsx","Cache-Control":"no-store"})` 返回。
  - 需 `from ...models.user import User`（新端点用 `user: User = Depends(get_current_user)`，漏导入会 NameError）。
- 路由重排（关键坑①）：`/stats`、`/export` 静态路由必须排在 `/{item_id}` 参数路由之前，否则被"吞掉"（带 token 返回 422 int_parsing "stats"）。在 `backend/app/api/v1/router.py` 末尾把这两路由提到 `api_router.routes` 最前（用**完整路径** `"/api/v1/test-items/stats"`/`"/api/v1/test-items/export"` 匹配，子 router 层是相对路径、api_router 层是完整路径）。
- 前端（`frontend/src/views/test_items/TestItemList.vue` + `api/testItems.js`）：
  - 工具栏加「导出汇总清单」按钮（Download 图标）→ `onExport()`（按当前 keyword/category 导出，文件名 `项目汇总清单.xlsx`）。
  - 查询结果前加「统计分类」概览栏 `.stats-bar`：总项目数 + 按类别 tag（点 `applyCategory` 筛选）+ 按品牌 tag（点 `exportByBrand(brand)` 直接导出该品牌 xlsx，文件名 `项目汇总清单_${brand}.xlsx`）。
  - 卡片 tag-row 加品牌标签（`<span class="tag-label">品牌</span>{{ extractBrand(row.calibrator) }}`，仅当解析到品牌才显示）。
  - `api/testItems.js` 新增 `getTestItemStats()`、`exportTestItems(params)`（`responseType:'blob'`）；复用 `api/documents.js` 的 `downloadBlob(blob, filename)`。
- 坑② 导出 `Response`：`StreamingResponse(buf)` 传 BytesIO 对象报 500（不可迭代、流式发送失败兜底 text/plain）；改用 `Response(content=buf.getvalue(), ...)` 直返 bytes 才成功（PK 头、208 行）。

## 项目查询：按用户完善的 Excel 回写（品牌改为存储字段）
- 用户桌面 `C:/Users/81526/Desktop/项目汇总清单.xlsx`（即 /export 同格式：项目编号/名称/品牌/类别/标本/方法学/单位/参考范围/线性范围/可报告范围/校准品/溯源性/使用仪器/仪器组）重填了 品牌/仪器及分组/方法，并删了重复项。要求按此改 DB。
- 核对（只读 reconciliation）：Excel 202 行 ↔ DB `test_items` 202 行，按 项目编号(=code)或 项目名称 匹配 1:1；**0 行待删**（"重复项"在当前 DB 已不存在）；字段差异仅 4 列：品牌 201、方法学 48、使用仪器 26、仪器组 85；其余 8 列(类别/标本/单位/参考范围/线性范围/可报告范围/校准品/溯源性) 均与 DB 一致。
- **关键决策：品牌改为存储字段**。原方案品牌从 `calibrator` 推导，但 126/201 行的用户品牌无法由校准品推出（如 BNP 校准品="B型钠尿肽校准液" 不含"贝克曼"）。故 `ALTER TABLE test_items ADD COLUMN brand VARCHAR(50)`，品牌=显式存储值，回退才用 calibrator 推导。
- 改动：
  - `models/test_item.py`：`brand` 列。
  - `schemas/__init__.py`：`TestItemBase` 加 `brand: str=""`（create/update 可写）；`TestItemRead` 的 computed 改名为 `brand_label`（返回 `self.brand or extract_brand(self.calibrator)`）——**不能同名覆盖字段**（Pydantic `you can't override a field with a computed field`）。
  - `core/brand.py`：新增 `resolve_brand(calibrator, brand="")` 优先用存储 brand。
  - `test_items.py`：`/stats`、`/export` 的 品牌 统计/列 改用 `resolve_brand(r.calibrator, r.brand)`；`/export` 的 brand 筛选 `or_(TestItem.brand==brand, calibrator.ilike(...))`；`_CN2EN` 增加 `"品牌":"brand"`（使批量导入也能写 brand）。
  - 前端 `TestItemList.vue`：`extractBrand(row)` 改为优先 `row.brand`，否则回退 calibrator 关键词匹配（模板调用 `extractBrand(row)`）。
- 回写：一次性脚本直连 `data/app.db` 按匹配 UPDATE brand/method/instrument/instrument_group（Excel 非空才覆盖）。DB 备份 `data/app.db.bak_before_brand_update_20260712_064957`。
- 重启后端（杀全部 python 再起 uvicorn，避免多实例占端口），验证：/stats total=202、brand_counts 现含 德赛/景源/旭化成/思塔高/Sebia/东曹… 仅 1 条未标识；BNP brand=贝克曼；/export 203 行(含头) 品牌列正确。前端 dev server(5173,PID 660) HMR 已生效，刷新即见。
- 教训：①用户显式填的品牌常与校准品文本不一致，不能仅靠推导，需存储字段；②Pydantic v2 computed_field 不得与同名普通字段冲突（用 brand_label 区分）；③回写前务必确认后端实际连的 DB（config 经 `BACKEND_DIR.parent/data/app.db` 解析到项目根 data/app.db，勿误改 backend/data/app.db 副本）。
- 坑③ list 接口不返回 `brand`：`crud_base.make_router` 的 list 直接返回 ORM 对象，不经 Pydantic 序列化，故 `TestItemRead` 上的 `@computed_field brand` 不生效；前端改用同款 `BRAND_KEYWORDS` 自行 `extractBrand(calibrator)`，不改 crud_base 以免波及所有模块。
- 坑④ curl 中文参数：curl 未编码中文参数（`brand=贝克曼`/`category=免疫`）会被 uvicorn 拒为 400 "Invalid HTTP request received" 或显空表；前端 axios 自动编码无此问题，验证须用 `--data-urlencode` 或 Python `urllib.parse.urlencode`。
- 验证：`vite build` 通过（TestItemList chunk 12.47kB，无错误）；后端干净运行（PID 9512，端口 8123）；真实 HTTP 登录(admin/admin123) → `/stats` 返回 total=207、category_counts 生化106/免疫87/凝血14、brand_counts 未标识129/贝克曼44/罗氏12/安图6/柏荣5/沃文特3/柏定3/日立3/利德曼2；`/export` 全量 28921 字节 PK 头 xlsx、category=免疫 87 行、brand=贝克曼 44 行（均含"品牌"列 + "分类统计"sheet）。
- 提示用户：129 条"未标识"是因 calibration 字段为空或不含已知品牌词，补齐校准品信息即可自动识别品牌；前端 dev server(5173) 热更新已生效，刷新即可见统计栏与品牌标签。

## 项目查询：点击统计分类标签筛选列表并导出当前筛选结果
- 需求：用户点击"统计分类"中的 类别/品牌 标签后，下方查询结果列表只显示对应项目；"导出汇总清单"按钮导出当前筛选结果。
- 后端：`test_items.py` 的 `make_router` 的 `filter_fields` 增加 `"brand"`，使 `GET /api/v1/test-items?brand=贝克曼&category=免疫` 支持品牌等值筛选。
- 前端 `TestItemList.vue`：
  - `query` 增加 `brand`；
  - 新增 `applyBrand(brand)`：点击切换品牌筛选并重置到第 1 页；
  - 类别/品牌标签改为 `:effect="query.xxx === 值 ? 'dark' : 'plain'"`，当前选中高亮；
  - 新增 "当前筛选" 条，显示已选 类别/品牌/关键词，可单独关闭或清除全部；
  - `onSearch` 与 `onExport` 同时携带 `q`/`category`/`brand`，实现列表与导出口径一致。
- 验证：`vite build` 通过；后端重启后真实 HTTP 验证 `GET /api/v1/test-items?category=免疫&brand=贝克曼` 返回 total=31 条；`/export?category=免疫&brand=贝克曼` 下载 9583 字节 xlsx，主表 32 行（含头）品牌/类别均正确。

## 品牌补齐：id=165 血气和酸碱分析 → 西门子
- 用户确认唯一"未标识"项（id=165，血气和酸碱分析 pH/PO2/PCO2/K/Na/Cl/Ca，生化，校准品空）品牌为"西门子"。
- 直连 `data/app.db` `UPDATE test_items SET brand='西门子' WHERE id=165`。无需重启（brand 为存储字段，/stats、/export 实时查库）。
- 结果：剩余未标识=0，品牌=西门子 1 条。前端刷新即见统计栏新增"西门子 (1)"标签。

## 项目查询：统计分类新增"按仪器"分组 + 统计栏可折叠/展开
- 需求：统计分类加"按仪器"维度；整个统计栏加 +/- 折叠展开按钮。
- 后端 `test_items.py`：
  - `make_router` 的 `filter_fields` 增加 `"instrument"`（make_router 等值筛选，`GET /api/v1/test-items?instrument=AU生化仪` 生效）。
  - `/stats` 返回 `instrument_counts`：按 `instrument` 原值分组；空或 `"/"` 归 `"未设置"`（仅展示计数，前端 disabled 不可点筛选，避免无意义筛选 4 条脏值）。
- 前端 `TestItemList.vue`：
  - `query` 加 `instrument`；`statsCollapsed` ref 控制折叠。
  - stats-head 加"收起/展开"按钮（`ArrowDown`/`ArrowRight` 图标）。
  - 新增"按仪器"分组标签，点击 `applyInstrument(inst)` 切换筛选；"未设置"标签 `:disabled` 不可点。
  - "当前筛选"条加仪器标签（关闭 `clearFilter('instrument')`）。
  - `onSearch`/`doExport`/`hasActiveFilter`/`clearAllFilters` 均纳入 `instrument`；导出文件名带仪器条件。
- 验证：`vite build` 通过；重启后端后 `/stats` 返回 instrument_counts（AU生化仪79/DxI80048/罗氏Cobas6000 17/安图A6200 15/日立HT7600 14/ACL TOP700 12/未设置4…）；`?instrument=DxI800` 列表 48 条、`?instrument=AU生化仪` 导出 203 行均正确。
- 提示用户：数据存在同仪器不同写法（罗氏Cobas6000 / 罗氏 Cobas6000 / 罗氏Cobas 6000 分散；HT7600 vs 日立HT7600 分散），统计按原值故会碎；未做臆测合并，建议用户在 Excel 统一 instrument 写法后重导。

## 仪器写法归一化（用户明确指定合并规则）
- 用户指定：罗氏Cobas6000(17)/罗氏 Cobas6000(1)/罗氏Cobas 6000(1) → 统一为 **罗氏 Cobas6000**；HT7600(2)/日立HT7600(14) → 统一为 **日立HT7600**。
- 直连 `data/app.db`（备份 `data/app.db.bak_before_instrument_normalize_20260712_114235`）执行精确 IN 替换：
  `UPDATE test_items SET instrument='罗氏 Cobas6000' WHERE instrument IN ('罗氏Cobas6000','罗氏Cobas 6000')`（18 行）；
  `UPDATE test_items SET instrument='日立HT7600' WHERE instrument IN ('HT7600')`（2 行）。
- 结果：`罗氏 Cobas6000`=19、`日立HT7600`=16，旧写法清零；在线 `/stats` 已合并（无需重启，brand/stats 实时查库）。
- 用户提醒：仪器组(instrument_group) 之前已在汇总表里填过——本次只动 `instrument` 列，`instrument_group` 完好（空值 42 条、去重 21 个）。可后续按需加"按仪器组"分组统计（未做，待用户确认）。

## 仪器"未设置(4)"脏值处理 → 胶体金法
- 用户查"未设置(4)"实为 4 条床旁快速检测（id 194/195/196/197：快速抗HIV抗体/快速抗梅毒螺旋体抗体/快速乙型肝炎病毒表面抗原/快速抗丙型肝炎病毒抗体(仅复检)），原 `instrument='/'` 是 Excel 残留分隔符脏值，均属胶体金法（手工快速法，无固定仪器）。
- 用户选"统一改成某值"，直连库 `UPDATE test_items SET instrument='胶体金法' WHERE id IN (194,195,196,197)`（4 行）。无需重启，/stats 现显示 `胶体金法:4`，`未设置` 归零（脏值清除）。

## 项目查询 → 关联仪器档案 跳转（一对多）
- 需求：项目表的 `instrument` 是"总型号"（罗氏 Cobas6000=AJ e601/e602/e411 三台；AU生化仪=AU5822/AU5800急诊/AU5821 A/B 四台…），用户点击项目里的仪器要能跳到对应仪器档案（多台则跳多个）。
- 后端新增 `backend/app/core/instrument_link.py`：`FAMILY_MAP`（15 个归一化总型号 → instruments.id 列表，覆盖全部 15 个去重值；胶体金法→[]）；`resolve_family_instruments(db, family)` 优先精确匹配 MAP，未命中则按型号关键词兜底匹配。
- 后端新增 `GET /api/v1/instruments/family-map`：返回 `{总型号: [{id,name,model,dept_no,has_archive}]}`，供前端卡片渲染芯片。
- **路由重排坑（同 test_items）**：`/family-map` 静态路由被 make_router 的 `/{item_id}` 吞掉（422 int_parsing）。子路由器内相对路径重排无效；最终在 `router.py` 用完整路径 `/api/v1/instruments/family-map` 把该路由提到 `api_router.routes` 最前才生效（与 /stats /export 同法）。
- 前端 `TestItemList.vue`：onMounted 拉 `getInstrumentFamilyMap()` 存 `familyMap`；卡片加"关联仪器档案"区块，按 `row.instrument` 取 `familyMap` 渲染芯片（名称+型号，无档案标"· 未建档"），点击芯片 `router.push({path:'/instruments', query:{focus: id}})`。
- 前端 `InstrumentList.vue`：加 `useRoute`，`onMounted`+`watch` 读 `route.query.focus`，调 `getInstrument(id)` 后 `openArchive(inst)` 自动打开该仪器档案抽屉（不受"仅用在用"筛选影响，因直接按 id 取）。
- 验证：`vite build` 通过；后端重启后 `/api/v1/instruments/family-map` 返回 15 个 family，罗氏 Cobas6000→[14,15,16]、AU生化仪→[2,5,67,68]、DxI800→10 台、胶体金法→[] 均正确；`/api/v1/instruments/14`(getInstrument) 正常。
- 设计取舍：映射先用后端 dict（准确、覆盖当前全部 15 值），未做 DB 关联表。若日后要逐项可编辑映射，再加 junction 表 + 管理 UI。

## 仪器关联改为 DB 关联表 + 管理入口（承接上条）
- 需求：① 项目查询页点"关联仪器档案"芯片跳转仪器档案时，**保持项目查询页不丢失**；② 把原写死在 `core/instrument_link.FAMILY_MAP` 的"总型号→仪器"映射迁成数据库关联表，并加管理界面可维护。
- 新增表（自动建表，`main.py` lifespan 的 `Base.metadata.create_all` 覆盖，无需手工 migrate）：
  - `instrument_families(id, name UNIQUE, description)`：总型号定义，`name` 对应 `test_items.instrument` 取值（如"罗氏 Cobas6000"）。
  - `instrument_family_members(id, family_id FK ondelete CASCADE, instrument_id, UNIQUE(family_id,instrument_id))`：关联行。
  - 文件 `backend/app/models/instrument_family.py`，已注册进 `models/__init__.py`；Schema `InstrumentFamily(Read/Create/Update)` + `InstrumentFamilyMemberOut` 加在 `schemas/__init__.py`。
- 管理 API `backend/app/api/v1/instrument_families.py`（prefix `/instrument-families`，已在 `router.py` 注册，**无需路由重排**——`/{family_id}/members` 比 `/{family_id}` 更具体，Starlette 正确匹配）：
  - `GET ""` 列表：每项含 `instrument_ids[]`/`member_count`/`used_count`(多少项目 instrument==name)/`members[]`(仪器数据+has_archive)；
  - `POST ""` 新增（name 唯一校验）；`GET /{id}` 详情；`PUT /{id}` 改名/备注；`DELETE /{id}` 级联删成员；
  - `PUT /{id}/members` 整体替换成员 `{instrument_ids:[...]}`（前端多选器保存用，先删后插，校验仪器存在）。
- `core/instrument_link.py` 重写：`resolve_family_instruments(db, family)` 现优先查 `instrument_families` 关联表（命中即返回其成员；命中但无成员如"胶体金法"返回 []），未登记的新总型号才按型号关键词兜底匹配（保留旧行为不丢失）。新增 `build_family_map(db)` 遍历全部已登记 family 出 `{name:[成员]}`。原 `FAMILY_MAP` 常量已删除。
- `GET /api/v1/instruments/family-map` 不变（仍调 `resolve_family_instruments`），现因读 DB 自动切到关联表数据；15 个 family 全部已迁入。
- 种子迁移：`seed.py` 新增 `seed_families(db)`（在 `run_seed` 中调用，按 name 去重、幂等），用子串匹配把原 15 个总型号→仪器写入关联表（对 id 变化不敏感）：
  - keys 需精确，避免误匹配：原 `东曹HLC-723 G8` 用 key `["HLC-723"]` 误把 id=77「HLC-723G11（新）」也纳入；改为 `["HLC-723G8"]` 后仅 G8(19) 命中。**已手动从线上库删除 id=77 的误关联成员**，并修正 seed key 防复发。
  - 其余 14 个 family 成员与 FAMILY_MAP 完全一致（已逐家族核对）。
- 前端：
  - 新增 `views/instruments/InstrumentFamilyManage.vue`：表格列出全部总型号（关联仪器数/项目使用数/已关联仪器标签），支持 新增/改名/删除 与 弹窗多选仪器关联保存（`setFamilyMembers`）。多选器选项来自 `listInstruments({page_size:1000})` 叠加 `getArchivesStatus` 标"未建档"。
  - `router/index.js` 加路由 `/instrument-families`（懒加载新页）。
  - `AppLayout.vue` 左侧导航加「仪器关联」菜单项（icon `Share`）。
  - `TestItemList.vue`：工具栏加「仪器关联管理」按钮（`goManage()` → `/instrument-families`）；`goInstrument(id)` 改为 `router.resolve` + `window.open(route.href,'_blank')` **新标签打开**仪器档案，保留项目查询页；芯片加 `title="新标签打开仪器档案"`。
  - `api/instruments.js` 加 `listInstrumentFamilies/createInstrumentFamily/updateInstrumentFamily/deleteInstrumentFamily/setFamilyMembers/getInstrumentFamily`。
- 验证：杀全部 python 重启 uvicorn(8123) 单实例；真实 HTTP 登录(admin/admin123) → `/instruments/family-map` 15 个 family 正确；`/instrument-families` 列表 15 项 member_count/used_count 正确；CRUD round-trip（建临时 family→设成员[14,16]→查→删→404）通过；HLC-723G8 现仅 1 成员；`vite build` 通过（`InstrumentFamilyManage` 新 chunk）。前端 dev server(5173,PID 660) HMR 已生效。
- 提示用户：进入「仪器关联」可随时增删总型号、调整每台总型号对应的具体仪器；项目查询页点芯片会在新标签页打开对应仪器档案，项目查询页保持不动。


## 停用仪器不显示/不跳转（关联仪器排除 停用）
- 需求：用户在仪器档案把某台仪器标为「停用」后，项目查询页「关联仪器档案」芯片不应显示它、也不应可跳转；管理页也不应建立/显示该关联。
- 后端（单一真相源）三处统一排除 `Instrument.status != "停用"`：
  - `core/instrument_link.py`：`resolve_family_instruments` 的关联表分支与兜底分支、以及 `build_family_map` 的仪器过滤，均加 `Instrument.status != "停用"`。
  - `api/v1/instrument_families.py`：新增 `_active_member_ids()`（仅返回未停用成员）；`_to_read` 与 `_members_detail` 改用它 → 列表/详情的 `instrument_ids`/`member_count`/`members` 均不含停用；`set_members` 改为只接受未停用 id（停用 id 静默丢弃，无法建立关联）。
  - `schemas/__init__.py`：`InstrumentFamilyMemberOut` 增加 `status: str` 字段（供前端显示备用/维修标签）。
  - `GET /api/v1/instruments/family-map`（项目芯片数据源）因此自动不含停用仪器 → 不显示、不可跳转。
- 前端 `views/instruments/InstrumentFamilyManage.vue`：`loadInstruments` 候选列表 `filter(i=>i.status!=='停用')`（无法勾选停用仪器）；`onEdit` 预填 `instrument_ids` 时剔除停用；成员标签对 `非在用` 状态加「· <状态>」标识（停用已被后端排除，不会出现在列表）。
- 验证：真实 HTTP 确认 11 台停用仪器（id 1,2,3,4,5,6,7,17,22,35,60，其中 2/3/5/6/7 属 family 2/3）无一泄露进 `family-map`（25 台全为在用/备用/维修）；`/instrument-families` 列表 member_count==显示成员数、无停用；`set_members` 带停用 id 实测未写入；`vite build` 通过。
- 注意：停用仪器的关联记录仍保留在 `instrument_family_members` 表中（惰性，不参与展示/跳转）。若日后需清理孤儿记录，可加一次性 SQL。

## 仪器关联管理页：修复候选列表空白 + 按键移左下角
- 需求：①管理页也不显示停用仪器；②"仪器关联管理"按键移到左下角、更小。
- 修复候选列表空白 bug：`InstrumentFamilyManage.vue` 原 `listInstruments({page_size:1000, status:''})` 中 `status:''` 会被 `crud_base` 当成 `status==''` 过滤，导致返回 0 条（候选下拉全空）。改为**不传 status**（拉全量 77 台），前端再 `.filter(i=>i.status!=='停用')` → 候选列表显示 66 台非停用仪器。停用已在后端 members 排除，三重保险。
- 验证：`GET /api/v1/instruments?status=` 返回 0 条；`?page_size=1000`（无 status）返回 77 条。前端改为不传后候选正常。
- 按键 reposition：`TestItemList.vue` 删除工具栏的"仪器关联管理"大按钮，改为 `.page` 内 `position:fixed; left:16px; bottom:16px; z-index:2000` 的 `size="small" plain` 浮按钮（class `.fab-assoc`），点击 `goManage()` → `/instrument-families`。左侧导航"仪器关联"保留。
- `vite build` 通过。

## 修复：左下角小按钮"没实现"
- 现象：用户反馈「仪器关联管理」左下角小按钮未生效。排查发现代码（浮动按钮 + `.fab-assoc` 样式 + `goManage`）其实早已写入，但存在多个残留 vite 进程（含未含本次编辑的旧实例绑定 5173），用户看到的预览指向旧 bundle，表现为未实现。
- 处理：杀掉全部 node/vite 进程，重启单一干净 vite（端口 5173），确认 curl /src/views/test_items/TestItemList.vue 已含 fab-assoc/goManage，即用新代码。
- 顺带把按钮 left:16px 改为 left:236px（避开左侧 220px 深色导航栏），落在内容区左下角更显眼；size=small + type=primary plain 保持小巧。
- 经验：前端改完后若预览未生效，先确认只有一个 vite 实例在跑（Get-Process -Name node | Stop-Process -Force 后再 npm run dev），否则旧进程会持续提供旧 bundle 掩盖改动。

## 左侧导航移除「仪器关联」菜单
- 用户要求不保留左侧导航「仪器关联」菜单，只保留「项目查询」页左下角小浮按钮入口。
- 已从 AppLayout.vue 的 menus 数组删除 `{ path:'/instrument-families', title:'仪器关联', icon:'Share' }`。路由仍在 router/index.js 注册，浮按钮 `goManage` 跳转正常。

## 仪器关联小按钮移到左侧导航栏左下角
- 用户要求把「仪器关联管理」小按钮放在左侧主菜单列（侧边栏）的左下角，而不是内容区浮按钮。
- 在 AppLayout.vue 的 `.aside` 内、菜单下方新增 `.aside-footer`（flex column + margin-top:auto 钉底），放一个占满宽度的小按钮 `class="families-btn" size="small" :icon="Share" @click="goFamilies"`，深色侧栏上用半透明白底浅色文字样式；`goFamilies` → router.push('/instrument-families')。
- 同步从 TestItemList.vue 删除内容区浮动按钮（模板、`.fab-assoc` 样式、`goManage` 函数、未用的 Share import），避免重复入口。
- `vite build` 通过；dev server 确认 AppLayout 含新按钮、TestItemList 不再含 fab-assoc/goManage。

## 精确关联：仪器组 token → 具体仪器
- 需求：项目「仪器组」里写的是具体机号/总型号缩写（如 AU58-1、①号机、e602），要按这些 token 精确关联到具体仪器档案，不再用总型号模糊匹配。用户给的映射：AU58-1→AU5821 A(67)、AU58-2→AU5821 B(68)、AU5800→AU5800急诊(5，用户确认已改在用)、HT7600→日立HT7600(12)、①~④号机→DXI800 1~4(69~72)、e601/e602/e411→罗氏e601/e602/e411(14/15/16)。
- 实现：① `seed.py` 新增 `_TOKEN_FAMILY_SEED`(11 token→精确id) + `seed_token_families(db)`（幂等），`run_seed` 调用；用同一函数把 token 家族写入线上 `data/app.db`（现共 26 个 family：15 总型号+11 token）。② 前端 `TestItemList.vue` 的 `linkedInstruments(row)` 改为解析 `instrument_group`：按 '/' 切分 token→逐个查 `familyMap[token]`（精确），去重；仪器组为空/未命中才回退总型号 `familyMap[row.instrument]`；`急诊`/`唐筛` 作工作模式跳过不生成芯片。③ 管理页 used_count 改为也统计 `instrument_group LIKE '%name%'`，让 token 家族显示真实用量。
- **关键 bug（必记）**：原 `GET /api/v1/instruments/family-map` 只遍历 `db.query(TestItem.instrument).distinct()`（总型号），所以名为「AU58-1」这类只存在于仪器组的 family 根本不返回 → 芯片永远走不到 token。改为直接 `return build_family_map(db)`（遍历全部 instrument_families，含停用过滤）。修复后 family-map 返回 26 个、token 全部命中，抽样芯片解析完全符合规格（如「AU58-1 / AU58-2/AU5800」→[A,B,急诊]、「e602」→仅 e602 而非三个全出）。
- 注意：急诊/唐筛未指定对应机器（档案里有专用的 DXI800急 id73 / DXI800唐 id74），用户没要求，暂跳过；若需 `急诊→DXI800急`、`唐筛→DXI800唐` 后续再加 token 家族即可。
- 又一次踩到**旧进程**：family-map 改完首次验证仍只返回 15（旧 uvicorn 缓存旧 DB 快照视图），杀光 python 重启后才见到 26。经验：后端改完务必 `Get-Process -Name python* | Stop-Process -Force` 干净重启，否则旧进程持续提供旧数据掩盖改动（前端同理 node）。

## 急诊 / 唐筛 精确映射补齐
- 用户要求把之前暂跳过的「急诊」「唐筛」也做成精确关联。
- 档案里两台专用机：id73=贝克曼DXI800急（dept_no MHZYY-JYK-SM-2008）、id74=贝克曼DXI800唐（dept_no MHZYY-JYK-SM-2009），均「在用」。
- 实现：① `seed.py` 的 `_TOKEN_FAMILY_SEED` 新增 `("急诊",[73])`、`("唐筛",[74])`（共 13 token，family 总数 28）；直接调用 `seed_token_families(db)` 写入线上 `data/app.db` 生效。② 前端 `TestItemList.vue` 删除 `SKIP_GROUP_TOKENS = new Set(['急诊','唐筛'])` 及其跳过分支，`linkedInstruments` 现在把 急诊/唐筛 当普通 token 走 `familyMap[token]` 解析，命中即生成跳转芯片。
- 验证：`GET /api/v1/instruments/family-map` 返回 28 个，含 `急诊->[73 贝克曼DXI800急]`、`唐筛->[74 贝克曼DXI800唐]`；杀光 node 重启单一干净 vite（5173）；`grep SKIP_GROUP_TOKENS` 无残留。

## 反向映射：仪器档案页显示对应项目
- 需求：与「项目→仪器」正向芯片对称，在仪器档案里反向显示该仪器对应的检验项目。
- 后端：① `instrument_link.py` 新增 `build_instrument_test_items_map(db)` —— 遍历全部 test_items，对每个项目按 instrument_group 的 '/' token 或总型号 instrument 命中 `build_family_map` 的 family，把项目归到对应 instrument_id（与前端 `linkedInstruments` 逻辑完全对称，仅统计 familyMap 中的在用仪器）。② `instruments.py` 新增 `GET /{instrument_id}/test-items`（response_model=list[TestItemRead]），并按 code 排序；两段路径不与 `/{id}` 冲突，无需路由重排。
- 前端：① `api/instruments.js` 加 `getInstrumentTestItems(id)`。② `InstrumentList.vue` 档案抽屉（archiveDrawer）新增「对应项目」区块：openArchive 时并行 `loadLinkedTestItems(id)`，表格展示 项目编号/名称/类别/仪器组，无数据时 `el-empty`；点击行 → `router.push('/test_items?q=项目名')` 跳项目查询页。③ `TestItemList.vue` 的 onMounted 读取 `route.query.q` 自动填入 `query.keyword` 并搜索定位。
- 验证（真实HTTP）：73=DXI800急→27项、74=DXI800唐→12项、67=AU5821 A(AU58-1)→51项、68=AU5821 B(AU58-2)→52项、14=罗氏e601→9项、5=AU5800急诊→62项，与正向芯片一一对应。`vite build` 通过；杀光 python/node 干净重启后端(8123)与 vite(5173)。

## 紧急修复：项目查询页白屏（route 未定义导致 setup 崩溃）
- 现象：项目查询页打不开/空白。后端接口、vite 代理、编译均正常，最终定位为前端运行时崩溃。
- 根因：`TestItemList.vue` 此前做"仪器档案→项目查询跳转(?q=)"时，给 `onMounted` 加了 `route.query.q`，但组件只 `import { useRouter }`，**没有 `useRoute`**，`route` 在 setup 顶层未定义 → `<script setup>` 执行抛 ReferenceError → 整个项目查询页挂载失败白屏。
- 修复：① 第280行 import 改为 `import { useRoute, useRouter } from 'vue-router'`；② 在 `const router = useRouter()` 上方加 `const route = useRoute()`；③ 原 `goInstrument` 内局部 `const route = router.resolve(...)` 改名为 `resolved`，避免与顶层 route 同名遮蔽。
- 加固：顺带把 `vite.config.js` 的 `server.host` 设为 `true`（监听 0.0.0.0 双栈），此前 vite 只监听 IPv6 `::1`，若用户系统 `localhost` 解析到 IPv4 会连不上（curl 127.0.0.1:5173 原返回 000，现返回 200）。
- 验证：杀光 node 重启单一干净 vite(5173)，IPv4/IPv6 均 200，TestItemList 编译 200，经代理取 test-items 数据 200 正常。用户侧建议 Ctrl+F5 强刷清掉崩溃模块的缓存。

## 路由拼写坑：/test_items(下划线) 错写成 /test-items(连字符) 导致跳工作台
- 现象：仪器档案「对应项目」点项目名跳到工作台(dashboard)而非项目查询页。
- 根因：路由表(router/index.js:18)项目查询页 path 是 `test-items`（连字符），但 InstrumentList.vue 的 `goTestItem` 写了 `router.push({path:'/test_items'})`（下划线），路径不匹配 → 落通配 `/*` → 重定向到 `/dashboard`。
- 修复：`goTestItem` 改为 `path:'/test-items'`（连字符）。配合 TestItemList.vue onMounted 读 `route.query.q` 自动定位。
- 约定提醒：本项目前端路由 path 一律用连字符（test-items / instrument-families / qc / iso15189...），写跳转时切勿写成下划线。

## 导入 23 个"操作+保养记录"docx + 关联到仪器档案抽屉（BG-SM-CZ-051~070/078/079/080）
- 需求：把 23 个操作/保养记录 docx 导入系统——"编号相同替换、其他新增、文件对应到仪器档案里"。AskUserQuestion 确认方案：① 同编号 20 个走「新版本」保留历史并更新标题；新编号 3 个（078/079/080）新增；② "对应到仪器档案里" = 仪器档案抽屉新增「操作/保养记录」区块（按仪器匹配显示关联文档、可预览/下载，文件仍归属文件管理模块）。
- 新建关联表模型 `backend/app/models/document_instrument.py`：`document_instruments`（document_id FK CASCADE + instrument_id + UniqueConstraint uq_document_instrument），已在 `models/__init__.py` 注册。参考 instrument_family_members 的多对多模式。
- 一次性导入脚本 `backend/scripts/import_operation_records.py`（已成功运行）：PLAN 字典含 23 编号→(源docx, [仪器id列表]) 的 curated 映射（人工核对，非自动解析文件名，避新旧设备歧义）。结果：3 新增(doc id 499/500/501=078/079/080)+20 新版本(bump_version+更新 file_path/title/status+DocumentVersion+FileChangeLog)+31 条 document_instruments 关联（覆盖 30 台仪器）。关联幂等：先 delete 旧再 add。
- 关键映射（仪器 id 依真实库 data/app.db）：051→[66,67]、052→[69,70]、053→[5]、054→[73]、055→[74]、056→[8,9,10]、057→[11]、058→[12]、059→[13,21]、060→[14]、061→[15]、062→[16]、063→[75,17,22]、064→[23]、065→[18]、066→[19]、067→[20]、068→[24]、069→[25]、070→[26]、078→[66,68]、079→[71]、080→[72]。
- 后端接口 `GET /api/v1/instruments/{id}/documents`（instruments.py:98）：join document_instruments 返回该仪器关联的非作废文档（id/doc_number/title/category/version/status/original_filename），按 doc_number 排序。与已有 `/test-items` 反向索引对称。
- 前端 `api/instruments.js` 加 `getInstrumentDocuments(id)`；`InstrumentList.vue` 档案抽屉在"仪器档案文件"区块后、"对应项目"前插入「操作/保养记录」区块（el-table 显示 编号/标题/版本 + 预览/下载）。预览复用 documents.js 的 `fetchDocumentBlob/previewBlob`（docx→mammoth 弹窗，pdf→新标签），下载用 `downloadBlob`。`openArchive` 里加 `loadLinkedDocs(id)`。
- 验证：真实 HTTP 测 /documents 接口 7 台仪器返回正确（66→051+078、5→053、12→058 等）；vite build 通过无报错；杀光 python/node 干净重启 8123+5173。
- 备份：`data/app.db.bak_before_oprecords_20260712_171343`。

## 导入纯水机操作保养记录 BG-SM-CZ-001 并关联到纯水机档案
- 需求：把 `BG-SM-CZ-001-生化免疫组纯水机使用监测记录表.doc` 放到纯水机仪器档案里（延续上批 CZ 文件"对应到仪器档案里"=抽屉「操作/保养记录」区块的约定）。
- 纯水机仪器：id=27（纯水机A/B/C，RJ-RO-300，MHZYY-JYK-SM-1029，在用）。
- 源文件是 OLE2 .doc（magic d0cf11e0），用 `convert_doc_bytes_to_docx` 转成真 docx 以便 mammoth 预览。
- 一次性脚本 `backend/scripts/import_purewater_record.py`：读取→转 docx→`storage.save("docs",...)`→查 doc_number 是否已存在（已有 document id=212 v1.0，故走「新版本」bump 到 v1.1 并更新 file_path/title/status+DocumentVersion+FileChangeLog）→重建 document_instruments 关联指向仪器 27（幂等：先清再加）。
- 落盘：UPLOAD_ROOT=`data/uploads`，真实路径 `data/uploads/docs/BG-SM-CZ-001-生化免疫组纯水机使用监测记录表_1.docx`（storage 因重名加 `_1`），PK 头合法 docx，20006 字节。
- 验证：HTTP `/instruments/27/documents` 返回该文档（title=生化免疫组纯水机使用监测记录表，v1.1）；`/documents/212/download`、`/preview` 均 200 返回 PK docx。无需重启前后端（直接写库，接口实时读库；前端区块早已就位）。

## QC 月结功能落地（室内质控月小结 CZ-012）
- 设计确认：粒度=月；单组14列；结构化CSV/XLSX自动解析 + 质控图PDF存档；每日测值跑Westgard自动判失控。
- 后端 models/qc.py 新增 QCMonthlySummary(含handling_note/pdf_path)+QCDailyValue；schemas 加 QCMonthlySummary*+QCDailyValueRead；services/qc_service.py 加 evaluate_westgard/aggregate/lookup_quality_goal；api/v1/qc_summaries.py 上传解析/明细/PDF/导出；router.py 注册。
- 两坑修复：① 路由重排 sort 必须放在所有自定义路由装饰器之后（之前放 make_router 后、自定义路由注册前，sort 时只有5条路由→无效）；② 导出中文文件名用 filename*=UTF-8''{quote(fname)} 否则 latin-1 500。
- 质量目标 lookup 修复：优先选非"/"命中、精确名优先（之前 人绒毛膜促性腺激素 误命中尿液条目的"/"）。
- 验证：合成CSV(注入1-3s/2-2s)跑通上传→聚合→Westgard(精准识别失控点)→质量目标(AFP7.5%/hCG8.3%/uE3空)→导出14列CZ-012 Excel；生产构建通过。
- 前端 views/qc/QCList.vue 改 el-tabs：月结(上传CSV/PDF+14列表格+展开看每日测值&失控点&处理说明+导出Excel)/原始记录(原CRUD)/其余5模块占位。api/qc.js 加月结接口。dev(5174)生产构建均通过。
- 待做：室间质评/仪器间比对/留样再测/室间比对/批号累计靶值 后端+前端（当前仅占位Tab）。
- 月结受控仪器绑定+文字部分(已HTTP验证)：qc_monthly_summaries 加 instrument_id(FK instruments,可空)/instrument_no(dept_no)；新增 qc_monthly_reports(每台仪器每年月一份, operation_status/drift_trend/cv_setting_ok/cv_calc_ok/freq_ok 五段)；POST /upload 接受 instrument_id(绑定instruments表,忽略CSV仪器列)；GET/PUT /report；GET /export 加 instrument_id 参数且每块后写5段文字+编号。qc_service.draft_report() 自动草拟5段(计算/设置CV%达标对照质量目标、漂移趋势按Westgard规则归类、频次按DEFAULT_MONTHLY_QC_MIN=20、运行情况按失控数)。前端下拉绑instruments表(名+编号)、上传先选仪器、块标题显编号、每块文字段可编辑保存。CompactMax(Stago)已在instruments id=26。main.py 加 _migrate_schema() 为旧表补列。演示数据已清理。
- 月结下拉限定QC受控仪器(已HTTP验证)：instruments 加 qc_instrument 布尔列(默认False)，schema 加字段；_migrate_schema 补列+启动打标21台白名单(AU 5/67/68, DXI800 1/3/4/急/唐 69/71/72/73/74, 罗氏14/15/16, TOP9/10/11, 血气13/21, 日立12, 东曹G8 19, 爱康23, Stago26, 安图A6200 75；不含DXI800 2号70)；新增 GET /qc-summaries/instruments 专用端点(返回qc_instrument=True按dept_no自然序)，前端 getQCInstruments() 替换 listInstruments，全量instruments接口不受影响。生产构建通过。

## 月结下拉显示「型号」(承接上条，已HTTP验证)
- 需求：月结受控仪器下拉里"不仅有名称，还要有型号"，方便日常把下拉项与真实机器对应上。
- 后端无需改：`GET /qc-summaries/instruments` 本就返回 `model`（规格型号）字段（21台均有值，如 贝克曼AU5800急诊/罗氏e601/CompactMax/贝克曼DXI800 急…）。
- 前端改动 `views/qc/QCList.vue`：① 新增 `instLabel(opt)` 辅助函数，标签格式 `名称（型号｜编号）`（model 或 dept_no 为空时省略对应段）；② 两个下拉（筛选+上传受控仪器）`el-option` 的 `:label` 由 `名称（编号）` 改为 `instLabel(opt)`；③ 分块标题 `g.instrument` 旁额外显示 `g.model`（型号），`groups` 计算属性按 `instrument_id` 从 `instrumentList` 取 model 填入，便于分块也对应型号。
- 验证：真实 HTTP 登录(admin/admin123)→ `/qc-summaries/instruments` 返回21台全含 model；下拉标签示例"全自动生化分析仪（贝克曼AU5800急诊｜MHZYY-JYK-SM-1005）""全自动化学发光仪（贝克曼DXI800 急｜MHZYY-JYK-SM-2008）"；`vite build` 通过；dev server(5173) 热更新已生效（如预览旧，Ctrl+F5）。
- 待用户确认的数据瑕疵：instruments 表 id=26 的 name 录为"凝血分析仪仪"（多一个"仪"字），下拉会显示"凝血分析仪仪（CompactMax｜…）"。本次未改台账数据，待用户决定是否修。

## 室间质评(EQA)功能：年度计划 + 检测提醒 + 半年总结（已HTTP端到端验证）
- 需求（用户原话）："开始后面内容。原始质控记录这个不要。做室间质评内容。我需要年度计划、检测提醒和半年总结功能。"——即删 QC 页「原始质控记录」tab，新增「室间质评」tab 含三大块。
- 删除「原始质控记录」tab：`views/qc/QCList.vue` 移除该 el-tab 及 CrudTable/EditDialog 引用、listQC/createQC/updateQC/deleteQC 导入与 fields/columns/rules/emptyForm/onAdd/onEdit/onSubmit/onDelete 全部相关代码（qc_records 表保留作底层，仅不再作为独立 tab 暴露）。
- 后端模型 `models/eqa.py` 新建：`EqaPlan`（year/org/program/item/round_no/sample_date/due_date/returned(Bool)/result/qualified(Bool)/score(String)/note）+ `EqaSummary`（year/half(1上半年2下半年)/summary_text/Text）；`models/__init__.py` 注册；`schemas/__init__.py` 加 EqaPlan*/EqaSummary*（注意：qualified 是 bool 非可空、score 是 str 非 int —— 前端表单与验证脚本务必按此类型传）。
- `core/crud_base.py` 的 make_router 加 `after_write: Callable|None=None` 回调：在 create/update/delete 的 commit 之后调 `after_write(db,"create"/"update"/"delete",obj)`，用于写后刷新提醒。
- 提醒服务 `services/notification_service.py`：加 `compute_eqa_alerts(db)`（遍历未回报计划，按 due_date 算 days_left，过滤掉 days_left>WARN_DAYS(30) 的远期计划只保留临期/逾期；level=danger(逾期)/warning(临期)）+ `refresh_eqa_notifications(db)`（先删旧 eqa_return 通知再重建，module="室间质评" ref_type="eqa_return"）。main.py lifespan 启动刷新 + after_write 在计划增删改时刷新。
- `api/v1/eqa.py`（新建）：make_router 建 /eqa-plans（search_fields org/program/item/round_no；filter_fields year/org/program/returned/qualified；after_write=_after_eqa_write 包装调 refresh_eqa_notifications）；自定义路由必须排在 /{id} 前（文件末 `router.routes.sort(key=lambda r: ("{" in getattr(r,"path",""),))`）：`GET /alerts`（检测提醒）、`GET /summary`（year+half(0全年/1上半年/2下半年)，用 func.substr(due_date,6,2) 分月，返回 total/returned/not_returned/return_rate/scored/qualified/unqualified/qualify_rate + plans 明细）、`GET|PUT /summary-text`（EqaSummary upsert）、`GET /export`（openpyxl 导出年度计划 11 列 Excel，中文名用 filename*=UTF-8''{quote(fname)}）。router.py 注册在 qc_summaries 之后。
- 前端 `api/eqa.js` 新建 9 个接口；`views/qc/QCList.vue` 重写「室间质评」tab：年度选择(el-date-picker year)+新增计划/导出按钮；检测提醒区(el-alert danger/warning)；年度计划表(回报/编辑/删除 + 已回报/合格标签)；半年总结区(选择上半年/下半年/全年 + 统计 + 文字段 + 保存)；新增/编辑对话框(planDialog 含全部字段)；切 tab 时加载。
- 全链路 HTTP 端到端验证（`outputs/test_eqa_e2e.py`，登录 admin/admin123）：建 4 条计划(逾期H1/临期H2/未来H2/已回报H2)→`/alerts` 仅返 2 条(逾期+临期，未来与已回报均排除，level 正确)→`/summary` 全年 total=4/returned=1/return_rate=25%/qualified=2，上半年 total=1，下半年 total=3/returned=1 →`/summary-text` PUT+GET 文字命中→`/export` 返回 PK 头 xlsx + 中文名 RFC6266→`/notifications` 含 eqa_return=2 条→清理后 2026 计划归零、alerts 为空。**全部通过 ✅**
- 运维坑（重要，已修复）：managed Python 3.13.12 缺 openpyxl（fastapi/uvicorn/sqlalchemy 有，openpyxl 没有），导致 test_items.py 导入即 ModuleNotFoundError、后端 8123 起不来。修复：`/c/Users/81526/.workbuddy/binaries/python/versions/3.13.12/python.exe -m pip install openpyxl`（装 3.1.5）。此后端必须在该 managed python 跑且需 openpyxl。→ 已补进 MEMORY.md 运维坑。
