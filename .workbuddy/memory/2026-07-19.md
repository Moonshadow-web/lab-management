# 2026-07-19 工作日志

## 质量要求综合比对矩阵视图 (a2f0bc2)
- 新增后端 `GET /api/v1/quality-requirements/_meta/matrix`：以 test_items 为行，三源(行标/北京/卫健委)为列，支持搜索+分页+模糊匹配
- 前端 `QualityRequirementList.vue` 新增"综合比对"标签页（最左第一 tab），默认进入
- 每行显示：项目名称/代码 + 行标(CV/Bias/TEa) + 北京互认(CV/EQA) + 卫健委(TEa)
- 有值 teal 色、空值灰色；原有三个源标签页完整保留
- 已 push + deploy（等待构建完成）

## 修复：仪器间比对报告生成 500 (NameError 're') (69eb5e3)
- 现象：仪器间比对「生成报告」「预览」均 500；定性比对(plan 2)正常，定量比对(plan 6/7/9/10)必崩。
- 根因：`backend/app/services/comparison_report.py` 内 `_parse_float` 被定义了两次（L84 简单 float 解析 / L892 Excel 数值提取 `re.search`）。模块按行加载，后者覆盖前者；本模块 `import re as _re`，L892 误用未定义的 `re` → `_compute_bias`(定量报告必走) 调到的版本抛 NameError。定性不走 `_compute_bias` 故不崩。
- 修复：L892 函数重命名为 `_extract_float` 并改 `re.search`→`_re.search`；`_resolve_te/_resolve_mode` 与 `_compute_bias` 改用 `_extract_float`（单位串如"5.2 mmol/L"可解析，避免偏倚显示"-"）；删除被覆盖的死 `_parse_float`。
- 本地 `scripts/repro_report.py` 可复现/回归（遍历全部 plan 跑 compute_data+build_html+build_docx）。
- 已 push + deploy（build mark=comparison-report-fix-2026-07-18-re-NameError）；线上 admin 登录后逐 plan 验证 preview/generate 均 200，ALL_OK。
- 教训：**禁止在模块内定义同名函数覆盖**；本模块字符串解析一律用 `_extract_float`，`re` 只以 `_re` 形式存在。

## 综合比对：沉底排序 + 二氧化碳同义词 (aec233a / e86cdb8)
- matrix 接口排序改为：被标准源提及数降序(均未提及=0沉底)，再 category/id；前端对 mentioned_count=0 显示「未收录」tag。
- 后端加 `_SYNONYMS`：项目库"二氧化碳"→标准源"碳酸氢根/HCO3/二氧化碳结合力"等，打通综合比对匹配。
- 已 push + deploy。

## 卫健委EQA匹配缺口核对 (口头+报告文件)
- 脚本比对 test_items(124) × NCCL_ITEMS(176)，归一化匹配后 14 项在卫健委EQA查不到。
- 限制：本地 app.db 缺 has_eqa 列（迁移前旧库），未精确按标记过滤；基于种子数据。
- 3 项疑似种子漏录（全国EQA一般有）：TPOAb/TgAb/HIV；11 项确属特殊项目建议核实 has_eqa 是否应为0。
- 连带：C肽(C-肽)仅差连字符，后端 `_find_best` 未做归一化，线上综合比对仍匹配不上（待办：加归一化）。
- 报告文件：`卫健委EQA匹配缺口核对.md`（项目根）。

## 卫健委标准缺口核对 - 线上真实数据版 (29032c7)
- **新增端点** `GET /api/v1/eqa-associations/_meta/wjw-gap`：
  基于系统真实关联 `_compute_associations(has_wjw)` + nccl-2026 标准源匹配
  返回「有卫健委EQA计划但卫健委标准源缺标准」的项目清单
- **线上真实结果**：186个有卫健委EQA → **89个(48%)缺nccl-2026标准**
  - 免疫46项：乙肝五项/HIV/丙肝/梅毒/TPOAb/TgAb/T3/T4/FT3/FT4/TSH/TRAb/AFP/CEA/CMV/HSV/风疹/弓形虫/戊肝/IL-6/VD/β-HCG等
  - 凝血11项全缺：PT/APTT/TT/FIB/DD/FDP/vWF/PC/PS/AT3/Anti-Xa
  - 生化32项：CysC/CRP/SAA/胆汁酸/β-HB/ApoA1/触珠蛋白/HA/轻链×4/尿2项/电泳8子项/血气6项/胶原/层粘连蛋白/腺苷脱氨酶×2
- 用户需手动补 `NCCL_ITEMS` seed 数据，补完调 `/_meta/seed` 灌库
- **教训**：不要自己写字符串匹配做分析，要用系统已有 `_compute_associations` 真实关联 + 线上API查真实数据
- 回退了质评关联整合前端改动（9b0ca69），只保留后端独立核对端点

## Docker/CFS
- .dockerignore 补排 `backend/venv`（175MB），修复 CLI `Array buffer allocation failed` OOM（99b1754）

## 卫健委EQA靶值补录 - 用户核对《2026全国EQA计划》(4806a53)
- **澄清关键误解**：系统 `has_wjw` 关联（eqa_plans 匹配）是对的——用户给的靶值项目**全部 has_wjw=True**（认了有卫健委EQA），不是"名字差异不认"。
  "89项缺口"的真实定义 = **有卫健委EQA计划，但 nccl-2026 可接受范围靶值库没录**。是真缺数据，非匹配漏判。
- 用户发来靶值（TPOAb±30%/FT3±20%/AFP±25%/PT±15%/D-Dimer±50% 等），即补录数据。
- 补录 30 项进 `NCCL_ITEMS`（用系统 test_items 真实名作 item_name 保证匹配）：甲状腺8+肿标(甲胎/CEA/β-HCG/总PSA/游离PSA)+IL-6+维D+骨钙素+降钙素+骨代谢(TP1NP/β-CTx)+非结合雌三醇+肾素+凝血7(PT/APTT/FIB/TT/DD/FDP/抗凝血酶III/蛋白C/蛋白S)。
  - **未补**：β2微球蛋白/铁蛋白/甲状旁腺激素(已在库)、血管紧张素Ⅱ(用户标"无")。
- 部署(4806a53)→seed 新增30条(gap 89→61)，30项全部消除✅。
- 剩余61项缺口：主要是病毒抗体(HCV/TP/HIV/乙肝五项)、CMV/风疹/弓形虫/HSV、糖代谢、血气、电泳、其他生化特殊蛋白等——用户未提供靶值，需继续核对补充。

## 月小结 QC 5 项改造 (d3e93eb + a5f9c8a)
### d3e93eb — 5 项基础改造
1. 修复爱康日期解析（`_parse_date` 扩展支持 `2026-6-1 10:10` 非零填充格式 + 正则兜底）
2. 导入时自动草拟文字报告（`_ensure_report_draft` 在 upload 末尾被调用）
3. 移除月小结 Excel 导出（前端按钮+后端 GET /export）
4. 移除质控图 PDF 上传（前端按钮+上传/预览/删除）
5. LJ 质控图新增多水平下拉 + 每日多水平状态汇总

### a5f9c8a — 后续修复
**问题 1：质控图 PDF 后端路由未删除**
- d3e93eb 只删了前端按钮，后端三路由 `POST/GET/DELETE /{summary_id}/pdf` 仍存在（返回 405/422）
- 彻底删除这三路由（POST/GET/DELETE）

**问题 2：月小结文字报告永远空（"预览还是打不开"真正根因）**
- `_ensure_report_draft` 旧逻辑：`if _find_report(...): return` —— 报告存在就跳过
- `GET /report` 旧逻辑：`if rep: return rep` —— 同上
- 后果：历史遗留的空文本报告永远不会被补字；docx 下载 200 但内容空白
- 修复：增加「五段文字全为空」判定，存在空报告时主动草拟/回填（保护用户已编辑字段）
- 验证：online `/report?instrument_id=23&year=2026&month=6` 现在五段全有内容：
  - operation_status: 本仪器本月运行正常，各项质控在控，未出现失控。仪器运行良好…
  - drift_trend: 各项目未见明显漂移或趋势性改变，质控稳定。
  - cv_setting_ok/cv_calc_ok: 艾滋病(水平1/2)/肺七质控(水平1/2)：无质量目标，无法判定（缺 quality_goal）
  - freq_ok: 是
- docx 下载：code=200 size=39015 bytes 有效 docx
- daily values 日期已正常：2026-06-02/04/09/16/18/23/25（不再全是 6-1）

**问题 3：CDN 418 阻塞（仍未解决）**
- 公开域名 `lab-management.tcloudbaseapp.com` 持续返回 HTTP 418 "X-Cache-Lookup: Return Directly"
- 容器内/TCB 内部 host `lab-management-282724-9-1408547492.sh.run.tcloudbase.com` 完全正常
- 容器状态 "normal"、Update Time 17:05 持续更新，但 CDN 缓存/规则返回 418
- 影响：用户浏览器访问公网域名会卡 418，但 API 调用内部 host 正常
- 需 TCB 平台侧排查 CDN 规则/缓存

**教训**
- 改前端时务必同步删后端路由（仅删前端按钮=安全漏洞）
- "不覆盖已有编辑"语义要细化：应区分「报告存在但文本全空」与「报告有用户编辑」
- 部署后必须用 online 真实数据验证，不能只看本地/单元测试


## 卫健委EQA标准补录 - 用户提供PDF 88-116页实数据 (c182c3e)
- **PDF 真实数据提取**：用户提供的 `2026年全国临床检验室间质量评价计划0805_94-116.pdf`（桌面）即卫健委EQA计划 PDF 88-116 页"8.室间质量评价标准"章节。
  用系统 Python 3.14（已装 pypdf 6.10.1 + PyMuPDF 1.27）提取 23 页 46KB 文本（脚本 `_extract_pdf.py` 已清理）。
- **关键发现**：PDF 88-116 可接受范围章节覆盖了**几乎所有定量项目**（常规化学/内分泌/特殊蛋白/血气/肝纤维化/骨代谢/凝血全套/产筛等），但**感染免疫（乙肝/HIV/HCV/梅毒/CMV/风疹/弓形虫/HSV/戊肝/TRUST/快速）无数值靶值**——卫健委用**定性符合率**判定（阴/阳一致即合格），不设 TEa 数字。
  另外 vWF/抗Xa 在 PDF 凝血因子章节（NCCL-H-12 只有 FⅡ/Ⅴ/Ⅶ/Ⅷ/Ⅸ/Ⅹ/Ⅺ/Ⅻ）也没给。
- **补录 31 项定量**：NCCL_ITEMS 206→237。覆盖特殊蛋白(κ/λ轻链/触珠蛋白)、常规化学B(β-羟丁酸/腺苷脱氨酶×2/总胆汁酸)、肝纤维化(HA/LN/PⅢNP/ColⅣ)、蛋白电泳 6 子项(M蛋白/α1/α2/β1/β2/γ)、血气 6 项(Ca/Cl/K/Na/PCO2/PO2)、胱抑素C、SAA、尿 IgG/α1微球/κλ轻链(沿用血清值)、高敏CRP、载脂A1。
  - **尿κ/λ轻链**沿用血清值±25%，remark 标注"待用户确认尿轻链独立靶值"。
- **部署+seed+验证**：c182c3e → total_seed=491, added=31 → wjw-gap **61→29**，本轮 31 项全消✅。
  剩余 29 = 生化 0 / 免疫 27（感染免疫定性+血管紧张素Ⅱ"无"）/ 凝血 2（vWF+抗Xa）。
- **wjw-gap 改进建议**（待办）：当前对感染免疫等定性项目报"缺标准"是误报，应在 wjw-gap 端点区分定量/定性（需 test_items 加 `eqa_qualitative` 字段或基于关键词/分类判断），定性项目显示为"定性符合"而非缺口。
- **教训**：用户明确授权后，用 pdfplumber/pypdf 读 PDF 真实数据匹配，**不要自己写字符串匹配做关联判断**。补录用系统 test_items 真实名作 item_name 才能命中 wjw-gap。

## 月结/LJ 质控图二次微调 (ffd0c56)
用户反馈：5 项需求中第 5 项"多水平下拉"放错位置（在 LJ 图而不是月结界面），且预览月小结无响应。

修复：
1. **预览月小结无响应** (`openDocxPreview`)：之前直接 `docxGroup.value=g; docxPreview.value=true`，但若 `reportMap[g.key]` 还没加载，弹窗内容是空白，用户感觉"没反应"。
   改为：先弹窗 → 异步 `loadReportFor(g)` 调 `getQCReport` 拉取/草拟 → 填入 reportMap。同时加 `g` 防御性检查。
2. **移除 LJ 图的"全部水平"多选下拉**：原 d3e93eb 把下拉放到 LJ 图工具栏，用户实际想要的是放在月结界面。删除该 el-select 块，提示文案改为"每月多水平状态见左侧表格"。
3. **月结界面加"多水平质控速览"框**（每个仪器分块下，若 ≥2 个水平才显示）：
   - 顶部水平下拉（全部水平 / 水平1 / 水平2 ...）
   - 表格：行=日期，列=水平，单元格=在控/失控 tag + 测值
   - 例如 6-1 行：水平1 在控 / 水平2 在控 一目了然
   - 黄色暖底样式（区别于上方蓝底文字报告框）

验证：build 成功（QCList chunk 1289 KB），deploy 18:41 完成，service "normal"。


## Word 月结落款左对齐去签字 + LJ 图警告/失控限/截止点 + 质量目标获取从表 (baa9f6b)
### 20:22 — Word 月结 (dfadb25)
- 删除"文字部分"标题
- 表格表头加 `align="center"` 显式居中

### 20:06 — 落款 + LJ 图 (8602f35)
- 落款从右对齐→左对齐，去"签字"二字
- LJ 图 ±2SD 黄色警告限、±3SD 红色失控限、最后日期绿色截止点
- 删"预览月小结"按钮 + "多水平质控速览"黄色框 + "每日多水平状态汇总"
- 修复模板残留孤儿标签

### 20:43 — 质量目标改造 (baa9f6b)
- `lookup_quality_goal` 新增 `db` 参数
- 新优先级：quality_requirements 表 wst403-2024.cv > bj-hr-2025.cv > nccl-2026.tea/3 > JSON 文件 > WST403_2024 TE/3 > 默认 10%
- 新增 `_extract_first_pct(s)` / `_lookup_qr_goal(db, test_item, aliases)`
用户反馈：
- 删"预览月小结"按钮（一直打不开）
- 删"多水平质控速览"黄色框（不需要）
- Word 落款"签字"二字去掉 + 改左对齐
- LJ 图：警告限/失控限要明显，6-30 截止点要标

### Word 模板 (qc_report.py)
- 落款段落 `RIGHT` → `LEFT` 对齐
- "仪器日常管理人：签字" → "仪器日常管理人："
- "审批人签字：" → "审批人："
- "质控总负责人审批意见：" 保留（用户没提这个）

### 月结界面 (QCList.vue) — 清理
- 删"预览月小结"按钮
- 删"多水平质控速览"黄色框
- 删 openDocxPreview/loadReportFor/docxPreview 弹窗/相关 refs
- 删 multiLevelMap/groupLevels/displayLevels/multiLevelByDate 及 .multilevel-box CSS
- 保留"下载 Word"按钮（已够用）

### LJ 质控图 — 警告/失控限 + 截止点
- ±2SD 标"警告限"（黄色虚线 #faad14，宽 1.4）
- ±3SD 标"失控限"（红色虚线 #f5222d，宽 1.6）
- ±1SD 改浅灰虚线（弱化 #aaa）
- 均值实线不变
- xAxis 加 `min:0, max:allDates.length-1`，防止参考线/曲线延伸到右侧空白区
- 最后一个日期加垂直绿色实线"截止点"（#52c41a）

deploy 20:06 完成，service "normal"。QCList chunk 1284KB（较上版 -5KB）。

## 418 CDN 问题（仍未根治）
- 公网 lab-management.tcloudbaseapp.com 持续返回 418 "X-Cache-Lookup: Return Directly"
- 容器在跑（内部 host 200），但 CDN 节点仍吐旧缓存
- 用户浏览器偶发能通（说明部分 CDN 节点已恢复）
- 内部 host 永久可用：lab-management-282724-9-1408547492.sh.run.tcloudbase.com

## LJ 质控图第四次清理 (cfacaaf)
用户反馈：截图里"每日多水平状态汇总"（下方文字式 6-1 水平1水平: 在控 水平2水平: 在控 那种）也要删掉。

修改：
- 删除 `.daily-status-summary` 整个 div（约 26 行模板）
- 保留上方 el-table "各水平测值"（带测值+在控/失控 tag，信息更全）
- 修复模板残留孤儿 `</template></span></div>` 和重复 `<div ref="chartRef">`
- 清理对应 `.daily-status-summary` 系列 CSS（6 条规则）
- 工具栏 hint 文案改为"左侧表格显示各日各水平测值与在控状态"

deploy 20:12 完成。


## 试剂管理模块开发 (已推送到远程HEAD，待CFS恢复后部署)
- **需求**：5大功能（试剂目录/盘库/订购/接收/月消耗）+ 从用户桌面两个Excel导入初始化数据
- **后端**：新增 9 张表（ReagentItem/ReagentStock/InventoryCheck/InventoryCheckItem/
  ReagentOrder/ReagentOrderItem/Receiving/ReceivingItem/ReagentConsumption）
  - 完整 CRUD + Excel 导入（支持 xls/xlsx）
  - 盘库/收货自动更新实时库存（reagent_stock）
  - 月消耗自动计算（=上期结余+本期入库-期末结余）
  - 路由注册在 /api/v1/reagent/ 下
- **前端**：6 个 Vue 页面（目录/库存/盘库/订购/接收/消耗），菜单侧边栏"试剂管理"
- **用户桌面Excel已确认**：生化试剂订购代码.xls（141条，贝克曼）+ 器械科订试剂编码.xlsx（79条，罗氏）
- **部署状态**：代码已 push（HEAD=11736ee），tcb deploy 已提交但 CFS 挂载问题导致容器未启动，腾讯云人员正在修
- **待办**：CFS恢复后重新部署 -> 登录验证菜单和API -> 从桌面Excel导入试剂数据 -> 测试完整业务流程
