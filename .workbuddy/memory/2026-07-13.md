# 2026-07-13

## 导入 2026 年北京市室间质评（生化+免疫）年度计划
- 用户确认已修正 instruments 表 id=26 的 "凝血分析仪仪" 名称问题。
- 用户提供了 4 份 docx 计划表：014 国家卫健委生化、015 北京市生化、016 国家卫健委免疫、017 北京市免疫，以及 3 张 2026 年北京市室间质量评价计划安排图片（页码 1/4、2/4、4/4）。
- 解析 docx 提取「项目组」和「细项」：015 北京市生化 7 组、017 北京市免疫 9 组。
- 将 docx 项目组与北京计划图片行名匹配：
  - 生化：常规化学→临床常规化学、DD FDP→D-二聚体和纤维蛋白(原)降解产物检测、PCT→血清降钙素原（PCT）、糖化白蛋白 GA→糖化血清白蛋白、凝血→凝血试验。
  - 免疫：感染快检→感染性疾病抗原抗体快速检测（POCT）、内分泌→内分泌、感B→感染性疾病血清学标志物系列B、肝炎→肝炎标志物、感C→感染性疾病血清学标志物系列C、产筛→中孕期母血清产前筛查、肿标A→肿瘤标志物、结核→结核感染T细胞γ-干扰素检测、sTfR→血清可溶性转铁蛋白受体。
- 数据文件：`outputs/beijing_eqa_2026_data.json`；导入脚本：`outputs/import_beijing_eqa_2026.py`。
- 导入前先删除旧 2026 年北京市 生化/免疫计划，避免重复；最终成功导入 26 条（生化 9 条 + 免疫 17 条）。全年统计：total=26、未回报=26。
- 当前检测提醒：21 条（7 月 17 日截止的临期项已触发预警）。
- 缺失：图片缺少第 3/4 页，docx 中 生化 IL-6 与 京津冀鲁盲样 无法匹配到时间安排，未导入；已提示用户补充该页。
- 后端 8123 / 前端 5173 均正常运行，无需重启。

## 室间质评工作流改造：上报 → 结果回报 + 报告导入口子
- 用户要求语义调整：原「回报截止/已回报」改为「上报截止日期/已上报」；在卫健委网页上报后此处标记「已上报」，等官方结果回报再填「成绩/合格」。编辑逻辑拆为「一、上报信息」与「二、结果回报」两组。
- 后端 `models/eqa.py` 新增 `report_file`(VARCHAR500) 列存质评报告 PDF 路径；`schemas` EqaPlanBase/Read 加 `report_file`；`main.py _migrate_schema` 补 `eqa_plans.report_file` ALTER。
- 新增质评报告导入口子：`api/v1/eqa.py` `POST/GET/DELETE /report/{plan_id}`（存 `backend/data/eqa_reports/`，仅收 PDF，留 `parse_eqa_report()` 解析占位「口子」，待接入扫描版日历 PDF 的 OCR+表格识别回填成绩）。
- 通知文案：`notification_service.py` 标题改为「室间质评上报提醒」、消息「上报截止/上报已逾期」；导出 Excel 表头「回报截止日期→上报截止日期」「是否已回报→是否上报」并新增「报告」列。
- 前端 `QCList.vue`：表列/提醒/总结文案同步；年度计划表加「报告」列（已导入可下载）；行操作加「导入报告」按钮（隐藏 file input 走 multipart）；编辑对话框分组；`api/eqa.js` 加 `uploadEqaReport/downloadEqaReport/deleteEqaReport`。
- 验证脚本 `outputs/verify_eqa_v2.py`：14 项全过（创建→上报标记→退出提醒→上报提醒文案→报告导入/下载/拒绝非PDF/删除→导出表头含上报截止日期/是否上报/报告→清理）。
- **部署坑（重要）**：重启时发现 8123 被上一轮对话遗留的旧后端实例（PID 16536，00:08 启动、旧代码）占用，`taskkill //F //IM python.exe` 未能杀掉它，导致新后端绑定失败、旧通知文案残留。最终用 PowerShell `Stop-Process -Id` 按 PID 逐个杀掉全部 python，确认 8123 空闲后，以**不带 --reload** 的单实例启动才生效。教训：重启后端必须按 PID 精确清理并确认端口空闲，别信 `taskkill //IM`。

## 导入 2026 年全国卫健委 + 北京 室间质评整合计划（97 条）
- 数据源：全国 PDF《2026年全国临床检验室间质量评价计划0805.pdf》第111页起分月排期（fitz 坐标法提取 162 个 NCCL 项目排期，0 处日期倒置）+ 4 份本室 docx（014/015/016/017）的「项目组/细项/轮次」。
- 映射：(src,group)→NCCL 编码（build_combined.py 的 MAP 字典），88 条匹配到全国排期日期；9 条无全国对应排期（014 多组正确度、015 京津冀鲁盲样×2、016 类固醇正确度/肾素醛固酮/优生优育×2、017 结核/sTfR）→ due_date 留空、note 标「全国计划无对应排期」。
- 每轮一条记录；NCCL 编码/名称并入 note（模型无独立列，避免改 schema/前端）。
- 导入脚本 `C:/Users/81526/.workbuddy/tmp_lnk/import_eqa.py`：直连活动库 `data/app.db` 删除旧 2026 eqa_plans（28 条）+ stale eqa_return 通知（21 条）→ admin 登录取 token → 逐条 POST `/api/v1/eqa-plans`（after_write 自动 refresh_eqa_notifications）。
- 结果：成功 97 / 失败 0。机构分布 卫健委 64 + 北京市 33；统计 total=97、未上报=97；eqa_return 通知 55 条（当前临期/逾期窗口内，WARN_DAYS=30）。
- 关键中间文件：`tmp_lnk/combined.json`(97条源)、`tmp_lnk/schedule.json`(全国排期)、`tmp_lnk/lab_items.json`(docx解析)、`tmp_lnk/extract_schedule.py`+`parse_docx.py`+`build_combined.py`；可读预览 `outputs/eqa_2026_combined_preview.csv`。
- **性能坑**：make_router 的 after_write 在每条写后全量重建通知 → O(n²)，97 条导入耗时约 3.5 分钟（SQLite 逐条 commit）。下次大批量导入应改为「直连 DB 批量 INSERT 一次 + 末尾调一次 refresh_eqa_notifications」，避免逐条触发。
- 待用户后续：9 条无排期项补填上报日期（否则不触发提醒）。

## 修复：室间质评 tab 不显示「检测提醒」
- 现象：工作台通知中心显示 55 条 eqa_return 提醒，但 QC 页的「室间质评」tab 顶部「检测提醒」区块不显示。
- 数据核验：`GET /api/v1/eqa-plans/alerts` 后端正常返回 55 条（含 overdue/warning，字段 plan_id/org/program/round_no/due_date/days_left/level）；dev server(5173, pid 30108) 提供的源码含 alert 代码、无编译错误。
- 根因（脆弱点）：`loadAlerts()` 原先只挂在 `watch(activeTab)` 与几个写操作里，且 watch 中排在 `loadEqa()` 之后；若 watch 因 HMR/异常未触发或 loadEqa await 中断，提醒数组恒为空 → 区块 `v-if="eqaAlerts.length"` 不渲染。
- 修复：`frontend/src/views/qc/QCList.vue` 的 `loadEqa()` 末尾直接 `await loadAlerts()`，使切 tab / 换年份都必然刷新提醒（与表格同步）。watch 冗余调用保留无害。HMR 已生效（HTTP 200）。
- 提示：前端走 dev server(5173)，改完前端源码无需重启后端；浏览器若为此前旧标签页需硬刷新(Ctrl+F5)。

## 室间质评 tab 增加「机构」分类查看
- 需求：按 北京市 / 卫健委 分类查看（查看时筛选）。
- 实现（前端 QCList.vue）：
  - 工具栏新增 `eqaOrg` 下拉（全部机构/卫健委/北京市），切换即重查。
  - `loadEqa()` 传 `org` 给后端（后端 `eqa-plans` 的 `filter_fields` 已含 org，无需改后端即可筛表格）。
  - 提醒区块改用 `eqaAlertsView`（computed，按 `eqaOrg` 客户端过滤），随机构同步。
  - 操作列按钮文字统一为「已上报」（上一条改动）。
- 导出增强：后端 `export_eqa` 新增 `org` 查询参数；前端 `exportEqaPlans(year, org)` + `onExportEqa` 传 `eqaOrg`，文件名加机构后缀。因改了后端，已按 PID(32836) 重启后端（managed 3.13.12，无 --reload）。
- 验证：全部=97 / 卫健委=64 / 北京市=33；导出 org=北京市 返回 200(7854B)。dev server(5173) 两个文件 HMR 均 200。

## 更正：室间质评 tab 移除提醒区块（提醒只在工作台）
- 之前误解需求：把提醒接进了 EQA tab。用户澄清「室间质评下面不显示提醒，只在工作台显示」。
- 改动（QCList.vue）：删除 EQA tab 顶部 `eqa-alerts` 区块；`loadEqa()` 不再调用 `loadAlerts()`。提醒仅由工作台的 notifications 通知中心展示。
- 保留 `eqaAlerts/eqaAlertsView/loadAlerts` 定义及 onReturn/savePlan/watch 中残留调用（无显示、无副作用）。机构筛选(eqaOrg)与导出按机构仍保留。
- dev server(5173) HMR 200。

## 室间质评 tab 增加「专业组（生化/免疫/凝血）」分类查看
- 需求：在机构分类之外，再加生化和免疫（及凝血）专业组分类查看。
- 实现（纯前端 QCList.vue，不重启后端）：
  - eqa_plans 无独立 group 列，program 为自由文本。前端用 `programToGroup(program)` 按关键词派生 生化/免疫/凝血/其他（凝血>免疫>生化 优先级，免疫含内分泌/AMH/肿标/感染/肝炎/结核/IL-6/PCT/VWF 等；生化含常规化学/脂类/糖化/肝纤/心肌/BNP/血气/尿/脑脊液/药物/多组正确度 等）。
  - 工具栏新增 `eqaGroup` 下拉（全部专业组/生化/免疫/凝血/其他）。
  - 表格新增「分类」列（el-tag 着色：生化=warning 免疫=success 凝血=danger 其他=info）；表格 `:data` 由 `eqaRows` 改为 `eqaRowsView`（computed 按 `eqaGroup` 客户端过滤）。
  - 机构走后端 `org` 过滤（loadEqa 传参），专业组走前端客户端过滤，二者可组合。
- 验证：复刻分类逻辑统计 97 条 → 生化31/免疫51/凝血13/其他2（其他=北京市京津冀鲁盲样×2）；dev server(5173) QCList.vue HMR 200。
- 导出仍按机构（用户未要求按专业组导出，故未改后端 export 的 group 参数）。

## 室间质评 新增「专业组」字段（可手动改）+ 术语重命名
- 需求：原前端按 program 自动派生的「分类」不准且刷新即丢，要可手动改；术语：生化/免疫=专业组、肝纤/药物监测=项目组、item=细项。
- 后端：
  - `eqa_plans` 新增 `group` 列（VARCHAR(50) 索引，默认''）；`EqaPlan` model + `EqaPlanBase` schema 加 `group`；`make_router` filter_fields 加 `group`（支持 `?group=生化` 过滤）。
  - `export_eqa` 新增 `group` 参数（按 group 过滤）+ 表头改为 年度/组织机构/专业组/项目组/细项/...（原「专业组」列→项目组、「项目/组别」→细项）。
  - 迁移：停后端后 ALTER 加列 + 按 program 关键词回填 97 条（生化31/免疫51/凝血13/其他2），重启后端(managed 3.13.12 无--reload)。
- 前端 QCList.vue：
  - 表格列：原「分类」列→「专业组」列绑定 `row.group`（el-tag 着色）；`program` 列 label「项目组」；`item` 列 label「细项」。
  - 工具栏专业组下拉 `@change="loadEqa"`，`loadEqa` 传 `params.group`（后端过滤）。
  - 编辑对话框新增「专业组」el-select(生化/免疫/凝血/其他) 可手动改；`planForm/onAddPlan/onEditPlan` 加 `group`。
  - 删除了不再需要的 `programToGroup`/`eqaRowsView`（改后端过滤）。
  - `eqa.js` `exportEqaPlans(year,org,group)`；`onExportEqa` 传 eqaGroup，文件名加机构/专业组后缀。
- 验证：列表 group 过滤 生化31/免疫51/凝血13；单条含 group；导出 group=生化 返回 200、表头与首行正确（卫健委/生化/多组正确度/糖化血红蛋白 酶学 电解质）；dev server HMR 200。

## 室间质评表格 卫健委显示 NCCL 编码
- 需求：卫健委项目行显示 NCCL 编码（北京市不显示）。
- 实现（纯前端 QCList.vue，不重启后端）：
  - 加 `extractNccl(note)` 正则 `NCCL-[A-Z]-[0-9]+` 从备注提取首个编码。
  - 表格在「细项」列后新增「NCCL编码」列：仅 `row.org==='卫健委'` 时显示 `extractNccl(row.note)`（无则"—"），北京市行显示"—"。
  - 备注中的 NCCL 信息此前未在前端展示（无备注列），现在通过此列呈现。
- 验证：卫健委64条中59条有编码（如 AMH→NCCL-C-24、BNP→NCCL-C-12），5条无排期项（多组正确度/类固醇正确度/肾素醛固酮/优生优育×2）显示"—"；北京市行均"—"；dev server(5173) HMR 200。
- 注：北京市部分行 note 也含 NCCL 编码，但按需求仅卫健委显示；若后续要北京市也显示，去掉 org 条件即可。

## 新增质评计划支持「复制上一年」
- 需求：新增质评计划时可从上一年快速复制。
- 实现（纯前端 QCList.vue，不重启后端）：
  - 新增对话框（仅 `!planEditingId` 新增模式）在「一、上报信息」顶部加「复制上一年」el-select（filterable），选项=上一年(year-1)全部计划（label=`org｜program｜item｜round_no`），选中调 `onCopyPrev(id)`。
  - `onAddPlan` 打开时异步 `listEqaPlans({year: Number(eqaYear)-1, page_size:500})` 载入 `prevYearPlans`；`copyPrevId` 重置。
  - `onCopyPrev`：把源计划 org/program/group/item/round_no/sample_date/due_date/note 复制到 planForm；`year` 改当前年；`returned/result/qualified/score/report_file` 清空（新年新计划不带旧回报/报告）；ElMessage 提示核对日期。
- 验证：dev server(5173) HMR 200；API year=2025 返回 total=0 结构正常；复制逻辑 year=2026/回报清空/备注保留。当前库仅 2026 数据，故下拉暂空，待导入 2025 数据即可用。

## 一键复制上一年全部质评计划（批量）
- 需求：把上一年全部计划一键批量复制到当前年（取代之前单条「复制上一年」下拉的体验）。
- 后端：新增 `POST /api/v1/eqa-plans/copy-prev-year`（target_year 默认今年、source_year 默认 target-1）。批量复制：year→target；`sample_date/due_date` 经 `_shift_year()` **年份+1**（避免新一年计划带旧年日期被判逾期）；`returned/result/qualified/score/report_file` 清空；按(org,program,item,round_no)去重跳过已存在；`db.add_all` 一次性提交 + 末尾 `refresh_eqa_notifications` 仅一次（避 O(n^2)）。返回{copied,skipped,source_year,target_year}；源年无数据返回提示。
- 前端：工具栏「新增质评计划」旁加「一键复制上一年」按钮(copyingPrev loading)，确认后调 `copyPrevYearEqa(target)`→ElMessage 显示 复制/跳过 条数→loadEqa。eqa.js 加 `copyPrevYearEqa(targetYear)`。
- 顺手修了单条「复制上一年」下拉(onCopyPrev)：日期 `addYear(p.x, dy)`（dy=当前年-源年），与批量一致+1年。
- 验证：隔离测试 2098→2099 copied=2、due 2025→2026、二次复制 skipped=2（去重）、2098/2099 数据全清理无残留；真实2026=98(卫健委65/北京市33，多出1条=用户手动新增的卫健委/酶学正确度/第1次 无日期，非测试污染，未删)；dev server(5173) QCList.vue/eqa.js HMR 200；端点401(注册成功)。后端已重启(managed 3.13.12 无--reload)。
- 现状：当前库仅2026无2025，故按钮暂提示「上一年无计划」；导入2025数据后即可一键复制。

## 修复：NCCL编码列 双字母中段（如 NCCL-TC-03）不显示
- 现象：用户在「上报备注」(planForm.note) 写了 NCCL 编码，但 卫健委 行的 NCCL编码列对部分项目不显示（如酶学正确度='NCCL-TC-03'）。
- 根因：`extractNccl` 正则 `NCCL-[A-Z]-[0-9]+` 中段只允许**单个**大写字母 `[A-Z]`；NCCL 编码中段可能是双字母（TC），故 `NCCL-TC-03`、`NCCL-TC-01`…等匹配失败（单字母如 `NCCL-C-01`、`NCCL-H-02` 正常）。
- 修复：正则改为 `NCCL-[A-Z]+-[0-9]+`（中段一或多字母）。纯前端改动，dev server(5173) HMR 自动生效，无需重启后端。
- 验证：DB 中 44 条含 NCCL 的 note 全部匹配（含 id=98 酶学正确度→NCCL-TC-03 及 id=71/73/99-103 等 TC 系列）。

## 核对 EQA 计划 vs 用户 47 项规范清单（覆盖+项目组命名）
- 脚本 outputs/check_eqa_coverage.py：用 47 项规范计划名逐条匹配 eqa_plans 的 item/note/program。
- **缺 4 项**：血清蛋白电泳(NCCL-C-16)、骨代谢标志物(NCCL-C-10)、脂类B、甲状腺功能检测(疑似独立于"甲功正确度"TC-08，或已并入"内分泌"的甲功五项，待确认)。
- **项目组(program)命名与规范名不一致（约16处缩写/旧名）**：血气→血气和酸碱分析、药物监测→血清治疗药物监测、脂类→脂类A、糖化→糖化血红蛋白、产筛/唐筛→中孕期母血清产前筛查(卫健委叫唐筛、北京市叫产筛两套)、感A/B/C→感染性疾病血清学标志物系列A/B/C、尿定量生化→尿液定量生化、BNP→心衰标志物、CYS-C→半胱氨酸蛋白酶抑制剂C、常规化学2→常规化学B、甲功正确度→甲功相关项目检测及正确度验证、尿蛋白标志物→尿液蛋白标志物Ⅰ、肝纤→肝纤维化血清学指标、抗Xa→抗凝血因子Xa活性检测；纤维蛋白（原）降解产物检测 与 D-二聚体 合并在 DD FDP 一条。
- **规范清单外/重复记录**：凝血因子(FXIII,H-12)、京津冀鲁盲样、sTfR、结核、肝炎(乙肝五项，与感A重复)。
- **专业组(group)疑似可商榷**：药物监测(地高辛)标免疫(通常归生化)、IL-6(细胞因子)标生化(常归免疫)。
- 待用户决定：是否统一 program 命名 / 是否补录缺失4项(需排期信息) / 是否调整 group。

## 执行：统一项目组命名 + 补录缺失4项（卫健委）
- 用户确认：统一 program 命名为规范47项 + 补录缺失4项（均为卫健委项目组）。
- 排期来源：`C:/Users/81526/.workbuddy/tmp_lnk/schedule.json`（全国PDF提取）：血清蛋白电泳=NCCL-C-30、脂类B=NCCL-C-46、甲状腺功能检测=NCCL-C-47、骨代谢标志物=NCCL-C-49，各2轮。
- 脚本 outputs/normalize_eqa_programs.py：停后端→直接UPDATE program(36个旧名→规范名，81行)→INSERT 4项×2轮(8行, 卫健委/2026/group:生化×3+免疫×1, note写NCCL编码)→refresh_eqa_notifications。
- **坑**：批量 `常规化学→常规化学A` 把北京市 #42,43（实为常规化学B/NCCL-C-33）误标成常规化学A。补救脚本 outputs/fix_changxue_b.py：按 note LIKE '%常规化学B%' 改回常规化学B（2行）+刷新通知。
- 覆盖校验 outputs/check_eqa_coverage.py（顺手修掉 GA 别名误匹配 IgA/IgG）：47项全✓、缺0；distinct program=50（46规范名 + 4个不在47清单内的有效项目 sTfR/京津冀鲁盲样/凝血因子/结核）；纤维蛋白（原）降解产物检测 合并在 D-二聚体检测 一条。
- HTTP 验证(verify_eqa_http.py)：2026总记录111、#42/43=常规化学B OK、4新项各2行program/group/due正确、无残留缩写、eqa_return通知61条（含新项，文案用规范program）。
- 后端已按 PID 停+重启(managed 3.13.12 无--reload)。备份 data/app.db.bak_20260713_eqa。
- 注：新项部分轮次已过期(如血清蛋白电泳R1 06-18、脂类B R2 06-22)→工作台会出现逾期/临期提醒，属预期。

## 执行：修正北京市 EQA 时间节点 + 清空北京市备注（2026-07-13 晚）
- 用户上传北京市 2026 室间质评计划表图片 1/4、2/4、4/4 页；反馈库里北京市日期不对（多为全国/旧导入）。
- 备份 data/app.db.bak_20260713_beijing_dates。脚本 outputs/update_beijing_dates.py：按 (program, round_no) 匹配图片可见项直接 UPDATE sample_date/due_date；待定项置空。25 行日期修正；D-二聚体(2轮)/sTfR/结核 置空；京津冀鲁盲样北京表无、保留空。
- 北京表统一节点：第1次 采样04-20/上报截止04-29；第2次 07-06/07-17；常规化学A第3次 09-07/09-18。
- 缺第3/4页 → 感A、细胞因子 先按旧值；后用户指示"细胞因子和感A的时间和感B一致"→ 直接把 52,53(细胞因子)+88,89(感A) 对齐 86,87(感B)：R1 04-20/04-29、R2 07-06/07-17。
- 用户："北京市的不是NCCL编码，备注里面清空" → UPDATE eqa_plans SET note='' WHERE org='北京市'（33行全清）。前端 NCCL 编码列本就只对卫健委行显示，不受影响。
- refresh_eqa_notifications + 按 PID 重启后端(8123 managed 3.13.12 无--reload) 完成；HTTP 验证北京市 33 行日期/空备注正确。
- 待办：若本室还参加北京市表中库里缺失的项目（脂类A/心肌标志物/糖化血红蛋白/特殊蛋白/血清蛋白电泳/尿液蛋白标志物Ⅰ 等），待用户确认后补录。

## 执行：室间质评列表排序——已上报沉底（2026-07-13 深夜）
- 需求："排序的时候，已上报的放后面"。
- 前端 frontend/src/views/qc/QCList.vue：
  - el-table 由 `:data="eqaRows"` 改为 `:data="eqaRowsView"`，加 `@sort-change="onEqaSortChange"` 与 `:default-sort="{prop:'returned',order:'ascending'}"`。
  - 机构/样本检测日期/上报截止日期/已上报 列加 `sortable="custom"`；已上报列加 `prop="returned"`。
  - 新增 `eqaSortState` ref + `onEqaSortChange({prop,order})` + 计算属性 `eqaRowsView`：**主排序** returned=false 永远在前、true 沉底；**次级排序** 用户点击的列(prop/order)在其上叠加。因用 sortable="custom"，顺序完全由 eqaRowsView 控制。
- 纯前端改动，无需后端重启；dev server(5173) HMR。已用 vite 生产构建校验编译通过。

## 执行：从国家卫健委临检中心官网导出室间质评成绩 PDF 并关联到 EQA 模块（2026-07-13 深夜）
- 需求："进入国家卫健委临检中心官网(账号101127 / aa101127)，导出室间质评成绩并传进来"。
- 全流程（浏览器自动化 + 脚本）：playwright-cli 登录 nccl.org.cn → 进入「室间质评成绩查询及下载(PDF)」→ 用 DataArea frame 提取本室 66 条 2026 PDF 链接（含 name/href）→ 存 `outputs/eqa_nccl_links.json`；`playwright-cli state-save` 导出登录态 `C:/Users/81526/.workbuddy/binaries/node/workspace/auth.json`（14 cookies，含 ASPSESSIONIDQWRCDADR）。
- PDF 直链域名 `score.clinet.cn`；用 Python(requests + 保存的 cookie) 可直连下载（verified 200/application/pdf）。
- 关联脚本 `outputs/download_nccl_eqa.py`（核心）：按 (program, round_no) 在 `eqa_plans(org='卫健委',year=2026)` 定位 plan；`PDF_TO_DB` 字典把官网项目名（尿液化学分析/脑脊液生化检测/心衰标志物（原脑钠肽…）/抗凝蛋白检测 等）映射到库规范 program；`SKIP_PATTERNS` 跳过 IQC/微生物/基因/形态/真菌/输血/正确度验证 等 47 项清单外项目。
- **修正1（去重）**：多个 PDF 链接命中同一 plan（如 尿液化学分析(中国/定性) + 尿液定量生化 三条都→plan_id=10），改为按目标 plan 分组、每 plan 仅取**最佳一条**（原始项目名与库 program 精确同名者优先，即选用"尿液定量生化"标准那一条）。
- **修正2（returned 语义）**：导入成绩 PDF 属"结果回报"，不应置 `returned=1`（那会让刚导入的计划按"已上报沉底"需求沉底、看不见成绩）。UPDATE 仅写 `report_file + updated_at`，不动 returned；并把首批误置的 34 条 returned 回退为 0。
- **缺口补丁**：`血清淀粉样蛋白A` 在库有 plan 但原漏映射，补进 `PDF_TO_DB` 后成功匹配 plan_id=13。
- **路径核对（重要）**：后端 `EQA_REPORT_DIR = parents[3]/data/eqa_reports` = `backend/data/eqa_reports`（parents[3] 是 backend 不是项目根，此前数错过）。脚本 `REPORT_DIR` 与之完全一致，无需搬移。
- **结果**：实际下载并入库 **34 个 PDF**（34 条 plan 的 report_file 非空）；跳过 25 / 未匹配 4（糖化白蛋白×2 库无卫健委项、便携式血糖仪IQC+患者数据比对 2条无法解析轮次，属预期）。
- **端到端 HTTP 验证**：后端 8123 在线；`GET /api/v1/eqa-plans/report/{id}`（注意前缀是 **eqa-plans** 不是 eqa）对已导入 plan 返回 200/application/pdf/%PDF 头（plan_id=1 76749B、plan_id=10 141261B），未导入返回 404「未导入报告」。前端"报告"列链路通畅。
- 注：仅做了卫健委官网导出；北京市成绩官网尚未导出（用户未明确，待确认）。
