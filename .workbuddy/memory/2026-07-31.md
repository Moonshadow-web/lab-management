# 2026-07-31 工作日志

## 人员继教管理：UI 调整 + 人员档案导入
- 用户反馈：左侧菜单仍显示「继教培训」、Hub 页多了「制度依据」副标题；要求先做人员档案并把桌面 `生免室人员档案` 里的人员导入。
- 修复：
  - `frontend/src/layout/AppLayout.vue` 菜单标题改为「人员继教管理」。
  - `frontend/src/views/training/StaffEducation.vue` 删除 `el-tag` 制度依据副标题，仅保留大标题。
- 桌面档案解析：16 个文件夹（实际 15 个有效 .doc），文件名格式不统一；用 antiword 提取 BG-KS-GL-017 基本情况表格文本，按 `|` 单元格解析出 姓名/性别/出生年月/学历/职称/职务/政治面貌/组内职责/参加工作时间/来院时间/来组时间。
- 生成 `backend/scripts/import_personnel.sql`（15 条 INSERT）。因当前线上仍是旧版本（education 表不存在）且沙箱无法直连 MySQL，SQL 需待新部署生效后由用户在 CloudBase 控制台执行，或后续通过 API 导入。
- `_BUILD_MARK` 更新为 `staff-education-ui-2026-07-31`（因本地 diag.py 已被另一 AI 改为 `fix-aliases-mismatch-2026-07-31`，需避免覆盖）。
- 前端 dist 用 `--emptyOutDir false` 重新构建（沙箱批量删除 guard 会阻止 Vite 默认清空 dist/assets）。
