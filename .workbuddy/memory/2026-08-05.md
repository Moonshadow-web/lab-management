# 2026-08-05 工作日志

## 组内培训签到表两处问题修复
- 现象：①编辑培训记录时「培训老师」字段=金子铮，且签到名单自动从人员表预填也含金子铮 → 看起来两个金子铮；②点「打印空白签到表」出来是空白。
- 根因①：签到名单 `onMounted` 无脑预填全部人员，把培训老师本人也列进去了（人员表仅 1 个金子铮，非数据重复）。
- 根因②：SignInSheet 可打印区在 `el-dialog`（fixed 浮层）内，`window.print()` 不渲染 fixed 浮层内容 → 空白。与早前 NewEmployeeTrainPrint 的坑同一类。
- 修复（`frontend/src/views/training/SignInSheet.vue` + `GroupTraining.vue`）：
  - 打印改 `<Teleport to="body">` + `@media print { body > *:not(.print-root){display:none} .print-root{display:block;position:static} }`（照搬 NewEmployeeTrainPrint 已验证方案）；屏显保留格式化预览（打印时隐藏）。
  - 自动填充名单排除 `header.teacher`（培训老师本人不再出现在签到名单）；新增 `savedNames` prop，已保存名单优先加载，手动增删不再因重开记录丢失。
- 部署：vite build 新 chunk `StaffEducation-DK07qPJg.js`；commit `65d10c1`；`_BUILD_MARK=signin-sheet-print-teacher-dedup-2026-08-05`；内网 build mark + chunk 均 200 生效。
- 注：培训老师作为「培训人」仍单独记录在表单头，打印签到表只列参训人员（不含老师本人）。若用户后续要求老师也要在签到表里签名，去掉 excludeTeacher 即可。

## 15189专项新增「规范文件」模块（2026-08-05）
- 来源：`C:/Users/81526/WorkBuddy/2026-08-05-01-40-06/cnas_standards` 下 15 个 CNAS/WS-T 规范 PDF（注意：文件夹缺 07，实际 01-06、08-16 共 15 个）。
- 新增独立后端模块（不污染 SOP 文档档案）：
  - 模型 `models/cnas_standard.py` → 表 `cnas_standards`（code/name/category/original_filename/cloud_key/data/file_size/sort_order/uploader）；已在 `models/__init__.py` 注册，由 `_ensure_missing_columns` 自动建表。
  - API `api/v1/cnas_standards.py`：`GET /` 列表（按 sort_order,code 排序）、`POST /upload`（admin，从文件名解析 code/name/category/sort_order，存 COS，COS 失败回退 BLOB）、`GET /{id}/preview`（inline PDF）、`GET /{id}/download`（attachment）。已在 router.py 挂载（prefix `/api/v1`）。
  - 文件名解析：`NN_CODE_名称.pdf` → code=CODE、name=名称、sort_order=NN、category 按 CODE 前缀（CNAS→CNAS认可规范，WS→卫生行业标准(WS/T)）。无下划线的（09）整体作名称、category 仍按 CNAS 推断。
  - 预览/下载复用鉴权 fetch+blob 模式（同 documents 模块），前端不依赖 window.open 带 token。
- 前端：新增 `api/cnasStandards.js`（list + fetchBlob，复用 documents.js 的 downloadBlob/previewBlob）、`views/iso15189/StandardsTab.vue`（表格+代号/名称/类别/大小/预览/下载），并在 `Iso15189Hub.vue` 加「规范文件」tab。
- 部署：`_BUILD_MARK=cnas-standards-module-2026-08-05`；vite build --emptyOutDir false；git add -f frontend/dist；tcb deploy；内网 curl build 核对新标记生效（滚动期有 ~5min 空响应，重试即可）。
- 播种：`scripts/seed_cnas_standards.py`（APPLY=1）登录后把 15 个 PDF 经 `/cnas-standards/upload` 上传，幂等（按 original_filename 去重）。结果：新增 15（id 1-15）、跳过 0、失败 0。
- 验证：`scripts/verify_cnas_standards.py` 确认列表=15、类别分布 CNAS×8/WS-T×7、抽样 preview/download 均返回合法 %PDF、Content-Disposition 正确。全部通过。
