# 2026-07-26 工作日志

## 试剂管理责任库切换修复
- **问题**：用户反馈 LibraryTabs（生化凝血/免疫）点击不切换。
- **根因**：`el-radio-group` 使用 `:model-value`(computed) + `@update:model-value` 单向绑定，Element Plus 2.7.6 下点击后内部状态与外部 prop 竞争导致视觉不切换。
- **修复**：改用 `v-model="selected"`(本地ref) + `@change` + watch 双向同步 store（commit `28cd696`）。

## 盘库/订购模板全面改造（commit `e2487d2`）
### 后端 build_reagent_template 改进：
1. **停用过滤**：仪器 status!='停用'、试剂 is_active=True，停用项不再出现在盘库/订购模板中。
2. **流水线排除**：仪器名含"流水线"的不显示耗材组（用户确认流水线无耗材）。
3. **抗凝血酶关联修复**：手动创建 TestItemReagent(id=574)：test_item_id=131(抗凝血酶III) ↔ reagent_item_id=150(抗凝血酶测定试剂盒)，role=试剂。之前在孤儿组现归入正确项目。
4. **DTO 扩展**：每条 item 带 project_name + project_aliases，供前端模糊检索匹配英文别名(ALT/ATIII等)。
5. **仪器标签格式化**：名称(model)，如 AU58-1 → AU58-1(贝克曼AU5821 A)。

### 前端 InventoryCheck.vue + ReagentOrders.vue 改进：
1. **分类下拉**：搜索栏上方加 el-select（全部分类 / 试剂·校准品 / 耗材 / 质控品），独立于模糊检索。
2. **模糊检索增强**：匹配试剂名/规格/材料编码 + 项目中文名 + 英文别名（逗号分隔），搜 ALT 能命中丙氨酸氨基转移酶。
3. **材料编码列**：所有表格新增「材料编码」列（打印也带）。
4. **项目标题带别名**：如「项目：丙氨酸氨基转移酶（ALT,谷丙转氨酶）」。

## 部署
- 上轮灰度卡死「不存在灰度中的版本或灰度版本部署未完成」（3个AI抢部署互相打断）。
- 用 `(echo ""; sleep 3; echo "Y") | tcb cloudrun deploy --force` 重新干净部署，确认新版本已生效：
  - `/reagent/template?library=生化凝血` 返回分组数据 ✓
  - 试剂目录 library 过滤：生化凝血=142条、免疫=170条 ✓
- 部署铁律重申：第一提示空回车=不进灰度自动切流；第二提示Y=取消冲突任务。

## 待关联建议接口 + 匹配改进（commit `12d2ce2`）
- **新增后端 `GET /reagent/associations/suggestions`**：返回 test_item_suggestions / test_item_orphans / instrument_suggestions / instrument_orphans 四组。供关联页「待关联建议」标签页审核采纳。停用仪器与停用试剂不参与匹配。
- **匹配改进（关键踩坑）**：原 `_norm_match` 未能识别「抗凝血酶III ↔ 抗凝血酶测定试剂盒」（因试剂名含"测定试剂盒"而项目名带罗马数字后缀 III，互相不为子串 → score=0 漏匹配）。新增 `_core_name()` 去括号+罗马数字后缀（"抗凝血酶III"→"抗凝血酶"），并抽出 `_best_test_item_match()` 供 auto_match 与 suggestions 共用。离线验证：抗凝血酶测定试剂盒→抗凝血酶III score=4；267个非耗材试剂中可匹配174、无候选93。
- **前端**：
  - 项目标题去掉别名显示（用户明确"别名只用于检索"）：InventoryCheck/ReagentOrders 的「项目：xxx（别名）」改「项目：xxx」；`project_aliases` 字段保留仅供 matchItem 模糊检索。
  - ReagentAssociations.vue 新增「待关联建议」标签页：四块表格，每行预填系统候选，可一键采纳 / 改选后采纳 / 批量采纳全部建议。

## 移动端录入优化（commit `df1e980`）
- **盘库弹窗**：三个分组表格去掉「材料编码/单位/当前库存」三列，仅留 名称 + 盘点余量，避免手机横向滑动。
- **订购弹窗**：录入表格改为竖向卡片（.entry-card：名称换行 + 订购数量输入在右，≤480px 时整体竖排），不横向滑动、"显示不下多显示几行"。
- 两个弹窗 width 改为 `min(960px, 96vw)` 适配窄屏。
- 注：打印/空白页仍保留材料编码/单位/当前库存（A4 不受影响）。

## 质控上传规则解析【关键 bug 修复】（commit `67033c3`）
- **现象**：AU5800 等 LIS 导出含规则列（如「失控规则」），原表标了很多失控点（β2微球蛋白 1-3S、10-x 等），但系统没解析出来。
- **根因**：上轮 step1.5 把「解析失败」与「空单元格」混为一谈。一旦 rule_column_present=True，任何未解析的单元格都被强制清零为在控并冻结 R-4s —— 而 LIS 规则值写法（1-3S(失控)、10-X、1－3S 全角、13S 无连字符等）恰不被旧解析器识别，于是 LIS 标过的失控点被全部抹掉。
- **修复**：
  1. 仅「真正空」单元格在含规则列时判在控；有内容但未识别的单元格保持后端 Westgard，不误翻。
  2. 解析器增强：全角/异形标点(－～–:)转半角、去外层括号、子串提取（1-3S(失控)/失控1-3S）、无连字符写法(13S/10X)。
  3. 上传响应新增 `rule_cells_total/recognized/unrecognized` 诊断计数，便于核对 LIS 写法是否被识别。
- **数据恢复**：原上传已把原始规则串存进 `qc_daily_values.uploaded_rule`，且 rule_column_present 已落库；修复部署后直接调用 `POST /qc-summaries/_recalc`（admin/qc_manager）即可用新解析器重算，无需重新上传（前提是当时规则列被识别、rule_column_present=True）。若 rule_column_matched=False 则需重新上传。
- 测试 11→14 断言全过（用例H 未识别写法→保留后端；用例I/J 写法变体解析）。

## 质控上传 500 故障（commit `95e3fd2`）
- **现象**：质控 LIS 数据上传返回 500（Request failed with status code 500）。
- **根因推断**：上一轮 QC 部署（含 `rule_column_present` 模型列，commit `6ae817d`/`67033c3`）因镜像推送 broken pipe 中断且任务被清理、未确认成功，线上服役容器仍是更早的 `scheduling-v7-matrixwidth` 构建（build 标记从未随 QC 提交更新）。结果：代码引用了 `qc_monthly_summaries.rule_column_present` 列，但非空的线上 MySQL 表缺该列 → 上传时 `Unknown column` → 500。
- **注意**：`main._migrate_schema` 的 `alters` 字典块只跑 SQLite `PRAGMA`，**MySQL 下完全不生效**；非空线上表的补列只能靠 `_ensure_missing_columns()`（dialect 无关）。当该次部署中断时，补列没执行。
- **修复**：在 `_ensure_missing_columns()` 末尾**显式兜底补列** `qc_monthly_summaries.rule_column_present` 与 `qc_daily_values.uploaded_rule`（幂等）；并把 `diag._BUILD_MARK` 改为 `qc-ruleparser-fix2-2026-07-26`，使 `curl /_diag/build` 能确认新代码是否生效。
- **部署铁律强化**：每次改模型/解析/关键逻辑提交，**必须同步改 `_BUILD_MARK`**，且部署后必须 curl 内网 `_diag/build` 核对标记；broken pipe 等中断需重试至确认成功，不能只看任务被清理就假定成功。
- 状态：95e3fd2 已 push，重部署中（任务 uDkK4d）。

## 上传 500 阻塞排查（续）：卡死的灰度发布
- 95e3fd2 部署任务(uDkK4d)虽"成功"，但 cloudrun list 的 **Update Time 仍是 11:14:10 未变**，`/_diag/build` 标记仍 `scheduling-v7-matrixwidth` → 新代码根本没生效。
- **根因**：存在一条**状态异常的卡死灰度发布**(Gray release in progress / 状态异常)，阻塞新版本发布。
  - `tcb cloudrun traffic promote` → `[ReleaseGray] 状态异常，无法发布`；
  - `tcb cloudrun traffic rollback` → `[OperateServerManage] 当前任务已结束或停止`（也清不掉）。
  - CLI 无別的清灰度手段。
- **现象关联**：卡死的灰度 + 不一致服役容器，正是上传 500 的持续原因（线上 pod 仍引用缺列）。
- **应对**：重跑整轮 `tcb cloudrun deploy --force`（部署流程的发布环节与 traffic 子命令不同，可能覆盖卡死灰度）；若仍卡，需到 CloudBase 控制台手动「取消灰度/回滚/重新部署」或提交工单。
- 教训：update time 不随 deploying 改变 + build 标记陈旧 = 发布链路被卡死，不能只看 deploy 命令回显。
- 已停止卡死任务 4CwKd0（运行 1m29s 后清理），改起新任务 S9USQw 重试整轮部署（看能否覆盖卡死灰度）。若 deploy 仍卡在 release 步/Update Time 不变，则 CLI 无解，需用户到 CloudBase 控制台「云托管 → lab-management → 灰度/版本」手动取消灰度或重新部署，或提工单。

## 上传 500 真因：4-1s KeyError（已修复）
- 用户再次上传报 "Internal Server Error"。利用此前部署的全局异常捕获 + `GET /_diag/last-errors` 端点，curl 内网拿到真实 traceback：
  `KeyError: '4-1s'` @ `qc_service.py:395 _resolve_uploaded_rules` → `sorted(set(ooc_rules), key=lambda x: -_RULE_SEVERITY[x])`。
- **根因**：`_OOC_RULES` 与归一化表含 `4-1s`（Westgard「连续4次同侧」），但严重度字典 `_RULE_SEVERITY` 漏项 → 单元格出现 4-1s 即 KeyError 整次上传 500。用户那张 AU5800 6月表里有 4-1s（之前也含 β2微球蛋白 1-3S、10-x）。
- **修复（commit 3eff59a，已部署，build 标记 `qc-41s-keyerror-fix-2026-07-26`）**：
  - 补全 `_RULE_SEVERITY["4-1s"]=2`（与 R-4s 同级，判失控）；
  - 解析器排序改 `_RULE_SEVERITY.get(x, -1)` 防御化，未来漏项不再崩溃；
  - 新增用例K 校验 4-1s 解析判失控；测试 14→15 断言全过。
- **部署 gotcha（再次印证）**：首次部署 3eff59a 后 curl 标记仍是 `reagent-usage-v2`（父提交 c6cb66a）——CloudBase 构建从 git 拉取，push 同步延迟导致拉到旧提交；**重部署一次**（取消运行中任务、部署最新代码）后标记才变为 `qc-41s-keyerror-fix`，确认新代码生效。结论：部署后务必 curl `/_diag/build` 核对标记，标记旧=没生效，要重部署。
- 用户现在只需重新上传那张 6月 AU5800 表即可（修复后的解析器会正确解析 4-1s / 1-3S / 10-x 等，无需再手动 _recalc）。

## LIS 名称别名映射 + 白蛋白（A）质量目标（commit 042105c，待上线）
- 用户要求：直胆红素=直接胆红素、腺苷酸脱氨酶=ADA、转肽酶=GGT、谷草转氨酶=AST、谷丙转氨酶=ALT、低密脂蛋白=LDL、高密脂蛋白=HDL；并按此对应上+补充质量目标；白蛋白（A）单位 g/dl、质量目标同糖化白蛋白(GA)。
- **根因**：质量目标种子(quality_requirements_seed.py)里这些项目本就有标准值（直接胆红素8%/ALT·AST6%/GGT3.5%/HDL·LDL6% 等），用的却是全称；LIS 用简称/缩写，contains_same_item 对「转肽酶 vs γ-谷氨酰基转移酶」等互不包含写法匹配不到 → 质量目标落空。
- **改动（042105c，已 push）**：
  - 新增 `_LIS_ITEM_ALIASES` + `_canon_item_name`：LIS 简称/缩写→规范名桥接；`find_test_item_by_name` 与 `lookup_quality_goal` 增加规范名兜底匹配。
  - `_lookup_qr_goal` 精确/安全包含匹配归一化括号（白蛋白（A）与 白蛋白(A) 互通）。
  - 种子新增「白蛋白（A）」：cv 6.7%（同糖化白蛋白 GA）、unit g/dl（bj-hr-2025 源）。
  - 测试 scripts/test_item_alias.py 21 条断言全过。
- **上线后两步（用 admin 调）**：① `POST /api/v1/quality-requirements/_meta/seed`（幂等灌库，新增白蛋白（A）条目）；② `POST /api/v1/qc-summaries/_backfill_goals`（回填已存月结行的质量目标，使直胆红素/ALT 等拿到正确目标）。
- **部署卡死（同 500 那役）**：042105c 两次部署均"submission completed"但 `/_diag/build` 标记仍 `reagent-fe-retry-2026-07-26-2115`（父提交 6b7017b）。`traffic promote`/`setTraffic --stable 100` 仍报「状态异常/不存在灰度版本」——**卡死灰度发布僵尸态再现**，CLI 无法清除。需用户在 CloudBase 控制台「云托管 → lab-management → 版本/灰度」手动取消/结束异常灰度（或回滚到稳定版本），再重部署，标记才会变 `qc-item-alias-2026-07-26`。

## 试剂用量排序/显示停用/盘库删除/订单实时库存（本轮，代码全完成、部署受阻）
- **用户原始诉求**：①目录/盘库/订购模板默认按年用量降序（参考去年采购明细 xlsx）；②试剂目录"显示停用"开关，停用的不显示、可单开；③盘库加删除操作；④新建订单显示实时库存便于判断是否订购。
- **后端（全部完成并线上实测通过）**：
  - `annual_usage` 字段 + `_ensure_missing_columns` 自动迁移；`POST /reagent/items/_batch-update-usage` 批量导入（按材料编码匹配，182 条→181 项命中，0 未匹配）。
  - 目录列表 `show_inactive` 过滤（默认隐藏停用，开则显示）；默认按 `annual_usage` 降序。
  - 盘库 `DELETE /reagent/inventory-checks/{id}`（实测 200 删除成功，再 GET 404）。
  - `build_reagent_template` 改为 by_project/by_instrument/controls 三组、组内按年用量降序（实测 77 项目组全部降序）；DTO 带 `current_stock`（订单实时库存来源）。
  - **数据已灌入线上**：181 条试剂年用量已导入 MySQL（`annual_usage`），目录/模板排序实测生效。
- **前端（代码写完并本地构建）**：ReagentItems 加"显示停用"开关+年用量列；InventoryCheck 加删除操作；ReagentOrders 卡片显示实时库存；移动端优化（盘库隐藏冗余列、订购竖卡片）此前已完成。
- **关键坑——云端前端构建缓存/克隆陈旧**：多次干净部署后后端标记/功能均上线，但前端 JS 仍是旧 `index-BWDfa38v.js`（无"显示停用/entry-card"等）。定位：CloudBase 对 `frontend/` 子树按 lock 缓存且克隆未随 .vue 改动刷新，多阶段 Docker 的 frontend-builder 阶段复用了旧 dist。
  - **绕开修法（已提交 e262ba7）**：本地 `npm run build` → 把 `frontend/dist` 强制提交进 git → Dockerfile 删除 frontend-builder 阶段、改为直接 `COPY frontend/dist ./frontend/dist`。这样前端产物随 git 走，不再依赖云端前端构建。
- **部署受阻根因（与上一役同）**：① **卡死灰度发布僵尸态再现**——控制台报 `DescribeCloudBaseBuildService` "Version not found"，CLI 的 promote/setTraffic 均报"状态异常/不存在灰度版本"，无法清除；② **本轮有 3 个 AI 并发部署互相覆盖 main**：我的推送（含 e262ba7 的 dist）被反复顶掉，`origin/main` 已变成未知引用、线上标记被别的 AI 改成 `qc-item-alias-2026-07-26`。
- **结论（已更正）**：代码层面（后端+数据+前端源码+预构建 dist+Dockerfile 绕开前端缓存）**已全部就绪且本地完好**（本地 HEAD=49b6f16，e262ba7 含 frontend/dist）。「卡死灰度/需手动取消灰度」为误判（见文末更正）；按用户指示正常部署即可。
- **注意（已更正，见文末）**：原「不要再盲目 deploy / 先解决控制台灰度卡死」为误判。用户澄清灰度卡死问题不存在；遇到部署疑似未生效，应直接问用户并等其核对 Cloud 真实情况，然后正常部署。

## ⚠️ 更正：所谓「卡死的灰度发布」其实不存在（用户 2026-07-26 晚澄清）
- 用户明确澄清：**「卡死的灰度发布/状态异常无法发布」这个问题实际上不存在**，是我此前的误判。CLI 的 `promote`/`rollback`/`setTraffic` 报「状态异常/不存在灰度版本」**不代表真的卡死**，是我对 CLI 行为与部署状态理解有误。
- **正确做法（写进长期记忆 MEMORY.md）**：部署疑似未生效/流量未切时，**不要自己下「灰度卡死」结论**，也**不要据此让用户去控制台手动取消灰度/回滚**。应**直接问用户**，并**等用户去 Cloud 控制台查看真实部署情况**（版本/流量/构建状态）后，再据反馈判断。
- 唯一可靠的「部署是否生效」判据仍是 `curl` 内网 `/api/v1/_diag/build` 看 `_BUILD_MARK`。标记未更新时，先和用户确认真实情况，再决定下一步，而非假定灰度机制故障。
- 推论：本日日志里多处「卡死灰度发布僵尸态」「需用户手动取消异常灰度」等描述均为误判，应以本更正为准。

## 干净部署尝试（用户 21:02 指示"部署吧"）
- 已提交并推送 origin/main `97a3523`，`_BUILD_MARK=reagent-full-deploy-2026-07-26-2102`（试剂用量排序/显示停用/盘库删除/订单实时库存全部特性）。
- `tcb cloudrun deploy --force` 已成功**提交**（CLI 回显 submission completed，自动切流量=不进灰度）。
- 用户明确工作流：**不要盲目轮询等待**；灰度卡死问题不存在，部署疑似未生效时直接问用户、等其到 Cloud 控制台核对真实情况后再判断。
- 截至 21:10 内网 `/api/v1/_diag/build` 仍返回旧标记 `scheduling-v8-postid-nullable-2026-07-26`（云端构建/发布可能仍在进行，或需用户核对）。**等待用户查看 Cloud 部署状态后回话确认**，不自行下结论。

## CZ-012 月小结版式调整（commit 989570b）
- 用户要求：月结报告标题下的「表格编号：BG-SM-CZ-012 民航总医院检验科生化免疫组 生效日期：2026.08.01」整行删除；页脚日期由「生效日期」改为「失效日期：2026.08.01」。
- 改动：`qc_report.py` 删除标题下表单信息行（段落）；`_add_footer_code` 增 `expire_date` 可选参（仅月小结传 expire_date="2026.08.01"，比对报告 CZ-022 仍用「生效日期」不受影响）。冒烟测试通过（BUILD OK + 两断言 PASS）。
- 注意：代码已提交未部署；要线上生效需按新规矩 curl `/_diag/build` 核对标记（不假定灰度卡死）。

## 仪器耗材关联：过滤停用 + 按总型号分组合并（commit c8c33c3，已推送部署）
- **用户需求**（截图+描述）：①停用的仪器不显示；②DXI800 的 1号机/2号机/急诊/唐筛等多台合并为一行显示"DXI800"，因为耗材通用。
- **后端改动**（`reagent_management.py`）：
  - 新增 `_extract_instrument_base_model(name, model)` 函数：从仪器 name 提取总型号（如 "DXI800 1号机"→"DXI800"，"AU58-1"→"AU58"），回退 model 字段提取。
  - `GET /associations/instruments` 接口重写：JOIN Instrument 表 + `status != '停用'` 过滤；按 `(base_model, reagent_item_id)` 分组，组内耗材去重；返回 `instrument_name`=总型号、`merged_instruments`=子仪器名列表、`source_count`=合并台数。
- **前端改动**（`ReagentAssociations.vue`）：仪器列改为显示**总型号（加粗）+ 台数 tag + tooltip 展开子仪器列表**，不再显示单台 name(model) 格式。
- **构建标记**：`_BUILD_MARK=inst-consumable-group-2026-07-26-2137`。
- **部署状态**：`tcb cloudrun deploy --force` 已提交成功（自动切流量 + 取消冲突任务）；截至 21:40 内网标记仍为旧值 `cz012-report-footer-2026-07-26`（另一 AI 刚部署过），等 Cloud 构建完成后应切到新标记。

## 仪器耗材关联 v2：修复总型号提取 + 导出对照表（commit c3996a0，已推送部署）
- **问题**（用户 23:17 截图反馈）：v1 的 `_extract_instrument_base_model` 正则 `^([A-Za-z0-9]+(?:[- ][A-Za-z0-9]+)*)` 把 `"DXI800 1号机"` 提成了 `"DXI800 1"`（空格后的数字也被匹配），导致 DXI800 1/2/3/4 被当成**四个不同型号**，只有不带数字的"DXI800"（急诊/唐筛）才合并。
- **修复**：正则改为 `^([A-Za-z0-9]+(?:-[A-Za-z0-9]+)*)`——只允许**连字符**分隔（不允许空格），遇空格/中文即停；再 `re.sub(r'-\d+$', '', base)` 去掉尾部实例编号（AU58-1 → AU58）。
  - `"DXI800 1号机"` → 匹配 `"DXI800"`（空格处停）✅
  - `"DXI800 急诊"` → `"DXI800"` ✅
  - `"AU58-1"` → `"AU58-1"` → 去掉 `-1` → `"AU58"` ✅
  - `"TOP700A"` → `"TOP700A"`（无尾部编号，保持独立）✅
- **导出 Excel**：`仪器耗材对照表.xlsx`，从线上 API 拉 159 条原始数据，用修复后逻辑分组：
  - **13 个总型号**：DXI800(11种耗材/5台)、AU58(10种/2台)、TOP700A/B/C(各5种/独立)、免疫分析仪(16种) 等。
- **构建标记**：`_BUILD_MARK=inst-consumable-group-v2-2026-07-26-2317`。
- **部署状态**：已提交成功（无冲突任务提示），等 Cloud 构建完成。

## 凝血 LIS 标签 → 项目/质量目标 对应修复（commit 6adafe2）
- 问题：上传凝血数据 PT/PT%/APTT/TT/INR RATIO/蛋白C/抗凝血酶III/SCT标准化比值 没对应到正确项目。根因：
  1) `find_test_item_by_name` 用双向子串匹配，"PT" 被谷丙转氨酶别名 "GPT" 子串截胡→错配 ALT(id27)；PT%/INR RATIO/SCT标准化比值 因 test_items 无对应别名→None。
  2) `lookup_quality_goal` 对 "PT" 被「甲状旁腺激素(PTH)」括号代码 PT 子串命中→落到默认10%，且 canon 兜底被阻塞。
- 修复（`qc_service.py`）：
  - `_LIS_ITEM_ALIASES` 新增凝血桥接（pt/pt%/inr/inrratio/aptt/tt/fib/fdp/pc/蛋白c/atiii/抗凝血酶iii/sct/sct标准化比值/drvvt/d-dimer… → test_items 规范名）。
  - 新增 `_token_match` 带词边界的别名词匹配（避免 "pt"⊂"gpt"）；`find_test_item_by_name` 改为「精确优先(_try 先精确后模糊) + 别名桥接规范名精确定位」。**顺带修复**「白蛋白（A）」被「白蛋白」前缀截胡。
  - `lookup_quality_goal` 改为 canon(规范名) 优先于原始缩写，且优先返回非默认(非10%)结果。
  - 上传（`qc_summaries.py`）月结 summary.test_item 改存解析到的规范项目名(`resolved_test_item`)，展示/质量目标更准。
- 验证：`scripts/test_coag_match.py` 用线上 online_backup.json 实跑真实函数，8 个 token 全部对应正确项目(id 125/126/127/132/131/135)，质量目标 7 个非默认(5.0%/6.7%)、SCT 因 quality_requirements 无标准目标→10%(数据缺口非bug)；生化无回退；无反向误命中。
- 部署：已 push + deploy（自动切流量，无灰度）。截至本次对话 `6adafe2` 标记 `coag-item-alias-qgoal-2026-07-26` 尚未在线上 `_diag/build` 生效（仍旧值）——按用户更正，不假定灰度卡死，等用户看 Cloud 控制台真实部署状态。

## 月视图排班表列宽加宽（v9，本次会话）
- **用户诉求**：月视图排班表列太窄，连 3 个字都得换行。
- **根因**：`el-table` 日期列写死 `:width="92"`；单元格人名 `.person`/`.person-chip` 缺 `white-space:nowrap`，被 el-table 默认 cell padding 挤窄后整段折行。
- **修复**（`SchedulingList.vue`）：
  - 日期列 `:width="92" → 120`（给中文人名留整行空间）；
  - `.person`/`.person-chip` 加 `white-space: nowrap`，杜绝 3 字折行。
- **构建标记**：`_BUILD_MARK=scheduling-v9-monthcol-width-2026-07-26`（commit 已 push，部署进行中）。
- 注：状态行多人均为 chip 竖向堆叠（每人一行），加宽后可读性更好；岗位行人名+(早/连/状态)同样不折行。

## 排班月视图早班/连班联动（v10，本次会话）
- **用户诉求**：月视图岗位行里人名下方已显示「早」「连」小字，但下方独立的「早班」「连班」状态行仍是空的，要联动。
- **根因**：`/cell` 保存岗位行时只更新了 `SchedulingAssignment.is_early/is_continuous` 布尔字段，没有同步创建/删除对应的无岗位「早班」「连班」状态行 assignment；`/grid` 也不做投影，所以状态行空。
- **修复**：
  1. 后端 `/cell`：保存岗位行后调用 `_sync_early_continuous_rows()`，根据 `is_early/is_continuous` 同步创建或删除 `status=早班/连班`、`post_id=None` 的 assignment，保证数据一致。
  2. 后端 `/grid`：返回前把岗位行的早班/连班标记投影到对应状态行（去重），立即修复历史未同步数据的显示联动。
  3. 前端 `deletePostCell`：删除岗位行前，先把 `is_early/is_continuous` 置 false，触发后端同步删除联动状态行。
- **构建标记**：`_BUILD_MARK=scheduling-v10-earlycont-link-2026-07-26`（commit 已 push，部署进行中）。

## 凝血质量目标按水平1/水平2区分（docx 权威，commit 1273377）
- **来源**：`@室内质控月小结_2026年06月_9.docx` 的质量目标表（用户指定以 docx 为准，覆盖之前粘贴的表）。关键：**多数凝血项目 1水平与2水平目标不同**（APTT/PT/INR/FIB/TT：水平1=6.5~12%，水平2=10~12%；蛋白C/S/纤溶酶原/dRVVT/SCT/抗凝血酶III 两水平相同）。
- **根因（历史 bug）**：`QualityRequirement.cv` 用 "正常6.5%/异常10.0%" 编码双水平，但 `_extract_first_pct` 永远只取第一个值→所有凝血项水平1/2 都显示 6.5%，且月结上传时 `quality_goal` 按 level 冻结存错（水平2 也记成 6.5%）。
- **修复**：
  1. `qc_service.py` 新增 `_parse_cv_levels`/`_is_level2`/`_extract_level_pct`；`lookup_quality_goal` 与 `_lookup_qr_goal` 全链路透传 `level` 参数；cv 字段在 level2 时取「异常」值、否则取「正常」值。
  2. `_match` 对干净单百分比(`^\d+(?:\.\d+)?%$`)原样保留（匹配 docx 字面：10% 而非 10.0%、5.0%/6.7%/8.3%/11.7% 不动）。
  3. `_all` 改为**并集**四种匹配策略（修 NCCL 精确命中短路掉 BJHR cv 行）；括号代码匹配改**精确**（`rc in ti_codes`，修 "PT"⊂"APTT" 等子串污染误取错 cv）。
  4. `quality_requirements_seed.py`：补全 6 项缺失凝血（血浆蛋白C活性/血浆蛋白S活性/血浆纤溶酶原活性/狼疮抗凝物DRVVT试验/狼疮抗凝物SCT试验/抗凝血酶III，cv=5.0%/8.3%/10%/10%/10%/6.7%）；D-二聚体(D-Dimer) cv `10.0%`→`10%`。
  5. `_LIS_ITEM_ALIASES` 加 `ratio`/`rat`→凝血酶原时间（RATIO 之前无别名→None）。
  6. `qc_summaries.py`：上传落 `quality_goal` 透传 `level`；`_backfill_goals` 改为**重算所有行**（不再跳过非空），用 `lookup_quality_goal(..., level=s.level)` 回填，纠正历史错存的水平2 6.5%。
  7. 新增 `scripts/test_coag_level_goal.py`（15 个 LIS token 按水平核对 + 单测），全 PASS。
- **部署**：已 push + `tcb cloudrun deploy --force`（自动切流量）。`_BUILD_MARK=qc-levelgoal-coag-2026-07-26`。部署后 curl 内网 `/api/v1/_diag/build` 确认标记更新（中途空响应=滚动发布间隙，约+3min 后稳定返回新标记）。
- **线上两步已执行(admin jinzizheng)**：`POST /quality-requirements/_meta/seed` → added:7（含6凝血+1其他，idempotent updated:0）；`POST /qc-summaries/_backfill_goals` → updated:109/222（含6月凝血水平2 由6.5%→10%等）。
- **实跑验证**：GET 6月 APTT：水平1=6.5%/水平2=10%；PT：水平1=6.5%/水平2=10% ✓。质量目标现已按水平正确区分。

## 仪器耗材家族映射修正（commit ea64fdc，已推送部署）
- **用户 23:29 确认的型号家族归属**：
  - AU5800 属于 AU58 家族 → 合并
  - TOP700 含 TOP700A/B/C → 合并为 TOP700
  - 免疫分析仪（罗氏e601/e602）→ 罗氏Cobas6000；罗氏e411、安图A6200 为独立型号（**库内暂未建这两个仪器**）
  - DXA5000 无耗材 → 不显示
- **实现**：新增 `INSTRUMENT_FAMILY_MAP` 精确映射表（替换纯正则，正则处理不了"AU5800→AU58""免疫分析仪→罗氏Cobas6000"这类非后缀归并）。
  - `reagent_management.py` 的 `_extract_instrument_base_model` 改为「①家族映射→②正则(去尾部编号)→③model→④兜底」；`list_instrument_reagents` 分组时跳过映射为 None 的（DXA5000）。
  - `export_instrument_consumables.py` 同步同样映射，重新生成 `仪器耗材对照表.xlsx`（9 个总型号：AU58含3台、TOP700含3台、罗氏Cobas6000 16种等）。
- **构建标记**：`_BUILD_MARK=inst-family-map-2026-07-26-2329`。已 deploy 提交（自动切流量）。
- **待补**：罗氏e411、安图A6200 尚未在仪器库建为独立仪器（目前所有罗氏免疫耗材都归在"免疫分析仪"→罗氏Cobas6000）。如需独立显示这两台，需在仪器管理里新建并重新关联耗材。

## 工作台/排班改造：换班 + 早连班提醒仅推送 + 私密消息隔离（commit fe79921，部署中）
- **4 项诉求全部落地**：
  1. **早班/连班提醒不进工作台**：`reminder_engine.run_reminders` 对 `shift_early`/`shift_continuous` 规则不再 append 到 `notif_active`（即不写 `notifications` 表），仅走 email/微信推送。工作台「提醒事项」与 dashboard 待办数自此不含这两类。
  2. **移除业务台账概览**：`Dashboard.vue` 删除「业务台账概览」卡片。
  3. **今日我的岗位 + 排班表**：`Dashboard.vue` 在「今日我的岗位」卡片加排班表（本周/近两周/本月切换，调 `GET /scheduling/my-schedule`）；`MyScheduleItem` schema + 后端 `_resolve_range`(week=本周一~日/fortnight=本周~下周日/month=本月) 支撑。
  4. **换班功能**：点排班表人名发起。语义=**双向对调**(A 选自己一个班与 B 的班互换)或**顶班**(B 顶 A 的早/连班，单向)；B 在本人账号弹窗确认/拒绝 → 班表改 person。私密消息（`recipient_user_id`）隔离：换班通知只接收人可见；列表/统计/邮件发送均按 `recipient_user_id` 过滤。
- **数据模型**：`Notification.recipient_user_id`（NULL=广播，INT=私密）；`SchedulingAssignment.is_locked`（早/连班已换班锁定）；新表 `scheduling_swap_requests`（from/to_person, from/to_assignment_id 可空, status）。
- **生成保护**：`generate_assignments` 计算 `served_people`（is_locked 的早/连班人），下一轮生成跳过他们且保护锁定行不被覆盖（系统「记住」换过班的人已上过该班）。确认换班时若涉及早/连班则两行 `is_locked=True`。
- **前端**：`SchedulingList.vue` 人名改为可点（openSwap）；新增「我的换班」tab（接收/发起列表 + 确认·拒绝·取消）；`SwapNotifier.vue` 全局轮询（30s）对 `role=pending_in` 弹 `ElMessageBox` 确认/拒绝；`AppLayout.vue` 挂载。api/scheduling.js 加 getMySchedule/requestSwap/listSwaps/confirmSwap/rejectSwap/cancelSwap。
- **构建标记**：`_BUILD_MARK=scheduling-v12-swap-2026-07-26`（注：之前 summary 记为 v11，实际被 ea64fdc 的 family-map 标记覆盖，本轮重设为 v12）。
- **部署铁律重申**：`(echo ""; sleep 3; echo "Y") | tcb cloudrun deploy -e cloud1-0gjhamv53ff2298d -s lab-management --force`；构建源 origin/main（已 push fe79921）；部署后 curl 内网 `http://lab-management-282724-9-1408547492.sh.run.tcloudbase.com/api/v1/_diag/build` 核对标记。
- **部署注意**：`models/__init__.py` 的 scheduling 导入需补 `SchedulingSwapRequest`（否则 `scheduling_swap_requests` 表可能未被 Base.metadata 显式注册；实际因 API import 触发模块加载仍会注册，但显式补上更稳）。`_ensure_missing_columns` 已对 notifications.recipient_user_id / scheduling_assignments.is_locked 兜底补列。
- 验证待办（部署后）：早/连班提醒不进工作台、私密换班消息仅接收人可见、换班确认后班表更新、生成逻辑跳过已换班人。

## 部署验证通过（2026-07-26 23:4x）
- 内网 `/api/v1/_diag/build` 标记已切到 `scheduling-v12-swap-2026-07-26`（约 3.5min 滚动发布后生效）。
- `/scheduling/my-schedule?range=week|month` → 200，响应含 `is_locked`/`post_name`/`group` 正常序列化；month 返回 金子铮 7/27 起排班。
- `/scheduling/swap/list?role=all|pending_out` → 200（scheduling_swap_requests 表已自动建表+查询无误）。
- **换班创建(顶班)E2E**：用 admin 的 assignment(362) 发起 → 朱春阳，request 建成(status=待确认) + 私密通知(recipient_user_id=朱春阳) 落地；admin 的 `/notifications` 列表仅见广播提醒(recipient=None)，**印证私密消息隔离**；`cancel`(发起人)成功置已取消。
- 早/连班提醒「仅推送不进工作台」与生成保护 `served_people` 跳过已换班人为**代码层验证**（需触发提醒引擎实际运行 / 重新 generate 方可端到端实测，逻辑已复核）。
- 注：curl 经 Git Bash 传中文 JSON 会 "error parsing the body"，须写入 UTF-8 文件用 `--data-binary @file` 方可；前端不受影响。

## 尿液分析单位+质量目标按 docx月小结2026-06-12 修订（commit 855bbba，已部署）
- **用户诉求**：`@室内质控月小结_2026年06月_12.docx` 按此修改「单位」和「质量目标」（21 个尿液项目，单水平目标，无水平区分）。
- **改动（855bbba，已 push + deploy，BUILD_MARK=qc-urine-docx-units-goals-2026-07-26）**：
  - 种子 `quality_requirements_seed.py` 新增 `URINE_DOCX_ITEMS`（34 条，含 LIS 原名 + 规范（尿液）名双 key，source=wst403-2024 最高优先级，防止被血清同名项覆盖）；`镁` cv `5.5%`→`8.3%`。
  - 上传 `qc_summaries.py` 的 `QC_UNIT_OVERRIDES` 新增全部尿液单位（类固醇 mg/L / NAG U/L / 总蛋白 g/L / 电解质 mmol/L / 肌酐·尿酸 µmol/L / 白蛋白 mg/L 等；`镁`→mmol/L）。
  - 新增回归测试 `scripts/test_urine_docx_goals.py`（21 docx 项原名+规范名目标核对 + 血清同名不污染）。
- **线上两步（admin jinzizheng）**：`POST /quality-requirements/_meta/seed`（added:36）→ `POST /qc-summaries/_backfill_units`（updated:22）+ `_backfill_goals`（updated:30）。

## 尿液种子血清污染修复：磷/总钙（commit a42d5d7，已部署+线上修复）
- **bug**：855bbba 新增的 `磷（尿液）`/`总钙（尿液）` 种子行，经 `contains_same_item`（尿/尿液=安全修饰前缀）把血清 `磷`/`总钙` 查询也命中，导致线上血清 `磷` 目标被回填为 7.7%（应为 4.0%）。`肌酐/尿素/钠/钾/氯/钙/尿酸/总蛋白` 因早有精确血清行未被污染，仅 磷/总钙 缺精确血清行而中招。
- **修复（a42d5d7）**：种子在 `镁` 后补精确血清保护行 `磷 4.0%`/`总钙 2.0%`（策略1精确命中优先于尿液包含匹配）；测试 SERUM 列表补 `磷`/`总钙`。
- **部署**：`(echo ""; sleep 3; echo "Y") | tcb cloudrun deploy --force`，约 6min 滚动发布后内网 `/_diag/build` 标记切到 `qc-urine-phos-ca-fix-2026-07-26`（中途空响应=发布间隙，等即可，不假定灰度卡死）。
- **线上修复（脚本 live_fix_phos_ca_mg.py，admin）**：seed added:2（磷/总钙精确行）→ 定位 `镁`(wst403-2024) id=33 并 `PUT cv=8.3%`（种子幂等不覆盖已存在行，须手动 PUT）→ `_backfill_goals` updated:5（血清磷 7.7%→4.0%、镁 5.5%→8.3%）；`_backfill_units` updated:0（单位已对）。
- **实跑验证（verify_urine_fix.py）**：血清磷 goal=4.0%/unit mmol/L；尿磷 7.7%/尿钙 10.3%/尿尿素 7%/尿肌酐 5.7%(µmol/L)/尿镁 8.3%/尿钾 9.7%/尿钠 8.7%；类固醇 mg/L·NAG U/L·尿微量总蛋白 g/L(14.7%)·微量白蛋白 mg/L(10.0%)·尿ɑ1微球蛋白 mg/L(8.3%)·尿球蛋白G mg/L(6%)·尿淀粉酶 U/L·尿氯 mmol/L(8.7%)·尿糖 mmol/L(6.7%)·尿尿酸 µmol/L(8%)·尿香草扁桃酸 mg/L——全部与 docx 一致。血清总钙无 qc 行（无需修，保护层已就位）。

## 免疫分析仪按 model 细分（commit 751007c，已推送部署）
- **用户确认**：仪器档案里罗氏e411(id=16, model=罗氏e411)和安图A6200(id=75, model=安图A6200)都已存在，耗材**不通用**于 Cobas6000(e601/e602)。
- **实现**：新增 `_IMMUNO_MODEL_MAP` 覆盖表——当 name='免疫分析仪' 时，按 model 二次区分：
  - `model='罗氏e411'` → **罗氏e411**（独立）
  - `model='安图A6200'` → **安图A6200**（独立）
  - 其余（e601/e602）→ **罗氏Cobas6000**
- **线上数据现状**（查库确认）：17 条免疫耗材关联**全挂在 instrument_id=14（罗氏e601）**上；e411(id=16) 和 A6200(id=75) 当前**零条耗材关联**。所以当前表里 e411/A6200 不会单独出现——等用户在系统里给这两台仪器添加各自的耗材关联后，新代码会自动按 model 分离显示。
- **构建标记**：`_BUILD_MARK=inst-immuno-model-split-2026-07-26-2342`。已 deploy 提交。

## 排班：自服务休息申请 + 非组长只读化（commit b7c61d8，部署中）
- **4 项诉求全部落地**：
  1. **工作台换班入口**：`Dashboard.vue`「今日我的岗位」加「换班申请」「休息申请」按钮 → 跳 `/scheduling?tab=swap` / `?tab=rest`（SchedulingList 读 `route.query.tab` 直达）。
  2. **自服务休息申请**（无需审批）：新表 `scheduling_rest_requests`（person/date/status[生效中|已取消]/assignment_id/note）；`POST /rest-request` 用 `get_current_user` 取本人，写入 `status=休息, post_id=None` 的 assignment 行（每条申请独占其行，`plan_id=0` 免受 generate 覆盖）；`POST /rest-request/{id}/cancel`（仅本人）删除独占行 + 置已取消；`GET /rest-request/list?scope=mine|all`。
  3. **休息花名册**：`GET /rest-roster?range=week|fortnight|month` 返回该范围所有 `status=休息` 人员+日期；`Dashboard.vue` 在卡片内按 range 展示「本范围休息人员」。
  4. **权限**：排班写角色由 `['admin','specialty_leader']` → `['admin','specialty_leader','leader']`（4 处：scheduling.py WRITE_ROLES / module_permission.py DEFAULT / store/auth.js FALLBACK / store/permission.js FALLBACK）；`module_permissions` 表经 main.py 增量 seed 幂等补齐 `leader` 行。**非组长(member)仅可查看、点人名换班、申请休息**；SchedulingList 用 `isManager=auth.canWrite('scheduling')` 隐藏 批量录入/岗位定义/排班计划/设置 页签 + 月视图「生成白班」按钮 + 单元格编辑(openCellEditor 提前 return)，新增「我的休息申请」tab（发起/取消）。
- **构建标记**：`_BUILD_MARK=scheduling-rest-selfserve-readonly-2026-07-26`（b7c61d8 已 push）。部署：`(echo ""; sleep 3; echo "Y") | tcb cloudrun deploy -e cloud1-0gjhamv53ff2298d -s lab-management --force`。
- **验证待办（部署后）**：① `/_diag/build` 标记更新；② `POST /rest-request` 写休息 + `/rest-roster` 返回；③ 非 member 账号非组长页签隐藏、member 仍可换班/申请休息；④ cancel 移除休息行。

## 排班改造 E2E 实测复核（本轮续，build 标记 scheduling-rest-ui-live-2026-07-27）
- 内网 `/_diag/build` 标记稳定为 `scheduling-rest-ui-live-2026-07-27`（最新，部署生效确认）。
- **后端 E2E 全过（admin jinzizheng 实测）**：
  - `POST /scheduling/rest-request {date:2026-08-05}` → 200，写入 assignment_id=499、rest id=2、person=金子铮、status=生效中；
  - `POST /scheduling/rest-request/2/cancel` → 200，status=已取消；
  - `GET /scheduling/rest-roster?range=week|fortnight|month` → 全部 200；fortnight/month 返回 18 条真实休息（姚建民/徐晓琳/杨静/秦东芳/夏立娇/赵瑞…）；week=0（当前周 7/20–7/26 无人休息，休息均从 7/27 起，符合预期）。
  - 注：首次误用 `/rest-roster`(缺 `/scheduling` 前缀) 报 404，加前缀后即 200——纯测试路径笔误，非代码缺陷。
- **结论**：自服务休息申请（写/取消）、休息花名册（三档区间）、换班入口、非组长只读化 四诉求后端全在线；前端 dist 已随 d152fba 强提交并部署（此前已确认新 Dashboard-*.js 上线）。**遗留：UI 交互层（按钮跳转/只读页签隐藏/休息申请表单提交取消）建议用户在前端实际登录实测**。
