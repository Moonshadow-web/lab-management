# 2026-07-26 工作日志

## 试剂管理责任库切换修复
- **问题**：用户反馈 LibraryTabs（生化凝血/免疫）点击不切换。
- **根因**：`el-radio-group` 使用 `:model-value`(computed) + `@update:model-value` 单向绑定，Element Plus 2.7.6 下点击后内部状态与外部 prop 竞争导致视觉不切换。
- **修复**：改用 `v-model="selected"`(本地ref) + `@change` + watch 双向同步 store（commit `28cd696`）。

## 盘库/订购模板全面改造（commit `e2487d2`）
### 后端 build_reagent_template 改进：
1. **停用过滤**：仪器 status!='停用'、试剂 is_active=True，停用项不再出现在盘库/订购模板中。
2. **流水线排除**：仪器名含"流水线"的不显示耗材组（用户确认流水线无耗材）。
3. **抗凝血酶关联修复**：手动创建 TestItemReagent(id=574)：test_item_id=131(抗凝血酶III) ↔ reagent_item_id=150(抗凝血酶测定试剂盒)，role=试剂。之前在孤儿组现归入正确项目。
4. **DTO 扩展**：每条 item 带 project_name + project_aliases，供前端模糊检索匹配英文别名(ALT/ATIII等)。
5. **仪器标签格式化**：名称(model)，如 AU58-1 → AU58-1(贝克曼AU5821 A)。

### 前端 InventoryCheck.vue + ReagentOrders.vue 改进：
1. **分类下拉**：搜索栏上方加 el-select（全部分类 / 试剂·校准品 / 耗材 / 质控品），独立于模糊检索。
2. **模糊检索增强**：匹配试剂名/规格/材料编码 + 项目中文名 + 英文别名（逗号分隔），搜 ALT 能命中丙氨酸氨基转移酶。
3. **材料编码列**：所有表格新增「材料编码」列（打印也带）。
4. **项目标题带别名**：如「项目：丙氨酸氨基转移酶（ALT,谷丙转氨酶）」。

## 部署
- 上轮灰度卡死「不存在灰度中的版本或灰度版本部署未完成」（3个AI抢部署互相打断）。
- 用 `(echo ""; sleep 3; echo "Y") | tcb cloudrun deploy --force` 重新干净部署，确认新版本已生效：
  - `/reagent/template?library=生化凝血` 返回分组数据 ✓
  - 试剂目录 library 过滤：生化凝血=142条、免疫=170条 ✓
- 部署铁律重申：第一提示空回车=不进灰度自动切流；第二提示Y=取消冲突任务。

## 待关联建议接口 + 匹配改进（commit `12d2ce2`）
- **新增后端 `GET /reagent/associations/suggestions`**：返回 test_item_suggestions / test_item_orphans / instrument_suggestions / instrument_orphans 四组。供关联页「待关联建议」标签页审核采纳。停用仪器与停用试剂不参与匹配。
- **匹配改进（关键踩坑）**：原 `_norm_match` 未能识别「抗凝血酶III ↔ 抗凝血酶测定试剂盒」（因试剂名含"测定试剂盒"而项目名带罗马数字后缀 III，互相不为子串 → score=0 漏匹配）。新增 `_core_name()` 去括号+罗马数字后缀（"抗凝血酶III"→"抗凝血酶"），并抽出 `_best_test_item_match()` 供 auto_match 与 suggestions 共用。离线验证：抗凝血酶测定试剂盒→抗凝血酶III score=4；267个非耗材试剂中可匹配174、无候选93。
- **前端**：
  - 项目标题去掉别名显示（用户明确"别名只用于检索"）：InventoryCheck/ReagentOrders 的「项目：xxx（别名）」改「项目：xxx」；`project_aliases` 字段保留仅供 matchItem 模糊检索。
  - ReagentAssociations.vue 新增「待关联建议」标签页：四块表格，每行预填系统候选，可一键采纳 / 改选后采纳 / 批量采纳全部建议。

## 移动端录入优化（commit `df1e980`）
- **盘库弹窗**：三个分组表格去掉「材料编码/单位/当前库存」三列，仅留 名称 + 盘点余量，避免手机横向滑动。
- **订购弹窗**：录入表格改为竖向卡片（.entry-card：名称换行 + 订购数量输入在右，≤480px 时整体竖排），不横向滑动、"显示不下多显示几行"。
- 两个弹窗 width 改为 `min(960px, 96vw)` 适配窄屏。
- 注：打印/空白页仍保留材料编码/单位/当前库存（A4 不受影响）。

## 质控上传规则解析【关键 bug 修复】（commit `67033c3`）
- **现象**：AU5800 等 LIS 导出含规则列（如「失控规则」），原表标了很多失控点（β2微球蛋白 1-3S、10-x 等），但系统没解析出来。
- **根因**：上轮 step1.5 把「解析失败」与「空单元格」混为一谈。一旦 rule_column_present=True，任何未解析的单元格都被强制清零为在控并冻结 R-4s —— 而 LIS 规则值写法（1-3S(失控)、10-X、1－3S 全角、13S 无连字符等）恰不被旧解析器识别，于是 LIS 标过的失控点被全部抹掉。
- **修复**：
  1. 仅「真正空」单元格在含规则列时判在控；有内容但未识别的单元格保持后端 Westgard，不误翻。
  2. 解析器增强：全角/异形标点(－～–:)转半角、去外层括号、子串提取（1-3S(失控)/失控1-3S）、无连字符写法(13S/10X)。
  3. 上传响应新增 `rule_cells_total/recognized/unrecognized` 诊断计数，便于核对 LIS 写法是否被识别。
- **数据恢复**：原上传已把原始规则串存进 `qc_daily_values.uploaded_rule`，且 rule_column_present 已落库；修复部署后直接调用 `POST /qc-summaries/_recalc`（admin/qc_manager）即可用新解析器重算，无需重新上传（前提是当时规则列被识别、rule_column_present=True）。若 rule_column_matched=False 则需重新上传。
- 测试 11→14 断言全过（用例H 未识别写法→保留后端；用例I/J 写法变体解析）。

## 质控上传 500 故障（commit `95e3fd2`）
- **现象**：质控 LIS 数据上传返回 500（Request failed with status code 500）。
- **根因推断**：上一轮 QC 部署（含 `rule_column_present` 模型列，commit `6ae817d`/`67033c3`）因镜像推送 broken pipe 中断且任务被清理、未确认成功，线上服役容器仍是更早的 `scheduling-v7-matrixwidth` 构建（build 标记从未随 QC 提交更新）。结果：代码引用了 `qc_monthly_summaries.rule_column_present` 列，但非空的线上 MySQL 表缺该列 → 上传时 `Unknown column` → 500。
- **注意**：`main._migrate_schema` 的 `alters` 字典块只跑 SQLite `PRAGMA`，**MySQL 下完全不生效**；非空线上表的补列只能靠 `_ensure_missing_columns()`（dialect 无关）。当该次部署中断时，补列没执行。
- **修复**：在 `_ensure_missing_columns()` 末尾**显式兜底补列** `qc_monthly_summaries.rule_column_present` 与 `qc_daily_values.uploaded_rule`（幂等）；并把 `diag._BUILD_MARK` 改为 `qc-ruleparser-fix2-2026-07-26`，使 `curl /_diag/build` 能确认新代码是否生效。
- **部署铁律强化**：每次改模型/解析/关键逻辑提交，**必须同步改 `_BUILD_MARK`**，且部署后必须 curl 内网 `_diag/build` 核对标记；broken pipe 等中断需重试至确认成功，不能只看任务被清理就假定成功。
- 状态：95e3fd2 已 push，重部署中（任务 uDkK4d）。

## 上传 500 阻塞排查（续）：卡死的灰度发布
- 95e3fd2 部署任务(uDkK4d)虽"成功"，但 cloudrun list 的 **Update Time 仍是 11:14:10 未变**，`/_diag/build` 标记仍 `scheduling-v7-matrixwidth` → 新代码根本没生效。
- **根因**：存在一条**状态异常的卡死灰度发布**(Gray release in progress / 状态异常)，阻塞新版本发布。
  - `tcb cloudrun traffic promote` → `[ReleaseGray] 状态异常，无法发布`；
  - `tcb cloudrun traffic rollback` → `[OperateServerManage] 当前任务已结束或停止`（也清不掉）。
  - CLI 无別的清灰度手段。
- **现象关联**：卡死的灰度 + 不一致服役容器，正是上传 500 的持续原因（线上 pod 仍引用缺列）。
- **应对**：重跑整轮 `tcb cloudrun deploy --force`（部署流程的发布环节与 traffic 子命令不同，可能覆盖卡死灰度）；若仍卡，需到 CloudBase 控制台手动「取消灰度/回滚/重新部署」或提交工单。
- 教训：update time 不随 deploying 改变 + build 标记陈旧 = 发布链路被卡死，不能只看 deploy 命令回显。
- 已停止卡死任务 4CwKd0（运行 1m29s 后清理），改起新任务 S9USQw 重试整轮部署（看能否覆盖卡死灰度）。若 deploy 仍卡在 release 步/Update Time 不变，则 CLI 无解，需用户到 CloudBase 控制台「云托管 → lab-management → 灰度/版本」手动取消灰度或重新部署，或提工单。

## 上传 500 真因：4-1s KeyError（已修复）
- 用户再次上传报 "Internal Server Error"。利用此前部署的全局异常捕获 + `GET /_diag/last-errors` 端点，curl 内网拿到真实 traceback：
  `KeyError: '4-1s'` @ `qc_service.py:395 _resolve_uploaded_rules` → `sorted(set(ooc_rules), key=lambda x: -_RULE_SEVERITY[x])`。
- **根因**：`_OOC_RULES` 与归一化表含 `4-1s`（Westgard「连续4次同侧」），但严重度字典 `_RULE_SEVERITY` 漏项 → 单元格出现 4-1s 即 KeyError 整次上传 500。用户那张 AU5800 6月表里有 4-1s（之前也含 β2微球蛋白 1-3S、10-x）。
- **修复（commit 3eff59a，已部署，build 标记 `qc-41s-keyerror-fix-2026-07-26`）**：
  - 补全 `_RULE_SEVERITY["4-1s"]=2`（与 R-4s 同级，判失控）；
  - 解析器排序改 `_RULE_SEVERITY.get(x, -1)` 防御化，未来漏项不再崩溃；
  - 新增用例K 校验 4-1s 解析判失控；测试 14→15 断言全过。
- **部署 gotcha（再次印证）**：首次部署 3eff59a 后 curl 标记仍是 `reagent-usage-v2`（父提交 c6cb66a）——CloudBase 构建从 git 拉取，push 同步延迟导致拉到旧提交；**重部署一次**（取消运行中任务、部署最新代码）后标记才变为 `qc-41s-keyerror-fix`，确认新代码生效。结论：部署后务必 curl `/_diag/build` 核对标记，标记旧=没生效，要重部署。
- 用户现在只需重新上传那张 6月 AU5800 表即可（修复后的解析器会正确解析 4-1s / 1-3S / 10-x 等，无需再手动 _recalc）。

## LIS 名称别名映射 + 白蛋白（A）质量目标（commit 042105c，待上线）
- 用户要求：直胆红素=直接胆红素、腺苷酸脱氨酶=ADA、转肽酶=GGT、谷草转氨酶=AST、谷丙转氨酶=ALT、低密脂蛋白=LDL、高密脂蛋白=HDL；并按此对应上+补充质量目标；白蛋白（A）单位 g/dl、质量目标同糖化白蛋白(GA)。
- **根因**：质量目标种子(quality_requirements_seed.py)里这些项目本就有标准值（直接胆红素8%/ALT·AST6%/GGT3.5%/HDL·LDL6% 等），用的却是全称；LIS 用简称/缩写，contains_same_item 对「转肽酶 vs γ-谷氨酰基转移酶」等互不包含写法匹配不到 → 质量目标落空。
- **改动（042105c，已 push）**：
  - 新增 `_LIS_ITEM_ALIASES` + `_canon_item_name`：LIS 简称/缩写→规范名桥接；`find_test_item_by_name` 与 `lookup_quality_goal` 增加规范名兜底匹配。
  - `_lookup_qr_goal` 精确/安全包含匹配归一化括号（白蛋白（A）与 白蛋白(A) 互通）。
  - 种子新增「白蛋白（A）」：cv 6.7%（同糖化白蛋白 GA）、unit g/dl（bj-hr-2025 源）。
  - 测试 scripts/test_item_alias.py 21 条断言全过。
- **上线后两步（用 admin 调）**：① `POST /api/v1/quality-requirements/_meta/seed`（幂等灌库，新增白蛋白（A）条目）；② `POST /api/v1/qc-summaries/_backfill_goals`（回填已存月结行的质量目标，使直胆红素/ALT 等拿到正确目标）。
- **部署卡死（同 500 那役）**：042105c 两次部署均"submission completed"但 `/_diag/build` 标记仍 `reagent-fe-retry-2026-07-26-2115`（父提交 6b7017b）。`traffic promote`/`setTraffic --stable 100` 仍报「状态异常/不存在灰度版本」——**卡死灰度发布僵尸态再现**，CLI 无法清除。需用户在 CloudBase 控制台「云托管 → lab-management → 版本/灰度」手动取消/结束异常灰度（或回滚到稳定版本），再重部署，标记才会变 `qc-item-alias-2026-07-26`。

## 试剂用量排序/显示停用/盘库删除/订单实时库存（本轮，代码全完成、部署受阻）
- **用户原始诉求**：①目录/盘库/订购模板默认按年用量降序（参考去年采购明细 xlsx）；②试剂目录"显示停用"开关，停用的不显示、可单开；③盘库加删除操作；④新建订单显示实时库存便于判断是否订购。
- **后端（全部完成并线上实测通过）**：
  - `annual_usage` 字段 + `_ensure_missing_columns` 自动迁移；`POST /reagent/items/_batch-update-usage` 批量导入（按材料编码匹配，182 条→181 项命中，0 未匹配）。
  - 目录列表 `show_inactive` 过滤（默认隐藏停用，开则显示）；默认按 `annual_usage` 降序。
  - 盘库 `DELETE /reagent/inventory-checks/{id}`（实测 200 删除成功，再 GET 404）。
  - `build_reagent_template` 改为 by_project/by_instrument/controls 三组、组内按年用量降序（实测 77 项目组全部降序）；DTO 带 `current_stock`（订单实时库存来源）。
  - **数据已灌入线上**：181 条试剂年用量已导入 MySQL（`annual_usage`），目录/模板排序实测生效。
- **前端（代码写完并本地构建）**：ReagentItems 加"显示停用"开关+年用量列；InventoryCheck 加删除操作；ReagentOrders 卡片显示实时库存；移动端优化（盘库隐藏冗余列、订购竖卡片）此前已完成。
- **关键坑——云端前端构建缓存/克隆陈旧**：多次干净部署后后端标记/功能均上线，但前端 JS 仍是旧 `index-BWDfa38v.js`（无"显示停用/entry-card"等）。定位：CloudBase 对 `frontend/` 子树按 lock 缓存且克隆未随 .vue 改动刷新，多阶段 Docker 的 frontend-builder 阶段复用了旧 dist。
  - **绕开修法（已提交 e262ba7）**：本地 `npm run build` → 把 `frontend/dist` 强制提交进 git → Dockerfile 删除 frontend-builder 阶段、改为直接 `COPY frontend/dist ./frontend/dist`。这样前端产物随 git 走，不再依赖云端前端构建。
- **部署受阻根因（与上一役同）**：① **卡死灰度发布僵尸态再现**——控制台报 `DescribeCloudBaseBuildService` "Version not found"，CLI 的 promote/setTraffic 均报"状态异常/不存在灰度版本"，无法清除；② **本轮有 3 个 AI 并发部署互相覆盖 main**：我的推送（含 e262ba7 的 dist）被反复顶掉，`origin/main` 已变成未知引用、线上标记被别的 AI 改成 `qc-item-alias-2026-07-26`。
- **结论（已更正）**：代码层面（后端+数据+前端源码+预构建 dist+Dockerfile 绕开前端缓存）**已全部就绪且本地完好**（本地 HEAD=49b6f16，e262ba7 含 frontend/dist）。「卡死灰度/需手动取消灰度」为误判（见文末更正）；按用户指示正常部署即可。
- **注意（已更正，见文末）**：原「不要再盲目 deploy / 先解决控制台灰度卡死」为误判。用户澄清灰度卡死问题不存在；遇到部署疑似未生效，应直接问用户并等其核对 Cloud 真实情况，然后正常部署。

## ⚠️ 更正：所谓「卡死的灰度发布」其实不存在（用户 2026-07-26 晚澄清）
- 用户明确澄清：**「卡死的灰度发布/状态异常无法发布」这个问题实际上不存在**，是我此前的误判。CLI 的 `promote`/`rollback`/`setTraffic` 报「状态异常/不存在灰度版本」**不代表真的卡死**，是我对 CLI 行为与部署状态理解有误。
- **正确做法（写进长期记忆 MEMORY.md）**：部署疑似未生效/流量未切时，**不要自己下「灰度卡死」结论**，也**不要据此让用户去控制台手动取消灰度/回滚**。应**直接问用户**，并**等用户去 Cloud 控制台查看真实部署情况**（版本/流量/构建状态）后，再据反馈判断。
- 唯一可靠的「部署是否生效」判据仍是 `curl` 内网 `/api/v1/_diag/build` 看 `_BUILD_MARK`。标记未更新时，先和用户确认真实情况，再决定下一步，而非假定灰度机制故障。
- 推论：本日日志里多处「卡死灰度发布僵尸态」「需用户手动取消异常灰度」等描述均为误判，应以本更正为准。

## 干净部署尝试（用户 21:02 指示"部署吧"）
- 已提交并推送 origin/main `97a3523`，`_BUILD_MARK=reagent-full-deploy-2026-07-26-2102`（试剂用量排序/显示停用/盘库删除/订单实时库存全部特性）。
- `tcb cloudrun deploy --force` 已成功**提交**（CLI 回显 submission completed，自动切流量=不进灰度）。
- 用户明确工作流：**不要盲目轮询等待**；灰度卡死问题不存在，部署疑似未生效时直接问用户、等其到 Cloud 控制台核对真实情况后再判断。
- 截至 21:10 内网 `/api/v1/_diag/build` 仍返回旧标记 `scheduling-v8-postid-nullable-2026-07-26`（云端构建/发布可能仍在进行，或需用户核对）。**等待用户查看 Cloud 部署状态后回话确认**，不自行下结论。
