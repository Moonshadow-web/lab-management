# 2026-07-20 工作日志

## 提醒事项 per-user 已读 + 移除发送邮件按钮（已部署）
- 新增 `notification_reads` 表（backend/app/models/notification.py）：user_id + notification_id 唯一约束，按账号记录已读。
- backend/app/api/v1/notifications.py：list_notifications / mark_read / mark_all_read / /send 全部改为按当前登录用户判定与写入已读（外连接 notification_reads 子查询，IS NULL 判未读）。前端不再依赖全局 Notification.is_read 列。
- 前端 Dashboard.vue：删除「发送提醒邮件」按钮 + onSendEmail + sending 状态 + import sendNotificationEmails；notifications.js 清理废弃导出。
- 提交 5ee51ac，部署 build=per-user-notif-read-2026-07-20（已上线）。
- 去重修复 ad95285（reminder_engine 清理旧版 instrument_calibration/eqa_return 通知）此前已部署。

## 关键纠正：系统已切 MySQL，验证须基于 MySQL
- 用户指出系统当前使用 CloudBase TDSQL-C MySQL（MEMORY.md #数据库与持久化 2026-07-20 变更），不是 SQLite+CFS。
- 之前关于「CFS 持久卷是命根子」「CFS 连着两天坏了怎么办」的讨论全部基于过时认知——volumes 已清空、CFS 挂载问题不再发生（数据在云数据库，与容器生命周期解耦）。
- 本次改动验证：先只在 SQLite 内存库验建表（不够）；后确认 main.py 启动逻辑对 MySQL 安全（PRAGMA/sqlite3 自愈代码均被 try/except 包裹，create_all 对 MySQL 自动建表），并实测 per-user 隔离查询逻辑（IS NULL/OUTER JOIN/ORDER BY 布尔表达式在 MySQL 合法）。部署后 build mark 切新即证明 MySQL 建表+启动成功。
- 教训：任何 schema/查询改动，验证应直接面向 MySQL（或至少确认 main.py 的 create_all 路径与查询 SQL 对 MySQL 合法），勿以 SQLite 内存库代替。

## 端到端验证 per-user 已读隔离（2026-07-20 22:5x，已用 jinzizheng 实测）
- 用户提供了账号 jinzizheng / Jzz6827556，实测可登录：该账号即 user id=2（管理员金子铮），roles=admin,specialty_leader。此前 401 是因用户名搞错，密码正确。
- 验证步骤：① jinzizheng 标记通知 1755（全自动凝胶电泳仪 Hydrasys 2 校准提醒）已读 → 回显 is_read=True（证明 notification_reads 在 MySQL 正常写入/回读）；② 建临时账号 iso_test_b(id=22) 登录 → 其视角下 1755 is_read=False，unread_only 过滤仍含 1755（未读总数 13=全部）→ 证明 A 已读不影响 B；③ 删除临时账号后登录被拒(401)。
- 结论：per-user 已读隔离在 MySQL 生产环境确实生效。验证脚本当场删除，未留测试数据。
- 备注：本次测试把 jinzizheng 的 1755 标记为已读（真实提醒），若用户希望恢复未读，需补一个「标记未读」接口或清 notification_reads 行（暂无 un-read API）。

## 试剂管理模块 bug 修复（2026-07-20 晚）
用户说"很多问题我已修复，你就看看试剂管理模块的问题"。排查后修复 6 处：
1. **Excel 导入字段名错配**：前端 `ReagentItems.vue` `fd.append('_file',…)` 但后端参数已因 Pydantic v2 改为 `file`（c73e2f5），→ 422。改为 `'file'`。
2. **到货接收必填缺失**：schema `ReceivingBase.receiver` 原必填，前端不传 → 422。改为 `Optional`，后端 `data.receiver or user.full_name` 兜底；前端 form 默认填当前用户。
3. **月消耗计算逻辑错乱**：原 `calculate_consumption` 把查询结果 list 赋给整数列 `total_received`（必崩），消耗公式写成 `opening-closing`，且"本月入库"未从 Receiving 取。重写为 `consumption=opening+received-closing`：closing=当前库存合计，received=当月 Receiving 合计，opening=上月期末结余；无数据不落库避免空行。
4. **订购导出未实现**：`export_order_form` 原返回占位 JSON，前端仅弹"实现中"。改用 openpyxl 生成含材料编码/名称/规格/品牌/数量/单位的设备科订购表并 StreamingResponse 下载；前端 `onExport` 触发 blob 下载。
5. **库存预警筛选无效**：`list_stock` 的 `low_stock_only` 收了没用。改为 join ReagentItem 按 `min_stock>0 且 quantity<min_stock` 过滤。
6. **6 子页仅 1 个可导航**：AppLayout 菜单加子菜单，试剂管理下挂全部 6 页（template 支持 el-sub-menu）。
- 后端 py_compile 通过；前端 vite build 验证中。尚未部署（待用户择机）。

## 室间比对 必做项目「状态/下次应做」自动判定（2026-07-20 深夜，已部署）
- 背景：mandatory_projects() 实时读库，按 item 名匹配最新 plan，状态由 plan.status 推导（done→已完成+顺延半年；其它→进行中+同周期不推进）。但录入结果(save_results)与生成报告(generate_report)都不置 status=done，导致仅靠录入不会显示「已完成」、下次应做不推进。
- 改动：backend/api/v1/interlab.py 的 generate_report 生成报告后 `p.status="done"`（生成即视为完成）。
- 保留手动切换：前端 InterlabComparison.vue 计划表单新增「状态」radio（进行中/已完成），openPlanCreate 默认 draft、openPlanEdit 回填 row.status，savePlan 经 updateInterlabPlan 写回（需 qc_manager/admin）。切回 draft 后可重新录入。
- 前端 vite build 通过；提交 69d1d42 推送 main，已 deploy。
