# 2026-09-17 工作日志

## 岗前培训考核及授权表页脚修正（commit eaa40a8b）
- 需求：人员能力管理 → 岗前培训考核及授权表，页脚「**失效日期**」改为「**生效日期**」。
- 位置：`frontend/src/views/training/staff/PreJobAuth.vue:513`（该打印模板的页脚常量）。
- 改后：`表格编号：BG-SM-PX-002　　　　民航总医院检验科生化免疫组　　　　生效日期：2026.9.15`
- 背景：全项目其他受控表（BG-SM-PX-006 培训签到表、室间比对报告等）都已是「生效日期」，唯独这张漏改，属遗留不一致。**日期 2026.9.15 未动**（用户只要求改标签）。
- `_BUILD_MARK=prejob-auth-footer-effective-date-2026-09-17`。
- **并发部署坑（本次再现）**：提交时发现 `_BUILD_MARK` 已被别的会话改成 `instr-group-code-2026-09-16`（对方提交 InstrumentBase 加 group_code、PPTX 预览等），我的 sed 目标字符串不存在导致静默失败。
  处置：①`git fetch` 确认自己提交已推送；②重新按当前标记值 sed；③部署时 tcb 提示「有部署在跑，是否取消」→ 需要**再跑一次**才真正提交；④首次验证仍是旧标记，**再等 4 分钟**才翻面。
  经验：**并发环境下部署要留足 5-10 分钟并验证两次以上**，别看到旧标记就以为失败。
# 2026-09-17 工作日志

## 项目SOP 101-154「血清+血浆」统一改为「血清」（已完成，42 个文件）
- 需求：把 101-154 中"血清和血浆"类表述统一改为"血清"；例外：**葡萄糖(101)、CK-MB、肌红蛋白、肌钙蛋白、BNP 为血浆项目保留原文**。
- 5 种写法全部替换：`血清和血浆`/`血浆和血清`/`血清或血浆`/`血清、血浆`/`血清/血浆` → `血清`。共 53 个文件待处理，实际改动 42 个。
- **排除逻辑坑**：最初按关键词"葡萄糖"排除，误伤 **SM-SOP-126 NAG（N-乙酰-β-D-氨基葡萄糖苷酶）**；改为**按编号 101 精确排除**血糖才对。
- ⚠️⚠️ **重大坑：Word COM `Find.Execute` 的 Replace 会把文本改坏**（如 `血清/血浆` → `血清/血浆血清`，在末尾追加而不删除原文）。**已从备份恢复后改用 XML 层替换**：
  - 34 个文件的目标文本在**单个 `<w:t>` 内** → 直接字符串 replace 即可；
  - 8 个文件**跨 `<w:t>` 标签** → 按 `<w:p>` 段落取所有 `<w:t>` 拼接 → 替换 → 回写首个 `<w:t>`、其余清空（段落级格式以此统一）。
  - zip 层重写 docx 时保留其他所有部件（`data = {n: z.read(n)}` → 逐项写回）。
- 验证：101-154 中仅 101（葡萄糖）保留"血清/血浆"；**无"血清血清"类重复损坏**；抽查 111/112/102 正确（111 检验目的已从"人血清和血浆中"→"人血清中"）。
- 备份：`生免组文件\_backup_项目作业指导书_血清血浆`（整目录 178 个文件）。
- **经验**：批量改 docx 文本，**优先 XML 层字符串替换**（先判断目标是否在单 w:t 内），比 Word COM 的 Find/Replace 可靠得多；Word COM 仅适合"整段文本替换"且需替换后逐一校验。

## 备份目录已清理（2026-09-17 00:45）
- 用户确认三项修改结果无误，已删除三个临时备份目录（共 356 个文件 / 约 30MB）：
  `_backup_项目作业指导书_字号`、`_backup_项目作业指导书_血清血浆`、`_backup_记录表格_页脚`。
- 现 `D:\民航总医院\15189\生免组文件` 目录：通用作业指导书 24 / 项目作业指导书 178 / 仪器作业指导书 31 / 记录表格 90 + 变更记录 docx。
- 注意：**备份已删，字号/血清/页脚三项修改不可回退**。

## 不确定度模块：支持「单一水平质控」计算 u_Rw（定性项目，已改并部署）
- 起因：用户问定性项目（HBsAg，有质控 COI/S-CO 值）如何评定不确定度，且要求**单水平也能算**。
- 原限制：`calc_single_u_rw` 有 `if len(levels) < 2: return 0.0` → 只填 L1 时 u_Rw=0，算不出来；前端 `singlePreview` 同样要求 `l2_n>=2`。
- **改动**：
  - 后端 `uncertainty.py::calc_single_u_rw`：水平数下限 `< 2` → `< 1`。单水平时公式自然退化为 **u_Rw = RSD_L1 = SD/Mean×100**（合并公式 √[Σ(RSD²(n-1))/Σ(n-1)] 在 1 个水平时 (n-1) 约去）。
  - 前端 `UncertaintyAssessment.vue`：`singlePreview` 改为 L1 必填、L2/L3 可选；公式文本按实际参与水平动态拼（单水平显示 `u_Rw = RSD_L1`）；表单提示补充定性项目说明。
  - 提交 `9d48eb75`，build mark `uncert-single-level-2026-09-17`。
- 冒烟：`f(2.5,0.25,30, 0,0,0)` = 10.0000（RSD 10%）；`f(2.5,0.25,30, 5,0.25,20)` = 8.3853（与手算一致）；n<2 返回 0。
- **评定要点（回答用户）**：定性项目仪器输出 S/CO 本质是定量值，方法学同定量；u_c=√(u_Rw²+u_cal²+bias²)，U=2u_c；HBsAg 用阳性质控 COI 的均值/SD/n 填 L1，L2 留空；真正关键是**临界值(cutoff=1.0)附近**的可靠性 → 灰区概念，弱阳性质控比高值质控更代表临界区。

## PPTX 在线预览（方案 A：前端直接渲染）—— 已跑通并交付（commits 1fe3b5a7 / d52fcf7d）
- 依赖 `pptx-preview`（`mod.init(dom, {width,height})` + `await preview(arrayBuffer)`），挂在 `frontend/src/components/AttachmentPreview.vue`。
- **两个卡壳点（根因，勿重犯）**：
  1. **`loading` 未先置 false**：模板是 `<div v-if="loading" v-loading>` + `<template v-else>`，`mode='pptx'` 时 loading 仍为 true → 内容区（含容器）根本没渲染 → `pptxBox.value` 恒为 null → 一直报"预览容器未就绪"。
     修法：pptx 分支里 **设完 mode 立刻 `loading.value = false`**，再 `for` 轮询 60 次 ×50ms 等 `pptxBox` 挂载。（原 `loading=false` 只在 `finally` 执行，救不了。）
  2. **兜底块与 pptx 同屏**：`<div v-else class="other-preview">` 是 v-if 链兜底，`mode==='pptx'` 不匹配前三项 → 落到 v-else，"无法在浏览器内直接预览"和幻灯片同时出现。
     修法：把 pptx 容器提到链外，兜底改为 `<div v-if="mode === 'other'">`。
- 验证：`outputs/wjx/test_pptx_preview.cjs`（Playwright，用真实课件 `BS-2000M生化分析仪介绍.pptx` 走 组内培训→编辑→课件页签→预览）。结果：容器 HTML 780 万字符 / 275 个图形元素 / 文本正确 / 无兜底残留，截图 `outputs/PPTX预览_实测.png`。
- 回归：`outputs/wjx/regress_preview_formats.cjs` 确认 docx（培训考题/效果评价）仍走 `.preview-html` 正常渲染。
- ⚠️ 遗留：**旧版 `.ppt`（OLE2）仍无法在线预览**，已给出"请另存为 .pptx"提示。
- ⚠️ 并发教训：本次线上曾出现别的会话构建的 `index-6T4q40eX.js`（引用旧 AttachmentPreview chunk），**只核对 `_diag/build` 标记不够**，必须核对 **`index.html` 里的入口 hash 与本机 `frontend/dist/index.html` 一致**，才算真翻面。

## 新增「定性项目」不确定度模式（已上线，build mark uncert-qualitative-mode-2026-09-17）
- 背景：定性项目（HBsAg 等，以 S/CO、COI 报告、按阈值判阴阳性）**无 TEa**，原系统查不到目标就走 `U<15%` 兜底 → 判读无依据。
- **方法学依据（检索确认）**：
  1. **ISO 15189:2022 7.3.4 f)**：定性结果基于定量输出数据 + 阈值判定时，应估计输出量值的 MU；**g)** 中间定量步骤/IQC 结果的不确定度宜作关键(高风险)部分。**不是所有定性项目都要做**（纯目测判读的不强制）。
  2. **CNAS-CL01-G003 6.2**：非数值结果（阴/阳性）宜用**其他方法**评估 MU，**例如假阳性/假阴性概率**。
  3. **GB/Z 43280-2023 / ISO/TS 20914:2019**《医学实验室 测量不确定度评定指南》。
  4. 定性分析三种量化法（Lab Med 2019, doi:10.1515/labmed-2019-0155）：贝叶斯定理、**正态分布法（最实用）**、信息论。
- **实现（新增 mode="qualitative"）**：
  - 分量用**绝对单位（S/CO）**而非相对 %：u_rep = 质控 SD；u_cal = 检测器/校准品绝对 u（厂家 U÷k）；u_c=√(u_rep²+u_cal²)；**U=2u_c（S/CO）**。
  - **灰区 = cutoff ± 1.3353×u_c**（对应 LR=10 边界，Φ⁻¹(1/11)）。
  - **判读用似然比**：z=(cutoff−r)/u_c，Φ 用 erf 实现；r≥cutoff 时 LR=TPR/FNR，r<cutoff 时 LR=TNR/FPR；分级 极强(≥1e6)/非常强烈(≥1e4)/强烈(≥1e3)/中等偏强(≥100)/中等(≥10)/微弱(灰区)。
  - 落在灰区 → passed=False（不能判定，建议复检/确认试验），**不再套 TEa/15% 兜底**。
- 改动文件：`models/uncertainty.py`（+7 字段：cutoff/ucal_abs/u_ext_abs/gray_low/gray_high/lr_value/lr_level）、`schemas/__init__.py`、`api/v1/uncertainty.py`（新增 `_compute_qualitative`+`_lr_level`，`_preview`/`batch` 加 `_is_qual` 跳过 TEa 覆盖）、`services/uncertainty_report_gen.py`（新增 `_report_qualitative_html`）、前端 `UncertaintyAssessment.vue`（模式按钮+输入区+erf+qualPreview）。
- **验证**：文献参数（u_rep 0.038 / u_cal 0.012 / cutoff 1.0 / r 1.15）→ 线上得 u_c=0.0398、**U=0.0797 S/CO**、**LR=11966**、**"非常强烈支持"**，与文献（u_c 0.04、U 0.08、LR≈11309）吻合。灰区 0.9468~1.0532。
- 数据库加列靠 `main.py::_ensure_missing_columns` 自动 ALTER（幂等），部署后线上已正常读写新字段。
- 提交 `d51e353f`。

## 临检组「维修二维码」PDF（28 台 / 5 页，每页 6 个）
- 需求：把临检组 12 类设备（血细胞/特定蛋白/循环增强荧光/尿干化学/尿液分析/血型/大便/卡尤迪/推片机/钙卫蛋白/流式/UC02）的维修二维码合成一个 PDF，每页 6 个。
- **关键前提**：系统按专业组隔离（`get_current_group`，仪器路由 `group_scoped=True`）—— 生免组账号（jinzizheng）**看不到临检组 45 台仪器**。临检组独立账号：**龚珂 / gk123456**（group_code=lj，roles 含 specialty_leader）。
- **源档案**：`C:\Users\81526\Desktop\BG-KS-GL-069-仪器设备一览表-临检组20260916.doc`（真 OLE2）→ Word COM `SaveAs2(out, 7)` 转 Unicode 文本再解析。
- **撞号事件**：源文件 33 个编号只有 30 个唯一值 —— `LJ-0016/0017/0018` 被用两次（序号16/17/18 = 卡尤迪C/D、UC02 在用；序号35/36/37 = AX4030、UF1000i×2 **停用/报废**）。核对后**不需要改数据**：停用设备本就不生成维修二维码，剔除后重号不争抢。经验：**先查 status 再决定要不要动编号**。
- 产物：`outputs/临检组维修记录二维码_A4六联.pdf`（5 页 = 6×4+4）、`临检组维修二维码_A4六联.html`、预览 `临检组二维码_第1页.png` / `第5页.png`。脚本 `scripts/gen_repair_qr_pdf_lj.mjs`（改自生免组 `gen_repair_qr_pdf.mjs`，同版式 + 新增类别行与位置行）。
- 清单：血细胞9 / 特定蛋白3 / 循环增强荧光2 / 尿干化学2 / 尿液分析2 / 血型1 / 大便1 / 卡尤迪4 / 推片机1 / 钙卫蛋白1 / 流式1 / UC02 1 = **28 台**。
- **验证**：28 个编号逐个调 `GET /api/v1/public/repairs/by-code/{code}`（注意公开路由前缀是 **`/api/v1/public/...`**，不是 `/public/...`），全部返回正确的名称+型号；PDF `/Count 5`、首页 6 格 / 末页 4 格。

## 定性项目 u_cal 的替代估算方法（已上线，build mark uncert-ucal-alternatives-2026-09-17）
- 问题：厂家普遍**不提供校准品/检测器的绝对不确定度**，u_cal 怎么取？
- **5 类方法（按优先级，已写入前端提示）**：
  1. 厂家给绝对 U → u_cal = U ÷ k（k 常为 2）
  2. 厂家给**相对 U(%)** → u_cal = U% ÷ k × 质控均值 x̄
  3. 厂家给**允差 ±a** → 矩形分布 u = a/√3；三角分布 a/√6；95% 区间 a/1.96
  4. **本室校准复现性（最自主，推荐）**：换校准品批号时比较各批号校准后同一质控的**均值离散**，其 SD ≈ 校准品批间不确定度
  5. 厂家完全没给 → 填 0，依 CNAS-CL01-G003 6.3 用**期间精密度**替代（须确保 IQC 覆盖过校准品/试剂批号更换）
- **实现**：报告模板 `_report_qualitative_html` 在 `ucal_abs==0` 时**自动附声明**（说明采用期间精密度替代、U 偏保守）；前端加 `u_cal 来源` 下拉（厂家/厂家相对值/允差估算/本室校准/未提供）+ 5 类取值方法提示。
- 线上验证（ucal_abs=0, SD=0.28, x̄=8.5, cutoff=1.0, r=1.15）：u_c=0.28、U=0.56 S/CO、灰区 0.6261~1.3739、LR=2.38（微弱支持，灰区）、passed=False ✓
- 提交 `8bb40b77`。
- 坑：`rm -rf frontend/dist` 会触发安全钩子 SAFE_DELETE_BULK_CONFIRM_REQUIRED（>50 文件）；改用 `npm run build`（vite 自带 emptyOutDir）即可。

## AC 临床化学新增 RF/ASO/IgG/IgA/IgM/C3/C4 的缺口盘点（仅盘点，未改数据）
- 7 项在系统里的 id：类风湿因子109 / 抗链球菌溶血素O115 / 免疫球蛋白A110 / G111 / M112 / 补体C3 113 / C4 114。统一为**免疫比浊法 + AU生化仪 + 德赛试剂**，归 AC 临床化学合理（NCCL-C-06「特殊蛋白」属临床化学专业）。
- **已有**：不确定度 7 项 9-16 已评；EQA 卫健委特殊蛋白 2026 年第1/2次均 100 分；室内质控均有（RF 16、IgA/G/M 各9、抗链-O 9、C3/C4 各6 条）；项目库方法/仪器组/校准品/溯源/线性/可报告范围/参考区间齐全；试剂库有对应试剂盒+校准品+质控品；项目手册挂了 7 份试剂说明书。
- **最大缺口**：`verification-reports`（83 条）里这 7 项**命中 0** → 性能验证报告未做/未录入；另干扰三项（溶血/黄疸/脂血）7 项全空、试剂库**无注册证号字段**（能力范围表现有条目的试剂名都带注册证号，需另找）。
- 仪器口径不一致：项目库 instrument_group 中 RF/ASO/IgG/IgA/IgM = "AU58-1 /AU5800"（SM-2002+SM-1005）、C3/C4 仅 "AU58-1"（SM-2002）；而 AC 现有能力范围条目写三台（SM-2002/2003/1005）。family-map：AU58-1=AU5821 A(2002)、AU58-2=AU5821 B(2003)、AU5800=AU5800急诊(1005)。多台开展→需仪器间比对（不同型号）。
- **发现的数据错误（待用户决定是否修）**：①项目手册 id587「抗链球菌溶血素O测定试剂盒」挂到了「甘油三酯」项目、brand 写成贝克曼；②**甘油三酯别名里混进了「抗链O, ASO, 抗链球菌溶血素O」**（应是别名录入污染，导致 587 挂错）；③id589 补体C3c 未关联项目；④质控项目名写作「Ig G」「抗链-O」，与项目库名不一致，建议统一别名。
- 能力范围表字段：category_l1/l2、seq、item_name、sample_type、method_name、instrument_*(id/name)、reagent_*(id/name)、calibrator、description、remark、perf_correctness/precision/linearity/reportable/other。

## 【已执行】AC 七项新增认可 + 4 个数据错误修复（2026-09-17）
- **修 4 个数据错误（全部线上改完并验证）**：
  1. `test_items/17` 甘油三酯 aliases 去掉污染段「抗链O, ASO, 抗链球菌溶血素O」→ 只留「TG Triglyceride Triacylglycerol 甘油三酯,甘油三脂」。
     **根因**：`documents.py::project_manuals` 用「项目名 + 别名拆分」做匹配键，遍历按 id 升序，甘油三酯(id17) 的污染别名"抗链球菌溶血素O"抢先命中 → doc587 被挂到甘油三酯名下。**教训：别名污染会连带把说明书挂错项目**。
  2. `test-items/113` 补体C3 加别名「补体C3c, C3c」（`_manual_core` 从"补体C3c测定试剂盒"提取到"补体c3c"，比 key"补体c3"长，任何方向都匹配不上 → 必须补别名）；`115` ASO 加别名「抗链-O, 抗链O」。
  3. 结果：7 份说明书（doc 587~593）归属 **7/7 正确**，品牌全部识别为德赛。
- **新建 7 条 AC 能力范围记录**（id 370~376，seq 29~35，AC 由 56→63 条，总数 103→110）：类风湿因子/抗链球菌溶血素O/免疫球蛋白A·G·M/补体C3·C4，血清、免疫比浊法、德赛试剂+校准品、线性与可报告范围取自项目库；仪器：RF/ASO/IgA/IgG/IgM 写 SM-2002+SM-1005，C3/C4 写 SM-2002（依项目库 instrument_group）。
- **从 7 份说明书 PDF 提取性能数据并回填**：
  - 项目库 `interference_*`（原全空）：溶血 RF/ASO ≤5.00 g/L、其余 ≤10 g/L；黄疸 RF/ASO ≤684 umol/L、其余 ≤1026 umol/L；脂血全部 ≤22.6 mmol/L。
  - 能力范围 `perf_precision`（批内/批间 CV 最大值）、`perf_correctness`（方法学比较 y/r）、`perf_other`（检测限+干扰），**末尾统一标注「（厂家说明书声明值，本实验室性能验证待补）」**，避免评审误认为本室验证数据。
- **PDF 解析坑**：①µ 符号在文本提取中丢失（"µmol/L"→" mol/L"）、≤ 符号也会丢 → 正则不要依赖这些符号；②表格数字会被拼成一串（"样品11683.432.04"）→ 用 `page.find_tables()`（fitz）或 pdfplumber `extract_tables()` 按列取；③本机 **系统 Python 3.14 有 pypdf/fitz/pdfplumber**，managed 3.13 没有。
- 仍待用户确认/补：仪器实际开展台数、试剂与校准品注册证号、本实验室性能验证报告、SOP 作业指导书挂接、仪器间比对。

## 【已执行】AC 七项：证件号、校准品说明书入库、ASO 溯源（2026-09-17 上午）
- **德赛注册证号（从 `D:\民航总医院\生免组管理体系文件\生免试剂说明书\德赛说明书` 各 PDF 提取，正则 `国械注进\d{8,}`）**：
  - 试剂：ASO 20152402319 / RF 20152402224 / IgG 20152402033 / IgM 20152402010 / IgA 20152402039 / C3c 20152402027 / C4 20152402051
  - 校准品：ASO校准品 20152402032 / RF校准品 20152402041 / 特定蛋白校准品 20152402053
  - 仪器沿用 `（苏械注准20222221910/国械注进20152221623）`。已写入能力范围 seq29~35 的 instrument_name / reagent_name / calibrator。
- **说明书入库**：本地德赛目录 17 份 vs 系统比对，缺 4 份 → 已上传（category=项目说明书，status=生效）：**659 抗链球菌溶血素O校准品、660 类风湿因子校准品、661 特定蛋白校准品、662 载脂蛋白A1-B校准品**；系统"项目说明书"235→239 份。
- **ASO 溯源（源：桌面 ASO.pdf，扫描件无文字层，fitz 渲染 170dpi PNG 后读图）**：TruCal ASO（货号I7010/试剂I7012）溯源至 DiaSys 内部工作校准品（批号183854，168±8.4 IU/mL，U=5.0%）→ 主校准品 Ref 03/10（5水平）→ 赋值至工作校准品；各水平相对扩展不确定度 <8%；依据 ISO GUM(1993)、EURACHEM Guide(1995)。已写入 `test-items/115.traceability`（原值仅"Internal working calibrator"）。
- **重要发现：线上 `project-manuals` 仍是旧逻辑（没有 step 0「manual_doc_ids 显式关联优先」）** —— 即使把 doc id 写进 `test_items.manual_doc_ids`，线上也不会关联；必须靠"标题 core ↔ 项目名/别名"匹配。
  - 处置：给相关项目**追加别名**兜底 —— ASO += 「抗链球菌溶血素O校准品」、RF += 「类风湿因子校准品」、IgA += 「特定蛋白校准品」、载脂蛋白A1 += 「载脂蛋白A1-B校准品」，4 份校准品说明书已全部挂上。
  - 局限：匹配逻辑第一个命中即 break，**一份通用校准品只能显示在 id 最小的那个项目下**（661 只挂"免疫球蛋白A"，IgG/IgM/C3/C4 不显示）。若要一文档挂多项目，必须部署含 step 0 的代码 + 去掉 break。

## 修复：单水平无法录入 + 定性模式保存 500（2026-09-17，build mark uncert-mode-len-fix-2026-09-17）
**问题A：单水平质控"录入无法计算"**
- 根因：前端 `save()` 校验仍是旧的 `if (form.l1_n < 2 || form.l2_n < 2)` → 只填 L1 直接被弹窗拦截；且 else 分支把 qualitative 当多系统校验。
- 我之前只改了 preview + 后端计算，**漏了 save 校验**。已改：single→L1 必填/L2可选；qualitative→cutoff+L1 必填（独立分支）。
**问题B：定性模式保存 500**
- 根因：`UncertaintyAssessment.mode` 原为 **VARCHAR(10)**，而 `"qualitative"` 有 **11 字符** → MySQL Data too long → 500（**所有**定性保存都失败，连"仅基础字段"也 500）。
- 修复：模型 `String(10)` → `String(20)`；因**自愈加列只 ADD 不改长度**，在 `main.py` 启动自愈中显式 `ALTER TABLE ... MODIFY mode VARCHAR(20)`（幂等，参考已有 project_code/instrument 兜底写法）。
**端到端验证（模拟前端流程：先 _preview 再 POST）**
- 单水平定量：u_rw=5.0（SD0.25/均值5.0）保存 201 ✓
- 定性单水平（ucal_abs=0）：u_c=0.28、U=0.56、LR=2.38（微弱支持·灰区）、保存 201 ✓、生成报告 200 ✓
**坑/经验**
- **保存流程 = 前端先 `POST /uncertainty/_preview` 拿结果，再把结果 spread 进 payload 一起 POST**；直接 POST 业务字段不会触发计算（u_rw 会是 0）。排查后端时务必模拟这个两步流程。
- **MySQL 字符串列长度要预留**：新增枚举值前先核对 VARCHAR 长度（本次 qualitative 11 > 10 踩坑）。
- 部署耗时长（本次 17 分钟，且需两轮等待才翻面）；`rm -rf frontend/dist` 会触发安全钩子，改用 `npm run build`。

## 仪器维修记录表打印页眉专业组写死（commit 3af498c1）
- 问题：仪器档案 → 汇总打印「仪器维修记录表」（BG-KS-CZ-909）页眉硬编码「实验室/专业组：检验科生化免疫组」，临检组打印也显示生免组。位置 `frontend/src/views/instruments/InstrumentList.vue:783`（`printRepairSummary909()`）。
- 修法：改为 `检验科${auth.groupName}`（`useAuthStore` 已在该文件引入，`groupName` 映射 sm→生化免疫组、lj→临检组、wsw→微生物组、fz→分子组、xk→血库），跟随当前专业组自动切换。
- 注意：该页脚 `表格编号：BG-KS-CZ-909` 里的 **CZ 前缀仍是写死的**（生免组体系）。若临检组需要独立表格编号（如 BG-LJ-…），需科室给号后再改。
- `_BUILD_MARK=repair-summary-group-name-2026-09-17`。
- **并发部署第 3 次再现**：部署前 `_BUILD_MARK` 已是别人的 `uncert-mode-len-fix-2026-09-17`（对方在做测量不确定度）。已改用**通配 sed**（`s/_BUILD_MARK = "[^"]*"/...`）避免目标串不存在导致静默失败——**这是并发环境下的正确姿势，以后一律用通配替换**。

## UI 调整：定性项目输入区改 label-position=top 四列布局（2026-09-17 10:15）
- 用户反馈输入框被长标签挤压（出现 "0.00C"、"0.1"、"厂…" 截断，"L2 质控 SD（可选）" 换行）。
- 改法：整块改为 **`label-position="top"` 四列（span=6）**，标签在输入框上方、输入框占满列宽；拆成「基本参数（cutoff / u_cal / u_cal来源 / 待判读 r）」与「室内质控水平（L1 均值·SD·n / L2 均值·SD·n）」两组，中间加小标题；标签统一缩短（如"L1 质控均值 x̄"→"L1 均值 x̄"、"检测器 u_cal (S/CO)"→"检测器 u_cal（S/CO）"）。u_cal 取值方法提示提到块首。
- 该页面多系统区原本就用 label-position="top"，风格统一。
- 提交 `59c118bf`；入口 hash `index-LHzPSTvG.js`。
- 部署结果：最终 `_BUILD_MARK=repair-summary-group-name-2026-09-17`，线上 `InstrumentList-itPjCCyS.js` 含动态 `${h.groupName}` ✓。
- **本此部署踩坑记录（并发环境血泪）**：连续 3 次部署失败——① tcb 交互提示「有部署在跑是否取消」，传多余 Y 会让命令挂死（需 TaskStop 停掉）；② 别人的部署晚于我的，把我的 dist 覆盖回旧 chunk；③ 每次部署耗时 5-8 分钟。
  **正确姿势**：部署前 `git fetch` 确认远端 tip 是自己的提交；部署后按「入口 chunk hash → 目标 chunk 文件名 → chunk 内关键词」三级验证，**并至少等待 5 分钟再验**；发现被覆盖就再部署一次。
