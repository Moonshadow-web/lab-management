# 2026-07-30 工作日志

## 人员继教管理模块：部署与修复
- 完成教育模块全部代码（后端 models/schemas/api + crud_base json_fields + 改名 + 前端 hub/7 子组件/签到表/通用附件），vite build 通过，提交 305afcb 并 push。
- 首次 `tcb cloudrun deploy` 后线上始终停在旧 mark（attach-cos-migrate），新 pod 启动即崩、流量回滚。
- 经本地 `import app.main` 测试抓出两处启动崩溃：
  1. schemas/education.py 用了 `@field_validator` 但未从 pydantic 导入 → NameError。
  2. crud_base.make_router 的 search_fields 是必填位置参，教育模块 6 个路由未传 → TypeError。
- 修复：补 field_validator 导入；make_router 的 search_fields 改可选(默认 None，函数内已守卫)。
- 重新提交 af71c2a、push、再次 deploy，_BUILD_MARK=staff-education-fix1-2026-07-30。
- 现状：CloudBase CLI `cloudrun list` 显示服务 normal/healthy，但线上仍返回旧 mark，新版本尚未接管流量；沙箱 curl 内网 host 频繁连接失败，无法可靠确认新版本是否起来。已请用户去 Cloud 控制台核对真实部署情况（构建是否完成/新版本是否健康/流量是否切换）。
- 经验：FastAPI 项目上线前必须做 `import app.main` 冒烟，py_compile/vite build 通过不等于运行时无 NameError/TypeError。
