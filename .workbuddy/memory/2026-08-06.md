## 替换 cnas_standards 中 CNAS-CL02-2023 文件（2026-08-06）
- 用户用微信收到的 `DTJY-WLWJ-2.0-01 CNAS-CL02：2023《医学实验室质量和能力认可准则》.pdf`（428884字节）替换系统内原有的 CNAS-CL02-2023 条目。
- 原 cnas_standards 无「替换文件」接口，新增 `POST /api/v1/cnas-standards/{id}/replace`（admin/specialty_leader）：保留 code/name/category/sort_order/original_filename 展示元数据，仅替换二进制（COS 优先，失败回退 BLOB），并清理旧 COS 对象、更新 file_size/uploader/updated_at。
- 目标：线上 id=1，code=CNAS-CL02-2023（半角连字符），原 file_size=3793431。
- 部署：`_BUILD_MARK=cnas-standards-replace-endpoint-2026-08-06`；tcb deploy；内网核对新标记后执行 `scripts/replace_cnas_cl02.py`（APPLY=1）。
- 结果：id=1 的 PDF 已替换为 428884 字节新文件（原 3793431）。验证 preview/download 均返回合法 %PDF-1.7、长度 428884。original_filename 更新为新来源文件名（DTJY-WLWJ-2.0-01 …）。
- 提交：backend/app/api/v1/cnas_standards.py + diag.py + scripts/replace_cnas_cl02.py（commit e847f94）。

## 补充 D:\民航总医院\15189\15189规范文件 目录 + 三类分类（2026-08-06）
- 用户要求把 `D:\民航总医院\15189\15189规范文件`（含子目录 `CNAS医学实验室认可申请书及附件20231201`）补充进系统「规范文件」模块，并查看时按三类分类：CNAS认可规范 / CNAS附件表 / 行标。
- 分类规则（按文件名）：含「附表/附件/认可合同」→ CNAS附件表；含 WS/T → 行标；其余 CNAS 文档 → CNAS认可规范。
- 后端改动 `api/v1/cnas_standards.py`：
  - `/upload` 支持显式传 code/name/category/sort_order（覆盖自动解析）。
  - 预览/下载按扩展名返回正确 MIME（`_guess_mime`）；仅 PDF 内联预览，非 PDF（如 .doc）改 attachment 直接下载（修掉原先写死 application/pdf 导致 .doc 显示为坏 PDF 的 bug）。
  - 新增 `PATCH /{id}` 改元数据（code/name/category/sort_order），admin/specialty_leader。
- 前端 `StandardsTab.vue`：加分类筛选下拉（全部/CNAS认可规范/CNAS附件表/行标），标签配色三类区分。
- 补充结果：15 个新文件入库（7 CNAS认可规范 + 8 CNAS附件表，其中 9 个 .doc 来自申请书及附件目录）。
- 统一分类：原 7 个 WS/T 记录 category 由「卫生行业标准(WS/T)」PATCH 改名为「行标」（脚本 `rename_ws_to_hangbiao.py`，APPLY=1，7/7 成功）。
- 最终分类分布：CNAS认可规范 15 / 行标 7 / CNAS附件表 8 = 30 条。验证通过（.doc→msword+attachment，pdf→inline）。
- 部署：`_BUILD_MARK=cnas-standards-categories-2026-08-06`；vite build；tcb deploy；内网核对新标记后执行 seed + rename。
- 提交：cnas_standards.py/diag.py/StandardsTab.vue + 脚本 seed_cnas_standards_extra.py、rename_ws_to_hangbiao.py（commit 见本次推送）。

## 规范文件 .doc → .docx 转换 + 前端渲染预览（2026-08-06 凌晨）
- 用户要求把 cnas_standards 里旧的 .doc 转成 .docx，并能在浏览器里「渲染预览」（而非只能下载）。
- 转换：`scripts/convert_doc_to_docx.py`（Word COM，skill doc-to-docx）。已排除 `~$` 临时锁文件、排除 `.docx` 自身。`DIRS` 指向 `D:\民航总医院\15189\15189规范文件\CNAS医学实验室认可申请书及附件20231201`（8 个 .doc）；顶层 `医学实验室质量和能力认可申请书填写指南.doc`（id=22）单独用 Word COM 转（不在子目录）。共转出 9 个 .docx。原始 .doc 保留未删。
- 替换入库：`scripts/replace_doc_with_docx.py`（APPLY=1）：本地 9 个 .docx 按文件名 stem 精确匹配系统 9 个 .doc 记录（id 16/22/24-30），调 `POST /{id}/replace`。replace 端点已会同步更新 original_filename/file_size/cloud_key，故记录扩展名也从 .doc 变为 .docx。结果 9/9 成功、0 失败。
- 前端渲染：`StandardsTab.vue` 预览改为 dialog —— pdf 走 iframe 内联、docx 用 `mammoth.convertToHtml` 渲染 HTML、其余类型回退下载（复用 fetchCnasStandardBlob + 现有 .preview-html 样式）。pdf 预览从原「新标签打开」统一为 dialog 内嵌，体验一致。无需改后端（replace 端点 + MIME 感知已就绪）。
- 部署：`_BUILD_MARK=cnas-standards-docx-render-2026-08-06`；vite build；tcb deploy；内网核对新标记后 `scripts/verify_docx_render.py` 校验：30 条记录、9 个 .docx、抽样 16/22/24 字节合法（PK 头+word/document.xml+MIME 正确）。
- 提交：convert_doc_to_docx.py(repoint DIRS + 跳过 `~$`)、replace_doc_with_docx.py、verify_docx_render.py、StandardsTab.vue、diag.py（commit 见本次推送）。
- 已知限制：mammoth 对 docx 中复杂元素（如文本框、部分图表）支持有限，但正文/表格/基础排版可正常渲染；这是浏览器预览 Word 的通用约束。
