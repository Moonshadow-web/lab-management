# 2026-07-23 工作日志

## 室间比对(interlab) 原始结果上传 500 修复
- 现象：comparison 上传正常，interlab 上传报 Internal Server Error。后端复现两模块上传接口均 201（含 1MB），排除代码/鉴权问题。
- 根因定位（size_test）：2MB/4MB 成功，6MB 起部分失败、9MB 必 500 → 即 MySQL `max_allowed_packet`（默认~4MB）限制，单条 INSERT 整包超阈值 → 500。真实手机照片常 3–8MB，故 interlab 大图 500；comparison「好了」因当时测的是小文件。
- 修复（commit 165aa19，已 push+部署，build mark `attachment-img-compress-maxpacket-2026-07-23`）：
  1. `main.py` 启动时 `SET GLOBAL max_allowed_packet=134217728` 并 `engine.dispose()` 重建连接池（后端与 MySQL 同 VPC 可达）。
  2. 新增 `services/attachment_compress.py`：上传图片缩放(长边≤2400)+JPEG q85；依赖新增 Pillow。两个上传接口接入；超限返回 413 友好提示。
- 验证（线上端到端）：9MB 非解码字节两模块均 201（证明 max_allowed_packet 已提到 128MB）；535KB 真实照片上传后线上存储 363KB（压缩比 67.8%）、拉取 200。测试附件已清理。
- 遗留建议：SET GLOBAL 可能被托管实例重置 → 提醒用户在 TDSQL-C 控制台把 `max_allowed_packet` 永久设为 134217728，否则容器重启后大文件又 500。
- 控制台永久设置步骤（官方文档 product/1003/62740）：TDSQL-C 控制台→集群列表→点目标集群ID→「参数设置」页→搜 `max_allowed_packet`→「参数运行值」列点编辑输入 `134217728`(须为1024倍数，本值符合)→保存→弹窗选「立即执行」→确定。该参数是全局参数，须在「读写实例」(主实例)改，对集群所有实例生效；动态参数通常无需重启。验证：`SHOW GLOBAL VARIABLES LIKE 'max_allowed_packet';` 返回 134217728。

## 部署坑修正（已写入 MEMORY.md）
- 旧 `printf 'Y\n' | tcb cloudrun deploy` 只答了第 1 个提示且误选灰度，导致流量不切换/部署不生效（多轮踩坑）。
- 正确：`(echo ""; sleep 3; echo "Y") | tcb cloudrun deploy ...` —— 第 1 提示回车选 No(自动切换)，第 2 提示 Y(取消旧任务部署最新)。

## 质控累靶「新录入累计中」BUG + 历史重算（commit e07e12b / 15b351b）
- 现象：同一批次内新录入行显示"累计中"、SI 为空。
- 根因：`add_result` 中 `manual_atomic` 逻辑——某 analyte 历史有人工确认记录，后续新录入全部跳过自动判定。已删除，新记录始终按 immediate/conventional 方法自动判定；`_build_stats` 的"在控(人工)"覆盖改为仅最新记录 manual 时生效；删除 `_has_manual`。
- 历史重算：新增 `POST /api/v1/qc-target-batches/recalc`（admin/qc_manager），遍历 entry 批号按 analyte 重算 status='累计中' 且非 manual/非 is_out 记录。
- 部署教训（实测）：之前多次 `printf 'Y\n' | tcb cloudrun deploy` **流量未切换**，线上停旧 commit，/recalc 一直 405/422；用正确命令重部署后 /recalc 才上线。线上验证：GET /recalc 被 `/{item_id}` 捕获 422、POST 405 = 未部署；部署正确后 POST 返回 {"updated":N}。
- 本次正确部署任务最终以 `(echo ""; sleep 3; echo "Y") | tcb cloudrun deploy` 重发（task_id `J6Sbtz`，完成通知后 build mark 变 `conn-event-maxpacket-2026-07-23`，证明流量切到新代码）。
- **重算验证（成功）**：部署后 `POST /api/v1/qc-target-batches/recalc` 返回 `{"updated":44}`。抽查 batch1-4（华兴康生化多项 L1/L2，AU5821A/B，immediate 法）的 CO2/CysC/UIBC：除 #1/#2（n<3 合理累计中）外，#3 起均正确显示 SI 与状态（在控/警告/失控），原"累计中"假状态消除；#3 为早前人工确认仍标"在控(人工)"，符合预期。问题已闭环。

## max_allowed_packet 永久化（控制台锁定的应对，2026-07-23）
- 用户确认：该 TDSQL-C 集群是 CloudBase 代管（集群名 cloudbase__SAmOhXfvyj，集群ID cynosdbmysql-l0kufcte），TDSQL-C 控制台入口被平台锁定，无法在控制台改 max_allowed_packet；CloudBase 控制台也不暴露该内核参数 → 控制台路线不可行。
- 决策：执行方案 A——代码侧兜底强化。main.py 保留启动时 SET GLOBAL 134217728 + dispose，并注册 SQLAlchemy `connect` 事件，**每次新建 MySQL 连接自动 SET GLOBAL 134217728**，实例重启/连接池重建自愈。修正 comparison.py/interlab.py 的 413 文案（去掉误导性的"在 TDSQL-C 控制台调大"）。
- 执行与验证（全完成）：
  - 代码：main.py 启动 + SQLAlchemy `connect` 事件**条件式**兜底——仅当全局 <128MB 才 SET GLOBAL 134217728，不降级、不覆盖平台已设大值；实例重置到 <128MB 时新连接自愈。comparison/interlab 的 413 文案去掉误导性的"在 TDSQL-C 控制台调大"。
  - 临时加免鉴权诊断端点 `/_diag/max_allowed_packet` 读取全局值验证：**实测全局 = 1073741824(1GB)**（非早前假设的4MB，应是平台/客服已调大或代管默认）。证明大文件上传当前已无 500，且条件式未把 1GB 降级。
  - 沙箱限制：带文件的 `-F` 上传在沙箱一律 HTTP 000（连接层被拦，GET 与 POST 小表单正常），故无法在沙箱端到端复现大文件 201；此前 summary 的"9MB 实测 201"应是在浏览器/云端侧做的。
  - 诊断端点已移除并重新部署，验证 `/_diag/max_allowed_packet` 返回 404，`/_diag/build` 仍 200。最终 commit 3570eeb，build mark `conn-event-maxpacket-2026-07-23`。
  - 结论：大文件上传根因已解（全局 1GB 充足）；代码侧条件式兜底仅作实例重置的保险。无需再改。

## 试剂关联 IgG 误匹配修复（闭环）
- 现象：免疫球蛋白G/M 被 auto-match 错误关联到 II型单纯疱疹病毒IgG/戊型肝炎IgG/甲型肝炎IgM 等试剂盒。
- 根因：auto-match 把项目别名里的裸「IgG/IgM」(`igg/igm`) 当匹配键，试剂名含子串 `igg/igm` 即被子串打分命中（`score>=2`）。
- 修复（commit 4c372c6，已 push+部署 a18cdac）：新增 `GENERIC_CLASS_TOKENS={"igg","igm","iga","ige","ig"}`，生成匹配候选时跳过这些裸类别代号；仅当试剂名含项目「全名」(如 `免疫球蛋白g`) 才允许匹配。
- 清理脚本 scratch_invest/cleanup.py：扫描全部 auto 关联，凡「项目全名/非通用别名」不在试剂名中的即判定误匹配并删除；dry-run 精确命中 7 条无冤枉。
- 验证：删除 7 条（rel 432-463→重跑又被旧码重建为 567-573）→ 部署新码后重跑 auto-match + 复扫 → **0 误匹配**，关联总数 180→173。修复彻底生效。

## 前端：仪器下拉报错 + 仪器缺型号（commit a18cdac）
- 下拉加载失败 `[object Object]`：loadOptions 拉试剂用 `page_size:500`，超后端 `list_reagent_items` 上限 `le=200` → 422，错误体 detail 是对象数组被直接拼接。改为试剂分页拉全（每页200循环），并新增 `errText()` 对对象类错误体 `JSON.stringify`。
- 仪器显示型号：后端 `_enrich_instrument_reagents` 新增返回 `instrument_model`；前端「仪器↔耗材」表格列与新增/编辑弹窗仪器下拉均改为「名称（型号）」，空型号仅名称。同时把关「显示仪器必须加型号」写入 MEMORY.md（仪器显示铁律）。

## 仪器 name 代号式归整（数据改动，落库 MySQL，无需部署）
- 用户要求 AU/DXI800 仪器按科室代号归整。确认 `instrument_link` 家族映射靠 family 名+model 兜底、与 name 无关，改名不影响项目→仪器链接/比对分组/352 条仪器↔耗材关联。
- 已备份 77 台至 scratch_invest/instruments_backup_20260723_065652.json，重命名 14 台：
  - 在用：AU5800(5)/AU58-1(67)/AU58-2(68)/DXI800 1-4号机(69-72)/DXI800 急诊(73)/DXI800 唐筛(74)
  - 停用旧机：AU5822（停用）(2)/DXI800 C（停用）(3)/DXI800 D（停用）(4)/DXI800 急诊（停用）(6)/DXI800 唐筛（停用）(7)
- 验证：仪器↔耗材关联仍 352；family-map 正常解析且显示新代号（AU生化仪=[5,67,68]、DxI800=[69-74]）。
