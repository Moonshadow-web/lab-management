# 2026-09-16 工作日志

## 并入「2026年临采.xls」重算最少采购（13:20）
- 源：`C:\Users\81526\Desktop\2026年临采.xls`（工作表`临采试剂支出`，61 行 / 93.07 万；结构=编码/物资名称/规格/数量/单位/单价/金额/**科室名称**/月份；科室=细菌42、免疫8、临检7、PCR4）。
- 三组（生化/免疫/凝血）临采仅 **8 行**（试剂部分 44,738 元，另 2 行属校准品：地高辛校准品1,000、胱抑素C校准品5,146）；影响 6 个项目：HIV抗原抗体诊断试剂 **13,200（全部来自临采·新增）**、乳酸测定 10,360、白介素6 7,200、铁测定 5,368、ALT(OSR6107) 4,390、地高辛测定试剂盒 4,220（＝2026 年"地高辛"的再次采购）。
- 产物：`C:\Users\81526\Desktop\试剂支出_最少采购分析_含临采_2026-09-16.xlsx`（5 表：最少采购试剂(含临采) / 临采带来的变化 / 只买过1次的项目(**21个**) / 关注项目&全组检索 / 临采全部明细 62 行），脚本 `outputs/wjx/reagent_least_with_lc.py`。
- **"博源"仍未找到**；含"博"的只有 **北京博盛医疗器械有限公司**：17-羟类固醇(25-12 3,600 + 26-01 10,800 + 26-05 10,800)、17-酮类固醇(同)、香草扁桃酸(4,500+13,500+13,500) —— 均为**生化组**。
- **肝纤四项确认不存在**（全 8 个专业组 + 临采均无 HA/层粘连蛋白/Ⅳ型胶原/Ⅲ型前胶原）；胶原类只有 CTX（β-胶原特殊序列）与 P1NP（总I型胶原氨基端延长肽），属**骨代谢**，非肝纤。
- 口径提醒：临采表无"供应商"列；其"科室名称"即专业组；并入时期间标记为 `2026临采`。

## 最少采购试剂分析（18 个月合并口径）+ 专项检索
- 口径：**2025全年 + 2026年1–6月 = 18 个月**；专业组 生化/免疫/凝血；仅「试剂」（按备注+名称剔除质控品/校准品/耗材）。
- 产物：`C:\Users\81526\Desktop\试剂支出_最少采购分析_2026-09-16.xlsx`（5 表：最少采购试剂Top60 / 指定项目明细 / **只买过1次的项目(19个)** / 关注项目明细 / 全部试剂排行232行），脚本 `outputs/wjx/reagent_least_analysis.py`。
- 关键发现：
  - 买得最少：促甲状腺素受体抗体 1,240、碱性清洗液 1,400、针清洗液 1,575、抗卵巢/抗子宫内膜/抗精子抗体 IgG·IgM 各 1,600（均2025年3月一次性）。
  - **抗精子抗体**：IgG 1,600(25-05 科柏泉)+IgG 1,300(25-12 美康百泰)+IgM 1,600(25-03 科柏泉)=**4,500/3支**；2026H1 为 0。
  - **抑制素B（化学发光法）**：8,560×2=**17,120**（2025-03/05，北京科柏泉医疗器械）。
  - **"博源"全表 0 命中**（材料名称/供应商/规格）；含"博"供应商仅 **北京博盛医疗器械有限公司**（17-酮类固醇32,400 / 17-羟类固醇43,200 / 香草扁桃酸54,000）→ 需用户确认是否即"博源"。
  - **肝纤四项（透明质酸/层粘连蛋白/Ⅳ型胶原/Ⅲ型前胶原）18 个月内无采购**，仅"肝纤维化质控品(低/高值)"各 360 元。
- 提示：抗精子/抗卵巢/抗子宫内膜/抑制素B 的**供应商都是北京科柏泉医疗器械**（可能是博源品牌的经销），采购口径按"供应商"看比按"品牌"更可行。

## 试剂支出分析·口径修正 + 项目排行（11:57 起）
- **用户确认口径**：`2025年12月表`=**2025 全年**（12 个月）；`2026年6月表`=**2026 年 1–6 月**（6 个月）→ 两期长度不同，**必须用月均对比**。
- **修正后的核心结论**（生化+免疫+凝血）：2025全年 3053.25 万（月均 **254.44 万**）vs 2026H1 1514.22 万（月均 **252.37 万**）→ **月均基本持平（−0.8%）**；此前按"单月 vs 单月"算出的 −35.5% 是口径错误导致的假象，已纠正。
  - 专业组月均：生化 57.5万→**59.1万(+2.7%)**；免疫 169.2万→**155.6万(−8.1%)**；凝血 27.7万→**37.8万(+36.5%)**。
- **项目归并规则（关键）**：名称里有多种写法 + 不同仪器配套不同材料编码 → 排行必须**按"项目名"归并**：去前导编码（`98200/`）、去 `/Access XXX` 后缀、统一全半角括号、**仅当尾部括号内容是"检测方法"时才去掉**（含 法/发光/比浊/终点/色原/酶/电极/夹心/层析 等关键词）；仪器型号括号（如 `清洗液(DxI800)`）**保留**。多次迭代教训：按材料编码归并 → 同项目拆开；把括号全去掉 → 清洗液/缓冲液被过度合并。
- 产物：`C:\Users\81526\Desktop\试剂支出分析汇总_项目排行_2026-09-16.xlsx`（5 表：口径与汇总 / 项目排行(全部 353行) / 项目排行(仅试剂 232行) / 买得多-买得少 / 月均增减榜），脚本 `outputs/wjx/reagent_spend_ranking.py`。
- 注意：上一版 `试剂支出分析汇总_2026-09-16.xlsx` 因**被预览占用（PermissionError）无法覆盖** → 另存为新文件名。

## 试剂支出分析（桌面两表，生化/免疫/凝血 × 试剂/校准品/耗材）
- 输入：`C:\Users\81526\Desktop\2026年6月试剂支出汇总.xls`（实含 1–6 月，1362 行）、`2025年12月试剂支出汇总.xlsx`（实含 1–12 月，2811 行）；两表同结构 11 列，**均无「类别」列**。
- 关键数字（生化+免疫+凝血）：2026年6月单月 **205.89 万**（试剂 88.1%/耗材 9.3%/校准品 0.97%/质控品 1.63%）；2026年1–6月 **1514.22 万**；2025年12月单月 **319.33 万**；2025全年 **3053.25 万**。环比单月 **−113.43 万（−35.5%）**；免疫 −42.2%、生化 −54.9%、凝血 **+434.7%**（6 月新增 D-二聚体 23.52 万/ACL TOP 冲洗液 7.45 万/分析杯）。
- 产物：桌面 `试剂支出分析汇总_2026-09-16.xlsx`（4 表：分析汇总/逐项目明细 2117 行/环比对比 268 行/数据质量），脚本 `outputs/wjx/reagent_spend_analysis.py`（pandas+openpyxl+xlrd）。**原始两表未修改**（已核验 .xls 仍只有「总表」）。
- 口径：类别按「备注(校准/质控) + 名称关键词(反应杯/分析杯/吸头/层析柱/过滤/试管/离心管/电极→耗材)」推导；质控品单列为第 4 类。
- **重要教训**：sheet-agent 子代理报告"已新增工作表 AI分析汇总"但**实际未落盘**（核验 .xls 仅 总表、.xlsx 仅 新冠/总表）；且其 `create_local_excel` 工具缺失 → 最终改用本地 Python 完成。**子代理的工具写入声明必须自行核验**。
- 环境：managed venv `C:/Users/81526/.workbuddy/binaries/python/envs/default/Scripts/python.exe` 已有 pandas 3.0.3 + openpyxl；**xlrd 需现装**（读 .xls 必需）。

## 生免组文件大检查 + 清单重填 + 上传（大任务，6 项）
目标目录 `D:\民航总医院\15189\生免组文件`，四个分类：通用/项目/仪器作业指导书 + 记录表格(管理·操作·培训)。

**1. 扫描结果（233 个 docx）**
- 含修订：2 个（通用 SM-SOP-023 del=4；仪器 SM-SOP-1030 del=1）
- 含批注：17 个（已列表报告用户，未删除，等用户决定）
- 文件名带 (N)：12 个（另有 3 个尾部空格）→ 实际清理 15 个
- 表头日期：233 个**全部已是 YYYY-MM-DD**，格式无需改
- 日期逻辑异常：8 个（审核日期 2026-09-09 晚于批准日期 2026-09-01）

**2. 接受修订（已完成）**
- Word COM `Revisions.AcceptAll()` 只清了**正文**，页眉/页脚残留（SM-SOP-023 在 header4.xml、SM-SOP-1030 在 footer2.xml）。
- 补法：直接改 XML——删整块 `<w:del …>…</w:del>`、去 `<w:ins>` 标签保留内容、清 `<w:del/>` 自闭合属性；重写 zip。验证 del=0/ins=0。

**3. 清单重填（BG-SM-GL-000）**
- 旧清单 333 行；新清单按 **通用→项目→仪器→记录表格(管理/操作/培训)** 顺序写入 **322 条**，覆盖原内容（备份 `_备份.docx`）。
- 列：文件类别｜文件标题｜文件编号｜编制人｜审核人｜批准人｜批准日期｜生效日期（日期统一 YYYY-MM-DD）。
- 差异：**新增 23 / 移除 36 / 内容变更 229**（主要是审核人 杨静→金子铮等、日期 2025-04-01→2026-09-01，属版本更新）。
- 变更记录已生成：`生免组文件\生化免疫组文件清单变更记录.docx`。

**4. 上传系统**：项目SOP 531-535、543-546 共 **9 个，全部成功**。

## 关键坑（复用价值高）
- **Word COM 的 AcceptAll 不处理页眉/页脚**，必须遍历 StoryRanges 或直接 XML 清理。
- 记录表格类（89 个）表头**没有"文件编号/编写者"等字段** → 编号从文件名正则提取（`BG-SM-XX-NNN`），标题要再剥掉编号前缀（表头 title 常直接等于文件名）。
- 人员/日期优先取文档表头，缺失时**按编号从旧清单继承**。
- 旧清单是 2025-04-01 版（审核人统一"杨静"），文档表头已更新为 2026-09-01、审核人各异 → 覆盖后差异会很大，属正常版本更新，不是数据错。
- 批量按文件名落盘前必查「同目录同名」，否则静默覆盖丢文件。

## 待用户确认
- 17 个含批注文件如何处理（保留/删除）
- 8 个日期逻辑异常：审核日期(09-09) 晚于批准日期(09-01)，建议改为批准日之前

## 追加：8 个日期逻辑异常已修正（2026-09-16 01:30）
- 用户确认：审核日期统一改为 **2026-08-10**（与 SM-SOP-149 等正常文件一致）。
- 已修改 8 个文件的表头「审核日期」2026-09-09 → 2026-08-10：
  - 项目作业指导书：SM-SOP-156、180、181、184、185
  - 仪器作业指导书：SM-SOP-1025、1026、1027
- 修正后 8 个文件均为：发布 2025-03-01 → 审核 2026-08-10 → 批准 2026-09-01 → 实施 2026-09-01，逻辑校验**全部通过，异常剩余 0**。
- 变更记录 `生化免疫组文件清单变更记录.docx` 第五节已同步改为「已于 2026-09-16 修正完毕」并更新说明文字。
- 改单元格日期的可靠写法：遍历 doc.tables → 找 cell.text 恰为「审核日期」的格 → 取同行 i+1 格 → **把该格第一个 run 设为新日期、其余 run 置空**（日期常被拆成多个 run，直接设 cell.text 会丢格式）。
- 仍待用户决定：17 个含批注文件是否清除批注。

## 追加：17 个文件 37 条批注内容已读出（2026-09-16 01:35）
- 读法：`word/comments.xml`，按 `<w:comment ` 切分，取 `w:author`/`w:date` + 段内 `<w:t>` 拼接。
- 3 位批注人：**隐月 27 条(2026-08-15)**、**秋天 5 条(2026-08-09)**、xuzl 1 条、sollyzr 1 条。
- **实质问题（需处理，共 6 条）**：
  - SM-SOP-105 低密度脂蛋白胆固醇：「？？？」×2
  - SM-SOP-109 小而密LDL：「和参考区间浓度不一致啊」×2 ← 参考区间前后矛盾
  - SM-SOP-110 前白蛋白：「矛盾了啊」
  - SM-SOP-111 白蛋白：「细胞受损会导致白蛋白升高吗？」
  - SM-SOP-550 TPSA：「无对应的参考文献」（sollyzr）
  - SM-SOP-573 丙肝抗体胶体金：「到底是英科创新还是艾博生物哈哈？」← 试剂厂家待确认
- **其余 31 条多为「选词标记」**（隐月）：内容是单个词/章节号/空（如「混合液」「与」「反映」「根据」「1.4」「1.5」「照射」「日常」），集中在 509 TRUST、1015 血气分析仪、2010 安图A6200、505/506/510/519/520 等，像是修订时选中词做标记、未写完整意见，多数空或仅一两字。
- 状态：仅读取展示，**未删除、未修改**，等用户决定。

## 【新模块】试剂验收（试剂/质控品批间性能验证）上线 + 发货记录初始化
- **需求**：试剂/质控品换批号时做批间性能验证。选项目 → 联动允许偏倚（**WS/T 403-2024 允许偏倚** 优先，其次**卫健委 EQA 允许总误差 ÷2**）→ 默认 **5 个样本**（质控+患者样本）→ 算相对偏倚 =（新−旧）/旧×100% → **≥4 个 |偏倚| ≤ 允许偏倚 即符合要求**。
- **产物**：
  - 模型 `ReagentLotVerification`（表 `reagent_lot_verifications`，models/reagent_management.py 第 10 张表，唯一键 item+旧批+新批）。
  - API 新文件 `backend/app/api/v1/reagent_lot_verification.py`（prefix `/reagent/lot-verifications`）：列表/详情/新建/更新/`{id}/calc` 重算/删除/`criteria` 取标准/`_generate` 批量生成。已在 router.py 注册。
  - 前端 `views/reagent/ReagentLotVerification.vue`，路由 `/reagent/lot-verification`，菜单挂在「试剂管理」下（AppLayout.vue）。打印页脚用 **BG-SM-CZ-037**。
- **踩坑 1（500）**：`order_by(...desc().nullslast())` → **MySQL 不支持 NULLS LAST 语法**，必 500。改用 `desc()`（MySQL 里 DESC 时 NULL 天然排最后）。同类写法在 diag.py 里也一并清了。
- **踩坑 2（误判）**：初版 `compute_samples` 在样本未录入时 `passed=0 < need=4` → 直接判「不符合要求」且置 status=已完成（46 条被误判）。改为四态：允许偏倚未填/有效样本为 0 → 待完成；passed≥need → 符合要求；样本全录完仍不达标 → 不符合要求；只录一部分 → 待完成。前端 `localConclusion` 同步。改完需对存量记录逐条调 `{id}/calc` 重算。
- **踩坑 3**：`_generate` **没有 dry_run 参数**，脚本 `--dry-run` 时误建了 72 条 → 已 `clear_lot_verifications.py --apply` 清空重来。**教训：新增批量写接口一律加 dry_run**。
- **匹配/去重规则（重要）**：系统试剂目录里同一支试剂常有多个重复条目（`丙型肝炎病毒抗体检测试剂盒` / `…（化学发光法）` / `…（电化学发光法）`；`SJ0H0122J/特异性生长因子…` / `特异性生长因子检测试剂盒`）。去重键 = **(归一化名称, 类型)**，归一化时**只去掉「方法学」括号**（化学发光/酶法/免疫比浊…），保留有区分意义的括号（质控品 `（540-1）`/`（540-2）` 必须保留），并带 type 维度避免「地高辛测定试剂盒(试剂)」与「地高辛校准品(校准品)」被合并。
- **名称解析** `_core()`：去斜杠货号前缀；连字符前缀要求「长度≥3 且含数字」才去（否则 `D-二聚体` 会被吃成 `二聚体`）；最后清理未配对括号（`（化学发光法））`）。
- **发货记录表**：`C:/Users/81526/Desktop/民航2026年发货记录_20260914(2).xlsx`（sheet dynreport，874 行，2026 全年发货）。列：业务日期/商品名/规格/批号/生产日期/有效期/数量/物料编码货号/器械货品名/生产厂商。
- **本次初始化结果**：
  - 补批号 **33 条**（系统库存无批号的试剂 → 用最新发货批号+效期），库存有批号物品 **127 → 160** 个。
  - 生成待验收记录 **65 条**（原始 72 条批号变更 → 去重后 67 → 再清 2 条重复 → 65）。免疫 38 / 生化凝血 27；试剂 61 / 质控品 4；**45 条自动匹配到判定标准，20 条需手工填**。
  - 20 条未匹配的主要是：免疫定性（乙肝/丙肝/梅毒/HIV/内因子抗体/抗缪勒管激素）、凝血（D-二聚体/抗凝血酶/狼疮抗凝物/蛋白C/S）、贝克曼电解质类（66318/66319/66320）、复合免疫分析质控品 540-1/540-2。
- **脚本**：`analyze_shipment.py`（分析发货记录→outputs/shipment_analysis.json）、`apply_shipment.py [--dry-run|--apply]`（补批号+生成验收，dry-run 安全）、`clear_lot_verifications.py`、`dedupe_lot_verifications.py`、`report_lot_verifications.py`→`outputs/试剂验收待办清单.xlsx`。
- 部署：commit 5915bb83，`_BUILD_MARK=reagent-lot-verification-conclusion-fix-2026-09-16`。

## 【新模块】试剂验收（试剂/质控品批间性能验证）上线 + 发货记录初始化
- **需求**：试剂/质控品换批号时做批间性能验证。选项目 → 联动允许偏倚（**WS/T 403-2024 允许偏倚** 优先，其次**卫健委 EQA 允许总误差 ÷2**）→ 默认 **5 个样本**（质控+患者样本）→ 算相对偏倚 =（新−旧）/旧×100% → **≥4 个 |偏倚| ≤ 允许偏倚 即符合要求**。
- **产物**：
  - 模型 `ReagentLotVerification`（表 `reagent_lot_verifications`，models/reagent_management.py 第 10 张表，唯一键 item+旧批+新批）。
  - API 新文件 `backend/app/api/v1/reagent_lot_verification.py`（prefix `/reagent/lot-verifications`）：列表/详情/新建/更新/`{id}/calc` 重算/删除/`criteria` 取标准/`_generate` 批量生成。已在 router.py 注册。
  - 前端 `views/reagent/ReagentLotVerification.vue`，路由 `/reagent/lot-verification`，菜单挂在「试剂管理」下（AppLayout.vue）。打印页脚用 **BG-SM-CZ-037**。
- **踩坑 1（500）**：`order_by(...desc().nullslast())` → **MySQL 不支持 NULLS LAST 语法**，必 500。改用 `desc()`（MySQL 里 DESC 时 NULL 天然排最后）。同类写法在 diag.py 里也一并清了。
- **踩坑 2（误判）**：初版 `compute_samples` 在样本未录入时 `passed=0 < need=4` → 直接判「不符合要求」且置 status=已完成（46 条被误判）。改为四态：允许偏倚未填/有效样本为 0 → 待完成；passed≥need → 符合要求；样本全录完仍不达标 → 不符合要求；只录一部分 → 待完成。前端 `localConclusion` 同步。改完需对存量记录逐条调 `{id}/calc` 重算。
- **踩坑 3**：`_generate` **没有 dry_run 参数**，脚本 `--dry-run` 时误建了 72 条 → 已 `clear_lot_verifications.py --apply` 清空重来。**教训：新增批量写接口一律加 dry_run**。
- **匹配/去重规则（重要）**：系统试剂目录里同一支试剂常有多个重复条目（`丙型肝炎病毒抗体检测试剂盒` / `…（化学发光法）` / `…（电化学发光法）`；`SJ0H0122J/特异性生长因子…` / `特异性生长因子检测试剂盒`）。去重键 = **(归一化名称, 类型)**，归一化时**只去掉「方法学」括号**（化学发光/酶法/免疫比浊…），保留有区分意义的括号（质控品 `（540-1）`/`（540-2）` 必须保留），并带 type 维度避免「地高辛测定试剂盒(试剂)」与「地高辛校准品(校准品)」被合并。
- **名称解析** `_core()`：去斜杠货号前缀；连字符前缀要求「长度≥3 且含数字」才去（否则 `D-二聚体` 会被吃成 `二聚体`）；最后清理未配对括号（`（化学发光法））`）。
- **发货记录表**：`C:/Users/81526/Desktop/民航2026年发货记录_20260914(2).xlsx`（sheet dynreport，874 行，2026 全年发货）。列：业务日期/商品名/规格/批号/生产日期/有效期/数量/物料编码货号/器械货品名/生产厂商。
- **本次初始化结果**：
  - 补批号 **33 条**（系统库存无批号的试剂 → 用最新发货批号+效期），库存有批号物品 **127 → 160** 个。
  - 生成待验收记录 **65 条**（原始 72 条批号变更 → 去重后 67 → 再清 2 条重复 → 65）。免疫 38 / 生化凝血 27；试剂 61 / 质控品 4；**45 条自动匹配到判定标准，20 条需手工填**。
  - 20 条未匹配的主要是：免疫定性（乙肝/丙肝/梅毒/HIV/内因子抗体/抗缪勒管激素）、凝血（D-二聚体/抗凝血酶/狼疮抗凝物/蛋白C/S）、贝克曼电解质类（66318/66319/66320）、复合免疫分析质控品 540-1/540-2。
- **脚本**：`analyze_shipment.py`（分析发货记录→outputs/shipment_analysis.json）、`apply_shipment.py [--dry-run|--apply]`（补批号+生成验收，dry-run 安全）、`clear_lot_verifications.py`、`dedupe_lot_verifications.py`、`report_lot_verifications.py`→`outputs/试剂验收待办清单.xlsx`。
- 部署：commit 5915bb83，`_BUILD_MARK=reagent-lot-verification-conclusion-fix-2026-09-16`。

## 试剂验收第二轮修正（用户反馈，commit 14c447cb）
用户五点反馈 → 逐条落实：
1. **试剂应自动关联项目**：新增 `GET /reagent/lot-verifications/_prepare?item_id=`，一次返回「关联项目(test_item_reagents 里 role=试剂) / 库存批号 / 最近到货批号候选 / 允许偏倚」。前端 `onPickReagent` 调一次即全部带出，项目默认为第一个关联项目（可改）。
2. **批号从库里调**：`_prepare` 里 old_batch = 库存中数量最大的批次；new_batch_candidates = 已确认收货单里最近 5 个不同批号（标注 already_in_stock）。前端两个批号框改为 `el-select filterable allow-create`（可选可填）。
3. **验收范围**：只针对 `type == "试剂"`，**排除校准品/质控品/耗材**；再按关键词排除电解质/辅助试剂（`EXCLUDE_KEYWORDS = 电解质/参比液/内标液/参比电极/缓冲液`）。后端 `in_scope()` 在 `_generate` 与 `create` 双重校验，前端也有提示。
4. **打印模板改正**：`BG-SM-CZ-037` → **`BG-SM-CZ-029`**，标题改为「**生化免疫组试剂批间性能验证平行试验记录表**」（`DOC_TITLE` 常量）。
5. **「有的新批号没显示」根因**：发货记录里 `33415-超敏胰岛素试剂盒定标液` 的批号 633724 被误当成 `33410-超敏胰岛素试剂盒` 的新批号（**该行有效期本身就是空的**）。修法：`analyze_shipment.py` 加 `NON_REAGENT_KW`（校准/定标/质控/标准品/参考品/电解质…）过滤发货行 + 只保留 type=试剂的目录项。修正后胰岛素为 `633272 → 633462`（都是试剂盒批号）✓。
6. 重新分析后批号变更 72 → **61 条**，去重后生成 **55 条**待验收（免疫/生化凝血；允许偏倚自动匹配 42 条，13 条需手工填）。
- 保留诊断端点 `/_diag/lot-verif-debug`（查表存在/查询报错），后续可删。

## 试剂验收第三轮：允许偏倚定值 + 绝对偏倚支持（commit c6407d41）
用户给了 10 项具体允许偏倚并明确 2 项不参与验收 → 全部落地：
### 新增能力
1. **支持绝对偏倚**：模型/schema 新增 `bias_mode`（`relative` 默认 / `absolute`）。`compute_samples(samples, allow, need, mode)`——
   - relative：(新−旧)/|旧|×100
   - absolute：新−旧（**旧值为 0 也有效**，适合 CO₂ 这类低值项目）
   前端加「偏倚方式」单选（相对% / 绝对），样本表头、列表单位、打印表头随之联动；绝对模式在列表里打「绝对」标签。
   默认列 `server_default="relative"`，新增列由 `_ensure_missing_columns()` 自动 ALTER。
2. **未确认收货单也进批号候选**：`_prepare` 的 `new_batch_candidates` 现同时查 `is_confirmed=True/False`，每项带 `confirmed` 标记；排序把「未入库」的排前面。前端下拉标注「已确认/未确认」「已在库」。
3. **排除项扩到**：`EXCLUDE_KEYWORDS` 增加「特异性生长因子」「抗凝血酶」（科室明确不做批间验证）。

### 允许偏倚定值（`scripts/fix_lot_criteria.py`，已执行）
| 项目 | 允许偏倚 |
|---|---|
| 丙肝抗体 / 梅毒抗体 / HIV / 乙肝表面抗原 / 内因子抗体 | 15% |
| 抗缪勒管激素 | 12.5% |
| 未结合雌三醇 / D-二聚体 | 15% |
| 乳酸 | 10% |
| 二氧化碳结合力 | 8%（相对）— **可一键切绝对 ±5 mmHg** |
- 已设置 10 条，删除 2 条（特异性生长因子、抗凝血酶）→ 验收记录 **55 → 53 条**，其中 52 条有允许偏倚。
- **仅剩 1 条未设标准**：#190 狼疮抗凝物测定试剂盒（用户未提，待定）。
### 部署
`_BUILD_MARK=lot-verif-abs-bias-criteria-2026-09-16`。注意本轮部署**连续两次**才翻面（首次 4 分钟后查到仍是旧标记），遇到就再等 3-4 分钟，别急着重部署。

## 试剂验收第三轮：允许偏倚定值 + 绝对偏倚支持（commit c6407d41）
用户给了 10 项具体允许偏倚并明确 2 项不参与验收 → 全部落地：
### 新增能力
1. **支持绝对偏倚**：模型/schema 新增 `bias_mode`（`relative` 默认 / `absolute`）。`compute_samples(samples, allow, need, mode)`——
   - relative：(新−旧)/|旧|×100
   - absolute：新−旧（**旧值为 0 也有效**，适合 CO₂ 这类低值项目）
   前端加「偏倚方式」单选（相对% / 绝对），样本表头、列表单位、打印表头随之联动；绝对模式在列表里打「绝对」标签。
   默认列 `server_default="relative"`，新增列由 `_ensure_missing_columns()` 自动 ALTER。
2. **未确认收货单也进批号候选**：`_prepare` 的 `new_batch_candidates` 现同时查 `is_confirmed=True/False`，每项带 `confirmed` 标记；排序把「未入库」的排前面。前端下拉标注「已确认/未确认」「已在库」。
3. **排除项扩到**：`EXCLUDE_KEYWORDS` 增加「特异性生长因子」「抗凝血酶」（科室明确不做批间验证）。

### 允许偏倚定值（`scripts/fix_lot_criteria.py`，已执行）
| 项目 | 允许偏倚 |
|---|---|
| 丙肝抗体 / 梅毒抗体 / HIV / 乙肝表面抗原 / 内因子抗体 | 15% |
| 抗缪勒管激素 | 12.5% |
| 未结合雌三醇 / D-二聚体 | 15% |
| 乳酸 | 10% |
| 二氧化碳结合力 | 8%（相对）— **可一键切绝对 ±5 mmHg** |
- 已设置 10 条，删除 2 条（特异性生长因子、抗凝血酶）→ 验收记录 **55 → 53 条**，其中 52 条有允许偏倚。
- **仅剩 1 条未设标准**：#190 狼疮抗凝物测定试剂盒（用户未提，待定）。
### 部署
`_BUILD_MARK=lot-verif-abs-bias-criteria-2026-09-16`。注意本轮部署**连续两次**才翻面（首次 4 分钟后查到仍是旧标记），遇到就再等 3-4 分钟，别急着重部署。

## 狼疮抗凝物也排除（commit 200c397e）
- 用户："这个也不验收" → 后端 `EXCLUDE_KEYWORDS` 增加「**狼疮抗凝物**」；`fix_lot_criteria.py` 的 EXCLUDE、`analyze_shipment.py` 的 `NON_REAGENT_KW` 三处同步，保证**以后重新生成也不会再出现**。
- 已删除 #190 狼疮抗凝物测定试剂盒（凝固法）。
- **最终验收范围（排除清单）**：校准品 / 质控品 / 耗材（按 type）+ 关键词{电解质、参比液、内标液、参比电极、缓冲液、**特异性生长因子**、**抗凝血酶**、**狼疮抗凝物**}。
- **最终状态：验收记录 52 条，全部 52 条都有允许偏倚，0 条待补**。
- `_BUILD_MARK=lot-verif-exclude-lupus-2026-09-16`。
