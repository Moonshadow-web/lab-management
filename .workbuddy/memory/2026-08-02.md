# 2026-08-02 工作日志

## 应用要求（CNAS 自查）全链路打通并上线
前一轮（375d8fe）只部署了去掉「实际分配与接收」、条款重新分配、活动级联删除、docx 解析含应用要求骨架；但应用要求始终未真正入库/显示。本日补齐并验证：

- **后端漏返回**：`api/v1/self_inspection.py` 的 `assignment_clauses` 与 `self_inspection_summary` 两个端点此前未在 clause dict 里带 `application_requirement`，导致前端 `item.clause.application_requirement` 永远空。已补上（无条件返回 `c.application_requirement or ""`）。
- **解析器崩溃 bug**：`scripts/parse_cnas_docx.py` header 占位条款 dict 缺 `application_requirement` 键，遇到「同一条款多行合并」走追加分支时 `KeyError` 中断，导致 `clauses_export.json` 一直停在旧版本（应用要求全空）。补键后重跑成功。
- **docx 真实列布局**（已核实）：`[条款][认可准则][应用要求][自查结果][自查说明]`。应用要求≠认可准则的条款才需显示，如 7.3.7.2(716字)/7.2.1(115)/7.2.5(92)/4.2.3/5.1；7.5、7.3.7.1 等认可准则与应用要求两列完全相同 → 置空（正确）。共 152 条款、33 条带应用要求。
- **同步**：`sync_clauses.py`（按 clause_no upsert，无重复）更新 33 条、119 条无变化。线上 `audit_clauses` 已含应用要求。

## 部署验证
- 提交 bac8d29（应用要求全链路+显示+报告+重新分配/级联删除+解析骨架）、7e7d916（解析器修复）、87f416f（重新生成 clauses_export.json）。
- tcb deploy 成功；内网 `/api/v1/_diag/build` 翻到 `audit-clause-app-req-display-2026-08-02`（约 80s 后生效）。
- API 实测：`/audit-clauses` 7.3.7.2 `app_len=716`；`/assignments/{id}/clauses` 返回 clause 含 `application_requirement` 键（7.2.1→115、7.2.5→92）。
- 前端产物 `Iso15189Hub-CP4BjIHF.js`(63KB) 内网 200 且含「应用要求」字样 → 显示逻辑已上线。
- 测试活动「2026 年度科室内审自查（测试）」(campaign id=1) 未分配 7.3.7.2，故其汇总不含该条款（符合预期，仅当条款被分配给员工才出现）。

## 无应用要求条款自动标记为不适用（2026-08-02 上午）
用户反馈：条款内容/应用要求显示为「(无)」的条目（如 7.2 检验前过程 章节头）不应再要求填写核查内容、问题、措施。
- 前端 `SelfInspectionTab.vue`：新增 `isNoRequirementClause` 判断（content、application_requirement、check_point 均为空或 `(无)`/`（无）`）；逐条填写弹窗对这类条款自动设置 result='不适用'、check_content=''，显示「无具体应用要求 / 无需填写」tag 并隐藏填报表单。
- 报告 `reportExport.js`：`buildSelfInspectionHtml` 与 `buildEmptySelfInspectionHtml` 对无要求行自动显示 result='不适用'、check_content/finding/action 显示为「—」，避免打印出「（未填写）」。
- 后端无需改动（记录字段无必填校验）。
- 提交 `50d8f81`，deploy 后内网 build mark 翻到 `self-inspection-na-clause-2026-08-02`；新 `Iso15189Hub-Cz6u7ImN.js`(64KB) 已含「无具体应用要求」标记。

## 自查空表与预览报告统一样式（2026-08-02）
用户反馈：预览报告好看，但"打印空表"格式不同、不美观。根因：空表 `buildEmptySelfInspectionHtml` 用固定 `height:72px` 方框行且空单元格用 `　`，与报告的彩色结果/自适应行高不一致。
- 重构 `reportExport.js`：抽出共用 `selfInspectionTableRows(rows,{empty})`，预览报告与空表共用同一套 `<table>` 渲染（列宽、`.pre`、结果颜色类、字体均一致）；空表仅把核查内容/问题/措施留空（无要求行仍标「不适用」「—」）。
- 去掉固定行高，行随条款内容自适应，与预览一致。
- 提交 `8c63e91`，deploy 后 build mark `self-inspection-empty-sheet-style-2026-08-02`。

## 文件评审删除计划失败（2026-08-02）
现象：组长点"删除计划"报"删除失败"。根因=权限前后端不一致：前端 `canManageIso15189` 白名单含 `leader`，但后端 15189 三个管理模块 `WRITE_ROLES` 漏了 `leader`（scheduling.py 已含，故排班一直正常）。
- 修复：review.py / self_inspection.py / corrective_action.py 的 `WRITE_ROLES` 均补 `"leader"`，与前端 `canManageIso15189` 对齐。级联删除端点（`DELETE /review/campaigns/{id}/cascade`）本身逻辑无问题（admin 测试一直正常）。
- 验证：建临时 `leader` 账号，能创建评审计划并级联删除（返回 ok），临时账号已清理。线上 4 个 leader / 2 个 specialty_leader 用户均受益。
- 提交 `9127af3`，deploy 后 build mark `iso15189-leader-write-roles-2026-08-02`。

## 文件评审删除失败（管理员也中招）真正根因（2026-08-02）
用户反馈"管理员也删除失败"。排查发现：带分配的评审计划走**标准 `DELETE /review/campaigns/{id}`（make_router 自带、非级联）会因 review_assignments 外键约束 500**；旧版前端调用的就是这个端点，故 admin 同样失败（admin 不被 FK 豁免）。当前前端已改调 `/cascade`，但只要浏览器缓存旧 `review.js` 就继续 500。
- 根治：移除 campaign_router 自带的非级联 DELETE，把 `DELETE /review/campaigns/{cid}` 与 `/review/campaigns/{cid}/cascade` 都指向同一级联删除函数（先删 review_records、review_assignments，再删 campaign）。本地路由表确认旧 DELETE 已移除、裸路径已级联。
- 验证：线上裸路径删除临时计划（含分配/接收状态/记录）返回 `{"ok": true}` 且列表中消失；此前同操作必 500。
- 提交 `6c61a85`，deploy 后 build mark `review-campaign-bare-delete-cascade-2026-08-02`。
- 结论：现在无论前端 JS 新旧，文件评审计划删除均可用；无需再依赖用户硬刷新。

## 文件评审排除「项目说明书」类文档（2026-08-02）
用户反馈截图：评审任务里出现"孕酮试剂盒说明书 项目说明书"，并明确要求"文件评审本就不包括项目说明书"。
- 先排查线上：扫描全部评审计划 assignment，发现 **0 个**"项目说明书"类分配——之前那批是我测试时塞进 `__tmp_bare_del__` 计划的，随该计划级联删除已清掉。确认线上真实计划(id=3)无污染。
- 防御性修复（即使无历史数据也堵住将来）：
  - 前端 `ReviewTab.vue`：`visibleAssignments` / `visibleMyTasks` 在 `isObsolete`(作废) 之外再加 `isAllowedCat`（仅允许 `ALLOWED_CATS=['通用SOP','项目SOP','仪器SOP']`），非三类 SOP（如「项目说明书」）不出现在我的任务/分配管理/汇总中。文档未纳入字典时保守显示，避免误隐藏。
  - 后端 `review.py` `assign_batch`：按 `REVIEW_DOC_CATEGORIES`（与前端一致）校验，非 SOP 类文档直接拒入 `rejected` 列表（不创建 assignment）。
- 验证：线上尝试把一份"项目说明书"(id=1 肌酐测定试剂盒) 分配进临时计划，返回 `rejected:[{reason:'非SOP类文档（项目说明书），不在文件评审范围内'}]`；临时计划已清理。
- 提交 `6362239`，deploy 后 build mark `review-exclude-project-spec-2026-08-02`。

## 测试数据污染教训（2026-08-02 重要）
本轮为复现"删除失败"多次用 admin 创建 `__tmp_*` 评审计划、把真实文档分配进去、写 A-027 记录，部分临时计划未及时清理，导致用户在生产界面看到 `__tmp_bare_del__`(乱码名) 和"孕酮试剂盒说明书 项目说明书"污染。
- 教训：在**生产环境**用真实 admin 账号做破坏性/污染性验证时，必须：(1) 测试后立即级联清理所有 `__tmp_*` 计划+分配+记录+临时用户；(2) 创建前先记录，结束时 `DELETE /review/campaigns/{id}` 清理并确认列表无残留；(3) 尽量不要用真实文档做分配测试，或只用后立即删。已被用户发现两次（删失败截图 + 乱码计划）。
- 已养成：每次测试后扫一遍 `__tmp_*` 计划与用户并清理。

## 用户要求 admin 删除 id=3 真实评审计划（2026-08-02）
用户反复"删除失败"，并要求"你先删了之前的计划"。线上当时仅 1 个计划 `id=3「2026年文件评审」`（91 条分配，真实评审数据，无 `__tmp_*` 残留）。
- 用 admin 调 `DELETE /review/campaigns/3`（bare 级联）成功 `{"ok":true}`，列表清空。同时验证 `/review/campaigns/{id}/cascade`（前端 deleteCampaign 实际调用端点）也正常 `{"ok":true}`（用临时计划测，已清理）。
- 结论：删除功能对 admin 完全可用（bare + cascade 双路径均级联成功）。用户此前"删除失败"= 浏览器缓存旧 `review.js`（旧版调非级联 bare delete 必 500）；已根治（bare delete 改级联），需用户 **Ctrl+F5 硬刷新**后自己点删即可。
- 注意：id=3 是真实评审活动，已按用户明确要求删除（无备份可恢复）。若用户后悔需重建（重新建计划+分配 91 份）。
- 线上现已无评审计划，用户可重新创建。

## 15189 新增「认可能力范围」模块（2026-08-02 下午）
用户要求：15189 专项加一块"认可能力范围"，参考 `D:/民航总医院/15189/生免认可申请附表/生免组申请认可的能力范围.xlsx`，其中方法/试剂/仪器是旧表文本、需在系统里查，模块需展示+编辑修改。

### 数据模型 `models/iso15189.py` → `AccreditedScope`
- 分组：`category_l1`/`category_l2`（如 "A 检验医学" / "AA 临床血液学"）；项目：`seq/item_name/sample_type`。
- 系统关联：方法存 `method_name`+`method_id`(null，方法非独立实体)；设备/仪器 `instrument_name`+`instrument_id`；试剂 `reagent_name`+`reagent_id`；`calibrator` 校准品。
- 分析性能 5 项：`perf_correctness/perf_precision/perf_linearity/perf_reportable/perf_other`。
- 说明 `description`、备注 `remark`。已注册进 `models/__init__.py` + schemas + `router.py`（`accredited_scope.router`）。

### 后端 `api/v1/accredited_scope.py`
- `make_router(AccreditedScope, ..., prefix="/accredited-scope", write_roles=WRITE_ROLES)`，WRITE_ROLES 同 15189 其他模块（含 `leader`）。
- 额外 `POST /accredited-scope/batch?replace=true`：一次事务清空+插入（供 xlsx 种子脚本）。`update` 走 **PUT**（非 PATCH，与 make_router 一致）。

### 导入脚本 `scripts/import_accredited_scope.py`
- openpyxl 解析 Sheet1（A1:R109）：A=序号/B=项目/C=样品类型/D=方法/E=设备/F=试剂/G=校准品/H=说明/I=备注/J=正确度/K=精密度/L=线性/O=可报告范围/Q=其他（数据实际落 Q(17)，R 全空）。
- 分组判定：A 非空前导字母 1 个→l1，≥2 个→l2。共 64 条。
- 方法/试剂/仪器先存旧文本到 `*_name`，`*_id` 留空（待前端下拉关联）。以脚本内嵌 admin 账号 POST 线上 API 导入（非本地 SQLite）。

### 前端
- `api/accreditedScope.js`：`listScopes/createScope/updateScope(用 request.put)/deleteScope/batchImportScopes`。
- `views/iso15189/AccreditedScope.vue`：按 `(l1,l2)` 分组表格展示（含分析性能列、已关联 tag）；编辑弹窗里方法/设备/试剂用 `el-select` 联动系统（test_items.method 去重 / instruments / reagent 列表），选中自动填 `*_id`+`*_name`；方法非实体仅存文本。搜索关键字过滤。`canManageIso15189` 控制增改删按钮。
- `Iso15189Hub.vue` 加第 4 个 `el-tab-pane`（"认可能力范围" → AccreditedScope）。

### 部署与验证
- 提交 `ebb2df2` → tcb deploy（--force，No 灰发）。约 1 小时后内网 build 翻到 `accredited-scope-2026-08-02`（中途 curl 多次空/旧，最后稳定为新值）。
- 跑 `scripts/import_accredited_scope.py --apply` → 导入 64 条（created=64）。
- 验证：`GET /accredited-scope?page_size=3` 返回 total=64 且数据正确；`PUT /accredited-scope/1` 改 remark 生效、回退成功（确认编辑路径用 PUT 而非 PATCH——PATCH 返回 405）。
- **注意**：make_router 的更新端点是 PUT，前端 updateScope 必须 `request.put`，否则编辑 405。

## 生化(AC 临床化学 28 项) 系统关联修正（用户确认口径）
- 试剂：全部用系统贝克曼 OSR 试剂盒；**去 "OSRxxxx/" 前缀**（含 ALT 的 "OSR6109-" 短横变体也去）。脚本 `scripts/fix_biochem_scope.py`（APPLY=1 落库，含 401 自动重登重试）。
- AST(id=18)：系统试剂 `OSR6109/天门冬氨酸氨基转移酶测定试剂盒`(reagent_id=176) → 已关联，方法 MDH法（取自 test_items）。
- C反应蛋白(id=34)：用户要求保留原 → 试剂还原 Excel 原文本「积水C反应蛋白检测试剂盒（国械注进20162401422）」、方法保留「免疫比浊法」，均不关联系统项。
- 糖化血红蛋白(id=33)：仪器关联东曹 HLC-723G8(id=19)；试剂**不关联**（系统里是 sebia 电泳试剂，错误），reagent_name 保留东曹原文本；方法取系统 HPLC法。
- 碱性磷酸酶(id=23)：系统 test_items 方法写「NNP底物-AMP缓冲液法」疑似笔误，保留 Excel 正确值「NPP底物-AMP缓冲液法」。
- 方法学：统一取自系统 `test_items` 的 method 字段（精确 analyte 核心匹配，避子串误配）；电解质(钾/钠/氯)方法=间接离子选择电极法。
- 仪器：27 项→AU5800(id=5)，糖化→东曹(id=19)。纯数据更新，无需重新部署。

## 生化(AC 临床化学)项目 系统关联（2026-08-02 晚）
用户要求：生化里把仪器/方法/试剂在系统里查并自己关联（用户确认）。共 28 项（id 7-34），已脚本化 PUT 关联（纯数据更新，无需部署）：
- **仪器**：全部 → id=5（系统名 `AU5800`，Excel 旧文本"贝克曼全自动生化分析仪AU5800…"）；糖化血红蛋白(33) → id=19（糖化血红蛋白分析仪/东曹HLC-723G8）。
- **试剂**：按 analyte 关联到系统**贝克曼 OSR** 生化试剂盒（cat=生化，brand 贝克曼库尔特）。映射见脚本 REAG 字典：肌酐183/葡萄糖185/尿素190/总蛋白189/白蛋白173/ALT175/AST无/总胆179/直胆177/ALP174/GGT182/甘油178/总胆固181/HDL196/LDL194/淀粉酶193/脂肪酶188/CK192/LDH187/尿酸199/钙172/镁197/磷186/钾钠氯→157(电解质参比液)/CRP→249(超敏C反应蛋白R1)。
- **关键提醒（用户待确认）**：Excel 旧试剂是 **柏定生物/尼普洛/积水** 品牌，系统生化试剂盒是 **贝克曼 OSR** 系列（不同产品）。关联后 reagent_name 已被改写为系统名（如"OSR61204/肌酐测定试剂盒（肌氨酸氧化酶法）"）。用户可能要保留原品牌(柏定)或新建设备试剂项。
- **未关联项**：AST(18) 系统无对应试剂（保留柏定旧文本）；糖化血红蛋白(33) 为多试剂(溶血剂/洗脱缓冲液/层析柱)且系统为 sebia、Excel 为东曹，品牌不符未关联；CRP(34) 系统只有"超敏C反应蛋白R1/R2"(积水)，关联 R1 并标记。
- **方法**：method 非独立实体（无 method_id），保留 Excel 原文本未动。
- 验证：`GET /accredited-scope/{12,18,33,34}` 确认 inst_id/reag_id 已落库。

## 项目查询「项目说明书」关联 bug：总胆固醇错配到总钙（2026-08-02 晚）
- 现象：项目查询里"总胆固醇"卡片没显示「项目说明书」按钮，但文档库里有 `id=18「总胆固醇测定试剂盒（酶法）」`(category=项目说明书)。
- 机制：前端 `TestItemList.rowManuals` 用 `row.name` 去 `manualMap`（key=接口返回的 `linked_project`）查；后端 `/documents/project-manuals` 把文档标题用 `_manual_core` 提取核心名（去括号/试剂盒/说明书）再与 test_items 的 name+aliases(逗号拆分) 做精确/子串匹配得 `linked_project`。
- 根因（数据污染，非算法）：`test_items` 里 **id=7「总钙」的 aliases 被错写成 'CA Calcium,钙, TC, CHOL, 总胆固醇'**——TC/CHOL/总胆固醇 是「总胆固醇」(id=28) 的别名，批量导入时串列到总钙。匹配时"总胆固醇测定试剂盒"→core='总胆固醇' 命中总钙的污染别名 → linked='总钙'，总胆固醇卡片自然查不到。
- 顺带发现 id=13「高密度脂蛋白胆固醇」aliases 含 'PFNA, Ⅰ型前胶原氨基端原肽'（PⅠNP 项目别名，同类串列）。
- 修复（纯数据 PUT，无需部署）：id=7 aliases→'CA Calcium,钙'；id=13 aliases→'HDL HDL-C High Density Lipoprotein'。
- 验证：`/documents/project-manuals` 中 id=18 现 linked='总胆固醇' ✓；4 个胆固醇文档全部正确归位（其余胆固醇此前已对，仅总胆固醇因总钙污染而错配）。
- **可复用排查套路**：项目说明书/试剂说明书不显示或错配 → 先查 `/documents/project-manuals` 看该文档 `linked_project` 是否为 None 或错配到别的项目 → 若是，查对应 test_items 的 aliases 是否被其它项目污染（导入串列常见）→ 修正 aliases 即可，一般不必改匹配算法。

## 认可能力范围 按更正后 xlsx 重建（2026-08-02 晚，第2轮）
用户更正了 `生免组申请认可的能力范围.xlsx`：大量项目新增「血浆」样品类型续行、部分项目（乙肝表面抗原/抗丙肝抗体）新增「罗氏 cobas e411」仪器续行、CRP 新增「全血(加EDTA抗凝剂)」、降钙素原新增「全血(EDTA)」。旧 `import_accredited_scope.py` 的解析器会跳过这些续行（B 空且非分组 → 直接 continue），故需重写。

### 新脚本 `scripts/import_accredited_scope_v2.py`（取代 v1 用于全量重建）
- 解析器展开续行：① B 空 + C(样品类型) 非空 → 新样品行（同一项目多样品类型）；② B 空 + C 空 + E/F/G 非空 → 同一样品类型的附加仪器/试剂/校准品，用 `_join` 以 `\n` 合并（去重）到当前样品行。
- 关联继承：按 `item_name` 从**现有线上数据**继承 `instrument_id/instrument_name/reagent_id/reagent_name/method_name/method_id`（chem 的 AU5800+贝克曼试剂、糖化→东曹、CRP→AU5800 等已确认关联），原样应用到该项目所有样品行（含新增血浆行）；无系统关联的（免疫/血凝）保留 xlsx 长文本、`*_id` 留空（系统无对应单一实体：DXI800 有多台、罗氏 cobas 无登记、沃芬 ACL 无对应）。
- 内容字段（校准品/分析性能5项/样品类型）一律以更正后 xlsx 为准；CRP 试剂文本继承 xlsx 更正值「积水C反应蛋白检测试剂盒（苏械注准20212400060）」（旧库是 国械注进20162401422）。

### 结果与验证
- `APPLY=1` 调 `POST /accredited-scope/batch?replace=true`：created=101（原 64）。
- 分类计数：AC 临床化学 56（28 项×血清+血浆）、AD 临床免疫学 39（30 项，其中 8 项加血浆、降钙素原加全血EDTA、乙肝/丙肝合并 e601+e411）、AA 临床血液学 6。
- 样品类型取值：血清 / 血浆 / 全血(加肝素抗凝剂) / 全血(加EDTA抗凝剂)。
- 纯数据重建，无需前端改动/重新部署；前端按 (l1,l2) 分组表格渲染，多样品类型自动成多行。用户 Ctrl+F5 即可见。
- 坑：验证脚本把 `instrument_name[:30]` 截断，一度误判 e411 丢失；实际 DB 全量存了 e601\n+e411。验证时别截断长字段。
