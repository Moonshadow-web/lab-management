
## 测量不确定度 EQA 偏倚功能（2026-08-20）
- 需求：EQA 合格/不合格需选择；不合格填 5 水平(靶值/测量值)算 RMS 偏倚并纳入 u_c；合格时偏倚已含于精密度，不填。
- 后端：
  - Model uncertainty 加 `bias_levels`(Text JSON [{target,measured}...])；`_ensure_missing_columns` 自动补列。
  - Schema 加 `bias_levels: list=[]`；make_router json_fields 加 bias_levels。
  - compute_record：pt_result=合格 → bias_rms=0；不合格 → RMS=√(Σ((测量-靶)/靶×100%)²/n)；u_c=√(u_Rw²+u_cal²+bias_rms²)。
- 前端 UncertaintyAssessment.vue：加 EQA radio(合格/不合格)、不合格时 5 水平靶值/测量值输入表+实时偏倚%+RMS 显示、合格时提示不填。
- 修复"未找到记录"：编辑保存 PUT 404 时降级 POST 新建（记录可能已被删）。
- 部署：uncert-eqa-bias-2026-08-20。
- 验证：合格 bias_rms=0 u_c=2.0122；不合格 bias=[5,2,2,2,-1]→RMS=2.7568 u_c=3.4131 精确匹配；保存持久化正常。

## 不确定度判定改为卫健委 EQA 允许总误差 TEa（2026-08-20）
- 需求：结论判定用"质量目标（允许总误差 TEa）"，取卫健委 EQA（nccl-2026）的 tea 值（不是行标 bias）。
- 系统"项目质量目标"模块（quality-requirements）数据：565 条 = nccl-2026(卫健委EQA)242 + bj-hr-2025(北京)188 + wst403-2024(行标)133 + temp2。
  - nccl tea 格式如"靶值 ±6%"（白蛋白）、"±16%"（ALT）、"±18%"（ALP）。
- 后端 uncertainty.py：
  - `_extract_core_name`：去掉项目名括号英文（"白蛋白（ALB）"→"白蛋白"）用于精确匹配。
  - `lookup_target_bias` 重写：只查 nccl-2026 的 tea（完整 TEa，不再 1/2、不再行标/北京优先级）；先精确(item_name in [core,name])再模糊(like %core%)。
  - 新增 `GET /uncertainty/_search_targets?q=`：返回 nccl-2026 模糊候选 [{id,item_name,tea,tea_pct}]，供前端手动选择。
  - `_preview`：前端已传 target_bias 则不覆盖（手动选择优先）。
- 前端 UncertaintyAssessment.vue：
  - 项目名下方加"质量目标"el-select（filterable+remote，搜 _search_targets），展示"项目名（TEa x%）"，选中写 target_bias/text/source；项目名 blur 自动搜并选第一个。
  - 标签"允许偏倚"→"允许总误差"，报告模板文字同步。
- 部署：uncert-tea-select-2026-08-20。

## 生化2号机性能验证报告批量导入（2026-08-20）
- 需求：把「生化2号机 靶机」36 份性能验证 xlsx 按之前 ALP 的方法（upload→parse_and_store）在系统生成验证记录+归档。
- 方法：遍历目录，逐文件 multipart upload 到 /api/v1/report-archives/upload（parse_and_store 自动生成 verification_report + report_archive）。
- 结果：35 个成功（跳过 2.ALP.xlsx，因已存在 id=1，仪器编号 2025107507 即2号机），0 失败。
  - verification-reports 共 36 条（ALP id=1 + 新 35 条 id=16~50）；每条 6 项 verify_items、8 项 result_summary 均有值、linear_low/high 正确。
  - report-archives 36 个（quantitative），与记录一一对应。
- 清理：3 个测量不确定度 HTML 孤儿归档（id=28/29/30，对应已删的 uncertainty 记录）。
- 复用脚本：C:/Users/81526/AppData/Local/Temp/batch_import_vrf_2hao.py。
- 备注：vrf_parser 现已稳定（本地 36 文件全解析成功），可复用于其他机器（3号机/4号机/唐筛/急诊）。

## 不确定度 12月周期+PDF下载+tip改正式（2026-08-20）
- 评定周期：6→12 个月（Model.cycle_months default + 前端 form + 报告模板两处 + tip 文字）。
- 报告下载 PDF/打印：前端用浏览器原生 window.print 方案（0 依赖）——「下载 PDF / 打印」按钮→新窗口打开 HTML→window.print()，用户选「另存为 PDF」或打印机。报告 HTML 加 @page{size:A4;margin:18mm 16mm} + @media print 样式。
- tip 框改正式：后端 + 前端两套 reportStyle 的 .note 都去掉黄底/左边框，改为普通段落；去掉 💡/⚠️ 图标；「允许偏倚」→「允许总误差 TEa」。
- 部署：uncert-pdf-print-12mo-2026-08-20。
- 验证：后端报告 HTML 含12月、TEa、@page A4；不含 💡/f9f9d0/旧偏倚文案。
## 系统价值重盘点（2026-08-20，不涉论文）
用户已提交软著件①②③，要求重新看系统找有价值部分（明确不提论文）。本次挖到两个此前未分析的新模块：
- **测量不确定度评定引擎 api/v1/uncertainty.py（新发现，方法学中高）**：top-down 合并公式(calc_single_u_rw/calc_multi_u_rw) + EQA不合格时偏倚RMS纳入合成不确定度(bias_rms) + 自动关联卫健委TEa质量目标库判定(lookup_target_bias, 核心名提取+精确/模糊匹配) + 患者结果区间换算。对应 BG-SM-CZ-072，CNAS/15189 强制刚需。top-down为公开指南方法，但'偏倚条件纳入+质量目标自动判定'组合有中等专利概率。
- **排班约束生成引擎 api/v1/scheduling.py（新发现，中低~中）**：固定岗优先级递减+发热白班每4工作日一班+早/连班流水线轮转+换班记忆锁定(is_locked)。检验科特有领域调度。
其余价值维度：多源质量目标基座(WS/T403+北京HR+NCCL同表)、不符合项/纠正措施闭环、审计追踪、CNAS标准库、15189能力范围=合规体系资产；多年真实运行数据=数据资产；质控/不确定度/比对/EQA可SaaS化推广=转化价值。
产出文档：系统价值盘点_2026-08-20.md。
## 性能验证模块评估（用户追问）
verification.py=纯文本记录CRUD；verification_report_gen.py=模板驱动报告生成器(加载BG-SM-CZ-039/040 xlsx模板填数据，CV/偏倚/回归计算留在Excel模板公式)。
结论：无方法创新/无专利价值；定位=合规体系资产(中)，ISO15189/CNAS强制刚需+起草单位(WS/T408)权威加成；与试剂管理、CNAS标准库同档。已补入系统价值盘点文档第六节。

## 性能验证后端化计算引擎（2026-08-20）
- 需求：新建性能验证把计算从 Excel 公式改到后端 Python，报告仍输出与模板一致，核心指标/结论静态写入（不依赖 Excel 重算）。
- 新增 `backend/app/services/verification_calc.py`：compute_verification 总入口 + 各验证项计算。
  - 精密度：EP15 组内方差平均（批内 Vr=Σs²/D、批间 Vb=SD(均值)²−Vr/n、实验室内 VT=Vr+Vb）；判定批内≤TEA/4、实验室内≤TEA/3（定性 7.5%/10%）。
  - 正确度：总均值/相对偏倚，|偏倚|≤TEA/2。线性：各点均值 vs 理论值(端点外推)相对偏倚≤TEA/2 + 线性回归 R²/斜率/截距。
  - 符合率≥95%、检出限阳性率≥95%、可报告低限≤TEA/高限≤1/2TEA、参考区间每组≤2、特异性偏倚≤10%。
- `verification_reports.py`：`_auto_fill_result_summary(data, db)` 改为调引擎强制重算；`_lookup_tea` 从 quality_requirements 表按项目名联动 TEa（wst403→bj-hr→nccl 优先级，首百分比）；TEA 未填自动取并写回 rec.tea。
- `verification_report_gen.py`：新增 `_fill_static_computed`——精密度 E58/K58均值、E59/K59批内CV、E60/K60批间CV、E61/K61实验室内CV(写小数+number_format 0.00%)、I30/I31/K30/L30/K31/L31判定、A34结论；正确度 B/C/E/G/I 28/29行 + A32；线性 G/H 40-45 行均值/理论值。
- 前端 VerificationArchive.vue：buildPayload 把 precision_within_cv_target/lab_cv_target 塞进 data._meta 传后端。
- 本地测试通过：定量样例（CV 0.19%/偏倚 0.03% 全符合）+ 定性样例（CV 7.18%≤7.5% 全符合），报告关键单元格为静态值。
- 提交 84e8274，_BUILD_MARK=vrf-backend-calc-engine-2026-08-20。
- 注：模板几百个中间统计公式（EP15 F检验/EP6多项式/EP9回归）仍保留，Excel 打开照常显示；核心判定/指标已被后端静态值覆盖，二者一致。

## 性能验证4问题修复（2026-08-20 晚）
1. **分析特异性只显示一个值**：vrf_parser 只读 R26 C6/C7（1个干扰物实测），丢 C2 的3个干扰物名。修：R26 行把 C2 干扰物名列表拼到 result → "干扰物：胆红素、甘油三酯、血红蛋白（实测：≤752.55umol/L）"。
2. **允许总误差显示 0.18**：verification_report.tea 存小数（0.18=18%）。修：前端 VerificationRecords.vue 加 teaPct() 转百分数。
3. **上传后需手动点"生成报告"**：upload 端点没自动 generate。修：upload 解析成功(vrep_id)后自动调 build_verification_report 生成 xlsx + 写 report_file_path + 建 generated 归档。踩坑：report_archives.py 未 `import json` → NameError（用 capture_exception 写 last-errors 才定位到）。
4. **归档结论不齐全**：list-by-project 聚合遍历 verify_items 用 base key，但 result_summary 用 subKey(precision1/2)。修：base key 找不到时合并 subKey1/subKey2。

## 重要坑（并发 AI 冲突）
- 另一 AI commit 84e8274 "新建性能验证后端化"：新增 verification_calc.py，`_auto_fill_result_summary` 改成**引擎全量重算**（旧版只填缺失）。上传解析记录的 data 字段为空{}，引擎算不出→把 vrf_parser 已解析好的 result_summary 覆盖成空/"不符合要求"。
- 修：`_auto_fill_result_summary` 开头加 `if not data_field: return data`（上传解析跳过引擎重算）。
- 部署：vrf-autofill-guard-2026-08-20。已重置重导36条，全正确。

## 不确定度form重设计（2026-08-20 晚）
- form字段：移除project_code/instrument；新增project_method(sample_type默认血清/analyte被测量)。
- 智能填充：onProjectChange → 模糊搜索 test_items(/api/v1/test-items?q=)→ 取method填project_method、按方法推断analyte(酶法/U/L→活性,否则浓度)、取unit填patient_unit、specimen默认血清。
- 测量方法下拉：el-select(filterable+remote+allow-create)，输入项目名弹候选。
- 报告模板动态化：第一节标题=`{方法}测量人{sample_type}{analyte}测量结果不确定度的评定`；结论=`实验室{方法}测量人{sample_type}{analyte}的性能符合要求`；删除💡/⚠️图标；去掉检测系统/仪器行；RSD每系统独立显示。
- 多系统输入框扩宽：el-col :span 3+5+4+3+5（名称+L1均值+L1 SD+L1 n+L1 RSD%），L2同行（无名称空位）。
- 校准品不确定度tip：「多个校准品时，保守选择相对标准不确定度最大的」。
- 后端：Model/schema 加sample_type(默认血清)、analyte；remove project_code/instrument。
- 部署：uncert-form-redesign-2026-08-20。

## 上传不再生成报告（2026-08-20 晚）
- 反馈：上传的不要再生成一份报告了。记录直接关联原 xlsx，点下载即下载原文件。
- 改：report_archives.py upload 端点移除 auto generate 块，改为写 `record.report_file_path = uploaded xlsx 路径`（仅当空时，避免覆盖新建向导已生成的 report）。
- 改：VerificationArchive.vue 删"重新解析"按钮（reparse函数+import 也删）；"查看记录"按钮改为 `v-if="row.ref_report_id"`（之前是 v-else）。
- 新建向导流程不变（仍生成 generated xlsx 报告 + 写 record.report_file_path）。
- 部署：vrf-upload-no-autogen-2026-08-20。

## 不确定度 form 精细打磨（2026-08-20 晚）
- 内部错误：保存 500 — TypeError 'project_method' is invalid keyword for UncertaintyAssessment。修：Model 加 project_method(Str100) + calibrator(Str200) + unit(Str30) 字段；Schema 同步。
- 测量方法下拉 label：「项目名 - 方法名」（methodLabel 函数）。
- 试剂/校准品/单位都改 el-select(remote filterable allow-create)，label「项目名 - 字段值」。
- onProjectChange 同步 searchMethod + searchItemField(reagent/calibrator/unit)，自动填所有字段。
- 患者结果 label：患者结果(可选) → 患者结果（1例）。
- 全部 el-input-number 加 :controls="false"（删除 +/- 按钮）。
- analyte 自动推断：去掉 method 含"酶"关键字的判断（避免葡萄糖被误判为活性），只依据 unit='U/L' → 活性，否则浓度（用户仍可手动改 analyte）。
- 部署：uncert-form-polish-2026-08-20。

## 性能验证记录显示优化（2026-08-20 晚）
- 特异性分行：vrf_parser 读 R26/R27/R28 三行（每行 6/7/8=名/限量/结论），拼成多行 `\n` 连接 → 前端表格用 white-space:pre-line 自动分行。
- 可报告范围分两行：subKey1/subKey2 各自一行（之前合并为"低限5/高限1500"）。
- TEA 后加（计算结果）：加 expandTea 函数解析 requirement 里的"≤ 1/4 TEA"/"≤ 1/3 TEA"/"≤ 1/2 TEA"等，按 TEA% 算出倍数（如 TEA=20% → 1/2 TEA=10%）括号显示。
- TEA 一位小数：teaPct toFixed(1)（20.00%→20.0%）。
- 批量用本地 vrf_parser 重新解析 36 个 xlsx，PUT 更新 record.result_summary（让历史数据生效）。
- 部署：vrf-tea-display-2026-08-20。

## 不确定度 form 第三次打磨（2026-08-20 晚）
- 删旧报告单位 el-input：之前改下拉时漏了删旧的 el-input 框，导致 2 个"报告单位"。删掉后只保留 el-select 下拉。
- 多系统输入框加宽：每个 :span 调大（名称 3→4、均值/SD 5→6、n 3→4、RSD 5→4），gutter 12→16。
- 患者结果标签去掉"(1例)"。
- payload 加 project_code: '' 兼容旧列（前端不传但 Model 没字段，列 NOT NULL）。
- 报 500 根因：MySQL 旧列 project_code/instrument NOT NULL 无 default（Model/Schema 早删了，列还在）。
- 修：main.py 加 relax NOT NULL 迁移：ALTER TABLE uncertainty_assessments MODIFY project_code/instrument VARCHAR NULL DEFAULT ''（仅 MySQL，幂等）。
- 部署：uncert-instrument-null-2026-08-20。

## 性能验证记录显示第4次打磨（2026-08-20 晚）
- 可报告范围 result 前加"低限/高限"前缀（原只显示纯数字"5"，改为"低限 5"）。
- 稀释倍数 "/" 时不做可报告范围验证：显示 2 行（content="可报告范围"，requirement="低限同线性范围下限"/"高限同线性范围上限"，result="—"，conclusion="无稀释倍数，不做可报告范围验证"），保留可读性但去掉了"≤ TEA"验证要求列。
- expandTea 加（计算结果）正式接入 add()：item.requirement = expandTea(item.requirement, r.tea)。之前函数定义了但从未调用（之前脚本说 "OK" 但实际没插入到正确位置）。
- 部署：vrf-dilution-tea-2026-08-20。

## 多系统输入框第2次扩宽（2026-08-20 晚）
- 之前 el-form-item label 在左 + label-width=40-65px 占了横向空间，input 区域太挤。
- 改：label-position="top"（label 在 input 上方）+ label-width="0" + 字段 span 4+5+5+4+6=24。
- label 在上，input 满宽，5 字段/行能清晰显示。
- 部署：multi-sys-label-top-2026-08-20。
- 截图2说明：质量目标/试剂/校准品/单位已经是 el-select 下拉（带 placeholder），不是普通 input框。需要先输入项目名触发 onProjectChange 才会自动搜出下拉候选（贝/沃/罗等品牌）。

## 验证要求 TEA 计算结果 bug 修复（2026-08-20 晚）
- 之前 expandTea(item.requirement, r.tea) 用了 item.requirement（rs[key] 的内容，result/conclusion 字段，无 requirement），永远空字符串，函数直接 return。
- 实际 requirement 文本在 requirements dict 的 req[key] 里。
- 修：conclusionRows 开头循环生成 reqLocal = {k: expandTea(req[k], r.tea) for k in req}，add() 用 reqLocal[key]。
- 每个 record 单独算 TEA 倍数（避免污染模块级 requirements）。
- 部署：records-tea-fix-2026-08-20。

## 性能验证结论格式化后端化 + 可报告范围稀释逻辑修复（2026-08-20 晚）
- 问题：① 可报告范围并非都做，稀释倍数="/"时应等同于线性范围（验证要求"等同线性范围"、结果=线性范围如1-15kU/L、结论"无"）；有稀释倍数才合并 low-high。② 验证报告归档页(ProjectVerificationArchive)结论未同步：顺序、前缀(批内CV/实验室内CV/相对偏倚)、单位、合并5-1200 都没复刻。③ 前端 conclusionRows 对 precision 重复加"批内CV"前缀(后端已加)→ 双前缀 bug。
- 架构：把结论文本的唯一真源移到后端 `compute_verification`——精密度=批内CV/实验室内CV、正确度=相对偏倚 低值X% 高值Y%、线性=声称范围+单位、可报告=合并 low-high+单位（dilution='/'→线性范围+结论"无"）。前端只原样展示，去掉重复前缀/transform。
- `verification_calc.py`：compute_verification 加 dilution/unit 参数；trueness/linearity/reportable 输出上述格式化串；新增 _fmt_num（整数去小数）。保留 reportable1/2 供 Excel 模板。
- `verification_reports.py`：_auto_fill_result_summary 传 dilution/unit；新增 _recompute_summary(不落库重算) + _build_ordered_conclusion(固定顺序 精密度→正确度→线性→可报告→参考区间→分析特异性)；list_by-project 用二者重算+排序，旧记录也按最新格式显示；新增 /project-verification-archive/conclusion-records 供性能验证记录页（同样重算，解决老记录旧格式）。
- 前端：VerificationRecords.vue conclusionRows 改为直接展示后端 result（去双前缀），稀释'/'改单行(等同线性范围)；listVerificationReports 改调 conclusion-records。ProjectVerificationArchive 读 latest_items_summary(后端已排好序+格式化)。
- 部署：vrec-conclusion-fmt-2026-08-21。本地 import app.main OK；compute_verification 单测验证 trueness/linearity/reportable/dilution 四态格式正确。
