# 2026-07-14 工作日志

## EQA 半年/年度总结按分类拆成两份独立报告（生化+凝血 / 免疫，两人分别负责）
- 需求：总结从"一份合并报告"改为按组出两份独立报告，各自统计/文字/Word存档/签字。用户确认：**「其他」类别不进任何报告**；**签字区留空手写**。
- 后端 `EqaSummary` 加 `category VARCHAR(20)` 列，存储 key 从 (year,half) 改为 **(year,half,category)**；main.py `_migrate_schema` 补 ALTER + 回填旧行 NULL/空 category→'生化+凝血'；schemas.EqaSummaryBase 加 category。
- `eqa.py`：新增 `_SUMMARY_CATEGORIES=["生化+凝血","免疫"]`、`_CATEGORY_SLUG={BC,IM}`、`_normalize_category()`；`_compute_summary_by_category(year,half,db,category=None)` 先过滤 plans 到两大类且（指定时）收窄到该组，「其他」永不纳入；order=[target] 或两类；返回加 category 字段。所有端点 summary-by-category/summary-generate/summary-docx/summary-text(GET+PUT) 加 category 参数；generate 文件名 `{year}_H{half}_{BC|IM}.docx`；docx_url 带 category(quote)。
- `services/eqa_summary_doc.py::build_eqa_summary_doc(...,category=None)`：标题 `{category}组室间质评总结报告表`、部门 `(xx组)`、合格分析 scope、签字 `{category}组负责人签字`；**单分类时不加「合计」行**（len(categories)>1 才加）。
- 前端 `api/eqa.js` 五个总结函数加 category 参数；`QCList.vue` 总结区加 el-radio-group（生化+凝血/免疫）切换，切换即重载统计+文字+docUrl，标题/按钮/预览标题/签字随 category 变；单分类隐藏"合计"块/行。
- HTTP 验证通过：生化+凝血 60计划/334细项、免疫 47计划/234细项，两组互不干扰、文字各自独立；生成 2026_H0_BC.docx + 2026_H0_IM.docx，标题/部门/负责人签字按组正确、无合计行。前端 build 通过。
- 后端启动坑复现：PowerShell `Start-Process -RedirectStandard*` 遇环境变量 PATH/Path 大小写冲突报错；改用 Bash 后台 `venv/Scripts/python.exe -m uvicorn ...` 启动成功。登录接口是 OAuth2 form（x-www-form-urlencoded），不是 JSON。

## 北京市临检中心官网成绩导出并关联 EQA 模块
- 用户需求：从北京市官网(corelab.clinet.com.cn, 账号 01110025/@mh01110025)导出室间质评成绩并传进来；并要求把 NCCL 账号(101127/aa101127)也记进 skill。
- 流程：playwright-cli 登录北京官网 → 室间质评 → 质评成绩回报(pdf) → DataArea frame。
- **最大坑：safedog WAF 拦截 HeadlessChrome UA**。浏览器里导航 DataArea 一律返回 safedog 404 页；必须改用桌面 UA + cookie 的 Python requests 直连才成功（200, 126 条链接）。
- 列表唯一可用 URL：`desktop/pdfptscoretitle.asp?t_id=4731&cclname=<GBK "北京市临床检验中心">&SpecType=`（whichsort/times 变体返回空壳；t_id 不带 cclname 也 404）。
- cookie 从浏览器 `document.cookie` 提取（ASPSESSIONIDAESQSADT/ASPSESSIONIDQGTTTABR/_CLINETFORMS），存 `outputs/bj_cookies.txt`。
- 结构：每个项目编码(01/D3/E7…)下含 定量/定性/汇总 多 PDF；取**汇总**作成绩回报。目前官网仅第1次出分。
- 映射：DB program(北京市)→官网编码 11 项（常规化学A→01、内分泌→04、肿瘤标志物A→05、凝血试验→11、糖化白蛋白→85、系列B→86、系列C→87、抗原抗体快速检测→88、血清降钙素原→89、中孕期→D3、细胞因子→E7）。
- 北京市库有但官网无成绩(跳过)：D-二聚体(44/45)、京津冀鲁盲样(54/55)、系列A(88/89)、结核(96)、sTfR(97)。北京市库无"尿液定量生化"（官网12尿液化学分析不映射）。
- 实跑：`outputs/download_bj_eqa.py --download` → **11 个 PDF 下载入库**（backend/data/eqa_reports/，report_file 仅写、未动 returned），跳过 20、未匹配 0。
- 验证：`GET /api/v1/eqa-plans/report/{40/94/46}` 均 200/application/pdf/%PDF；未导入 404。eqa_reports 目录共 45 PDF(34 卫健委+11 北京)。
- 账号已固化进用户级 skill `nccl-eqa-export`（含 NCCL 101127/aa101127 与北京 01110025/@mh01110025 及完整北京 WAF 绕过流程）。
- 待确认：用户未指定「汇总」还是「定量」PDF，默认取汇总；如需定量可改 `download_bj_eqa.py` 的 `links.get("汇总") or links.get("定量")` 顺序。第2次成绩官网未出分，后续可重跑脚本。

## 用户三问 + 每月自动抓取自动化
- 用户问：(1) 报告在哪看 → 前端「质控管理 → 室间质评」tab 表格的「报告」列，已导入行显示绿色「已导入」按钮可下载（硬刷新 Ctrl+F5）；(2) skill 怎么用 → 用户直接说"导出室间质评成绩"即可，由我加载 nccl-eqa-export 执行；(3) 能否每月自动抓 → 已建**每月自动化**。
- 自动化：`automation-1783959860633`「每月自动导出室间质评成绩」，recurring `FREQ=MONTHLY;BYMONTHDAY=1;BYHOUR=9;BYMINUTE=0`，cwds=项目根，ACTIVE。设计要点：**每次重新登录刷新 cookie**（避免会话 cookie 过期失效）+ 北京 WAF 绕过（桌面UA+真实cookie的requests直连）。用户可随时在自动化列表暂停/删除/改时间。

## EQA 表格 UI 调整（室间质评 tab）
- 需求：操作列要能点击「预览报告」；行列间距调整至合适。
- 改动 `frontend/src/views/qc/QCList.vue`：
  1. 操作列新增「预览」按钮（`v-if="row.report_file"` 时才显示），点击 `onPreviewReport` 复用 `downloadEqaReport` 取 blob → `URL.createObjectURL` → 在 `el-dialog`(width 82%) 内 `<iframe>` 内嵌预览；关闭时 `revokeObjectURL`。操作列宽 220→330 容纳 5 个按钮。
  2. 表格加 `class="eqa-table"`，scoped 样式收紧单元格内边距(`padding:6px 0` + `.cell padding:0 10px; line-height:1.45`)、行高 46px、按钮 `margin:2px 4px`，列宽与行距更紧凑适中。
- 验证：`vite build` 通过（QCList 分块 27.63kB）。dev(5173) HMR 或硬刷新 Ctrl+F5 即见。纯前端改动，无需后端重启。

## 导入报告时一并回填成绩/合格与否
- 需求：导入报告后，要把成绩、合格与否填上（之前导入只存 PDF，不填 result/score/qualified）。
- 后端 `backend/app/api/v1/eqa.py` `POST /report/{plan_id}` 增加可选表单字段 `score:str=Form("")`、`qualified:bool=Form(False)`、`result:str=Form("")`；导入时一并写入 plan（score/result 有值才覆盖、空值保留原值；qualified 始终按传入布尔写）。需 `from fastapi import ... Form`。
- 前端 `frontend/src/api/eqa.js` `uploadEqaReport(planId, file, extra)` 支持把 `{result,score,qualified}` 追加进 FormData（qualified 转 'true'/'false'）。
- 前端 `QCList.vue`：把操作列「导入报告」按钮从直接触发隐藏 file input，改为打开 `importDialog` 对话框（选 PDF + 填「成绩/结果」「得分」「是否合格」switch），`submitImportReport` 一并提交。删除了旧的 `reportInput/reportTargetId/onImportReport/onReportFileChange`。新增样式 `.file-tip`/`.file-tip.muted`。
- 验证：后端重启(8123)后 HTTP 实测——上传测试 PDF + score=95/qualified=true/result=合格 返回 plan 三字段均回填成功（200）；测试 plan_id=3 已用 sqlite 复位、测试 PDF 已删。vite build 通过（QCList 29.41kB）。
- 注：每月自动化(download 脚本)只导 PDF 不填成绩（成绩自动解析待 OCR，未接），与手动导入对话框填成绩互补，无需改自动化。

## 室间质评提醒阈值 30天 → 14天
- 需求：室间质评的上报提醒提前天数从 30 天改为 14 天（校准提醒保持 30 天不变）。
- 改动 `backend/app/services/notification_service.py`：保留 `WARN_DAYS = 30`（仅用于校准），新增 `EQA_WARN_DAYS = 14`；`compute_eqa_alerts` 与 `refresh_eqa_notifications` 两处判断由 `> WARN_DAYS` 改为 `> EQA_WARN_DAYS`，相关注释同步更新。
- 重启后端(8123)生效（精确杀旧 PID，确认端口空闲后用 managed python 单实例 uvicorn 启动；openpyxl 3.1.5 已装）。
- 端到端验证（HTTP 真连）：用 plan113(原 due_date 2026-09-11) 实验——改 due=今天+20天 触发刷新后**不在** eqa_return 提醒（排除旧30天阈值），改 due=今天+10天 触发刷新后**在**提醒（阈值≥10），还原后正常。阈值确为14天，数据已复位。

## 室间质评「成绩待填」标记
- 需求：给「已导入报告 PDF 但成绩/得分均未回填」的计划留个可视化标记，便于后续 OCR 接上时批量处理。
- 判定（纯前端）：`row.report_file && !row.result && !row.score`（`qualified` 默认 False 不可作依据）。
- 改动 `frontend/src/views/qc/QCList.vue`：
  1. 「报告」列：已导入时若出现「成绩待填」黄色 `effect="plain"` 徽标（`.pending-tag` 块级、列宽120 不变）。
  2. 工具栏新增常驻 `el-tag`「成绩待填 N 条」(v-if>0)，由 `pendingScoreCount` computed 基于 `eqaRows` 计算。
- 验证：`vite build` 通过（QCList 29.82kB）。纯前端，无需后端重启。
- 数据侧可查性：将来 OCR 脚本按 `report_file 非空 AND score='' AND result=''` 直接批量取即可，无需新增 DB 字段（避免冗余状态）。当前未接 OCR。

## 库内「成绩待填」报告离线解析回填（替代多模态）
- 背景：用户要求把库里 45 份「成绩待填」报告用多模态解析回填。环境调查：**无外网(openai.com 不可达)、无大模型 API 凭证、未装 openai/pdfplumber/PIL**，仅 pypdf/fitz(PyMuPDF) 可用；且实测 45 份 PDF 均文本可抽取（非纯扫描），故用**离线文本智能解析**达成同等效果，免外部多模态 API。
- 实现 `backend/app/api/v1/eqa.py::parse_eqa_report` 真实逻辑（fitz 抽文本 + 规则）：
  - 卫健委：评价词「通过/不通过」+ 末行「成绩NN％」→ result="成绩NN%"/"不合格"，qualified 布尔。
  - 北京市：汇总「满意/不满意」+「及格数 总数 得分%」→ result="合格"/"含不合格项(待复核)"；北京多项目得分不一，score 留空。
  - 特殊态：评价列「不评价/成绩不适用」→ result="成绩不适用(不予评价)"，qualified=None（不填布尔，避免误标）。
  - 低置信（无关键词）不写，保留待人工。
- 上传端点 `POST /report/{plan_id}` 现捕获解析结果，回填优先级：表单显式输入 > 自动解析（空白字段用解析补全）。
- 前端 `QCList.vue`：「合格」列对 result 含「不适用/不评价/不予评价」显示中性灰标「不评价」（避免误标红"不合格"）；「成绩待填」徽标逻辑不变。
- 批量脚本 `outputs/parse_pending_eqa.py`（内联同款 parse，需与 eqa.py 同步）：连 data/app.db 回填 45 份，生成 `outputs/eqa_parse_preview.csv`（核查）+ `outputs/eqa_pending_backup.json`（原值空，回退说明）。
- 结果：45 份全部处理，**剩余待填=0**；44 份合格(qualified=1，其中卫健委 2 份成绩100%)，1 份(id=13 SAA)成绩不适用(不予评价)；无 medium/low 残留。
- 验证：后端重启(WnJixH)加载新代码；vite build 通过；DB 待填清零、前端刷新即 0。
- 多模态升级路径：若未来提供 OpenAI 兼容网关/key，可在 parse_eqa_report 内对「抽文本为空/低置信」分支调视觉大模型（图片渲染+function calling 抽 {result,qualified,score}）；当前文本解析已覆盖全部样本。

## 月度自动化接入自动回填成绩（automation-1783959860633）
- 用户问：自动化导入报告后能否自动填成绩合格与否。答：能。
- 在自动化 prompt 步骤3(下载入库写 report_file)后新增步骤4：运行 outputs/parse_pending_eqa.py 对「report_file 非空且成绩空」批量文本解析回填 result/score/qualified；低置信保留待人工、不误填；明细 eqa_parse_preview.csv、备份 eqa_pending_backup.json。
- 自动化仍每月 1、15 号 20:00 跑；现在实现「下载 PDF → 自动回填成绩」全链路无人工。

## 修复：卫健委合项报告「不适用」子项被误标合格（脂蛋白(a)）
- 用户反馈：卫健委脂类A 报告含多个子项，脂蛋白(a) 是「不适用」但整行被标成「成绩100%/合格」。
- 根因：① id=4（脂类A）在第一轮批量脚本（加 has_na 逻辑前）被填成成绩100%；② 卫健委「合项报告」(一个 program 含多个 analyte，如胆固醇/脂蛋白(a)…) 是逐项分节 PDF，单值 result 模型不匹配，把整张 PDF 的"成绩100%"套到整行。
- 扫描：卫健委34份报告仅 id=4、id=13 含"不适用"；id=13(SAA)单项不适用已正确标"成绩不适用(不予评价)"，仅 id=4 需修正。
- 修复 `parse_eqa_report(pdf_path, item_text=None)`：新增 `item_text` 参数，按子项识别不适用——把 item 清单(、,/分隔)逐项在报告正文定位，子项分节200字内含"不适用/成绩不适用/不评价"即记为不适用子项；仅部分不适用→ result="成绩100%（脂蛋白（a）不适用）"且 qualified=True（整体合格、备注不适用子项，不误判为不合格）；全部不适用→"成绩不适用(不予评价)" qualified=None。无 item_text 时保留旧整体判定(仅整篇无合格词且含不适用词才判不适用，避免合项误判)。
- 上传端点 `POST /report/{plan_id}` 现传 `plan.item` 进解析；批量脚本同步(查询加 item 字段并传入)。
- 前端 `QCList.vue`「合格」列灰色规则：`/不适用|不评价|不予评价/.test(row.result)`(包含即灰) 改为 `/^成绩不适用|^不适用|^不评价/.test(row.result||'')`(以不适用/不评价开头才灰)，避免"成绩100%（脂蛋白(a)不适用）"这种合项合格项被误显灰。
- 已修正 id=4：result="成绩100%（脂蛋白（a）不适用）" qualified=1 score=100；HTTP 端到端重上传验证 PASS；后端重启(TBtdEa)、vite build 通过；批量脚本 smoke(total=0) 正常。
- **通用教训**：卫健委合项报告必须按子项解析，不能把整篇"成绩100%"套到含不适用子项的整行；解析函数需 item 清单才能正确分项。

## 全量误标诊断（2026-07-14 追加）
- 用户问"其他合项报告是否也有某项不过但总体写通过"。对全部45份已导入报告做三项诊断：①逐项解析重算对比库值 ②原始扫描"不适用/不评价"上下文 ③原始扫描"不合格/未通过/不满意"失败词 + 卫健委各合项子项成绩数字分布。
- 结论：**除 id=4 脂类A（部分不适用已备注"脂蛋白(a)不适用"，整体合格，非误标）、id=13 SAA（整体不适用，已正确标）外，无其他漏标**。全库无任何报告含"不合格/未通过/不满意"失败词；卫健委所有合项各子项成绩数字均为100%，无隐性<100%未过项。
- 即"某项不过但总体写通过"的情况不存在（脂类A是"部分不适用但整体合格"，属正确标注）。

## 解析器升级：逐项点名不适用/非100分（2026-07-14 晚）
- 用户要求"以后有不适用、或非100分的，要标注出项目"。重写 `backend/app/api/v1/eqa.py` 与 `outputs/parse_pending_eqa.py` 的 `parse_eqa_report`：
  - 卫健委按"成绩"锚点切块(`_extract_nhc_blocks`)，北京按"满意/不满意"锚点切块(`_extract_bj_lines`)；**项目名=块内第一条"非表头/非标题/非单位/非数字/非日期/非汇总"的行**（analyte 名权威，不受 item 别名录入差异影响）。
  - 新增 `_is_name_line` + `_NONAME` 排除集：含 室间质量评价/实验室编码/统计结果/你室结果/允许范围/所属组/方法/仪器/试剂/校准物/成功(北京评价列表头)/及格数/总数/得分%/汇总 等；纯数字/纯单位/日期/页码行均排除。
  - 点名规则：卫健委不适用子项→"（XX不适用）"、非100→"成绩未达标（XX(Y分)）"、全部不适用→"成绩不适用(不予评价)"；北京非100得分%→"合格（XX(Y分)）"、不满意→"不合格（XX不满意）"、非名块(汇总行)直接跳过。
- 坑：卫健委是表格格式，Lp(a)块"不适用"出现在"允许范围"列值里，旧逻辑误取"允许范围"作项目名；北京"成功"是评价列表头被误当项目名；"计分汇总/所有汇总"是汇总行(得分99)被误当项目。均靠 `_NONAME`+块过滤解决。
- 验证：45份 dry-run 仅 id=40 由"合格"变"合格（锌(80分)）"（锌真实子项得分80，汇总行已排除），其余44份无回归；HTTP 端到端重上传 id=4→"成绩100%（脂蛋白(a)不适用）"、id=40→"合格（锌(80分)）" 均 PASS。

## EQA 总结再拆分：部门(卫健委/北京市) × 专业组，细项去重，合格分析模版
- 需求追加：总结除按专业组(生化+凝血/免疫)拆两份，还要**按质评部门**拆（国家卫健委临检中心 / 北京市临检中心 是俩部门，要分别出）。统计细项**要去重**（多个项目组同名算1项，之前把合项A/B/正确度等重复算，量虚高）。「合格项目分析」块给**模版**。点选：4份独立报告（部门×专业组矩阵，用户确认）。部门显示名=国家卫健委临检中心/北京市临检中心。
- 后端 `EqaSummary` 加 `department VARCHAR(50)`（存 org：卫健委/北京市）；存储 key 变 **(year,half,department,category)**；main.py `_migrate_schema` 补 ALTER + 回填旧行 department NULL→''；schemas.EqaSummaryBase 加 department。
- `eqa.py` 新增 `_ORG_DISPLAY={"卫健委":"国家卫健委临检中心","北京市":"北京市临检中心"}`、`_ORG_SLUG={NCCL,BJ}`、`_normalize_department()`；`_compute_summary_by_category(year,half,db,category,department)` 加 `EqaPlan.org==department` 过滤，并**细项按名称去重**：每条唯一细项继承其评价状态——全部合格→合格、任一不合格→不合格、全不评价→不评价、无回报→只计入细项总数不进分母；unqualified/not_evaluated_list 按唯一项列出（带所属项目组）。
- 5 个端点全加 department：summary-by-category(GET)、summary-generate(POST, body 加 department, 文件名 `{year}_H{half}_{NCCL|BJ}_{BC|IM}.docx`)、summary-docx(GET)、summary-text(GET+PUT)；docx_url 带 department(quote)。
- `services/eqa_summary_doc.py::build_eqa_summary_doc(...,category,department)`：标题 `{dept_display}{cat}组室间质评总结报告表`、元信息 `质评机构：{dept_display} 科室：民航总医院检验科（{cat}组）`、合格分析 scope 含 dept、签字 `{cat}组负责人签字`；单分类无合计行。
- 前端 `api/eqa.js` 5 函数加 department；`QCList.vue` 总结区加「质评部门」el-radio-group(卫健委/北京市→org)，report-title/按钮/预览标题/元信息/签字均随部门+专业组变；**新增 buildSummaryTemplate()**：文字为空时自动预填「合格项目分析」模版（含动态数字：年份/期间/部门/组/计划数/细项数(已去重)/合格/不合格/不评价/合格率 + 一二三四分析段落），仅本地预填、保存后才落库。
- HTTP 验证(2026全年4组合)：卫健委|生化+凝血 计划29/细项(去重)95/评价87/合格86/不评价1/100%；卫健委|免疫 17/65/56/56；北京市|生化+凝血 6/37/35/35；北京市|免疫 9/39/31/31。去重对比：卫健委+生化+凝血 naive(未去重)=233 → 去重后=95（减138）。4 份 Word 标题/质评机构/签字均按部门+组正确。vite build 通过。旧合并文件 2026_H0_BC/IM.docx 已删。
- 前端 `QCList.vue`：成绩列宽 90→170 + `show-overflow-tooltip`，长备注(含点名)悬停可见；灰标规则仍按"以不适用/不评价开头"判定，合项合格项正常显绿。
- 注：自动化任务(每月1/15号)与上传端点均自动用新逻辑；现有数据仅 id=40 因含锌(80分)被点名更新，其余不变。

## 未来部署方向：腾讯云开发(CloudBase) + 发送功能可行性（2026-07-14 晚）
- 用户意向：以后通过腾讯云开发部署本系统，并关心「短信/邮箱/微信」发送是否可行。
- 可行性结论：**完全可行，且天然契合**。映射方案：
  - 前端 Vue3 SPA → 静态网站托管（build 后上传，改动极小）
  - FastAPI 后端 → 云托管(CloudRun) 容器模式（加 Dockerfile，改动小；保留现有架构，优于拆云函数）
  - SQLite → 云数据库 MySQL（需换 SQLAlchemy driver + 迁移数据；注意方言差异：自增/日期函数/JSON 列）
  - 本地 WorkBuddy 自动化(每月1/15号) → 云函数定时触发器（触发 refresh_eqa_notifications + 发送）
  - PDF 报告本地盘 → 云存储 / CFS（勿再写本地磁盘）
- 发送三通道均可行：
  - 短信：云托管内直接调腾讯云 SMS SDK（tencentcloud-sdk-python sms），需实名+签名/模板审核+按条付费，医疗行业审核更严
  - 邮件：SMTP（QQ/企业邮箱）或腾讯云邮件推送
  - 微信：需公众号/企业微信/小程序账号+认证，腾讯系原生支持（云开发内置微信登录/通知，主要适配小程序）
- 前置改造：User 表当前仅 username/name/role/dept，**缺 phone/email/wechat 字段与接收开关**；需扩展并按 role(组长/管理员)筛选接收人。
- 注意：费用各计（云托管/数据库/短信/邮件有免费额度）；发送层可在本地先做好（凭证占位+无凭证降级日志），再上云配真实凭证。
- 参考控制台：云托管 `#/cloudrun`、静态托管 `#/hosting`、MySQL `#/db/mysql`、云函数 `#/scf`、身份认证 `#/identity`（含短信/邮箱/微信登录管理）。

## 仅做邮箱发送：录入管理员邮箱 + 邮件服务（2026-07-14 晚）
- 用户决定：先只做邮箱通道，录入自己的邮箱（管理员）`815268425@qq.com`，暂不接短信/微信。
- 数据层：`models/user.py` 新增 `email VARCHAR(120)` + `notify_email BOOLEAN(默认True)`；`schemas/__init__.py` 的 `UserBase` 同步加字段，并加 `notify_email` 的 `None→True` 校验器（兼容历史 NULL，防编辑保存 422 / 详情 500）。
- 配置：`core/config.py` 新增 SMTP_HOST/PORT/USER/PASS/TLS/MAIL_FROM/SYSTEM_NAME（均带默认值、可 env 覆盖；QQ 示例 smtp.qq.com:465 + 授权码）。
- 新服务 `services/email_service.py`：`send_email` 用标准库 smtplib，**SMTP_HOST+USER 齐备才真发**，否则降级 `logger.warning("[EMAIL-DEGRADED]...")` 打印拟发内容（不阻塞、不抛错）；`send_notifications_email` 把一组 Notification 渲染成文本+HTML 汇总邮件发给多接收人。端口 465→SMTP_SSL，否则 STARTTLS。
- 接收人：`notification_service.get_email_recipients(db)` = `email!='' AND notify_email=True AND role IN('admin','leader')`。
- 端点 `api/v1/notifications.py`：`POST /send`（管理员；把全部未读提醒汇总发邮件给接收人，返回 sent/logged/recipients/results）、`POST /test-email`（管理员；发测试邮件校验 SMTP，to 缺省发往本人邮箱）。
- 迁移 `main.py::_migrate_schema`：users 补 email/notify_email 列；admin 账号 email 为空时填 `815268425@qq.com`（幂等，不覆盖人工修改）；notify_email NULL 回填 1。`seed.py` 建 admin 时一并写该邮箱。
- 前端：`views/Dashboard.vue` 提醒事项卡片加「发送提醒邮件」按钮（调 `sendNotificationEmails`，降级时提示配置 SMTP）；`api/notifications.js` 加 `sendNotificationEmails`/`testEmail`。
- 验证（HTTP 真连，后端 8123 已重启）：`/auth/me` 返回 email=815268425@qq.com、role=admin、notify_email=true；`POST /test-email` 与 `POST /send` 均走降级日志（SMTP 未配），日志正确渲染 23 条未读提醒；`vite build` 通过。
- 后续：配 SMTP_* 环境变量即真发；上云用云函数定时触发器调 `/send` 替代本地自动化；可再加 mark-read-after-send 避免重复发送（当前手动重复点会重发）。

## 用户嘱记：下一步别忘（2026-07-14 15:27）
- 用户原话"这个下一步我记不住，你记着"。已将其固化进 `MEMORY.md`「待办/下一步」段，内容：
  1. 接 SMTP 真发测试（QQ 815268425@qq.com + 授权码，环境变量，非登录密码）；
  2. 加「发完后标记已读」避免重复发送（send 成功后 is_read=True）。
- 下次用户提"接着做/做下一步/真发一下"即触发这两项。

## 室间质评「录入结果」矩阵 + 双人签字审核单（2026-07-14 15:31）
- 需求：室间质评每行加「录入结果」操作，点开是一张**样本×项目矩阵表**（类似用户 `D:\民航总医院\...\免疫.xls`：行=样本编号202511-15，列=检测项目，格=实测结果），可填、可保存，并能**打印成带双人签字栏的审核单**。
- 参考 Excel 结构：每个 sheet = 一个项目组×一轮；多 sheet 含 第一次/第二次；还有「总结报告表」带 填表人/审核人/生免室员工签字（即双人签字审核范式）。
- 数据：`models/eqa.py` 的 `EqaPlan` 加 `result_data TEXT` 字段；`main.py::_migrate_schema` 的 alters 给 eqa_plans 补 `result_data` 列。`result_data` 存 JSON：`{samples:[], items:[], cells:{样本:{项目:值}}, meta:{tester,reviewer,test_date,return_date,note}}`。
- 接口 `api/v1/eqa.py`：`GET/PUT /eqa-plans/{plan_id}/result`（登录可用）。GET 无数据返回基于计划的骨架（默认 5 样本 202511-15、items 取 plan.item 拆分、meta 取 plan.sample_date/due_date）；PUT 覆盖存 JSON。
- 前端 `views/qc/QCList.vue`：操作列加「录入结果」按钮 → 对话框含「样本编号/检测项目」双文本域 + 「生成/刷新表格」+「+增加样本行/+增加项目列」；el-table 可编辑矩阵（首列样本编号可改、列头为项目、单元为结果输入）；下方 测定人/审核人/测定日期/回报日期/备注；底部「保存」(PUT) 与「打印（双人签字审核）」(新窗口渲染 HTML：标题含机构+项目组+轮次、矩阵表、测定人/审核人签字栏+日期)。`api/eqa.js` 加 `getEqaResult/saveEqaResult`。
- 坑：① 仅在 `_migrate_schema` 加列忘了在模型声明 `result_data` → GET 500 `no attribute`，已在 `models/eqa.py` 补 `Mapped[str]=mapped_column(Text, default="")`（**通用教训：补列必须同时改模型**）。② 路由含 `{plan_id}` 被路由重排归到末组，不与 make_router 的 `/{id}` 冲突（已验证）。
- 验证（HTTP 真连，后端 8123 重启）：对某免疫计划 GET 返回骨架(200)、PUT 5样本×2项目、再 GET 持久化正确(round-trip PASS)；vite build 通过(QCList 37.54kB)。验证后已清空测试数据(pid=111 result_data 置空)。

## 样本编号规则修正（2026-07-14 晚，用户「首先是样本编号问题」）
- 用户明确 EQA 样本编号规则：**年份(4位) + 轮次(1位) + 样本序号(1位)**。例：2026年第1轮5样本=`202611–202615`；2025年第1轮=`202511–202515`（此公式对2025也自然成立）。
- 原 `_build_skeleton` 把默认样本**写死** `202511–202515`，导致 2026 年计划打开仍是 2025 编号 → 错。
- 修复 `backend/app/api/v1/eqa.py`：新增 `_parse_round(round_no)` 从轮次文本取轮次数字（取最后一段数字：'第1次'/'2026-1'/'2026年第3次' 均正确；空→1）；`_build_skeleton` 改为 `samples=[f"{year}{rnd}{i}" for i in range(1,6)]`，year=plan.year 或当前年。
- 同时去掉前端 `QCList.vue` 第 973 行写死 `202511–202515` 的兜底（改为取后端样本、空则空表）。
- 验证（HTTP）：库内 109 条全是 2026 年；`第1次`→`202611-202615`(plan112/110/108)，`第2次`→`202621-202625`(plan113)，`第3次`→`202631` 均正确；轮次解析对 `2026-1`/`2026年第3次` 等格式也正确。vite build 通过。
- **项目约定（固化）**：EQA 录入结果默认样本编号必须由 计划的 year + round_no 动态生成，禁止写死任何年份。若库内某计划 year=0 或 round_no 为空，则 year 取当前年、round 取 1。

## EQA 录入结果：单位联动项目查询库 + 第二轮项目澄清（2026-07-14 晚）
- 用户三点诉求：① 各轮(含第二轮)的「具体项目」要录进 plan.item（录入结果才能有列）；② 录入结果要显示单位；③ 把项目与「项目查询库」(test_items)对应上。
- **数据核查结论**：库 `data/app.db`（与后端同路径，未改错库）中 **所有计划行的 item 均已填好（0 空行）**，包括第二轮；之前 GROUP BY 报「第二轮缺项目」是终端换行误读。仅 `尿液定量生化` 的 item 带「具体项目：」前缀（会污染首项目为"具体项目：钾"），已用脚本清理（幂等 UPDATE，1 行）。所有"具体项目/项目/检测项目："前缀均已清除。
- **单位联动**：`backend/app/api/v1/eqa.py` 新增 `_norm_item`（去全半角括号/空白、小写）+ `_lookup_units(names,db)`（按 test_items.name/aliases 匹配单位，先精确后子串，覆盖率≈74%）；`_split_items` 清理 item 前缀并按 、,/ 拆分；`_build_skeleton(plan,db)` 加 `units` 字段；GET /result 传 db 并在已存数据缺 units 时补算；`EqaResultData` 加 `units: dict`；PUT 存 units。
- **前端 `QCList.vue`**：录入结果对话框新增「单位」文本域（与项目逐行对应，默认来自项目库，可改）；gridItems 带 unit，表头显示「项目 (单位)」，保存 payload 含 units，打印审核单表头显示单位。
- 验证（HTTP）：第二轮 plan113 纤维蛋白（原）降解产物检测→样本202621-25、项目FDP、单位mg/L(自动匹配)；尿液定量生化→钾/钠/氯=mmol/L，钙未匹配返回空(可手填)；PUT units+cells 往返正确。vite build 通过。
- **遗留**：EQA item 与 test_items 匹配覆盖约 74%，未命中（如 钙→总钙、血清学简码 CMV G/TOX M、总T3/总T4）需手动填单位或补 test_items 别名；参考范围(ref)暂未展示（用户仅要求单位）。
- **注意**：若用户想要"为缺第二轮行的 program 创建第二轮计划"，属建计划操作（非录入结果），需另做，可能与「复制年度」功能重叠，谨慎。

## EQA 录入结果：单位换算功能（2026-07-14 深夜）
- 用户要求：部分激素项目需换算单位后上报，列出 11 项固定系数。已在录入结果界面加换算功能。
- **换算表（固化，前后端各一份保持一致）** `EQA_UNIT_CONVERSIONS`：
  - 游离雌三醇(uE3) ×3.467 → nmol/L（别名 雌三醇/ue3）
  - 雌二醇(e2) ×3.67 → pmol/L；孕酮 ×3.18 → nmol/L；PTH ×0.106 → pmol/L
  - 皮质醇 ×27.64 → nmol/L；ACTH ×0.2202 → pmol/L；醛固酮 ×2.775 → nmol/L
  - 胰岛素 ×6.965 → pmol/L；C肽 ×0.333 → nmol/L；VD ×2.494 → nmol/L；肾素 ×1.2 → uIU/mL
  - 匹配：归一化(去空白小写)后精确或子串；覆盖库内 uE3(产前筛查 plan92)、雌二醇/孕酮/PTH/皮质醇/胰岛素/C肽/VD(内分泌 plan85)、ACTH(plan66)、醛固酮/肾素(plan73)。
- **后端 `eqa.py`**：`EqaResultData` 加 `conv`、`cells_report`；新增 `_match_conversion(name)`；`_build_skeleton` 给需换算项加 conv 且把 units 设为目标单位；GET /result 回填 conv/units/cells_report（兼容旧数据）。`units[it]` 对换算项恒为 to（上报单位）。
- **前端 `QCList.vue`**：mirror `EQA_CONV`+`matchConv`；gridItems 带 conv、gridRows 加 report；conv 项列渲染「原值(仪器单位) → ×系数 → 上报(to)」双输入，`@input` 自动乘系数填上报值（原值保留）；applyGrid/addCol 自动识别 conv 且保留 report；保存 payload 含 conv+cells_report；打印审核单显示上报值，换算项附「原 X(from)」小字。
- 验证（HTTP）：11 项目标单位全对；雌二醇164.23×3.67=602.7241、孕酮25.6×3.18=81.408、C肽3.54×0.333=1.1788 上报值与原值往返正确；vite build 通过；测试数据已清空。

## EQA 录入结果：单位对应补全（2026-07-14 深夜，用户「哪些项目没对应上」）
- 用户问室间质评哪些项目没和项目查询库(test_items)对应上。复刻 `eqa.py::_lookup_units` 逻辑跑 data/app.db：168 个去重 item，48 个当时单位空。根因：① test_items 别名多为**空格拼成的长串**（如 "T3 Triiodothyronine 三碘甲状腺原氨酸"），归一化删空格后变不可搜 blob，导致 总T3/总T4 等匹配不上；② 单字中文(磷/钙)被 `len>=2` 子串规则拦截；③ `结核感染T细胞检测` 已在库但单位即空(定性)。
- **用户逐项决策并执行**（脚本 `apply_eqa_aliases.py`，commit 到 data/app.db）：
  1. `不饱和铁结合力` 重命名为 **`不饱和铁结合力/总铁结合力`**，别名 `UIBC, TIBC, 总铁结合力`，unit=`µmol/L`，reference 注 UIBC（保留原数值 22.4~57.8 µmol/L）。
  2. `离子钙` 加别名挂到「血气和酸碱分析」行（panel 含 mmol/L，reference 已含 Ca2+ 1.12-1.32 mmol/L）。
  3. 给 38 个库内已有项目加**逗号分隔别名**（EQA 简码→库项目名）：CMV G/M、Cys-C、HCVAb、HCV(胶体金)、HEV-IgM、HSV-1/2 G/M、RV G/M、TOX G/M、VD、β2微球蛋白、βHCG/总βHCG、γ-谷氨酸氨基转移酶、免疫球蛋白轻链κ/λ(血清/尿液)、总T3/游离T3/总T4/游离T4、抗FXa活性、甘油三脂、碳酸氢根、磷、肌钙蛋白-I、脂蛋白（a）、血清蛋白电泳（M蛋白）、血糖、钙、铜离子、总铁结合力。
  4. EQA 计划 item 字段修正：`TGAg`→`TgAb`（用户确认 TGAg 实际是抗甲状腺球蛋白抗体，非甲状腺球蛋白）；`甲功五项`→`游离T3、总T3、游离T4、总T4、TSH`（单位单列）；`乙肝五项`→`HBsAg、HBsAb、HBeAg、HBeAb、HBcAb`（定性，单位无——但拆分后各子项实际带库里单位 COI/IU·L，已向用户说明）。
  5. 暂不动（按用户）：京津冀鲁盲样、代谢物、电解质正确度、酶学正确度、脂类正确度。
- **结果复跑**：去重 170，单位空仅剩 6 个 = 上述 5 个暂不动 + `结核感染T细胞检测`(定性无单位，已对应)。其余全部命中。
- 验证工具保留 `check_eqa_unmatched.py`（复刻 eqa.py 匹配，列未匹配项）；诊断脚本已删。apply_eqa_aliases.py 亦保留可重跑。
- **注意**：已存 result_data 若 units 有空串不会自动刷新（仅 units 整体缺失才重算）；用户打开未保存过的计划录入结果即见新单位，已保存的如需刷新重开即可（一般不丢数据）。

## EQA 录入结果：感染性疾病抗原抗体快速检测 4 项 = 胶体金(定性)（2026-07-14 深夜）
- 用户明确：「感染性疾病抗原抗体快速检测」(program) 里的 4 项 **HIV、HBsAg、TP、HCV 对应的都是胶体金方法的项目**（定性，单位应为 定性（阴性/阳性））。
- **根因**：`eqa.py::_lookup_units` 按短码 HIV/HBsAg/TP 子串匹配会命中**定量 COI/IU·mL** 行（如 HIV→人类免疫缺陷病毒抗原及抗体 COI、HBsAg→定量 IU/mL），且 系列A/B/C 也用这些短码作标准血清学(COI)，无法靠短码区分。库里虽有 `快速抗HIV抗体`(194)/`快速抗梅毒螺旋体抗体`(195)/`快速抗HCV`(197) 三个 定性（胶体金）行，但仅 HCV 有 `HCV（胶体金）` 别名；HBsAg 缺胶体金行。
- **修复**（脚本已跑，commit 到 data/app.db；`verify_colloidal.py` 复刻匹配逻辑做回归验证）：
  1. 新建 `test_items` id=209 `快速抗HBsAg抗体`，别名 `HBsAg（胶体金）,HBsAg胶体金,HBsAg金标,乙肝表面抗原（胶体金）`，unit=`定性（阴性/阳性）`，method=胶体金法，reference=阴性。
  2. 给 194/195/197 补胶体金别名：`HIV（胶体金）,HIV胶体金,HIV金标,HIV快速` / `TP（胶体金）,TP胶体金,TP金标,梅毒（胶体金）,梅毒胶体金,梅毒快速` / `HCV（胶体金）,HCV胶体金,HCV金标,HCV快速`。
  3. 把 4 个「感染性疾病抗原抗体快速检测」计划(78/79/82/83) 的 `item` 显式标注胶体金：`HIV（胶体金）、HBsAg（胶体金）、TP（胶体金）、HCV（胶体金）`（82/83 原仅 HCV 带胶体金后缀，现统一 4 项全带）。
- **验证结果**：快速检测 4 项 → 全部 `定性（阴性/阳性）` ✅；标准血清学(系列A/B/C 的 HIV/HBsAg/TPAb/HCVAb 等) → 仍 COI/IU·mL 定量，**未被定性覆盖** ✅（无回归）。`verify_colloidal.py` 保留作回归测试；`apply_eqa_aliases.py` 一并保留。
- **约定（固化）**：EQA 快速检测(胶体金)四项是 **HIV/HBsAg/TP/HCV = 定性（阴性/阳性）**；凡 program=感染性疾病抗原抗体快速检测 的计划 item 应带「（胶体金）」后缀以正确命中性状行。TP 在此语境=梅毒螺旋体(胶体金)，非总蛋白。

## EQA 录入结果：TT3/TT4 = 总T3/总T4（2026-07-14 深夜）
- 用户明确：内分泌 program 里的 **TT3 = 总T3**、**TT4 = 总T4**（总三碘甲状腺原氨酸 / 总甲状腺素，单位 nmol/L）。
- 现状：计划 84/85（内分泌）的 item 用短码 `TT3、TT4`，但 `test_items` 的 总T3(id=53)/总T4(id=54) 行仅有 `总T3/总T4` 别名、无 `TT3/TT4`，导致这两项目前单位解析为空。
- 修复：给 id=53 追加别名 `TT3`、id=54 追加别名 `TT4`（逗号分隔，保留原别名）。无需改计划 item 文本（TT3/TT4 是规范缩写，列头显示「TT3（nmol/L）」即可自明）。
- 验证（复刻 `_lookup_units`）：计划84/85 中 TT3→nmol/L、TT4→nmol/L、FT3/FT4→pmol/L（游离，不变）、其余项目无回归。

## 室间质评表格 UI 重排：操作顺序 / NCCL 放最后 / 紧凑化（2026-07-14 深夜）
- 用户诉求：① 操作列按钮顺序固定为「录入结果 → 已上报 → 导入报告 → 预览回报 → 编辑 → 删除」；② 原在前列的「NCCL编码」列移到**最后（紧挨操作列前）**；③ 表格整体收窄，避免右拉（想一眼看到成绩）；④ 细项不必全显，移到展开行看全部。
- 改动 `frontend/src/views/qc/QCList.vue`（纯前端，无需后端重启；vite build 通过）：
  1. 操作列顺序重排：`录入结果(openResultDialog) → 已上报(onReturn) → 导入报告(openImportDialog) → 预览回报(onPreviewReport，原名"预览"，常驻显示、无报告时 disabled) → 编辑(onEditPlan) → 删除(onDelPlan)`；列宽 330→410，按钮包进 `.op-cell` flex 可换行兜底。
  2. NCCL编码列由原来第5列（细项后）移到**倒数第二列**（报告列之后、操作列之前）。
  3. 列宽整体收窄：组织机构170→140、专业组90→76、项目组110→104、轮次80→64、已上报80→76、成绩170→150、合格80→76、报告120→96；并将「样本检测日期/上报截止日期」两列**移出主表**→放展开行（细项/检测日期/截止日期/NCCL/备注/得分）。细项加 `show-overflow-tooltip` 截断。
  4. 新增 `type="expand"` 展开列（宽48，最左）：点箭头展开看全部细项全文+日期+NCCL编码+上报备注+得分，满足"细项不全部显示、但能看全部"。
  5. 新 CSS：`.eqa-expand/.lbl/.val`、`.eqa-table .op-cell`(flex-wrap gap4)。
- 效果：主表总宽 ~998px（非固定区），在大多数屏无需横向滚动即可见「成绩」列（位于第7列，约 580px 处）；操作列 fixed right 始终可见。
- 验证：`npm run build` 通过（QCList-DJmZ_ILT.js 41.47kB）。dev(5173) HMR 生效，硬刷新 Ctrl+F5 即见。
- 注意：成绩列仍是 prop=result 文本 + 合格列 tag，未改变数据逻辑；展开行只读展示，不影响录入/导入/编辑。

## 室间质评表格追加：检测日期/上报截止日期放回主表（2026-07-14 深夜，紧接上条）
- 用户补充："检测日期和上报截止日期也需要一目了然"——要求这两个日期也在主表一眼可见（上条把它们移进了展开行）。
- 改动 `QCList.vue`：在「轮次」后、「已上报」前插回两列 `sample_date=检测日期(width108)`、`due_date=上报截止日期(width110)`，均 `sortable="custom"`；并微调列宽（组织机构140→132、专业组76→74、项目组104→100、细项min120→110、轮次64→62、成绩150→144、合格76→74、报告96→94）给两列腾位。
- 展开行同步精简：移除「样本检测日期/上报截止日期/NCCL编码」三行（现已在主表），仅保留 细项全文 / 上报备注 / 得分。
- 列序逻辑：组织机构→专业组→项目组→细项→轮次→**检测日期→上报截止日期**→已上报→成绩→合格→报告→NCCL编码→操作。日期落在第7-8列（约 634/742px），成绩第10列（约 818px），在 1280 宽屏可见区(≈870px)内无需右拉即全见；1920 宽屏整表无滚动。
- 验证：`npm run build` 通过。纯前端，dev(5173) HMR 生效。

## 室间质评「半年/年度总结」改造：按分类合格率 + 自动生成 Word（2026-07-14 深夜）
- 用户诉求：半年/年度总结处按 **生化+凝血、免疫** 分类，显示多少项目组/多少细项，算**细项合格率**；**不予评价不算不合格、要单列**；保存总结文字后**生成年度总结 Word（参考 BG-SM-CZ-020 格式）**，自动存档、可预览、可导出。
- 后端：
  - 安装 `python-docx` 到 managed python 3.13.12（同 openpyxl 方式），供生成 docx。
  - `models/eqa.py` 的 `EqaSummary` 增 `docx_path`(VARCHAR500)、`generated_at`(TIMESTAMP)；`main.py _migrate_schema` 补 ALTER（部署旧库必备）。
  - `schemas/__init__.py` 的 `EqaSummaryBase/Read` 加 `docx_path`、`generated_at`。
  - `api/v1/eqa.py` 新增：`GET /summary-by-category`（按 生化+凝血/免疫/其他 分类统计：项目组数、细项数、合格/不合格/不评价细项数、细项合格率=合格/(合格+不合格)，并给 unqualified_list / not_evaluated_list 明细）；`POST /summary-generate`（算统计+生成 docx+upsert EqaSummary 返回 docx_url+generated_at+stats）；`GET /summary-docx`（下载存档 docx，Cache-Control:no-store）。
  - 关键逻辑：`_category_of`(生化/凝血→生化+凝血，免疫→免疫，其他→其他)；`_is_not_evaluated`(result 以 不评价/不适用/成绩不适用 开头 → 单列、不进合格率分母)；细项 = `_split_items(item)` 按 `、/,` 拆分，每条继承所在行 qualified 状态。
  - docx 生成 `services/eqa_summary_doc.py` 严格按 BG-SM-CZ-020：标题「生化免疫组室间质评总结报告表」、年份/质评部门、一·分类统计表、二·不合格项目表(测定项目/上报结果/预期结果允许范围/原因分析及纠正措施)、三·不评价单列、四·合格项目分析(自动合格率行+用户输入文字)、五·签字区。
  - 存档目录 `DATA_DIR/eqa_summaries/{year}_H{half}.docx`（与 DB 同项目根/data）。
- 前端 `QCList.vue` + `api/eqa.js`：
  - 总结区改按分类卡片展示（生化+凝血/免疫/其他 + 合计），含项目组/细项/合格/不合格/不评价/合格率。
  - 「保存并生成总结报告(Word)」按钮 → `generateEqaSummary`（保存文字+生成+存档）；「预览」→ HTML 预览对话框（与 docx 一致，含不合格/不评价表与合格率自动行）；「导出Word」→ `window.open(eqaSummaryDocUrl)` 下载。
  - 新增 `getEqaSummaryByCategory / generateEqaSummary / eqaSummaryDocUrl` API；computed `unqItems/neItems/passLine/periodLabel`。
- HTTP 全链路验证通过（admin 登录）：2026 全年 109 计划 / 570 细项；生化+凝血 31组334细项、免疫 20组234细项、其他 1组2细项；合格率 100%（合格263/评价264，不评价1单列 SAA→成绩不适用）；生成 docx 37.7KB、MIME 正确、下载成功、docx_path 持久化。`vite build` 通过。
- 注意：下半年(half=2)计划暂无回报数据→合格/不合格均为0（数据状态，非 bug）。不合格项「原因分析及纠正措施」列留占位「（待填）」，用户可在 docx 中补；如需回填可后续扩展。
- UI 微调（23:0x）：室间质评主表**删除左侧展开列**(`type="expand"`)，细项仍可悬停看全文；**操作列宽 410→320→176**，最终改为**窄列(176)多行显示、文字完整不缩写**(录入结果/已上报/导入报告/预览回报/编辑/删除，`flex-wrap:wrap`)，用户反馈双字缩写看不懂。纯前端，`vite build` 通过。
- **BUG 修复（录入结果项目为空）**：纤维蛋白(113)、骨代谢(111)、内分泌(85) 的 `result_data` 之前被存成空 items（`stored_items_len=0`），而 `get_eqa_result` 有已存数据就直接用、不重建，导致录入结果项目列表为空。根因：早期某次保存把空 items 落库。修复：`eqa.py::get_eqa_result` 加**自愈**——若 `stored.items` 为空但 `plan.item` 有内容，则用 `_build_skeleton` 重建骨架（保留未来免疫）。重启后端 HTTP 验证：111→6项(骨钙素/PTH/TP1NP/VD/β胶原特殊序列/降钙素，单位正确)、113→FDP、85→14项(TT3/TT4 nmol/L、FT3/FT4 pmol/L)，87 不受影响。前端无需改（忠实渲染后端 items）。

## EQA 总结 Word 改为「基于真实 BG-SM-CZ-020 模板填充」（2026-07-14 深夜）
- 用户质疑：之前生成的总结 Word 不像真 BG-SM-CZ-020 格式。根因：之前没用项目里现成的真实模板，而是自己从头画表（仅"参考格式"）。真实模板一直在 data/uploads/docs/BG-SM-CZ-020-生化免疫组室间质评总结报告表.docx（14行×5列受控表格：标题/年份+质评部门/测定项目概览/不合格项目明细表/合格项目分析/填表人+审核人+员工签字）。
- 改造 services/eqa_summary_doc.py::build_eqa_summary_doc：改为「复制真实模板→按字段填数→另存」，不再从头画线框。保留模板字体/合并单元格/边框/签字区；仅填：年份、质评部门({国家卫健委临检中心/北京市临检中心}（{生化+凝血/免疫}组)）、测定项目概览(去重后细项数/合格/不合格)、不合格明细(前3项)、合格项目分析(动态合格率行+用户文字多行)。模板路径由 __file__.parents[3]/data/uploads/docs/ 自动定位。
- 标题保持模板原样「生化免疫组室间质评总结报告表」，部门×组区分体现在「质评部门」字段；签字区保留模板原样(填表人/审核人/生化免疫组员工签字)——即真·BG-SM-CZ-020。
- 前端 QCList.vue 预览对话框同步：标题改回模板原样、meta 用「质评部门：{显示名}（{组}）」、签字区改回「生化免疫组员工签字」。
- HTTP 验证(2026全年4组合)全 PASS：4份 docx 标题/质评部门/概览/分析/签字均为真模板样式；卫健委|生化+凝血 95/合格86(不评价1)、免疫 65/56；北京市|生化+凝血 37/35、免疫 39/31。vite build 通过。

## 生化+凝血 EQA 项目核对（2026-07-14 深夜）
- 用户提供 113 项「生化+凝血」EQA 清单要求核对系统缺失项。结论：约84项已覆盖（多为缩写/别名差异：PT/APTT/FIB/D-Dimer/FDP/CRP/ASO/RF/C3/C4/IgA/IgM/碳酸氢根(总二氧化碳)/腺苷脱氨酶(ADA)/尿IgG(尿球蛋白G)/IL-6/胱抑素C/SAA/GA/HbA1c等）。
- **真正缺失（系统无对应）**：①单项 血清锌、触珠蛋白；②尿液化学 11 项（钠/钾/氯/葡萄糖/尿素/肌酐/尿酸/总钙/镁/磷/淀粉酶 的尿液版，系统仅有微量总蛋白/微量白蛋白/轻链κλ(尿液)/α1微球蛋白/尿IgG/尿转铁蛋白）；③脑脊液 5 项（微量总蛋白/氯/葡萄糖/微量白蛋白/乳酸脱氢酶 的脑脊液版，系统"脑脊"零命中）；④血清蛋白电泳组分 6 项（清蛋白/α1/α2/β/γ + M蛋白定性，系统仅有合并项"血清蛋白电泳（M蛋白）"）。
- **命名不一致（系统有但写法不同，非缺失）**：不饱和铁结合力(系统存总铁结合力)、血气和酸碱分析K/Na/CL/Ca(系统存钾/钠/氯/离子钙)。
- 清单本身：免疫球蛋白轻链κ（尿液）重复一次。
- 交付：outputs/生化+凝血质评项目核对_缺失清单.md（勾选清单）。缺项需建对应 EqaPlan（部门+轮次+截止日期）后才能进总结统计；待用户确认是否批量录入。

## 生化+凝血 EQA 项目核对——更正（2026-07-15 凌晨）
- 用户纠正："锌在项目查询模块找得到"。经核查属实：test_items id=97 = 锌/Zn/µmol/L。此前"缺失24项"结论**错误**——根因是只搜了 EqaPlan 表、未比对 项目查询(test_items)，且别名/合并未处理。
- 复核两层：① 项目查询(test_items) 主数据；② EQA计划(eqa_plans) 录入。结论：用户114项清单中约103项已作为 EQA 计划录入（含别名/合并覆盖）：FDP(纤维蛋白原降解产物)、抗FXa活性、总铁结合力(覆盖不饱和铁结合力)、尿IgG(覆盖免疫球蛋白G4/尿球蛋白G)、血清蛋白电泳（M蛋白）合并覆盖6组分+M蛋白定性、ASO/C3/C4 等。
- **真正未录入 EQA 计划（进不了总结统计）的缺口=11项**：血清锌(id=97主数据有)、触珠蛋白(id=24)、总钙（尿液）(id=146)、血气和酸碱分析PH/PO2/PCO2(项目查询id=165合并面板未录入)、脑脊液5项[微量总蛋白(id=151)/氯(id=152)/葡萄糖(id=153)/乳酸脱氢酶(id=205) 主数据有；微量白蛋白（脑脊液）两表皆无]。除微量白蛋白（脑脊液）外，其余10项主数据已在 项目查询，只需建对应 EqaPlan 即可进统计。
- 教训：核对"系统缺失"必须同时查 test_items 与 eqa_plans 两张表，且处理别名(ASO/C3)与合并条目(蛋白电泳/血气面板)。已向用户更正并给11项真缺口清单。
