
## 成片 v2：按用户逐条意见重剪
用户意见 → 落实：
1. **补全遗漏镜头**：新增 3.mp4（大量标本）、4.mp4（质控品）、5-1.mp4（质控品结果界面）、6-2.mp4（流水线处理）；此前未用的 3/4/5-1 全部入片。
2. **重复镜头**：12.mp4 与 13.mp4 确认同场景重复 → 删 13，12 分两段使用（0.8s / 4.4s 起点）。
3. **实拍模糊排查**：主因是 11.jpg 仅 183×286、被放大 5.9 倍 → 改为 PIL 侧先 3 倍放大 + 居中显示；所有 `scale` 加 `flags=lanczos`，unsharp 提到 0.8。
4. **补片头**：新增 2.5s 片头卡（深蓝渐变 + 标题「一管血的旅行」+ 副标题 + 署名），全片 86.0s。
5. **镜头重排**：段2 加大量标本；段4 改「质控品 → 模拟病人卡通 → 质控结果界面」；段5 用 6-2.mp4（流水线）；段6 末用 12.mp4；段7 用干扰卡通 + 12.mp4 另一段；段8 走廊（14.mp4）接手机报告卡通 —— 走廊作为「报告走向临床」的过渡，与前后衔接自然。
6. **9.jpg 病历减少遮挡**：模糊半径 4.5→2.2，清晰区从 1 行扩到多行、宽度从 314px 扩到近全宽。
7. **10.jpg 两种试剂取消遮蔽**：直接用两个试剂盒全貌（1240×2210，比例接近 9:16 不必补背景）；另存 `reagent_softmask.jpg` 合规备选（仅柔化标签文字区）。
8. **5-1.mp4 界面**：样本条码号列可间接识别患者 → `drawbox` 遮挡该列。

### 本次踩到的新坑（重要）
- **片段目录与拼接目录是两层**：`build_video.py` 从 `clips/` 处理到 `clips_pad/` 再 concat。新增任何片段（如片头 opening）**必须同时加进 PADS 列表**，否则 concat 引用的 `clips_pad/opening.mp4` 不存在 → **拼接失败但沿用上一版 main.mp4**，而音频按新时长铺 → 画面与配音整体错位、且时长看着"对"（因为取两流较长者）。**症状：成片时长正确但内容全是旧镜头。**
  → 教训：**每步产物都要校验**，`main.mp4` 生成后必须核对它的 Duration 是否等于所有片段之和（本次 83.5 vs 应有 86.0 就是破绽）。
- ffmpeg 的 `-ss` 起点 + 时长超过素材总长时会静默截短（s7b 取 12.mp4 @5.5+4.52 > 9.40 → 实得 3.90s）→ 生成后逐片段比对时长可发现。
 / 卡通 zoompan 动效）
5. `build_video.py` — tpad 段间定格（把间隙并入前段尾部，保证配音起点与画面严格对齐）→ 片尾卡（深蓝底定格字幕）→ concat 主视频 **83.50s**
6. `build_final.py` — 28 条字幕（每屏≤14 字，按字数比例分配段内时间）生成 TIFF 叠加 + numpy 精确拼音轨 + 导出

### ffmpeg 工具链关键坑（本机 ffmpeg 4.4.5，`C:\Users\81526\AppData\Local\kzip_sogou\ffmpeg.exe`）
- **无 PNG 解码器**（`Decoder (codec png) not found`）→ 透明字幕图改用 **TIFF**（PIL 保存 RGBA TIFF，ffmpeg 正常解码叠加）。
- **无 drawtext / subtitles 滤镜**（未编译 freetype/libass）→ 字幕只能用图片叠加或 PIL 逐帧绘制。
- **图片输入默认 25fps**：`-loop 1 -t N` 配 `zoompan fps=30` 会让成片时长只有 5/6 → 必须加 `-framerate 30`。
- **码率达不到 25Mbps 的解决**：x264 对静态画面会自动压低，`-b:v` / `-minrate` 单独用都无效；**必须严格 CBR**：`-b:v 30M -minrate 30M -maxrate 30M -bufsize 30M -x264-params nal-hrd=cbr`（实测 29.96 Mbps）。
- 超扁图（如病历条 667x66）用"模糊背景铺满"观感脏 → 改 `color=c=0xeef1f4:s=1080x1920` 纯色底 + 前景居中，并在 PIL 侧先放大到位（避免 ffmpeg 二次缩放发虚）。

### 成片规格（`《一管血的旅行》成片.mp4`）
- 1080x1920 竖屏 / 30fps / CBR 约 30Mbps / 83.5s / 约 300MB / AAC 音轨 / 片尾卡含"本片部分画面及配音由 AI 生成"。
- 附带 `一管血的旅行-字幕.srt`（30 条，含片尾两条）。

### 最终校验与待用户确认事项
- 成片实测：83.50s / 1080x1920 / 30fps / CBR 29.96 Mbps / 299.8 MB / AAC 单声道；主视频与计划时长逐段比对一致（曾出现 s6b 短 0.83s，根因是 `color` 源默认 25fps，补 `:r=30` 解决）。
- 抽帧复核：品牌行、水印、二维码均已不可见；病历段改为纯色底 + 居中病历条（仅"羟苯磺酸钙胶囊2片 bid"清晰）；片尾卡正常。
- 待用户确认：① 12/13 号素材有同事正面出镜（戴口罩），需肖像同意；② 病历截图虽已脱敏，建议科室再确认可否使用；③ 缺"结尾抬头"镜头，现用卡通手机报告收尾。
- 已沉淀用户级技能 `~/.workbuddy/skills/local-shortform-video/SKILL.md`（本地短视频成片全流程 + 四个 ffmpeg 4.4.5 限制的替代方案）。

## 成片 v3：平滑转场 + 音画精确对齐（用户第二轮意见）
1. **快进感** → 首镜 s1 慢放 0.6x；其余实拍（2/3/4/6-2/12 号）统一 0.85x 轻微慢放。`vclip(slow=)` 改为「输出时长固定、自动换算取值长度」(`grab = dur*slow`)。
2. **切换生硬** → 拼接从 concat 硬切改为 **xfade 交叉淡化 0.3s**（`build_video.py` 链式 xfade），并输出 `build/timeline.json` 记录每个片段的实际起点。
3. **标本接收 → 成批样本** → 段3 改为「2.mp4 接收 → 3.mp4 成批样本」；文案补「先核对接收，成批的标本排队上机」。
4. **白框** → 5-1 界面条码列改用**局部 boxblur**（crop+blur+overlay），不用白块。坐标按 1080×1920 换算：crop=160:1005:160:596。
5. **糖尿病眼底病 → 糖尿病视网膜病变** → 改配音稿 + 字幕，重新 TTS。
6. **音画不对齐** → 段6 六个镜头时长按**字幕区间反推**：趋势表 5.92 / 病历 6.49 / 干扰动画 4.06 / 两种试剂 4.33 / 申请单 3.57 / 人 2.80。验证：49s「查用药史」=病历画面、53.5s「结果假性偏低」=干扰动画、56s「换一种方法重测」=试剂盒 ✓
7. **走廊效果不好** → 弃用 14.mp4，段7 改「人核对 → 5-3 质控界面」，段8 全用手机报告卡通收尾。
8. `build_final.py` 新增**配音越界自动校验**（每段配音结束 vs 段末，逐条打印余量），本次全部 +0.24~+2.53s。

成片 v3：**84.70s / 1080×1920 / 29.97 Mbps / 304.0 MB**，字幕 29 条（最长 13 字）。

## 成片 v4：删倍速镜头 + 文案修订 + 静态图改推镜动画（用户第三轮意见）
用户 10 条意见 → 落实：
1. **2-3s 倍速镜头** → 直接删除（原 s1 采血），**新生成一张开场动画**（ImageGen：采血管沿轨道"启程"+速度线）作为段1 画面，标题卡缩到 2.0s。
2. **文案改**：seg3「每一管都有自己独有的条码号」；seg4「它通过了，患者的报告才能发出」（逻辑由否定改肯定）；seg6「由酶法改为苦味酸法检测」；seg7「还有的项目结果会假性偏高，通常是类风湿因子、异嗜性抗体在干扰」。四段重录 TTS，配音总长 70.16→75.39s。
3. **30s 质控界面** → 去掉条码列模糊（界面完整展示）。
4. **37-39s 间隔长** → s5 尾部定格 1.30→0.50s，间隙由 0.99s 收到 0.19s。
5. **47s 病历** → 不再遮蔽，直接裁切放大（无患者身份信息）。
6. **64s 跳帧感** → 诊断为「同一素材 12.mp4 的两段不连续画面被剪在一起」（帧间隔检测正常，非技术丢帧）→ s7a 起点从 4.4s 改 2.4s，与 s6f（@0.8，1.67s）**素材区间连续**。
7. **静态图变动画** → 新增 `zoompan` **定点推镜模式 push=(px,py,zend)**：从全景（z=1 自动居中）线性推到目标区域，趋势表推到肌酐行、病历推到用药行、试剂推到上手写标签、申请单推到"肌酐（苦味酸法）"、质控界面推到列表。卡通同样用 push 保持统一动感。
8. **新增隐患修复**：`prep_assets.py` 里卡通图原按文件名排序 zip 标签，新增第 7 张图后按 Unicode 排序会**整体错位** → 改为按文件名关键字（MAP）匹配。

成片 v4：**90.53s / 1080×1920 / 29.97 Mbps / 325.0 MB**，字幕 31 条（最长 13 字），配音对位余量 +0.19~+3.14s。

## 成片 v5：真动画（AI 图生视频）+ 文案全篇优雅化
### 文案重写（用户要求：优雅、与"一管血的旅行"口吻一致）
- 段1「要独自进行一段漫长的旅行」；段2「管帽分颜色，采血有先后，只为不让添加剂互相打扰；请你空腹，是不愿一顿饭改变结果」；
- 段3「标本抵达检验科，先核对身份，再成批汇入流水线」；段4「每一批标本启程前，我们先让一个模拟的病人走一遍同样的路——它通过了，患者的报告才能发出」；
- **段5 重写**：原「机器很快，几分钟就能给出一个数字」（时间不准确）→「流水线昼夜不停，仪器不知疲倦地运转，一次次加样、反应、比色，把血液里的信息变成一串数字」（强调高效、机械、持续，不凸显时间）；
- 段6「但数字，未必就是终点…报告发出时，我们添了一句说明」；段7「也有项目的结果会假性偏高，常见的干扰，来自类风湿因子或异嗜性抗体」；
- **段8 收尾呼应片名**：「要有人一行行看过去。机器给出数字，人给出判断——这，才是这段旅行真正的终点。」
- 配音 70.16→88.08s；成片 103.5s。

### 动画：从"镜头缩放"升级为"画面真动"
- 用户明确「我要的动画不是画面在抖动，你应该能图生视频」→ 改用 **VideoGen 图生视频**（6 段，每段 ~5s，1080×1920/24fps，转 30fps）。
- 提示词要点：**逐元素描述运动**（"采血管沿轨道前行、速度线向后流动、背景仪器缓缓掠过"）、强调"保持扁平矢量风格与配色不变""元素不变形""不出现任何文字"。抽帧对比可确认元素真的在动。
- 截图类（表格/病历/试剂/申请单/界面）不能图生视频（文字会变形）→ 改用 **PIL 逐帧渲染的平滑推镜**（kb.py）：在 2 倍大图上按浮点坐标裁窗 + LANCZOS 缩放，彻底消除 zoompan 的亚像素抖动。

### 关键新坑
- **环境安全钩子会拦截批量删除**：`shutil.rmtree(临时帧目录)` 一次删 255 个文件触发 `SAFE_DELETE_BULK_CONFIRM_REQUIRED`，**整个 Python 脚本当场中断且不报错**（只输出一行 JSON）→ 后续片段全部沿用旧文件，表现为"改参数后 main 时长纹丝不动"。**教训：生成脚本里不要清理临时文件；每步都要核对产物时长**。
- **画面与底部字幕会撞车**：`height` 适配的竖图（试剂盒、申请单）居中后延伸到 y≈1600，与 y=1490 的字幕重叠 → `kb.layout` 增加 `offset_y` 参数把画面上移。

# 2026-09-22 工作日志

## 修复：李东（分子组）无法编辑/导入试剂目录（commit 214d3866）
用户反馈：李东在分子组的「试剂管理 → 试剂目录」里应能编辑、导入。
- **李东账号**：id=50，username/full_name=李东，`group_code='fz'`（分子组），`role='leader'`，**`roles='specialty_leader,deputy_director'`**。
- **根因：三处权限不一致**（前端能看到按钮，一点就 403）
  | 位置 | 原权限 |
  |---|---|
  | 前端 `reagents` 模块 | `[admin, reagent_manager, **specialty_leader**]` |
  | 后端 `POST /items`、`PUT /items/{id}` | `[admin, reagent_manager, lab_technician]` ← 缺 specialty_leader |
  | 后端 `POST /items/_import-excel` | **仅 `[admin]`** |
- **修复**：
  1. `POST /items`、`PUT /items/{id}` 角色补 `specialty_leader`；
  2. `PUT /items/{item_id}` 新增**跨组保护**：非 admin 只能改「本组 / 生免组兼容空值 / material_code 含 KS 的科室共享」的试剂，否则 403「只能维护本专业组的试剂」；
  3. `_import-excel` 角色改为 `[admin, reagent_manager, specialty_leader]`，新增 `group=Depends(get_current_group)`；
     **并修复一个真 bug——导入新建记录时漏设 `group_code`**（`item = ReagentItem(name=name)` → 一律落到模型默认值 `sm`，即分子组导入的试剂全跑进生免组）；
     导入更新已有记录时也加了跨组保护（跨组记录跳过）。
  4. 前端兜底权限表**两处都要改**：`store/auth.js` 的 `FALLBACK_MODULE_WRITE_ROLES` **和** `store/permission.js` 里的 `FALLBACK`
     （`canWrite` 实际读的是 permission.js 那个；只改 auth.js 不生效——本次踩过）。
- 新增辅助：`_item_in_scope(item, group)`（与列表 `_rg_scope` 口径一致）、`_is_admin(user)`。
- **教训**：权限相关的改动要同时核对 ①前端模块权限表 ②后端 `require_roles` ③组隔离校验，三者不一致就会出现「按钮可见但点了 403」。
