# 专利再评估（基于当前代码库，2026-07-30）

> 相比 2026-07-22/24 评估，本次重读 backend/app 现行代码。结论：**系统整体做"管理系统"专利仍不行，但新增的「室内质控多规则协同判定引擎」是当前最强、最接近"高"的专利点，已超过之前最看好的 EQA 闭环。**

## 一、当前系统各功能专利等级（重排）

| 等级 | 功能 | 说明 |
|---|---|---|
| **中高（最强）** | 室内质控多规则协同判定引擎（qc_service.py） | 见第二节，含两项真方法创新 |
| 中高 | EQA 全流程闭环（报告解析→单位校正→偏倚判定→总结生成） | 已知手段组合，仍为强候选 |
| 低 | 仪器间/室间比对（执行 WS/T 403/415）、累靶（即刻法） | 执行公开标准/公式，不单报 |
| 低 | 排班、试剂配送、文件存储 | 纯 CRUD/角色/工程，无方法创新 |

## 二、质控引擎的两项真方法创新（已核实代码）

### 创新点 A：跨水平 SD 归一化 R-4s 判定（qc_service.py:696-880, 12-18）
- 经典 Westgard R-4s 只在**同一水平(同浓度质控品)**内判相邻差。本系统将其**跨水平(高低浓度质控品)扩展**：每点先按"本水平"靶值归一化为 z=(value-target_mean)/target_sd，再判 |z_前-z_后|>4。
- **解决的技术问题**：高低浓度原始值差异巨大（如甲肝IgM水平1≠水平2），直接比原始值会误报 R-4s；归一化后只反映各自偏离靶值的程度，消除伪 R-4s。
- **附加冻结语义**：已失控(ooc)点即时冻结，不参与后续相邻对（避免级联污染）；同天两水平→两者都判失控，跨天→只标后点失控、前点不标 R-4s 警告。
- 这是面向"真实脏数据"对公开 Westgard 规则的**非显而易见改造**，非单纯执行标准。

### 创新点 B：多源规则冲突消解与冻结（qc_service.py:821-863, 562-635）
- LIS 导出若带"规则列"，则以 LIS 声明值**覆盖后端 Westgard 计算**。
- **关键非显而易见规则**：严格区分「真正空单元格」（=LIS 未标失控→一律在控、并冻结其 R-4s）与「有内容但解析不出有效规则码」（=保留后端计算，**绝不当空清零**）。
- 解决了真实集成故障：LIS 用了后端不认识的写法时，旧逻辑会把已标失控点误翻成在控——这是**危险的假"在控"翻转**。还含全角/异形标点归一化、子串提取、无连字符(13S)、严重度合并(1-3S 覆盖 1-2S)。
- 这是"仪器声明状态 vs 统计推导状态"的多源数据一致性方法，有明确技术效果。

## 三、可包装的专利主题
- **首选**：「一种医学检验室内质控多源规则协同判定方法」——把 A(跨水平归一化R-4s+冻结) + B(多源冲突消解+冻结) + 严重度合并打包为一个方法发明。
- 比单报 EQA 更稳：EQA 各单点已有大量现有技术；质控引擎的"跨水平归一化+冻结+空/不可解析区分"组合在检索中未见完整覆盖。

## 四、诚实风险与升级建议
1. **风险**：Westgard 是公开统计法，审查员可能以"已知规则简单改造/显而易见"质疑。防御点=强调 A/B 针对的具体失败模式(浓度差伪R-4s、假在控翻转)与技术效果。
2. **强化证据**（冲专利必备）：用系统真实数据量化技术效果——
   - 归一化消除的伪 R-4s 例数（代码已含甲肝IgM案例）；
   - 空/不可解析区分避免的"假在控翻转"例数（代码已含 AU5800 β2微球蛋白案例）。
3. **仍无 AI/ML**：确认 backend 当前无 PBRTQC 实现，用户此前已说明未做。若要做"高"等成果，仍建议把 PBRTQC 落地成模块。
4. **动作**：先出正式查新报告(省医学情报所/高校查新站)覆盖中英文库+CNKI，再找代理；软著对副高无用，但可照办(单位/防抄/推广)。

*注：本评估为技术可行性分析，非法律意见，最终以审查员/代理师为准。*
